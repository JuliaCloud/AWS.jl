# This file is auto-generated by AWS.jl
# Note: `SERVICE_FEATURE_SET` is embedded by `@service`
using AWS: AbstractAWSConfig, current_aws_config, _merge
using AWS.AWSServices: nova_act
using AWS.UUIDs: uuid4

"""
    create_act(session_id, task, workflow_definition_name, workflow_run_id)
    create_act(session_id, task, workflow_definition_name, workflow_run_id, params::Dict{String,<:Any})

Creates a new AI task (act) within a session that can interact with tools and perform
specific actions.

# Arguments

- `session_id`: The unique identifier of the session to create the act in.
- `task`: The task description that defines what the act should accomplish.
- `workflow_definition_name`: The name of the workflow definition containing the session.
- `workflow_run_id`: The unique identifier of the workflow run containing the session.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"clientToken"`: A unique, case-sensitive identifier that you provide to ensure the
  idempotency of the request.
- `"toolSpecs"`: A list of tool specifications that the act can invoke to complete its task.
"""
function create_act end

function create_act(
    sessionId,
    task,
    workflowDefinitionName,
    workflowRunId;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return nova_act(
        "PUT",
        "/workflow-definitions/$(workflowDefinitionName)/workflow-runs/$(workflowRunId)/sessions/$(sessionId)/acts",
        Dict{String,Any}("task" => task, "clientToken" => string(uuid4()));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_act(
    sessionId,
    task,
    workflowDefinitionName,
    workflowRunId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return nova_act(
        "PUT",
        "/workflow-definitions/$(workflowDefinitionName)/workflow-runs/$(workflowRunId)/sessions/$(sessionId)/acts",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("task" => task, "clientToken" => string(uuid4())),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_session(workflow_definition_name, workflow_run_id)
    create_session(workflow_definition_name, workflow_run_id, params::Dict{String,<:Any})

Creates a new session context within a workflow run to manage conversation state and acts.

# Arguments

- `workflow_definition_name`: The name of the workflow definition containing the workflow
  run.
- `workflow_run_id`: The unique identifier of the workflow run to create the session in.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"clientToken"`: A unique, case-sensitive identifier that you provide to ensure the
  idempotency of the request.
"""
function create_session end

function create_session(
    workflowDefinitionName,
    workflowRunId;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return nova_act(
        "PUT",
        "/workflow-definitions/$(workflowDefinitionName)/workflow-runs/$(workflowRunId)/sessions",
        Dict{String,Any}("clientToken" => string(uuid4()));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_session(
    workflowDefinitionName,
    workflowRunId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return nova_act(
        "PUT",
        "/workflow-definitions/$(workflowDefinitionName)/workflow-runs/$(workflowRunId)/sessions",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("clientToken" => string(uuid4())), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_workflow_definition(name)
    create_workflow_definition(name, params::Dict{String,<:Any})

Creates a new workflow definition template that can be used to execute multiple workflow
runs.

# Arguments

- `name`: The name of the workflow definition. Must be unique within your account and
  region.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"clientToken"`: A unique, case-sensitive identifier that you provide to ensure the
  idempotency of the request.
- `"description"`: An optional description of the workflow definition's purpose and
  functionality.
- `"exportConfig"`: Configuration for exporting workflow execution data to Amazon Simple
  Storage Service.
"""
function create_workflow_definition end

function create_workflow_definition(
    name; aws_config::AbstractAWSConfig=current_aws_config()
)
    return nova_act(
        "PUT",
        "/workflow-definitions",
        Dict{String,Any}("name" => name, "clientToken" => string(uuid4()));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_workflow_definition(
    name, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return nova_act(
        "PUT",
        "/workflow-definitions",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("name" => name, "clientToken" => string(uuid4())),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_workflow_run(client_info, model_id, workflow_definition_name)
    create_workflow_run(client_info, model_id, workflow_definition_name, params::Dict{String,<:Any})

Creates a new execution instance of a workflow definition with specified parameters.

# Arguments

- `client_info`: Information about the client making the request, including compatibility
  version and SDK version.
- `model_id`: The ID of the AI model to use for workflow execution.
- `workflow_definition_name`: The name of the workflow definition to execute.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"clientToken"`: A unique, case-sensitive identifier that you provide to ensure the
  idempotency of the request.
- `"logGroupName"`: The CloudWatch log group name for storing workflow execution logs.
"""
function create_workflow_run end

function create_workflow_run(
    clientInfo,
    modelId,
    workflowDefinitionName;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return nova_act(
        "PUT",
        "/workflow-definitions/$(workflowDefinitionName)/workflow-runs",
        Dict{String,Any}(
            "clientInfo" => clientInfo,
            "modelId" => modelId,
            "clientToken" => string(uuid4()),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_workflow_run(
    clientInfo,
    modelId,
    workflowDefinitionName,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return nova_act(
        "PUT",
        "/workflow-definitions/$(workflowDefinitionName)/workflow-runs",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "clientInfo" => clientInfo,
                    "modelId" => modelId,
                    "clientToken" => string(uuid4()),
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_workflow_definition(workflow_definition_name)
    delete_workflow_definition(workflow_definition_name, params::Dict{String,<:Any})

Deletes a workflow definition and all associated resources. This operation cannot be undone.

# Arguments

- `workflow_definition_name`: The name of the workflow definition to delete.
"""
function delete_workflow_definition end

function delete_workflow_definition(
    workflowDefinitionName; aws_config::AbstractAWSConfig=current_aws_config()
)
    return nova_act(
        "DELETE",
        "/workflow-definitions/$(workflowDefinitionName)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_workflow_definition(
    workflowDefinitionName,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return nova_act(
        "DELETE",
        "/workflow-definitions/$(workflowDefinitionName)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_workflow_run(workflow_definition_name, workflow_run_id)
    delete_workflow_run(workflow_definition_name, workflow_run_id, params::Dict{String,<:Any})

Terminates and cleans up a workflow run, stopping all associated acts and sessions.

# Arguments

- `workflow_definition_name`: The name of the workflow definition containing the workflow
  run.
- `workflow_run_id`: The unique identifier of the workflow run to delete.
"""
function delete_workflow_run end

function delete_workflow_run(
    workflowDefinitionName,
    workflowRunId;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return nova_act(
        "DELETE",
        "/workflow-definitions/$(workflowDefinitionName)/workflow-runs/$(workflowRunId)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_workflow_run(
    workflowDefinitionName,
    workflowRunId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return nova_act(
        "DELETE",
        "/workflow-definitions/$(workflowDefinitionName)/workflow-runs/$(workflowRunId)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_workflow_definition(workflow_definition_name)
    get_workflow_definition(workflow_definition_name, params::Dict{String,<:Any})

Retrieves the details and configuration of a specific workflow definition.

# Arguments

- `workflow_definition_name`: The name of the workflow definition to retrieve.
"""
function get_workflow_definition end

function get_workflow_definition(
    workflowDefinitionName; aws_config::AbstractAWSConfig=current_aws_config()
)
    return nova_act(
        "GET",
        "/workflow-definitions/$(workflowDefinitionName)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_workflow_definition(
    workflowDefinitionName,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return nova_act(
        "GET",
        "/workflow-definitions/$(workflowDefinitionName)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_workflow_run(workflow_definition_name, workflow_run_id)
    get_workflow_run(workflow_definition_name, workflow_run_id, params::Dict{String,<:Any})

Retrieves the current state, configuration, and execution details of a workflow run.

# Arguments

- `workflow_definition_name`: The name of the workflow definition containing the workflow
  run.
- `workflow_run_id`: The unique identifier of the workflow run to retrieve.
"""
function get_workflow_run end

function get_workflow_run(
    workflowDefinitionName,
    workflowRunId;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return nova_act(
        "GET",
        "/workflow-definitions/$(workflowDefinitionName)/workflow-runs/$(workflowRunId)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_workflow_run(
    workflowDefinitionName,
    workflowRunId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return nova_act(
        "GET",
        "/workflow-definitions/$(workflowDefinitionName)/workflow-runs/$(workflowRunId)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    invoke_act_step(act_id, call_results, session_id, workflow_definition_name, workflow_run_id)
    invoke_act_step(act_id, call_results, session_id, workflow_definition_name, workflow_run_id, params::Dict{String,<:Any})

Executes the next step of an act, processing tool call results and returning new tool calls
if needed.

# Arguments

- `act_id`: The unique identifier of the act to invoke the next step for.
- `call_results`: The results from previous tool calls that the act requested.
- `session_id`: The unique identifier of the session containing the act.
- `workflow_definition_name`: The name of the workflow definition containing the act.
- `workflow_run_id`: The unique identifier of the workflow run containing the act.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"previousStepId"`: The identifier of the previous step, used for tracking execution flow.
"""
function invoke_act_step end

function invoke_act_step(
    actId,
    callResults,
    sessionId,
    workflowDefinitionName,
    workflowRunId;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return nova_act(
        "PUT",
        "/workflow-definitions/$(workflowDefinitionName)/workflow-runs/$(workflowRunId)/sessions/$(sessionId)/acts/$(actId)/invoke-step/",
        Dict{String,Any}("callResults" => callResults);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function invoke_act_step(
    actId,
    callResults,
    sessionId,
    workflowDefinitionName,
    workflowRunId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return nova_act(
        "PUT",
        "/workflow-definitions/$(workflowDefinitionName)/workflow-runs/$(workflowRunId)/sessions/$(sessionId)/acts/$(actId)/invoke-step/",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("callResults" => callResults), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_acts(workflow_definition_name)
    list_acts(workflow_definition_name, params::Dict{String,<:Any})

Lists all acts within a specific session with their current status and execution details.

# Arguments

- `workflow_definition_name`: The name of the workflow definition containing the session.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"maxResults"`: The maximum number of acts to return in a single response.
- `"nextToken"`: The token for retrieving the next page of results.
- `"sessionId"`: The unique identifier of the session to list acts for.
- `"sortOrder"`: The sort order for the returned acts (ascending or descending).
- `"workflowRunId"`: The unique identifier of the workflow run containing the session.
"""
function list_acts end

function list_acts(
    workflowDefinitionName; aws_config::AbstractAWSConfig=current_aws_config()
)
    return nova_act(
        "POST",
        "/workflow-definitions/$(workflowDefinitionName)/acts";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_acts(
    workflowDefinitionName,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return nova_act(
        "POST",
        "/workflow-definitions/$(workflowDefinitionName)/acts",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_models(client_compatibility_version)
    list_models(client_compatibility_version, params::Dict{String,<:Any})

Lists all available AI models that can be used for workflow execution, including their
status and compatibility information.

# Arguments

- `client_compatibility_version`: The client compatibility version to filter models by
  compatibility.
"""
function list_models end

function list_models(
    clientCompatibilityVersion; aws_config::AbstractAWSConfig=current_aws_config()
)
    return nova_act(
        "POST",
        "/models",
        Dict{String,Any}("clientCompatibilityVersion" => clientCompatibilityVersion);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_models(
    clientCompatibilityVersion,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return nova_act(
        "POST",
        "/models",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "clientCompatibilityVersion" => clientCompatibilityVersion
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_sessions(workflow_definition_name, workflow_run_id)
    list_sessions(workflow_definition_name, workflow_run_id, params::Dict{String,<:Any})

Lists all sessions within a specific workflow run.

# Arguments

- `workflow_definition_name`: The name of the workflow definition containing the workflow
  run.
- `workflow_run_id`: The unique identifier of the workflow run to list sessions for.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"maxResults"`: The maximum number of sessions to return in a single response.
- `"nextToken"`: The token for retrieving the next page of results.
- `"sortOrder"`: The sort order for the returned sessions (ascending or descending).
"""
function list_sessions end

function list_sessions(
    workflowDefinitionName,
    workflowRunId;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return nova_act(
        "POST",
        "/workflow-definitions/$(workflowDefinitionName)/workflow-runs/$(workflowRunId)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_sessions(
    workflowDefinitionName,
    workflowRunId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return nova_act(
        "POST",
        "/workflow-definitions/$(workflowDefinitionName)/workflow-runs/$(workflowRunId)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_workflow_definitions()
    list_workflow_definitions(params::Dict{String,<:Any})

Lists all workflow definitions in your account with optional filtering and pagination.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"maxResults"`: The maximum number of workflow definitions to return in a single response.
- `"nextToken"`: The token for retrieving the next page of results.
- `"sortOrder"`: The sort order for the returned workflow definitions (ascending or
  descending).
"""
function list_workflow_definitions end

function list_workflow_definitions(; aws_config::AbstractAWSConfig=current_aws_config())
    return nova_act(
        "POST", "/workflow-definitions"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_workflow_definitions(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return nova_act(
        "POST", "/workflow-definitions", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    list_workflow_runs(workflow_definition_name)
    list_workflow_runs(workflow_definition_name, params::Dict{String,<:Any})

Lists all workflow runs for a specific workflow definition with optional filtering and
pagination.

# Arguments

- `workflow_definition_name`: The name of the workflow definition to list workflow runs for.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"maxResults"`: The maximum number of workflow runs to return in a single response.
- `"nextToken"`: The token for retrieving the next page of results.
- `"sortOrder"`: The sort order for the returned workflow runs (ascending or descending).
"""
function list_workflow_runs end

function list_workflow_runs(
    workflowDefinitionName; aws_config::AbstractAWSConfig=current_aws_config()
)
    return nova_act(
        "POST",
        "/workflow-definitions/$(workflowDefinitionName)/workflow-runs";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_workflow_runs(
    workflowDefinitionName,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return nova_act(
        "POST",
        "/workflow-definitions/$(workflowDefinitionName)/workflow-runs",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_act(act_id, session_id, status, workflow_definition_name, workflow_run_id)
    update_act(act_id, session_id, status, workflow_definition_name, workflow_run_id, params::Dict{String,<:Any})

Updates an existing act's configuration, status, or error information.

# Arguments

- `act_id`: The unique identifier of the act to update.
- `session_id`: The unique identifier of the session containing the act.
- `status`: The new status to set for the act.
- `workflow_definition_name`: The name of the workflow definition containing the act.
- `workflow_run_id`: The unique identifier of the workflow run containing the act.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"error"`: Error information to associate with the act, if applicable.
"""
function update_act end

function update_act(
    actId,
    sessionId,
    status,
    workflowDefinitionName,
    workflowRunId;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return nova_act(
        "PUT",
        "/workflow-definitions/$(workflowDefinitionName)/workflow-runs/$(workflowRunId)/sessions/$(sessionId)/acts/$(actId)",
        Dict{String,Any}("status" => status);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_act(
    actId,
    sessionId,
    status,
    workflowDefinitionName,
    workflowRunId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return nova_act(
        "PUT",
        "/workflow-definitions/$(workflowDefinitionName)/workflow-runs/$(workflowRunId)/sessions/$(sessionId)/acts/$(actId)",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("status" => status), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_workflow_run(status, workflow_definition_name, workflow_run_id)
    update_workflow_run(status, workflow_definition_name, workflow_run_id, params::Dict{String,<:Any})

Updates the configuration or state of an active workflow run.

# Arguments

- `status`: The new status to set for the workflow run.
- `workflow_definition_name`: The name of the workflow definition containing the workflow
  run.
- `workflow_run_id`: The unique identifier of the workflow run to update.
"""
function update_workflow_run end

function update_workflow_run(
    status,
    workflowDefinitionName,
    workflowRunId;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return nova_act(
        "PUT",
        "/workflow-definitions/$(workflowDefinitionName)/workflow-runs/$(workflowRunId)",
        Dict{String,Any}("status" => status);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_workflow_run(
    status,
    workflowDefinitionName,
    workflowRunId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return nova_act(
        "PUT",
        "/workflow-definitions/$(workflowDefinitionName)/workflow-runs/$(workflowRunId)",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("status" => status), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end
