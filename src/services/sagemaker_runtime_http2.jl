# This file is auto-generated by AWS.jl
# Note: `SERVICE_FEATURE_SET` is embedded by `@service`
using AWS: AbstractAWSConfig, current_aws_config, _merge
using AWS.AWSServices: sagemaker_runtime_http2
using AWS.UUIDs: uuid4

"""
    invoke_endpoint_with_bidirectional_stream(body, endpoint_name)
    invoke_endpoint_with_bidirectional_stream(body, endpoint_name, params::Dict{String,<:Any})

Invokes a model endpoint with bidirectional streaming capabilities. This operation
establishes a persistent connection that allows you to send multiple requests and receive
streaming responses from the model in real-time.

Bidirectional streaming is useful for interactive applications such as chatbots, real-time
translation, or any scenario where you need to maintain a conversation-like interaction with
the model. The connection remains open, allowing you to send additional input and receive
responses without establishing a new connection for each request.

For an overview of Amazon SageMaker AI, see [How It Works](https://docs.aws.amazon.com/sagemaker/latest/dg/how-it-works.html).

Amazon SageMaker AI strips all POST headers except those supported by the API. Amazon
SageMaker AI might add additional headers. You should not rely on the behavior of headers
outside those enumerated in the request syntax.

Calls to `InvokeEndpointWithBidirectionalStream` are authenticated by using Amazon Web
Services Signature Version 4. For information, see [Authenticating Requests (Amazon Web Services Signature Version 4)](https://docs.aws.amazon.com/AmazonS3/latest/API/sig-v4-authenticating-requests.html)
in the *Amazon S3 API Reference*.

The bidirectional stream maintains the connection until either the client closes it or the
model indicates completion. Each request and response in the stream is sent as an event with
optional headers for data type and completion state.

!!! note
    Endpoints are scoped to an individual account, and are not public. The URL does not
    contain the account ID, but Amazon SageMaker AI determines the account ID from the
    authentication token that is supplied by the caller.

# Arguments

- `body`: The request payload stream.
- `endpoint_name`: The name of the endpoint to invoke.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"X-Amzn-SageMaker-Model-Invocation-Path"`: Model invocation path.
- `"X-Amzn-SageMaker-Model-Query-String"`: Model query string.
- `"X-Amzn-SageMaker-Target-Variant"`: Target variant for the request.
"""
function invoke_endpoint_with_bidirectional_stream end

function invoke_endpoint_with_bidirectional_stream(
    Body, EndpointName; aws_config::AbstractAWSConfig=current_aws_config()
)
    return sagemaker_runtime_http2(
        "POST",
        "/endpoints/$(EndpointName)/invocations-bidirectional-stream",
        Dict{String,Any}("Body" => Body);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function invoke_endpoint_with_bidirectional_stream(
    Body,
    EndpointName,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return sagemaker_runtime_http2(
        "POST",
        "/endpoints/$(EndpointName)/invocations-bidirectional-stream",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("Body" => Body), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end
