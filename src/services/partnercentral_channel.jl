# This file is auto-generated by AWS.jl
# Note: `SERVICE_FEATURE_SET` is embedded by `@service`
using AWS: AbstractAWSConfig, current_aws_config, _merge
using AWS.AWSServices: partnercentral_channel
using AWS.UUIDs: uuid4

"""
    accept_channel_handshake(catalog, identifier)
    accept_channel_handshake(catalog, identifier, params::Dict{String,<:Any})

Accepts a pending channel handshake request from another AWS account.

# Arguments

- `catalog`: The catalog identifier for the handshake request.
- `identifier`: The unique identifier of the channel handshake to accept.
"""
function accept_channel_handshake end

function accept_channel_handshake(
    catalog, identifier; aws_config::AbstractAWSConfig=current_aws_config()
)
    return partnercentral_channel(
        "AcceptChannelHandshake",
        Dict{String,Any}("catalog" => catalog, "identifier" => identifier);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function accept_channel_handshake(
    catalog,
    identifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_channel(
        "AcceptChannelHandshake",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("catalog" => catalog, "identifier" => identifier),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    cancel_channel_handshake(catalog, identifier)
    cancel_channel_handshake(catalog, identifier, params::Dict{String,<:Any})

Cancels a pending channel handshake request.

# Arguments

- `catalog`: The catalog identifier for the handshake request.
- `identifier`: The unique identifier of the channel handshake to cancel.
"""
function cancel_channel_handshake end

function cancel_channel_handshake(
    catalog, identifier; aws_config::AbstractAWSConfig=current_aws_config()
)
    return partnercentral_channel(
        "CancelChannelHandshake",
        Dict{String,Any}("catalog" => catalog, "identifier" => identifier);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function cancel_channel_handshake(
    catalog,
    identifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_channel(
        "CancelChannelHandshake",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("catalog" => catalog, "identifier" => identifier),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_channel_handshake(associated_resource_identifier, catalog, handshake_type)
    create_channel_handshake(associated_resource_identifier, catalog, handshake_type, params::Dict{String,<:Any})

Creates a new channel handshake request to establish a partnership with another AWS account.

# Arguments

- `associated_resource_identifier`: The identifier of the resource associated with this
  handshake.
- `catalog`: The catalog identifier for the handshake request.
- `handshake_type`: The type of handshake to create (e.g., start service period, revoke
  service period).

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"clientToken"`: A unique, case-sensitive identifier to ensure idempotency of the request.
- `"payload"`: The payload containing specific details for the handshake type.
- `"tags"`: Key-value pairs to associate with the channel handshake.
"""
function create_channel_handshake end

function create_channel_handshake(
    associatedResourceIdentifier,
    catalog,
    handshakeType;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_channel(
        "CreateChannelHandshake",
        Dict{String,Any}(
            "associatedResourceIdentifier" => associatedResourceIdentifier,
            "catalog" => catalog,
            "handshakeType" => handshakeType,
            "clientToken" => string(uuid4()),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_channel_handshake(
    associatedResourceIdentifier,
    catalog,
    handshakeType,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_channel(
        "CreateChannelHandshake",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "associatedResourceIdentifier" => associatedResourceIdentifier,
                    "catalog" => catalog,
                    "handshakeType" => handshakeType,
                    "clientToken" => string(uuid4()),
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_program_management_account(account_id, catalog, display_name, program)
    create_program_management_account(account_id, catalog, display_name, program, params::Dict{String,<:Any})

Creates a new program management account for managing partner relationships.

# Arguments

- `account_id`: The AWS account ID to associate with the program management account.
- `catalog`: The catalog identifier for the program management account.
- `display_name`: A human-readable name for the program management account.
- `program`: The program type for the management account.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"clientToken"`: A unique, case-sensitive identifier to ensure idempotency of the request.
- `"tags"`: Key-value pairs to associate with the program management account.
"""
function create_program_management_account end

function create_program_management_account(
    accountId,
    catalog,
    displayName,
    program;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_channel(
        "CreateProgramManagementAccount",
        Dict{String,Any}(
            "accountId" => accountId,
            "catalog" => catalog,
            "displayName" => displayName,
            "program" => program,
            "clientToken" => string(uuid4()),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_program_management_account(
    accountId,
    catalog,
    displayName,
    program,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_channel(
        "CreateProgramManagementAccount",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "accountId" => accountId,
                    "catalog" => catalog,
                    "displayName" => displayName,
                    "program" => program,
                    "clientToken" => string(uuid4()),
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_relationship(associated_account_id, association_type, catalog, display_name, program_management_account_identifier, sector)
    create_relationship(associated_account_id, association_type, catalog, display_name, program_management_account_identifier, sector, params::Dict{String,<:Any})

Creates a new partner relationship between accounts.

# Arguments

- `associated_account_id`: The AWS account ID to associate in this relationship.
- `association_type`: The type of association for the relationship (e.g., reseller,
  distributor).
- `catalog`: The catalog identifier for the relationship.
- `display_name`: A human-readable name for the relationship.
- `program_management_account_identifier`: The identifier of the program management account
  for this relationship.
- `sector`: The business sector for the relationship.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"clientToken"`: A unique, case-sensitive identifier to ensure idempotency of the request.
- `"requestedSupportPlan"`: The support plan requested for this relationship.
- `"resaleAccountModel"`: The resale account model for the relationship.
- `"tags"`: Key-value pairs to associate with the relationship.
"""
function create_relationship end

function create_relationship(
    associatedAccountId,
    associationType,
    catalog,
    displayName,
    programManagementAccountIdentifier,
    sector;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_channel(
        "CreateRelationship",
        Dict{String,Any}(
            "associatedAccountId" => associatedAccountId,
            "associationType" => associationType,
            "catalog" => catalog,
            "displayName" => displayName,
            "programManagementAccountIdentifier" => programManagementAccountIdentifier,
            "sector" => sector,
            "clientToken" => string(uuid4()),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_relationship(
    associatedAccountId,
    associationType,
    catalog,
    displayName,
    programManagementAccountIdentifier,
    sector,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_channel(
        "CreateRelationship",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "associatedAccountId" => associatedAccountId,
                    "associationType" => associationType,
                    "catalog" => catalog,
                    "displayName" => displayName,
                    "programManagementAccountIdentifier" =>
                        programManagementAccountIdentifier,
                    "sector" => sector,
                    "clientToken" => string(uuid4()),
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_program_management_account(catalog, identifier)
    delete_program_management_account(catalog, identifier, params::Dict{String,<:Any})

Deletes a program management account.

# Arguments

- `catalog`: The catalog identifier for the program management account.
- `identifier`: The unique identifier of the program management account to delete.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"clientToken"`: A unique, case-sensitive identifier to ensure idempotency of the request.
"""
function delete_program_management_account end

function delete_program_management_account(
    catalog, identifier; aws_config::AbstractAWSConfig=current_aws_config()
)
    return partnercentral_channel(
        "DeleteProgramManagementAccount",
        Dict{String,Any}(
            "catalog" => catalog,
            "identifier" => identifier,
            "clientToken" => string(uuid4()),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_program_management_account(
    catalog,
    identifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_channel(
        "DeleteProgramManagementAccount",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "catalog" => catalog,
                    "identifier" => identifier,
                    "clientToken" => string(uuid4()),
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_relationship(catalog, identifier, program_management_account_identifier)
    delete_relationship(catalog, identifier, program_management_account_identifier, params::Dict{String,<:Any})

Deletes a partner relationship.

# Arguments

- `catalog`: The catalog identifier for the relationship.
- `identifier`: The unique identifier of the relationship to delete.
- `program_management_account_identifier`: The identifier of the program management account
  associated with the relationship.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"clientToken"`: A unique, case-sensitive identifier to ensure idempotency of the request.
"""
function delete_relationship end

function delete_relationship(
    catalog,
    identifier,
    programManagementAccountIdentifier;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_channel(
        "DeleteRelationship",
        Dict{String,Any}(
            "catalog" => catalog,
            "identifier" => identifier,
            "programManagementAccountIdentifier" => programManagementAccountIdentifier,
            "clientToken" => string(uuid4()),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_relationship(
    catalog,
    identifier,
    programManagementAccountIdentifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_channel(
        "DeleteRelationship",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "catalog" => catalog,
                    "identifier" => identifier,
                    "programManagementAccountIdentifier" =>
                        programManagementAccountIdentifier,
                    "clientToken" => string(uuid4()),
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_relationship(catalog, identifier, program_management_account_identifier)
    get_relationship(catalog, identifier, program_management_account_identifier, params::Dict{String,<:Any})

Retrieves details of a specific partner relationship.

# Arguments

- `catalog`: The catalog identifier for the relationship.
- `identifier`: The unique identifier of the relationship to retrieve.
- `program_management_account_identifier`: The identifier of the program management account
  associated with the relationship.
"""
function get_relationship end

function get_relationship(
    catalog,
    identifier,
    programManagementAccountIdentifier;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_channel(
        "GetRelationship",
        Dict{String,Any}(
            "catalog" => catalog,
            "identifier" => identifier,
            "programManagementAccountIdentifier" => programManagementAccountIdentifier,
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_relationship(
    catalog,
    identifier,
    programManagementAccountIdentifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_channel(
        "GetRelationship",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "catalog" => catalog,
                    "identifier" => identifier,
                    "programManagementAccountIdentifier" =>
                        programManagementAccountIdentifier,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_channel_handshakes(catalog, handshake_type, participant_type)
    list_channel_handshakes(catalog, handshake_type, participant_type, params::Dict{String,<:Any})

Lists channel handshakes based on specified criteria.

# Arguments

- `catalog`: The catalog identifier to filter handshakes.
- `handshake_type`: Filter results by handshake type.
- `participant_type`: Filter by participant type (sender or receiver).

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"associatedResourceIdentifiers"`: Filter by associated resource identifiers.
- `"handshakeTypeFilters"`: Type-specific filters for handshakes.
- `"handshakeTypeSort"`: Type-specific sorting options for handshakes.
- `"maxResults"`: The maximum number of results to return in a single call.
- `"nextToken"`: Token for retrieving the next page of results.
- `"statuses"`: Filter results by handshake status.
"""
function list_channel_handshakes end

function list_channel_handshakes(
    catalog,
    handshakeType,
    participantType;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_channel(
        "ListChannelHandshakes",
        Dict{String,Any}(
            "catalog" => catalog,
            "handshakeType" => handshakeType,
            "participantType" => participantType,
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_channel_handshakes(
    catalog,
    handshakeType,
    participantType,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_channel(
        "ListChannelHandshakes",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "catalog" => catalog,
                    "handshakeType" => handshakeType,
                    "participantType" => participantType,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_program_management_accounts(catalog)
    list_program_management_accounts(catalog, params::Dict{String,<:Any})

Lists program management accounts based on specified criteria.

# Arguments

- `catalog`: The catalog identifier to filter accounts.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"accountIds"`: Filter by AWS account IDs.
- `"displayNames"`: Filter by display names.
- `"maxResults"`: The maximum number of results to return in a single call.
- `"nextToken"`: Token for retrieving the next page of results.
- `"programs"`: Filter by program types.
- `"sort"`: Sorting options for the results.
- `"statuses"`: Filter by program management account statuses.
"""
function list_program_management_accounts end

function list_program_management_accounts(
    catalog; aws_config::AbstractAWSConfig=current_aws_config()
)
    return partnercentral_channel(
        "ListProgramManagementAccounts",
        Dict{String,Any}("catalog" => catalog);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_program_management_accounts(
    catalog,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_channel(
        "ListProgramManagementAccounts",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("catalog" => catalog), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_relationships(catalog)
    list_relationships(catalog, params::Dict{String,<:Any})

Lists partner relationships based on specified criteria.

# Arguments

- `catalog`: The catalog identifier to filter relationships.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"associatedAccountIds"`: Filter by associated AWS account IDs.
- `"associationTypes"`: Filter by association types.
- `"displayNames"`: Filter by display names.
- `"maxResults"`: The maximum number of results to return in a single call.
- `"nextToken"`: Token for retrieving the next page of results.
- `"programManagementAccountIdentifiers"`: Filter by program management account identifiers.
- `"sort"`: Sorting options for the results.
"""
function list_relationships end

function list_relationships(catalog; aws_config::AbstractAWSConfig=current_aws_config())
    return partnercentral_channel(
        "ListRelationships",
        Dict{String,Any}("catalog" => catalog);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_relationships(
    catalog,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_channel(
        "ListRelationships",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("catalog" => catalog), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_tags_for_resource(resource_arn)
    list_tags_for_resource(resource_arn, params::Dict{String,<:Any})

Lists tags associated with a specific resource.

# Arguments

- `resource_arn`: The Amazon Resource Name (ARN) of the resource to list tags for.
"""
function list_tags_for_resource end

function list_tags_for_resource(
    resourceArn; aws_config::AbstractAWSConfig=current_aws_config()
)
    return partnercentral_channel(
        "ListTagsForResource",
        Dict{String,Any}("resourceArn" => resourceArn);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_tags_for_resource(
    resourceArn,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_channel(
        "ListTagsForResource",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("resourceArn" => resourceArn), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    reject_channel_handshake(catalog, identifier)
    reject_channel_handshake(catalog, identifier, params::Dict{String,<:Any})

Rejects a pending channel handshake request.

# Arguments

- `catalog`: The catalog identifier for the handshake request.
- `identifier`: The unique identifier of the channel handshake to reject.
"""
function reject_channel_handshake end

function reject_channel_handshake(
    catalog, identifier; aws_config::AbstractAWSConfig=current_aws_config()
)
    return partnercentral_channel(
        "RejectChannelHandshake",
        Dict{String,Any}("catalog" => catalog, "identifier" => identifier);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function reject_channel_handshake(
    catalog,
    identifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_channel(
        "RejectChannelHandshake",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("catalog" => catalog, "identifier" => identifier),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    tag_resource(resource_arn, tags)
    tag_resource(resource_arn, tags, params::Dict{String,<:Any})

Adds or updates tags for a specified resource.

# Arguments

- `resource_arn`: The Amazon Resource Name (ARN) of the resource to tag.
- `tags`: Key-value pairs to associate with the resource.
"""
function tag_resource end

function tag_resource(resourceArn, tags; aws_config::AbstractAWSConfig=current_aws_config())
    return partnercentral_channel(
        "TagResource",
        Dict{String,Any}("resourceArn" => resourceArn, "tags" => tags);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function tag_resource(
    resourceArn,
    tags,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_channel(
        "TagResource",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("resourceArn" => resourceArn, "tags" => tags),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    untag_resource(resource_arn, tag_keys)
    untag_resource(resource_arn, tag_keys, params::Dict{String,<:Any})

Removes tags from a specified resource.

# Arguments

- `resource_arn`: The Amazon Resource Name (ARN) of the resource to remove tags from.
- `tag_keys`: The keys of the tags to remove from the resource.
"""
function untag_resource end

function untag_resource(
    resourceArn, tagKeys; aws_config::AbstractAWSConfig=current_aws_config()
)
    return partnercentral_channel(
        "UntagResource",
        Dict{String,Any}("resourceArn" => resourceArn, "tagKeys" => tagKeys);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function untag_resource(
    resourceArn,
    tagKeys,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_channel(
        "UntagResource",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("resourceArn" => resourceArn, "tagKeys" => tagKeys),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_program_management_account(catalog, identifier)
    update_program_management_account(catalog, identifier, params::Dict{String,<:Any})

Updates the properties of a program management account.

# Arguments

- `catalog`: The catalog identifier for the program management account.
- `identifier`: The unique identifier of the program management account to update.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"displayName"`: The new display name for the program management account.
- `"revision"`: The current revision number of the program management account.
"""
function update_program_management_account end

function update_program_management_account(
    catalog, identifier; aws_config::AbstractAWSConfig=current_aws_config()
)
    return partnercentral_channel(
        "UpdateProgramManagementAccount",
        Dict{String,Any}("catalog" => catalog, "identifier" => identifier);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_program_management_account(
    catalog,
    identifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_channel(
        "UpdateProgramManagementAccount",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("catalog" => catalog, "identifier" => identifier),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_relationship(catalog, identifier, program_management_account_identifier)
    update_relationship(catalog, identifier, program_management_account_identifier, params::Dict{String,<:Any})

Updates the properties of a partner relationship.

# Arguments

- `catalog`: The catalog identifier for the relationship.
- `identifier`: The unique identifier of the relationship to update.
- `program_management_account_identifier`: The identifier of the program management account
  associated with the relationship.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"displayName"`: The new display name for the relationship.
- `"requestedSupportPlan"`: The updated support plan for the relationship.
- `"revision"`: The current revision number of the relationship.
"""
function update_relationship end

function update_relationship(
    catalog,
    identifier,
    programManagementAccountIdentifier;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_channel(
        "UpdateRelationship",
        Dict{String,Any}(
            "catalog" => catalog,
            "identifier" => identifier,
            "programManagementAccountIdentifier" => programManagementAccountIdentifier,
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_relationship(
    catalog,
    identifier,
    programManagementAccountIdentifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_channel(
        "UpdateRelationship",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "catalog" => catalog,
                    "identifier" => identifier,
                    "programManagementAccountIdentifier" =>
                        programManagementAccountIdentifier,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end
