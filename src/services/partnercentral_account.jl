# This file is auto-generated by AWS.jl
# Note: `SERVICE_FEATURE_SET` is embedded by `@service`
using AWS: AbstractAWSConfig, current_aws_config, _merge
using AWS.AWSServices: partnercentral_account
using AWS.UUIDs: uuid4

"""
    accept_connection_invitation(catalog, client_token, identifier)
    accept_connection_invitation(catalog, client_token, identifier, params::Dict{String,<:Any})

Accepts a connection invitation from another partner, establishing a formal partnership
connection between the two parties.

# Arguments

- `catalog`: The catalog identifier where the connection invitation exists.
- `client_token`: A unique, case-sensitive identifier that you provide to ensure the
  idempotency of the request.
- `identifier`: The unique identifier of the connection invitation to accept.
"""
function accept_connection_invitation end

function accept_connection_invitation(
    Catalog, ClientToken, Identifier; aws_config::AbstractAWSConfig=current_aws_config()
)
    return partnercentral_account(
        "AcceptConnectionInvitation",
        Dict{String,Any}(
            "Catalog" => Catalog, "ClientToken" => ClientToken, "Identifier" => Identifier
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function accept_connection_invitation(
    Catalog,
    ClientToken,
    Identifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_account(
        "AcceptConnectionInvitation",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "Catalog" => Catalog,
                    "ClientToken" => ClientToken,
                    "Identifier" => Identifier,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    associate_aws_training_certification_email_domain(catalog, email, email_verification_code, identifier)
    associate_aws_training_certification_email_domain(catalog, email, email_verification_code, identifier, params::Dict{String,<:Any})

Associates an email domain with AWS training and certification for the partner account,
enabling automatic verification of employee certifications.

# Arguments

- `catalog`: The catalog identifier for the partner account.
- `email`: The email address used to verify domain ownership for AWS training and
  certification association.
- `email_verification_code`: The verification code sent to the email address to confirm
  domain ownership.
- `identifier`: The unique identifier of the partner account.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"ClientToken"`: A unique, case-sensitive identifier that you provide to ensure the
  idempotency of the request.
"""
function associate_aws_training_certification_email_domain end

function associate_aws_training_certification_email_domain(
    Catalog,
    Email,
    EmailVerificationCode,
    Identifier;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_account(
        "AssociateAwsTrainingCertificationEmailDomain",
        Dict{String,Any}(
            "Catalog" => Catalog,
            "Email" => Email,
            "EmailVerificationCode" => EmailVerificationCode,
            "Identifier" => Identifier,
            "ClientToken" => string(uuid4()),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function associate_aws_training_certification_email_domain(
    Catalog,
    Email,
    EmailVerificationCode,
    Identifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_account(
        "AssociateAwsTrainingCertificationEmailDomain",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "Catalog" => Catalog,
                    "Email" => Email,
                    "EmailVerificationCode" => EmailVerificationCode,
                    "Identifier" => Identifier,
                    "ClientToken" => string(uuid4()),
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    cancel_connection(catalog, client_token, connection_type, identifier, reason)
    cancel_connection(catalog, client_token, connection_type, identifier, reason, params::Dict{String,<:Any})

Cancels an existing connection between partners, terminating the partnership relationship.

# Arguments

- `catalog`: The catalog identifier where the connection exists.
- `client_token`: A unique, case-sensitive identifier that you provide to ensure the
  idempotency of the request.
- `connection_type`: The type of connection to cancel (e.g., reseller, distributor,
  technology partner).
- `identifier`: The unique identifier of the connection to cancel.
- `reason`: The reason for canceling the connection, providing context for the termination.
"""
function cancel_connection end

function cancel_connection(
    Catalog,
    ClientToken,
    ConnectionType,
    Identifier,
    Reason;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_account(
        "CancelConnection",
        Dict{String,Any}(
            "Catalog" => Catalog,
            "ClientToken" => ClientToken,
            "ConnectionType" => ConnectionType,
            "Identifier" => Identifier,
            "Reason" => Reason,
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function cancel_connection(
    Catalog,
    ClientToken,
    ConnectionType,
    Identifier,
    Reason,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_account(
        "CancelConnection",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "Catalog" => Catalog,
                    "ClientToken" => ClientToken,
                    "ConnectionType" => ConnectionType,
                    "Identifier" => Identifier,
                    "Reason" => Reason,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    cancel_connection_invitation(catalog, client_token, identifier)
    cancel_connection_invitation(catalog, client_token, identifier, params::Dict{String,<:Any})

Cancels a pending connection invitation before it has been accepted or rejected.

# Arguments

- `catalog`: The catalog identifier where the connection invitation exists.
- `client_token`: A unique, case-sensitive identifier that you provide to ensure the
  idempotency of the request.
- `identifier`: The unique identifier of the connection invitation to cancel.
"""
function cancel_connection_invitation end

function cancel_connection_invitation(
    Catalog, ClientToken, Identifier; aws_config::AbstractAWSConfig=current_aws_config()
)
    return partnercentral_account(
        "CancelConnectionInvitation",
        Dict{String,Any}(
            "Catalog" => Catalog, "ClientToken" => ClientToken, "Identifier" => Identifier
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function cancel_connection_invitation(
    Catalog,
    ClientToken,
    Identifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_account(
        "CancelConnectionInvitation",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "Catalog" => Catalog,
                    "ClientToken" => ClientToken,
                    "Identifier" => Identifier,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    cancel_profile_update_task(catalog, identifier, task_id)
    cancel_profile_update_task(catalog, identifier, task_id, params::Dict{String,<:Any})

Cancels an in-progress profile update task, stopping any pending changes to the partner
profile.

# Arguments

- `catalog`: The catalog identifier for the partner account.
- `identifier`: The unique identifier of the partner account.
- `task_id`: The unique identifier of the profile update task to cancel.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"ClientToken"`: A unique, case-sensitive identifier that you provide to ensure the
  idempotency of the request.
"""
function cancel_profile_update_task end

function cancel_profile_update_task(
    Catalog, Identifier, TaskId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return partnercentral_account(
        "CancelProfileUpdateTask",
        Dict{String,Any}(
            "Catalog" => Catalog,
            "Identifier" => Identifier,
            "TaskId" => TaskId,
            "ClientToken" => string(uuid4()),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function cancel_profile_update_task(
    Catalog,
    Identifier,
    TaskId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_account(
        "CancelProfileUpdateTask",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "Catalog" => Catalog,
                    "Identifier" => Identifier,
                    "TaskId" => TaskId,
                    "ClientToken" => string(uuid4()),
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_connection_invitation(catalog, client_token, connection_type, email, message, name, receiver_identifier)
    create_connection_invitation(catalog, client_token, connection_type, email, message, name, receiver_identifier, params::Dict{String,<:Any})

Creates a new connection invitation to establish a partnership with another organization.

# Arguments

- `catalog`: The catalog identifier where the connection invitation will be created.
- `client_token`: A unique, case-sensitive identifier that you provide to ensure the
  idempotency of the request.
- `connection_type`: The type of connection being requested (e.g., reseller, distributor,
  technology partner).
- `email`: The email address of the person to send the connection invitation to.
- `message`: A custom message to include with the connection invitation.
- `name`: The name of the person sending the connection invitation.
- `receiver_identifier`: The identifier of the organization or partner to invite for
  connection.
"""
function create_connection_invitation end

function create_connection_invitation(
    Catalog,
    ClientToken,
    ConnectionType,
    Email,
    Message,
    Name,
    ReceiverIdentifier;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_account(
        "CreateConnectionInvitation",
        Dict{String,Any}(
            "Catalog" => Catalog,
            "ClientToken" => ClientToken,
            "ConnectionType" => ConnectionType,
            "Email" => Email,
            "Message" => Message,
            "Name" => Name,
            "ReceiverIdentifier" => ReceiverIdentifier,
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_connection_invitation(
    Catalog,
    ClientToken,
    ConnectionType,
    Email,
    Message,
    Name,
    ReceiverIdentifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_account(
        "CreateConnectionInvitation",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "Catalog" => Catalog,
                    "ClientToken" => ClientToken,
                    "ConnectionType" => ConnectionType,
                    "Email" => Email,
                    "Message" => Message,
                    "Name" => Name,
                    "ReceiverIdentifier" => ReceiverIdentifier,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_partner(alliance_lead_contact, catalog, email_verification_code, legal_name, primary_solution_type)
    create_partner(alliance_lead_contact, catalog, email_verification_code, legal_name, primary_solution_type, params::Dict{String,<:Any})

Creates a new partner account in the AWS Partner Network with the specified details and
configuration.

# Arguments

- `alliance_lead_contact`: The primary contact person for alliance and partnership matters.
- `catalog`: The catalog identifier where the partner account will be created.
- `email_verification_code`: The verification code sent to the alliance lead contact's email
  to confirm account creation.
- `legal_name`: The legal name of the organization becoming a partner.
- `primary_solution_type`: The primary type of solution or service the partner provides
  (e.g., consulting, software, managed services).

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"ClientToken"`: A unique, case-sensitive identifier that you provide to ensure the
  idempotency of the request.
- `"Tags"`: A list of tags to associate with the partner account for organization and
  billing purposes.
"""
function create_partner end

function create_partner(
    AllianceLeadContact,
    Catalog,
    EmailVerificationCode,
    LegalName,
    PrimarySolutionType;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_account(
        "CreatePartner",
        Dict{String,Any}(
            "AllianceLeadContact" => AllianceLeadContact,
            "Catalog" => Catalog,
            "EmailVerificationCode" => EmailVerificationCode,
            "LegalName" => LegalName,
            "PrimarySolutionType" => PrimarySolutionType,
            "ClientToken" => string(uuid4()),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_partner(
    AllianceLeadContact,
    Catalog,
    EmailVerificationCode,
    LegalName,
    PrimarySolutionType,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_account(
        "CreatePartner",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "AllianceLeadContact" => AllianceLeadContact,
                    "Catalog" => Catalog,
                    "EmailVerificationCode" => EmailVerificationCode,
                    "LegalName" => LegalName,
                    "PrimarySolutionType" => PrimarySolutionType,
                    "ClientToken" => string(uuid4()),
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    disassociate_aws_training_certification_email_domain(catalog, domain_name, identifier)
    disassociate_aws_training_certification_email_domain(catalog, domain_name, identifier, params::Dict{String,<:Any})

Removes the association between an email domain and AWS training and certification for the
partner account.

# Arguments

- `catalog`: The catalog identifier for the partner account.
- `domain_name`: The domain name to disassociate from AWS training and certification.
- `identifier`: The unique identifier of the partner account.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"ClientToken"`: A unique, case-sensitive identifier that you provide to ensure the
  idempotency of the request.
"""
function disassociate_aws_training_certification_email_domain end

function disassociate_aws_training_certification_email_domain(
    Catalog, DomainName, Identifier; aws_config::AbstractAWSConfig=current_aws_config()
)
    return partnercentral_account(
        "DisassociateAwsTrainingCertificationEmailDomain",
        Dict{String,Any}(
            "Catalog" => Catalog,
            "DomainName" => DomainName,
            "Identifier" => Identifier,
            "ClientToken" => string(uuid4()),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function disassociate_aws_training_certification_email_domain(
    Catalog,
    DomainName,
    Identifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_account(
        "DisassociateAwsTrainingCertificationEmailDomain",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "Catalog" => Catalog,
                    "DomainName" => DomainName,
                    "Identifier" => Identifier,
                    "ClientToken" => string(uuid4()),
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_alliance_lead_contact(catalog, identifier)
    get_alliance_lead_contact(catalog, identifier, params::Dict{String,<:Any})

Retrieves the alliance lead contact information for a partner account.

# Arguments

- `catalog`: The catalog identifier for the partner account.
- `identifier`: The unique identifier of the partner account.
"""
function get_alliance_lead_contact end

function get_alliance_lead_contact(
    Catalog, Identifier; aws_config::AbstractAWSConfig=current_aws_config()
)
    return partnercentral_account(
        "GetAllianceLeadContact",
        Dict{String,Any}("Catalog" => Catalog, "Identifier" => Identifier);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_alliance_lead_contact(
    Catalog,
    Identifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_account(
        "GetAllianceLeadContact",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("Catalog" => Catalog, "Identifier" => Identifier),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_connection(catalog, identifier)
    get_connection(catalog, identifier, params::Dict{String,<:Any})

Retrieves detailed information about a specific connection between partners.

# Arguments

- `catalog`: The catalog identifier where the connection exists.
- `identifier`: The unique identifier of the connection to retrieve.
"""
function get_connection end

function get_connection(
    Catalog, Identifier; aws_config::AbstractAWSConfig=current_aws_config()
)
    return partnercentral_account(
        "GetConnection",
        Dict{String,Any}("Catalog" => Catalog, "Identifier" => Identifier);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_connection(
    Catalog,
    Identifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_account(
        "GetConnection",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("Catalog" => Catalog, "Identifier" => Identifier),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_connection_invitation(catalog, identifier)
    get_connection_invitation(catalog, identifier, params::Dict{String,<:Any})

Retrieves detailed information about a specific connection invitation.

# Arguments

- `catalog`: The catalog identifier where the connection invitation exists.
- `identifier`: The unique identifier of the connection invitation to retrieve.
"""
function get_connection_invitation end

function get_connection_invitation(
    Catalog, Identifier; aws_config::AbstractAWSConfig=current_aws_config()
)
    return partnercentral_account(
        "GetConnectionInvitation",
        Dict{String,Any}("Catalog" => Catalog, "Identifier" => Identifier);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_connection_invitation(
    Catalog,
    Identifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_account(
        "GetConnectionInvitation",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("Catalog" => Catalog, "Identifier" => Identifier),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_connection_preferences(catalog)
    get_connection_preferences(catalog, params::Dict{String,<:Any})

Retrieves the connection preferences for a partner account, including access settings and
exclusions.

# Arguments

- `catalog`: The catalog identifier for the partner account.
"""
function get_connection_preferences end

function get_connection_preferences(
    Catalog; aws_config::AbstractAWSConfig=current_aws_config()
)
    return partnercentral_account(
        "GetConnectionPreferences",
        Dict{String,Any}("Catalog" => Catalog);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_connection_preferences(
    Catalog,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_account(
        "GetConnectionPreferences",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("Catalog" => Catalog), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_partner(catalog, identifier)
    get_partner(catalog, identifier, params::Dict{String,<:Any})

Retrieves detailed information about a specific partner account.

# Arguments

- `catalog`: The catalog identifier for the partner account.
- `identifier`: The unique identifier of the partner account to retrieve.
"""
function get_partner end

function get_partner(
    Catalog, Identifier; aws_config::AbstractAWSConfig=current_aws_config()
)
    return partnercentral_account(
        "GetPartner",
        Dict{String,Any}("Catalog" => Catalog, "Identifier" => Identifier);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_partner(
    Catalog,
    Identifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_account(
        "GetPartner",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("Catalog" => Catalog, "Identifier" => Identifier),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_profile_update_task(catalog, identifier)
    get_profile_update_task(catalog, identifier, params::Dict{String,<:Any})

Retrieves information about a specific profile update task.

# Arguments

- `catalog`: The catalog identifier for the partner account.
- `identifier`: The unique identifier of the partner account.
"""
function get_profile_update_task end

function get_profile_update_task(
    Catalog, Identifier; aws_config::AbstractAWSConfig=current_aws_config()
)
    return partnercentral_account(
        "GetProfileUpdateTask",
        Dict{String,Any}("Catalog" => Catalog, "Identifier" => Identifier);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_profile_update_task(
    Catalog,
    Identifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_account(
        "GetProfileUpdateTask",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("Catalog" => Catalog, "Identifier" => Identifier),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_profile_visibility(catalog, identifier)
    get_profile_visibility(catalog, identifier, params::Dict{String,<:Any})

Retrieves the visibility settings for a partner profile, determining who can see the profile
information.

# Arguments

- `catalog`: The catalog identifier for the partner account.
- `identifier`: The unique identifier of the partner account.
"""
function get_profile_visibility end

function get_profile_visibility(
    Catalog, Identifier; aws_config::AbstractAWSConfig=current_aws_config()
)
    return partnercentral_account(
        "GetProfileVisibility",
        Dict{String,Any}("Catalog" => Catalog, "Identifier" => Identifier);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_profile_visibility(
    Catalog,
    Identifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_account(
        "GetProfileVisibility",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("Catalog" => Catalog, "Identifier" => Identifier),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_verification(verification_type)
    get_verification(verification_type, params::Dict{String,<:Any})

Retrieves the current status and details of a verification process for a partner account.
This operation allows partners to check the progress and results of business or registrant
verification processes.

# Arguments

- `verification_type`: The type of verification to retrieve information for. Valid values
  include business verification for company registration details and registrant verification
  for individual identity confirmation.
"""
function get_verification end

function get_verification(
    VerificationType; aws_config::AbstractAWSConfig=current_aws_config()
)
    return partnercentral_account(
        "GetVerification",
        Dict{String,Any}("VerificationType" => VerificationType);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_verification(
    VerificationType,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_account(
        "GetVerification",
        Dict{String,Any}(
            mergewith(
                _merge, Dict{String,Any}("VerificationType" => VerificationType), params
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_connection_invitations(catalog)
    list_connection_invitations(catalog, params::Dict{String,<:Any})

Lists connection invitations for the partner account, with optional filtering by status,
type, and other criteria.

# Arguments

- `catalog`: The catalog identifier for the partner account.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"ConnectionType"`: Filter results by connection type (e.g., reseller, distributor,
  technology partner).
- `"MaxResults"`: The maximum number of connection invitations to return in a single
  response.
- `"NextToken"`: The token for retrieving the next page of results in paginated responses.
- `"OtherParticipantIdentifiers"`: Filter results by specific participant identifiers.
- `"ParticipantType"`: Filter results by participant type (inviter or invitee).
- `"Status"`: Filter results by invitation status (pending, accepted, rejected, canceled,
  expired).
"""
function list_connection_invitations end

function list_connection_invitations(
    Catalog; aws_config::AbstractAWSConfig=current_aws_config()
)
    return partnercentral_account(
        "ListConnectionInvitations",
        Dict{String,Any}("Catalog" => Catalog);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_connection_invitations(
    Catalog,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_account(
        "ListConnectionInvitations",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("Catalog" => Catalog), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_connections(catalog)
    list_connections(catalog, params::Dict{String,<:Any})

Lists active connections for the partner account, with optional filtering by connection type
and participant.

# Arguments

- `catalog`: The catalog identifier for the partner account.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"ConnectionType"`: Filter results by connection type (e.g., reseller, distributor,
  technology partner).
- `"MaxResults"`: The maximum number of connections to return in a single response.
- `"NextToken"`: The token for retrieving the next page of results in paginated responses.
- `"OtherParticipantIdentifiers"`: Filter results by specific participant identifiers.
"""
function list_connections end

function list_connections(Catalog; aws_config::AbstractAWSConfig=current_aws_config())
    return partnercentral_account(
        "ListConnections",
        Dict{String,Any}("Catalog" => Catalog);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_connections(
    Catalog,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_account(
        "ListConnections",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("Catalog" => Catalog), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_partners(catalog)
    list_partners(catalog, params::Dict{String,<:Any})

Lists partner accounts in the catalog, providing a summary view of all partners.

# Arguments

- `catalog`: The catalog identifier to list partners from.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"NextToken"`: The token for retrieving the next page of results in paginated responses.
"""
function list_partners end

function list_partners(Catalog; aws_config::AbstractAWSConfig=current_aws_config())
    return partnercentral_account(
        "ListPartners",
        Dict{String,Any}("Catalog" => Catalog);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_partners(
    Catalog,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_account(
        "ListPartners",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("Catalog" => Catalog), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_tags_for_resource(resource_arn)
    list_tags_for_resource(resource_arn, params::Dict{String,<:Any})

Lists all tags associated with a specific AWS Partner Central Account resource.

# Arguments

- `resource_arn`: The Amazon Resource Name (ARN) of the resource to list tags for.
"""
function list_tags_for_resource end

function list_tags_for_resource(
    ResourceArn; aws_config::AbstractAWSConfig=current_aws_config()
)
    return partnercentral_account(
        "ListTagsForResource",
        Dict{String,Any}("ResourceArn" => ResourceArn);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_tags_for_resource(
    ResourceArn,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_account(
        "ListTagsForResource",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("ResourceArn" => ResourceArn), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    put_alliance_lead_contact(alliance_lead_contact, catalog, identifier)
    put_alliance_lead_contact(alliance_lead_contact, catalog, identifier, params::Dict{String,<:Any})

Creates or updates the alliance lead contact information for a partner account.

# Arguments

- `alliance_lead_contact`: The alliance lead contact information to set for the partner
  account.
- `catalog`: The catalog identifier for the partner account.
- `identifier`: The unique identifier of the partner account.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"EmailVerificationCode"`: The verification code sent to the alliance lead contact's email
  to confirm the update.
"""
function put_alliance_lead_contact end

function put_alliance_lead_contact(
    AllianceLeadContact,
    Catalog,
    Identifier;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_account(
        "PutAllianceLeadContact",
        Dict{String,Any}(
            "AllianceLeadContact" => AllianceLeadContact,
            "Catalog" => Catalog,
            "Identifier" => Identifier,
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function put_alliance_lead_contact(
    AllianceLeadContact,
    Catalog,
    Identifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_account(
        "PutAllianceLeadContact",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "AllianceLeadContact" => AllianceLeadContact,
                    "Catalog" => Catalog,
                    "Identifier" => Identifier,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    put_profile_visibility(catalog, identifier, visibility)
    put_profile_visibility(catalog, identifier, visibility, params::Dict{String,<:Any})

Sets the visibility level for a partner profile, controlling who can view the profile
information.

# Arguments

- `catalog`: The catalog identifier for the partner account.
- `identifier`: The unique identifier of the partner account.
- `visibility`: The visibility setting to apply to the partner profile.
"""
function put_profile_visibility end

function put_profile_visibility(
    Catalog, Identifier, Visibility; aws_config::AbstractAWSConfig=current_aws_config()
)
    return partnercentral_account(
        "PutProfileVisibility",
        Dict{String,Any}(
            "Catalog" => Catalog, "Identifier" => Identifier, "Visibility" => Visibility
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function put_profile_visibility(
    Catalog,
    Identifier,
    Visibility,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_account(
        "PutProfileVisibility",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "Catalog" => Catalog,
                    "Identifier" => Identifier,
                    "Visibility" => Visibility,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    reject_connection_invitation(catalog, client_token, identifier)
    reject_connection_invitation(catalog, client_token, identifier, params::Dict{String,<:Any})

Rejects a connection invitation from another partner, declining the partnership request.

# Arguments

- `catalog`: The catalog identifier where the connection invitation exists.
- `client_token`: A unique, case-sensitive identifier that you provide to ensure the
  idempotency of the request.
- `identifier`: The unique identifier of the connection invitation to reject.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Reason"`: The reason for rejecting the connection invitation.
"""
function reject_connection_invitation end

function reject_connection_invitation(
    Catalog, ClientToken, Identifier; aws_config::AbstractAWSConfig=current_aws_config()
)
    return partnercentral_account(
        "RejectConnectionInvitation",
        Dict{String,Any}(
            "Catalog" => Catalog, "ClientToken" => ClientToken, "Identifier" => Identifier
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function reject_connection_invitation(
    Catalog,
    ClientToken,
    Identifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_account(
        "RejectConnectionInvitation",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "Catalog" => Catalog,
                    "ClientToken" => ClientToken,
                    "Identifier" => Identifier,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    send_email_verification_code(catalog, email)
    send_email_verification_code(catalog, email, params::Dict{String,<:Any})

Sends an email verification code to the specified email address for account verification
purposes.

# Arguments

- `catalog`: The catalog identifier for the partner account.
- `email`: The email address to send the verification code to.
"""
function send_email_verification_code end

function send_email_verification_code(
    Catalog, Email; aws_config::AbstractAWSConfig=current_aws_config()
)
    return partnercentral_account(
        "SendEmailVerificationCode",
        Dict{String,Any}("Catalog" => Catalog, "Email" => Email);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function send_email_verification_code(
    Catalog,
    Email,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_account(
        "SendEmailVerificationCode",
        Dict{String,Any}(
            mergewith(
                _merge, Dict{String,Any}("Catalog" => Catalog, "Email" => Email), params
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    start_profile_update_task(catalog, identifier, task_details)
    start_profile_update_task(catalog, identifier, task_details, params::Dict{String,<:Any})

Initiates a profile update task to modify partner profile information asynchronously.

# Arguments

- `catalog`: The catalog identifier for the partner account.
- `identifier`: The unique identifier of the partner account.
- `task_details`: The details of the profile updates to be performed.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"ClientToken"`: A unique, case-sensitive identifier that you provide to ensure the
  idempotency of the request.
"""
function start_profile_update_task end

function start_profile_update_task(
    Catalog, Identifier, TaskDetails; aws_config::AbstractAWSConfig=current_aws_config()
)
    return partnercentral_account(
        "StartProfileUpdateTask",
        Dict{String,Any}(
            "Catalog" => Catalog,
            "Identifier" => Identifier,
            "TaskDetails" => TaskDetails,
            "ClientToken" => string(uuid4()),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function start_profile_update_task(
    Catalog,
    Identifier,
    TaskDetails,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_account(
        "StartProfileUpdateTask",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "Catalog" => Catalog,
                    "Identifier" => Identifier,
                    "TaskDetails" => TaskDetails,
                    "ClientToken" => string(uuid4()),
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    start_verification()
    start_verification(params::Dict{String,<:Any})

Initiates a new verification process for a partner account. This operation begins the
verification workflow for either business registration or individual registrant identity
verification as required by AWS Partner Central.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"ClientToken"`: A unique, case-sensitive identifier that you provide to ensure the
  idempotency of the request. This prevents duplicate verification processes from being
  started accidentally.
- `"VerificationDetails"`: The specific details required for the verification process,
  including business information for business verification or personal information for
  registrant verification.
"""
function start_verification end

function start_verification(; aws_config::AbstractAWSConfig=current_aws_config())
    return partnercentral_account(
        "StartVerification",
        Dict{String,Any}("ClientToken" => string(uuid4()));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function start_verification(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return partnercentral_account(
        "StartVerification",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("ClientToken" => string(uuid4())), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    tag_resource(resource_arn, tags)
    tag_resource(resource_arn, tags, params::Dict{String,<:Any})

Adds or updates tags for a specified AWS Partner Central Account resource.

# Arguments

- `resource_arn`: The Amazon Resource Name (ARN) of the resource to tag.
- `tags`: A list of tags to add or update for the specified resource.
"""
function tag_resource end

function tag_resource(ResourceArn, Tags; aws_config::AbstractAWSConfig=current_aws_config())
    return partnercentral_account(
        "TagResource",
        Dict{String,Any}("ResourceArn" => ResourceArn, "Tags" => Tags);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function tag_resource(
    ResourceArn,
    Tags,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_account(
        "TagResource",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("ResourceArn" => ResourceArn, "Tags" => Tags),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    untag_resource(resource_arn, tag_keys)
    untag_resource(resource_arn, tag_keys, params::Dict{String,<:Any})

Removes specified tags from an AWS Partner Central Account resource.

# Arguments

- `resource_arn`: The Amazon Resource Name (ARN) of the resource to remove tags from.
- `tag_keys`: A list of tag keys to remove from the specified resource.
"""
function untag_resource end

function untag_resource(
    ResourceArn, TagKeys; aws_config::AbstractAWSConfig=current_aws_config()
)
    return partnercentral_account(
        "UntagResource",
        Dict{String,Any}("ResourceArn" => ResourceArn, "TagKeys" => TagKeys);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function untag_resource(
    ResourceArn,
    TagKeys,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_account(
        "UntagResource",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("ResourceArn" => ResourceArn, "TagKeys" => TagKeys),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_connection_preferences(access_type, catalog, revision)
    update_connection_preferences(access_type, catalog, revision, params::Dict{String,<:Any})

Updates the connection preferences for a partner account, modifying access settings and
exclusions.

# Arguments

- `access_type`: The access type setting for connections (e.g., open, restricted,
  invitation-only).
- `catalog`: The catalog identifier for the partner account.
- `revision`: The revision number of the connection preferences for optimistic locking.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"ExcludedParticipantIdentifiers"`: The updated list of participant identifiers to exclude
  from connections.
"""
function update_connection_preferences end

function update_connection_preferences(
    AccessType, Catalog, Revision; aws_config::AbstractAWSConfig=current_aws_config()
)
    return partnercentral_account(
        "UpdateConnectionPreferences",
        Dict{String,Any}(
            "AccessType" => AccessType, "Catalog" => Catalog, "Revision" => Revision
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_connection_preferences(
    AccessType,
    Catalog,
    Revision,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_account(
        "UpdateConnectionPreferences",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "AccessType" => AccessType, "Catalog" => Catalog, "Revision" => Revision
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end
