# This file is auto-generated by AWS.jl
# Note: `SERVICE_FEATURE_SET` is embedded by `@service`
using AWS: AbstractAWSConfig, current_aws_config, _merge
using AWS.AWSServices: compute_optimizer_automation
using AWS.UUIDs: uuid4

"""
    associate_accounts(account_ids)
    associate_accounts(account_ids, params::Dict{String,<:Any})

Associates one or more member accounts with your organization's management account, enabling
centralized implementation of optimization actions across those accounts. Once associated,
the management account (or a delegated administrator) can apply recommended actions to the
member account. When you associate a member account, its organization rule mode is
automatically set to "Any allowed," which permits the management account to create
Automation rules that automatically apply actions to that account. If the member account has
not previously enabled the Automation feature, the association process automatically enables
it.

!!! note
    Only the management account or a delegated administrator can perform this action.

# Arguments

- `account_ids`: The IDs of the member accounts to associate. You can specify up to 50
  account IDs.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"clientToken"`: A unique identifier to ensure idempotency of the request. Valid for 24
  hours after creation.
"""
function associate_accounts end

function associate_accounts(accountIds; aws_config::AbstractAWSConfig=current_aws_config())
    return compute_optimizer_automation(
        "AssociateAccounts",
        Dict{String,Any}("accountIds" => accountIds, "clientToken" => string(uuid4()));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function associate_accounts(
    accountIds,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return compute_optimizer_automation(
        "AssociateAccounts",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "accountIds" => accountIds, "clientToken" => string(uuid4())
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_automation_rule(name, recommended_action_types, rule_type, schedule, status)
    create_automation_rule(name, recommended_action_types, rule_type, schedule, status, params::Dict{String,<:Any})

Creates a new automation rule to apply recommended actions to resources based on specified
criteria.

# Arguments

- `name`: The name of the automation rule.

- `recommended_action_types`: The types of recommended actions this rule will automate.

- `rule_type`: The type of rule.

  !!! note
      Only the management account or a delegated administrator can set the ruleType to be
      OrganizationRule.

- `schedule`: The schedule for when the rule should run.

- `status`: The status of the rule

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"clientToken"`: A unique identifier to ensure idempotency of the request.

- `"criteria"`: A set of conditions that specify which recommended action qualify for
  implementation. When a rule is active and a recommended action matches these criteria,
  Compute Optimizer implements the action at the scheduled run time.

- `"description"`: A description of the automation rule.

- `"organizationConfiguration"`: Configuration for organization-level rules. Required for
  OrganizationRule type.

- `"priority"`: A string representation of a decimal number between 0 and 1 (having up to 30
  digits after the decimal point) that determines the priority of the rule. When multiple
  rules match the same recommended action, Compute Optimizer assigns the action to the rule
  with the lowest priority value (highest priority), even if that rule is scheduled to run
  later than other matching rules.

- `"tags"`: The tags to associate with the rule.
"""
function create_automation_rule end

function create_automation_rule(
    name,
    recommendedActionTypes,
    ruleType,
    schedule,
    status;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return compute_optimizer_automation(
        "CreateAutomationRule",
        Dict{String,Any}(
            "name" => name,
            "recommendedActionTypes" => recommendedActionTypes,
            "ruleType" => ruleType,
            "schedule" => schedule,
            "status" => status,
            "clientToken" => string(uuid4()),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_automation_rule(
    name,
    recommendedActionTypes,
    ruleType,
    schedule,
    status,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return compute_optimizer_automation(
        "CreateAutomationRule",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "name" => name,
                    "recommendedActionTypes" => recommendedActionTypes,
                    "ruleType" => ruleType,
                    "schedule" => schedule,
                    "status" => status,
                    "clientToken" => string(uuid4()),
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_automation_rule(rule_arn, rule_revision)
    delete_automation_rule(rule_arn, rule_revision, params::Dict{String,<:Any})

Deletes an existing automation rule.

# Arguments

- `rule_arn`: The ARN of the rule to delete.
- `rule_revision`: The revision number of the rule to delete.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"clientToken"`: A unique identifier to ensure idempotency of the request.
"""
function delete_automation_rule end

function delete_automation_rule(
    ruleArn, ruleRevision; aws_config::AbstractAWSConfig=current_aws_config()
)
    return compute_optimizer_automation(
        "DeleteAutomationRule",
        Dict{String,Any}(
            "ruleArn" => ruleArn,
            "ruleRevision" => ruleRevision,
            "clientToken" => string(uuid4()),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_automation_rule(
    ruleArn,
    ruleRevision,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return compute_optimizer_automation(
        "DeleteAutomationRule",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "ruleArn" => ruleArn,
                    "ruleRevision" => ruleRevision,
                    "clientToken" => string(uuid4()),
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    disassociate_accounts(account_ids)
    disassociate_accounts(account_ids, params::Dict{String,<:Any})

Disassociates member accounts from your organization's management account, removing
centralized automation capabilities. Once disassociated, organization rules no longer apply
to the member account, and the management account (or delegated administrator) cannot create
Automation rules for that account.

!!! note
    Only the management account or a delegated administrator can perform this action.

# Arguments

- `account_ids`: The IDs of the member accounts to disassociate.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"clientToken"`: A unique identifier to ensure idempotency of the request.
"""
function disassociate_accounts end

function disassociate_accounts(
    accountIds; aws_config::AbstractAWSConfig=current_aws_config()
)
    return compute_optimizer_automation(
        "DisassociateAccounts",
        Dict{String,Any}("accountIds" => accountIds, "clientToken" => string(uuid4()));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function disassociate_accounts(
    accountIds,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return compute_optimizer_automation(
        "DisassociateAccounts",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "accountIds" => accountIds, "clientToken" => string(uuid4())
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_automation_event(event_id)
    get_automation_event(event_id, params::Dict{String,<:Any})

Retrieves details about a specific automation event.

# Arguments

- `event_id`: The ID of the automation event to retrieve.
"""
function get_automation_event end

function get_automation_event(eventId; aws_config::AbstractAWSConfig=current_aws_config())
    return compute_optimizer_automation(
        "GetAutomationEvent",
        Dict{String,Any}("eventId" => eventId);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_automation_event(
    eventId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return compute_optimizer_automation(
        "GetAutomationEvent",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("eventId" => eventId), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_automation_rule(rule_arn)
    get_automation_rule(rule_arn, params::Dict{String,<:Any})

Retrieves details about a specific automation rule.

# Arguments

- `rule_arn`: The ARN of the rule to retrieve.
"""
function get_automation_rule end

function get_automation_rule(ruleArn; aws_config::AbstractAWSConfig=current_aws_config())
    return compute_optimizer_automation(
        "GetAutomationRule",
        Dict{String,Any}("ruleArn" => ruleArn);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_automation_rule(
    ruleArn,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return compute_optimizer_automation(
        "GetAutomationRule",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("ruleArn" => ruleArn), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_enrollment_configuration()
    get_enrollment_configuration(params::Dict{String,<:Any})

Retrieves the current enrollment configuration for Compute Optimizer Automation.
"""
function get_enrollment_configuration end

function get_enrollment_configuration(; aws_config::AbstractAWSConfig=current_aws_config())
    return compute_optimizer_automation(
        "GetEnrollmentConfiguration"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function get_enrollment_configuration(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return compute_optimizer_automation(
        "GetEnrollmentConfiguration", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    list_accounts()
    list_accounts(params::Dict{String,<:Any})

Lists the accounts in your organization that are enrolled in Compute Optimizer and whether
they have enabled Automation.

!!! note
    Only the management account or a delegated administrator can perform this action.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"maxResults"`: The maximum number of results to return in a single call.
- `"nextToken"`: The token for the next page of results.
"""
function list_accounts end

function list_accounts(; aws_config::AbstractAWSConfig=current_aws_config())
    return compute_optimizer_automation(
        "ListAccounts"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_accounts(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return compute_optimizer_automation(
        "ListAccounts", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    list_automation_event_steps(event_id)
    list_automation_event_steps(event_id, params::Dict{String,<:Any})

Lists the steps for a specific automation event. You can only list steps for events created
within the past year.

# Arguments

- `event_id`: The ID of the automation event.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"maxResults"`: The maximum number of automation event steps to return in a single
  response. Valid range is 1-1000.
- `"nextToken"`: A token used for pagination to retrieve the next set of results when the
  response is truncated.
"""
function list_automation_event_steps end

function list_automation_event_steps(
    eventId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return compute_optimizer_automation(
        "ListAutomationEventSteps",
        Dict{String,Any}("eventId" => eventId);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_automation_event_steps(
    eventId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return compute_optimizer_automation(
        "ListAutomationEventSteps",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("eventId" => eventId), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_automation_event_summaries()
    list_automation_event_summaries(params::Dict{String,<:Any})

Provides a summary of automation events based on specified filters. Only events created
within the past year will be included in the summary.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"endDateExclusive"`: The end date for filtering automation event summaries, exclusive.
  Events created before this date will be included.
- `"filters"`: The filters to apply to the list of automation event summaries.
- `"maxResults"`: The maximum number of automation event summaries to return in a single
  response. Valid range is 1-1000.
- `"nextToken"`: A token used for pagination to retrieve the next set of results when the
  response is truncated.
- `"startDateInclusive"`: The start date for filtering automation event summaries,
  inclusive. Events created on or after this date will be included.
"""
function list_automation_event_summaries end

function list_automation_event_summaries(;
    aws_config::AbstractAWSConfig=current_aws_config()
)
    return compute_optimizer_automation(
        "ListAutomationEventSummaries"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_automation_event_summaries(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return compute_optimizer_automation(
        "ListAutomationEventSummaries", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    list_automation_events()
    list_automation_events(params::Dict{String,<:Any})

Lists automation events based on specified filters. You can retrieve events that were
created within the past year.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"endTimeExclusive"`: The end of the time range to query for events.
- `"filters"`: The filters to apply to the list of automation events.
- `"maxResults"`: The maximum number of results to return in a single call.
- `"nextToken"`: The token for the next page of results.
- `"startTimeInclusive"`: The start of the time range to query for events.
"""
function list_automation_events end

function list_automation_events(; aws_config::AbstractAWSConfig=current_aws_config())
    return compute_optimizer_automation(
        "ListAutomationEvents"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_automation_events(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return compute_optimizer_automation(
        "ListAutomationEvents", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    list_automation_rule_preview(recommended_action_types, rule_type)
    list_automation_rule_preview(recommended_action_types, rule_type, params::Dict{String,<:Any})

Returns a preview of the recommended actions that match your Automation rule's configuration
and criteria.

# Arguments

- `recommended_action_types`: The types of recommended actions to include in the preview.

- `rule_type`: The type of rule.

  !!! note
      Only the management account or a delegated administrator can set the ruleType to be
      OrganizationRule.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"criteria"`: A set of conditions that specify which recommended action qualify for
  implementation. When a rule is active and a recommended action matches these criteria,
  Compute Optimizer implements the action at the scheduled run time.
- `"maxResults"`: The maximum number of automation rule preview results to return in a
  single response. Valid range is 1-1000.
- `"nextToken"`: A token used for pagination to retrieve the next set of results when the
  response is truncated.
- `"organizationScope"`: The organizational scope for the rule preview.
"""
function list_automation_rule_preview end

function list_automation_rule_preview(
    recommendedActionTypes, ruleType; aws_config::AbstractAWSConfig=current_aws_config()
)
    return compute_optimizer_automation(
        "ListAutomationRulePreview",
        Dict{String,Any}(
            "recommendedActionTypes" => recommendedActionTypes, "ruleType" => ruleType
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_automation_rule_preview(
    recommendedActionTypes,
    ruleType,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return compute_optimizer_automation(
        "ListAutomationRulePreview",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "recommendedActionTypes" => recommendedActionTypes,
                    "ruleType" => ruleType,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_automation_rule_preview_summaries(recommended_action_types, rule_type)
    list_automation_rule_preview_summaries(recommended_action_types, rule_type, params::Dict{String,<:Any})

Returns a summary of the recommended actions that match your rule preview configuration and
criteria.

# Arguments

- `recommended_action_types`: The types of recommended actions to include in the preview.
- `rule_type`: The type of rule.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"criteria"`:
- `"maxResults"`: The maximum number of automation rule preview summaries to return in a
  single response. Valid range is 1-1000.
- `"nextToken"`: A token used for pagination to retrieve the next set of results when the
  response is truncated.
- `"organizationScope"`: The organizational scope for the rule preview.
"""
function list_automation_rule_preview_summaries end

function list_automation_rule_preview_summaries(
    recommendedActionTypes, ruleType; aws_config::AbstractAWSConfig=current_aws_config()
)
    return compute_optimizer_automation(
        "ListAutomationRulePreviewSummaries",
        Dict{String,Any}(
            "recommendedActionTypes" => recommendedActionTypes, "ruleType" => ruleType
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_automation_rule_preview_summaries(
    recommendedActionTypes,
    ruleType,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return compute_optimizer_automation(
        "ListAutomationRulePreviewSummaries",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "recommendedActionTypes" => recommendedActionTypes,
                    "ruleType" => ruleType,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_automation_rules()
    list_automation_rules(params::Dict{String,<:Any})

Lists the automation rules that match specified filters.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"filters"`: The filters to apply to the list of automation rules.
- `"maxResults"`: The maximum number of automation rules to return in a single response.
  Valid range is 1-1000.
- `"nextToken"`: A token used for pagination to retrieve the next set of results when the
  response is truncated.
"""
function list_automation_rules end

function list_automation_rules(; aws_config::AbstractAWSConfig=current_aws_config())
    return compute_optimizer_automation(
        "ListAutomationRules"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_automation_rules(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return compute_optimizer_automation(
        "ListAutomationRules", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    list_recommended_action_summaries()
    list_recommended_action_summaries(params::Dict{String,<:Any})

Provides a summary of recommended actions based on specified filters.

!!! note
    Management accounts and delegated administrators can retrieve recommended actions that
    include associated member accounts. You can associate a member account using
    `AssociateAccounts`.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"filters"`: A list of filters to apply when retrieving recommended action summaries.
  Filters can be based on resource type, action type, account ID, and other criteria.
- `"maxResults"`: The maximum number of recommended action summaries to return in a single
  response. Valid range is 1-1000.
- `"nextToken"`: A token used for pagination to retrieve the next set of results when the
  response is truncated.
"""
function list_recommended_action_summaries end

function list_recommended_action_summaries(;
    aws_config::AbstractAWSConfig=current_aws_config()
)
    return compute_optimizer_automation(
        "ListRecommendedActionSummaries"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_recommended_action_summaries(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return compute_optimizer_automation(
        "ListRecommendedActionSummaries",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_recommended_actions()
    list_recommended_actions(params::Dict{String,<:Any})

Lists the recommended actions based that match specified filters.

!!! note
    Management accounts and delegated administrators can retrieve recommended actions that
    include associated member accounts. You can associate a member account using
    `AssociateAccounts`.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"filters"`: The filters to apply to the list of recommended actions.
- `"maxResults"`: The maximum number of recommended actions to return in a single response.
  Valid range is 1-1000.
- `"nextToken"`: A token used for pagination to retrieve the next set of results when the
  response is truncated.
"""
function list_recommended_actions end

function list_recommended_actions(; aws_config::AbstractAWSConfig=current_aws_config())
    return compute_optimizer_automation(
        "ListRecommendedActions"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_recommended_actions(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return compute_optimizer_automation(
        "ListRecommendedActions", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    list_tags_for_resource(resource_arn)
    list_tags_for_resource(resource_arn, params::Dict{String,<:Any})

Lists the tags for a specified resource.

# Arguments

- `resource_arn`: The ARN of the resource to list tags for.
"""
function list_tags_for_resource end

function list_tags_for_resource(
    resourceArn; aws_config::AbstractAWSConfig=current_aws_config()
)
    return compute_optimizer_automation(
        "ListTagsForResource",
        Dict{String,Any}("resourceArn" => resourceArn);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_tags_for_resource(
    resourceArn,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return compute_optimizer_automation(
        "ListTagsForResource",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("resourceArn" => resourceArn), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    rollback_automation_event(event_id)
    rollback_automation_event(event_id, params::Dict{String,<:Any})

Initiates a rollback for a completed automation event.

!!! note
    Management accounts and delegated administrators can only initiate a rollback for events
    belonging to associated member accounts. You can associate a member account using
    `AssociateAccounts`.

# Arguments

- `event_id`: The ID of the automation event to roll back.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"clientToken"`: A unique, case-sensitive identifier that you provide to ensure the
  idempotency of the request. Must be 1-64 characters long and contain only alphanumeric
  characters, underscores, and hyphens.
"""
function rollback_automation_event end

function rollback_automation_event(
    eventId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return compute_optimizer_automation(
        "RollbackAutomationEvent",
        Dict{String,Any}("eventId" => eventId, "clientToken" => string(uuid4()));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function rollback_automation_event(
    eventId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return compute_optimizer_automation(
        "RollbackAutomationEvent",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("eventId" => eventId, "clientToken" => string(uuid4())),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    start_automation_event(recommended_action_id)
    start_automation_event(recommended_action_id, params::Dict{String,<:Any})

Initiates a one-time, on-demand automation for the specified recommended action.

!!! note
    Management accounts and delegated administrators can only initiate recommended actions
    for associated member accounts. You can associate a member account using
    `AssociateAccounts`.

# Arguments

- `recommended_action_id`: The ID of the recommended action to automate.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"clientToken"`: A unique, case-sensitive identifier that you provide to ensure the
  idempotency of the request. Must be 1-64 characters long and contain only alphanumeric
  characters, underscores, and hyphens.
"""
function start_automation_event end

function start_automation_event(
    recommendedActionId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return compute_optimizer_automation(
        "StartAutomationEvent",
        Dict{String,Any}(
            "recommendedActionId" => recommendedActionId, "clientToken" => string(uuid4())
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function start_automation_event(
    recommendedActionId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return compute_optimizer_automation(
        "StartAutomationEvent",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "recommendedActionId" => recommendedActionId,
                    "clientToken" => string(uuid4()),
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    tag_resource(resource_arn, rule_revision, tags)
    tag_resource(resource_arn, rule_revision, tags, params::Dict{String,<:Any})

Adds tags to the specified resource.

# Arguments

- `resource_arn`: The ARN of the resource to tag.
- `rule_revision`: The revision number of the automation rule to tag. This ensures you're
  tagging the correct version of the rule.
- `tags`: The tags to add to the resource.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"clientToken"`: A unique, case-sensitive identifier that you provide to ensure the
  idempotency of the request. Must be 1-64 characters long and contain only alphanumeric
  characters, underscores, and hyphens.
"""
function tag_resource end

function tag_resource(
    resourceArn, ruleRevision, tags; aws_config::AbstractAWSConfig=current_aws_config()
)
    return compute_optimizer_automation(
        "TagResource",
        Dict{String,Any}(
            "resourceArn" => resourceArn,
            "ruleRevision" => ruleRevision,
            "tags" => tags,
            "clientToken" => string(uuid4()),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function tag_resource(
    resourceArn,
    ruleRevision,
    tags,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return compute_optimizer_automation(
        "TagResource",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "resourceArn" => resourceArn,
                    "ruleRevision" => ruleRevision,
                    "tags" => tags,
                    "clientToken" => string(uuid4()),
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    untag_resource(resource_arn, rule_revision, tag_keys)
    untag_resource(resource_arn, rule_revision, tag_keys, params::Dict{String,<:Any})

Removes tags from the specified resource.

# Arguments

- `resource_arn`: The ARN of the resource to untag.
- `rule_revision`: The revision number of the automation rule to untag. This ensures you're
  untagging the correct version of the rule.
- `tag_keys`: The keys of the tags to remove from the resource.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"clientToken"`: A unique, case-sensitive identifier that you provide to ensure the
  idempotency of the request. Must be 1-64 characters long and contain only alphanumeric
  characters, underscores, and hyphens.
"""
function untag_resource end

function untag_resource(
    resourceArn, ruleRevision, tagKeys; aws_config::AbstractAWSConfig=current_aws_config()
)
    return compute_optimizer_automation(
        "UntagResource",
        Dict{String,Any}(
            "resourceArn" => resourceArn,
            "ruleRevision" => ruleRevision,
            "tagKeys" => tagKeys,
            "clientToken" => string(uuid4()),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function untag_resource(
    resourceArn,
    ruleRevision,
    tagKeys,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return compute_optimizer_automation(
        "UntagResource",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "resourceArn" => resourceArn,
                    "ruleRevision" => ruleRevision,
                    "tagKeys" => tagKeys,
                    "clientToken" => string(uuid4()),
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_automation_rule(rule_arn, rule_revision)
    update_automation_rule(rule_arn, rule_revision, params::Dict{String,<:Any})

Updates an existing automation rule.

# Arguments

- `rule_arn`: The ARN of the rule to update.
- `rule_revision`: The revision number of the rule to update.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"clientToken"`: A unique, case-sensitive identifier that you provide to ensure the
  idempotency of the request. Must be 1-64 characters long and contain only alphanumeric
  characters, underscores, and hyphens.
- `"criteria"`:
- `"description"`: The updated description of the automation rule. Can be up to 1024
  characters long and contain alphanumeric characters, underscores, hyphens, spaces, and
  certain special characters.
- `"name"`: The updated name of the automation rule. Must be 1-128 characters long and
  contain only alphanumeric characters, underscores, and hyphens.
- `"organizationConfiguration"`: Updated configuration settings for organization-wide rules,
  including rule application order and target account IDs.
- `"priority"`: The updated priority level of the automation rule, used to determine
  execution order when multiple rules apply to the same resource.
- `"recommendedActionTypes"`: Updated list of recommended action types that this rule can
  execute, such as SnapshotAndDeleteUnattachedEbsVolume or UpgradeEbsVolumeType.
- `"ruleType"`: The updated type of automation rule. Can be either OrganizationRule for
  organization-wide rules or AccountRule for account-specific rules.
- `"schedule"`: The updated schedule configuration for when the automation rule should
  execute, including cron expression, timezone, and execution window.
- `"status"`: The updated status of the automation rule. Can be Active or Inactive.
"""
function update_automation_rule end

function update_automation_rule(
    ruleArn, ruleRevision; aws_config::AbstractAWSConfig=current_aws_config()
)
    return compute_optimizer_automation(
        "UpdateAutomationRule",
        Dict{String,Any}(
            "ruleArn" => ruleArn,
            "ruleRevision" => ruleRevision,
            "clientToken" => string(uuid4()),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_automation_rule(
    ruleArn,
    ruleRevision,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return compute_optimizer_automation(
        "UpdateAutomationRule",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "ruleArn" => ruleArn,
                    "ruleRevision" => ruleRevision,
                    "clientToken" => string(uuid4()),
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_enrollment_configuration(status)
    update_enrollment_configuration(status, params::Dict{String,<:Any})

Updates your account’s Compute Optimizer Automation enrollment configuration.

# Arguments

- `status`: The desired enrollment status.

  - Active - Enables the Automation feature for your account.
  - Inactive - Disables the Automation feature for your account and stops all of your
    automation rules. If you opt in again later, all rules will be inactive, and you must
    enable the rules you want to run. You must wait at least 24 hours after opting out to
    opt in again.

  !!! note
      The `Pending` and `Failed` options cannot be used to update the enrollment status of
      an account. They are returned in the response of a request to update the enrollment
      status of an account.

      If you are a member account, your account must be disassociated from your
      organization’s management account before you can disable Automation. Contact your
      administrator to make this change.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"clientToken"`: A unique, case-sensitive identifier that you provide to ensure the
  idempotency of the request. Must be 1-64 characters long and contain only alphanumeric
  characters, underscores, and hyphens.
"""
function update_enrollment_configuration end

function update_enrollment_configuration(
    status; aws_config::AbstractAWSConfig=current_aws_config()
)
    return compute_optimizer_automation(
        "UpdateEnrollmentConfiguration",
        Dict{String,Any}("status" => status, "clientToken" => string(uuid4()));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_enrollment_configuration(
    status, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return compute_optimizer_automation(
        "UpdateEnrollmentConfiguration",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("status" => status, "clientToken" => string(uuid4())),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end
