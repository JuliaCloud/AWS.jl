# This file is auto-generated by AWS.jl
# Note: `SERVICE_FEATURE_SET` is embedded by `@service`
using AWS: AbstractAWSConfig, current_aws_config, _merge
using AWS.AWSServices: rtbfabric
using AWS.UUIDs: uuid4

"""
    accept_link(gateway_id, link_id, log_settings)
    accept_link(gateway_id, link_id, log_settings, params::Dict{String,<:Any})

Accepts a link request between gateways.

When a requester gateway requests to link with a responder gateway, the responder can use
this operation to accept the link request and establish the connection.

# Arguments

- `gateway_id`: The unique identifier of the gateway.
- `link_id`: The unique identifier of the link.
- `log_settings`: Settings for the application logs.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"attributes"`: Attributes of the link.
- `"timeoutInMillis"`: The timeout value in milliseconds.
"""
function accept_link end

function accept_link(
    gatewayId, linkId, logSettings; aws_config::AbstractAWSConfig=current_aws_config()
)
    return rtbfabric(
        "POST",
        "/gateway/$(gatewayId)/link/$(linkId)/accept",
        Dict{String,Any}("logSettings" => logSettings);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function accept_link(
    gatewayId,
    linkId,
    logSettings,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return rtbfabric(
        "POST",
        "/gateway/$(gatewayId)/link/$(linkId)/accept",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("logSettings" => logSettings), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_inbound_external_link(client_token, gateway_id, log_settings)
    create_inbound_external_link(client_token, gateway_id, log_settings, params::Dict{String,<:Any})

Creates an inbound external link.

# Arguments

- `client_token`: The unique client token.
- `gateway_id`: The unique identifier of the gateway.
- `log_settings`: Settings for the application logs.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"attributes"`: Attributes of the link.
- `"tags"`: A map of the key-value pairs of the tag or tags to assign to the resource.
"""
function create_inbound_external_link end

function create_inbound_external_link(
    clientToken, gatewayId, logSettings; aws_config::AbstractAWSConfig=current_aws_config()
)
    return rtbfabric(
        "POST",
        "/responder-gateway/$(gatewayId)/inbound-external-link",
        Dict{String,Any}("clientToken" => clientToken, "logSettings" => logSettings);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_inbound_external_link(
    clientToken,
    gatewayId,
    logSettings,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return rtbfabric(
        "POST",
        "/responder-gateway/$(gatewayId)/inbound-external-link",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "clientToken" => clientToken, "logSettings" => logSettings
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_link(gateway_id, log_settings, peer_gateway_id)
    create_link(gateway_id, log_settings, peer_gateway_id, params::Dict{String,<:Any})

Creates a new link between gateways.

Establishes a connection that allows gateways to communicate and exchange bid requests and
responses.

# Arguments

- `gateway_id`: The unique identifier of the gateway.
- `log_settings`: Settings for the application logs.
- `peer_gateway_id`: The unique identifier of the peer gateway.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"attributes"`: Attributes of the link.
- `"httpResponderAllowed"`: Boolean to specify if an HTTP responder is allowed.
- `"tags"`: A map of the key-value pairs of the tag or tags to assign to the resource.
- `"timeoutInMillis"`: The timeout value in milliseconds.
"""
function create_link end

function create_link(
    gatewayId,
    logSettings,
    peerGatewayId;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return rtbfabric(
        "POST",
        "/gateway/$(gatewayId)/create-link",
        Dict{String,Any}("logSettings" => logSettings, "peerGatewayId" => peerGatewayId);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_link(
    gatewayId,
    logSettings,
    peerGatewayId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return rtbfabric(
        "POST",
        "/gateway/$(gatewayId)/create-link",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "logSettings" => logSettings, "peerGatewayId" => peerGatewayId
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_outbound_external_link(client_token, gateway_id, log_settings, public_endpoint)
    create_outbound_external_link(client_token, gateway_id, log_settings, public_endpoint, params::Dict{String,<:Any})

Creates an outbound external link.

# Arguments

- `client_token`: The unique client token.
- `gateway_id`: The unique identifier of the gateway.
- `log_settings`: Settings for the application logs.
- `public_endpoint`: The public endpoint of the link.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"attributes"`: Attributes of the link.
- `"tags"`: A map of the key-value pairs of the tag or tags to assign to the resource.
"""
function create_outbound_external_link end

function create_outbound_external_link(
    clientToken,
    gatewayId,
    logSettings,
    publicEndpoint;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return rtbfabric(
        "POST",
        "/requester-gateway/$(gatewayId)/outbound-external-link",
        Dict{String,Any}(
            "clientToken" => clientToken,
            "logSettings" => logSettings,
            "publicEndpoint" => publicEndpoint,
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_outbound_external_link(
    clientToken,
    gatewayId,
    logSettings,
    publicEndpoint,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return rtbfabric(
        "POST",
        "/requester-gateway/$(gatewayId)/outbound-external-link",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "clientToken" => clientToken,
                    "logSettings" => logSettings,
                    "publicEndpoint" => publicEndpoint,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_requester_gateway(client_token, security_group_ids, subnet_ids, vpc_id)
    create_requester_gateway(client_token, security_group_ids, subnet_ids, vpc_id, params::Dict{String,<:Any})

Creates a requester gateway.

# Arguments

- `client_token`: The unique client token.
- `security_group_ids`: The unique identifiers of the security groups.
- `subnet_ids`: The unique identifiers of the subnets.
- `vpc_id`: The unique identifier of the Virtual Private Cloud (VPC).

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"description"`: An optional description for the requester gateway.
- `"tags"`: A map of the key-value pairs of the tag or tags to assign to the resource.
"""
function create_requester_gateway end

function create_requester_gateway(
    clientToken,
    securityGroupIds,
    subnetIds,
    vpcId;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return rtbfabric(
        "POST",
        "/requester-gateway",
        Dict{String,Any}(
            "clientToken" => clientToken,
            "securityGroupIds" => securityGroupIds,
            "subnetIds" => subnetIds,
            "vpcId" => vpcId,
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_requester_gateway(
    clientToken,
    securityGroupIds,
    subnetIds,
    vpcId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return rtbfabric(
        "POST",
        "/requester-gateway",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "clientToken" => clientToken,
                    "securityGroupIds" => securityGroupIds,
                    "subnetIds" => subnetIds,
                    "vpcId" => vpcId,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_responder_gateway(client_token, port, protocol, security_group_ids, subnet_ids, vpc_id)
    create_responder_gateway(client_token, port, protocol, security_group_ids, subnet_ids, vpc_id, params::Dict{String,<:Any})

Creates a responder gateway.

!!! important
    A domain name or managed endpoint is required.

# Arguments

- `client_token`: The unique client token.
- `port`: The networking port to use.
- `protocol`: The networking protocol to use.
- `security_group_ids`: The unique identifiers of the security groups.
- `subnet_ids`: The unique identifiers of the subnets.
- `vpc_id`: The unique identifier of the Virtual Private Cloud (VPC).

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"description"`: An optional description for the responder gateway.
- `"domainName"`: The domain name for the responder gateway.
- `"gatewayType"`: The type of gateway. Valid values are `EXTERNAL` or `INTERNAL`.
- `"listenerConfig"`:
- `"managedEndpointConfiguration"`: The configuration for the managed endpoint.
- `"tags"`: A map of the key-value pairs of the tag or tags to assign to the resource.
- `"trustStoreConfiguration"`: The configuration of the trust store.
"""
function create_responder_gateway end

function create_responder_gateway(
    clientToken,
    port,
    protocol,
    securityGroupIds,
    subnetIds,
    vpcId;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return rtbfabric(
        "POST",
        "/responder-gateway",
        Dict{String,Any}(
            "clientToken" => clientToken,
            "port" => port,
            "protocol" => protocol,
            "securityGroupIds" => securityGroupIds,
            "subnetIds" => subnetIds,
            "vpcId" => vpcId,
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_responder_gateway(
    clientToken,
    port,
    protocol,
    securityGroupIds,
    subnetIds,
    vpcId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return rtbfabric(
        "POST",
        "/responder-gateway",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "clientToken" => clientToken,
                    "port" => port,
                    "protocol" => protocol,
                    "securityGroupIds" => securityGroupIds,
                    "subnetIds" => subnetIds,
                    "vpcId" => vpcId,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_inbound_external_link(gateway_id, link_id)
    delete_inbound_external_link(gateway_id, link_id, params::Dict{String,<:Any})

Deletes an inbound external link.

# Arguments

- `gateway_id`: The unique identifier of the gateway.
- `link_id`: The unique identifier of the link.
"""
function delete_inbound_external_link end

function delete_inbound_external_link(
    gatewayId, linkId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return rtbfabric(
        "DELETE",
        "/responder-gateway/$(gatewayId)/inbound-external-link/$(linkId)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_inbound_external_link(
    gatewayId,
    linkId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return rtbfabric(
        "DELETE",
        "/responder-gateway/$(gatewayId)/inbound-external-link/$(linkId)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_link(gateway_id, link_id)
    delete_link(gateway_id, link_id, params::Dict{String,<:Any})

Deletes a link between gateways.

Permanently removes the connection between gateways. This action cannot be undone.

# Arguments

- `gateway_id`: The unique identifier of the gateway.
- `link_id`: The unique identifier of the link.
"""
function delete_link end

function delete_link(gatewayId, linkId; aws_config::AbstractAWSConfig=current_aws_config())
    return rtbfabric(
        "DELETE",
        "/gateway/$(gatewayId)/link/$(linkId)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_link(
    gatewayId,
    linkId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return rtbfabric(
        "DELETE",
        "/gateway/$(gatewayId)/link/$(linkId)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_outbound_external_link(gateway_id, link_id)
    delete_outbound_external_link(gateway_id, link_id, params::Dict{String,<:Any})

Deletes an outbound external link.

# Arguments

- `gateway_id`: The unique identifier of the gateway.
- `link_id`: The unique identifier of the link.
"""
function delete_outbound_external_link end

function delete_outbound_external_link(
    gatewayId, linkId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return rtbfabric(
        "DELETE",
        "/requester-gateway/$(gatewayId)/outbound-external-link/$(linkId)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_outbound_external_link(
    gatewayId,
    linkId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return rtbfabric(
        "DELETE",
        "/requester-gateway/$(gatewayId)/outbound-external-link/$(linkId)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_requester_gateway(gateway_id)
    delete_requester_gateway(gateway_id, params::Dict{String,<:Any})

Deletes a requester gateway.

# Arguments

- `gateway_id`: The unique identifier of the gateway.
"""
function delete_requester_gateway end

function delete_requester_gateway(
    gatewayId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return rtbfabric(
        "DELETE",
        "/requester-gateway/$(gatewayId)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_requester_gateway(
    gatewayId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return rtbfabric(
        "DELETE",
        "/requester-gateway/$(gatewayId)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_responder_gateway(gateway_id)
    delete_responder_gateway(gateway_id, params::Dict{String,<:Any})

Deletes a responder gateway.

# Arguments

- `gateway_id`: The unique identifier of the gateway.
"""
function delete_responder_gateway end

function delete_responder_gateway(
    gatewayId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return rtbfabric(
        "DELETE",
        "/responder-gateway/$(gatewayId)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_responder_gateway(
    gatewayId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return rtbfabric(
        "DELETE",
        "/responder-gateway/$(gatewayId)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_inbound_external_link(gateway_id, link_id)
    get_inbound_external_link(gateway_id, link_id, params::Dict{String,<:Any})

Retrieves information about an inbound external link.

# Arguments

- `gateway_id`: The unique identifier of the gateway.
- `link_id`: The unique identifier of the link.
"""
function get_inbound_external_link end

function get_inbound_external_link(
    gatewayId, linkId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return rtbfabric(
        "GET",
        "/responder-gateway/$(gatewayId)/inbound-external-link/$(linkId)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_inbound_external_link(
    gatewayId,
    linkId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return rtbfabric(
        "GET",
        "/responder-gateway/$(gatewayId)/inbound-external-link/$(linkId)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_link(gateway_id, link_id)
    get_link(gateway_id, link_id, params::Dict{String,<:Any})

Retrieves information about a link between gateways.

Returns detailed information about the link configuration, status, and associated gateways.

# Arguments

- `gateway_id`: The unique identifier of the gateway.
- `link_id`: The unique identifier of the link.
"""
function get_link end

function get_link(gatewayId, linkId; aws_config::AbstractAWSConfig=current_aws_config())
    return rtbfabric(
        "GET",
        "/gateway/$(gatewayId)/link/$(linkId)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_link(
    gatewayId,
    linkId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return rtbfabric(
        "GET",
        "/gateway/$(gatewayId)/link/$(linkId)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_outbound_external_link(gateway_id, link_id)
    get_outbound_external_link(gateway_id, link_id, params::Dict{String,<:Any})

Retrieves information about an outbound external link.

# Arguments

- `gateway_id`: The unique identifier of the gateway.
- `link_id`: The unique identifier of the link.
"""
function get_outbound_external_link end

function get_outbound_external_link(
    gatewayId, linkId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return rtbfabric(
        "GET",
        "/requester-gateway/$(gatewayId)/outbound-external-link/$(linkId)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_outbound_external_link(
    gatewayId,
    linkId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return rtbfabric(
        "GET",
        "/requester-gateway/$(gatewayId)/outbound-external-link/$(linkId)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_requester_gateway(gateway_id)
    get_requester_gateway(gateway_id, params::Dict{String,<:Any})

Retrieves information about a requester gateway.

# Arguments

- `gateway_id`: The unique identifier of the gateway.
"""
function get_requester_gateway end

function get_requester_gateway(
    gatewayId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return rtbfabric(
        "GET",
        "/requester-gateway/$(gatewayId)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_requester_gateway(
    gatewayId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return rtbfabric(
        "GET",
        "/requester-gateway/$(gatewayId)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_responder_gateway(gateway_id)
    get_responder_gateway(gateway_id, params::Dict{String,<:Any})

Retrieves information about a responder gateway.

# Arguments

- `gateway_id`: The unique identifier of the gateway.
"""
function get_responder_gateway end

function get_responder_gateway(
    gatewayId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return rtbfabric(
        "GET",
        "/responder-gateway/$(gatewayId)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_responder_gateway(
    gatewayId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return rtbfabric(
        "GET",
        "/responder-gateway/$(gatewayId)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_links(gateway_id)
    list_links(gateway_id, params::Dict{String,<:Any})

Lists links associated with gateways.

Returns a list of all links for the specified gateways, including their status and
configuration details.

# Arguments

- `gateway_id`: The unique identifier of the gateway.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"maxResults"`: The maximum number of results that are returned per call. You can use
  `nextToken` to obtain further pages of results.

  This is only an upper limit. The actual number of results returned per call might be fewer
  than the specified maximum.

- `"nextToken"`: If `nextToken` is returned, there are more results available. The value of
  `nextToken` is a unique pagination token for each page. Make the call again using the
  returned token to retrieve the next page. Keep all other arguments unchanged. Each
  pagination token expires after 24 hours. Using an expired pagination token will return an
  *HTTP 400 InvalidToken error*.
"""
function list_links end

function list_links(gatewayId; aws_config::AbstractAWSConfig=current_aws_config())
    return rtbfabric(
        "GET", "/gateway/$(gatewayId)/links/"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_links(
    gatewayId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return rtbfabric(
        "GET",
        "/gateway/$(gatewayId)/links/",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_requester_gateways()
    list_requester_gateways(params::Dict{String,<:Any})

Lists requester gateways.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"maxResults"`: The maximum number of results that are returned per call. You can use
  `nextToken` to obtain further pages of results.

  This is only an upper limit. The actual number of results returned per call might be fewer
  than the specified maximum.

- `"nextToken"`: If `nextToken` is returned, there are more results available. The value of
  `nextToken` is a unique pagination token for each page. Make the call again using the
  returned token to retrieve the next page. Keep all other arguments unchanged. Each
  pagination token expires after 24 hours. Using an expired pagination token will return an
  *HTTP 400 InvalidToken error*.
"""
function list_requester_gateways end

function list_requester_gateways(; aws_config::AbstractAWSConfig=current_aws_config())
    return rtbfabric(
        "GET", "/requester-gateways"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_requester_gateways(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return rtbfabric(
        "GET", "/requester-gateways", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    list_responder_gateways()
    list_responder_gateways(params::Dict{String,<:Any})

Lists reponder gateways.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"maxResults"`: The maximum number of results that are returned per call. You can use
  `nextToken` to obtain further pages of results.

  This is only an upper limit. The actual number of results returned per call might be fewer
  than the specified maximum.

- `"nextToken"`: If `nextToken` is returned, there are more results available. The value of
  `nextToken` is a unique pagination token for each page. Make the call again using the
  returned token to retrieve the next page. Keep all other arguments unchanged. Each
  pagination token expires after 24 hours. Using an expired pagination token will return an
  *HTTP 400 InvalidToken error*.
"""
function list_responder_gateways end

function list_responder_gateways(; aws_config::AbstractAWSConfig=current_aws_config())
    return rtbfabric(
        "GET", "/responder-gateways"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_responder_gateways(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return rtbfabric(
        "GET", "/responder-gateways", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    list_tags_for_resource(resource_arn)
    list_tags_for_resource(resource_arn, params::Dict{String,<:Any})

Lists tags for a resource.

# Arguments

- `resource_arn`: The Amazon Resource Name (ARN) of the resource for which you want to
  retrieve tags.
"""
function list_tags_for_resource end

function list_tags_for_resource(
    resourceArn; aws_config::AbstractAWSConfig=current_aws_config()
)
    return rtbfabric(
        "GET", "/tags/$(resourceArn)"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_tags_for_resource(
    resourceArn,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return rtbfabric(
        "GET", "/tags/$(resourceArn)", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    reject_link(gateway_id, link_id)
    reject_link(gateway_id, link_id, params::Dict{String,<:Any})

Rejects a link request between gateways.

When a requester gateway requests to link with a responder gateway, the responder can use
this operation to decline the link request.

# Arguments

- `gateway_id`: The unique identifier of the gateway.
- `link_id`: The unique identifier of the link.
"""
function reject_link end

function reject_link(gatewayId, linkId; aws_config::AbstractAWSConfig=current_aws_config())
    return rtbfabric(
        "POST",
        "/gateway/$(gatewayId)/link/$(linkId)/reject";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function reject_link(
    gatewayId,
    linkId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return rtbfabric(
        "POST",
        "/gateway/$(gatewayId)/link/$(linkId)/reject",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    tag_resource(resource_arn, tags)
    tag_resource(resource_arn, tags, params::Dict{String,<:Any})

Assigns one or more tags (key-value pairs) to the specified resource.

# Arguments

- `resource_arn`: The Amazon Resource Name (ARN) of the resource that you want to tag.
- `tags`: A map of the key-value pairs of the tag or tags to assign to the resource.
"""
function tag_resource end

function tag_resource(resourceArn, tags; aws_config::AbstractAWSConfig=current_aws_config())
    return rtbfabric(
        "POST",
        "/tags/$(resourceArn)",
        Dict{String,Any}("tags" => tags);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function tag_resource(
    resourceArn,
    tags,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return rtbfabric(
        "POST",
        "/tags/$(resourceArn)",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("tags" => tags), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    untag_resource(resource_arn, tag_keys)
    untag_resource(resource_arn, tag_keys, params::Dict{String,<:Any})

Removes a tag or tags from a resource.

# Arguments

- `resource_arn`: The Amazon Resource Name (ARN) of the resource that you want to untag.
- `tag_keys`: The keys of the key-value pairs for the tag or tags you want to remove from
  the specified resource.
"""
function untag_resource end

function untag_resource(
    resourceArn, tagKeys; aws_config::AbstractAWSConfig=current_aws_config()
)
    return rtbfabric(
        "DELETE",
        "/tags/$(resourceArn)",
        Dict{String,Any}("tagKeys" => tagKeys);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function untag_resource(
    resourceArn,
    tagKeys,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return rtbfabric(
        "DELETE",
        "/tags/$(resourceArn)",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("tagKeys" => tagKeys), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_link(gateway_id, link_id)
    update_link(gateway_id, link_id, params::Dict{String,<:Any})

Updates the configuration of a link between gateways.

Allows you to modify settings and parameters for an existing link.

# Arguments

- `gateway_id`: The unique identifier of the gateway.
- `link_id`: The unique identifier of the link.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"logSettings"`: Settings for the application logs.
- `"timeoutInMillis"`: The timeout value in milliseconds.
"""
function update_link end

function update_link(gatewayId, linkId; aws_config::AbstractAWSConfig=current_aws_config())
    return rtbfabric(
        "PATCH",
        "/gateway/$(gatewayId)/link/$(linkId)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_link(
    gatewayId,
    linkId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return rtbfabric(
        "PATCH",
        "/gateway/$(gatewayId)/link/$(linkId)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_link_module_flow(client_token, gateway_id, link_id, modules)
    update_link_module_flow(client_token, gateway_id, link_id, modules, params::Dict{String,<:Any})

Updates a link module flow.

# Arguments

- `client_token`: The unique client token.
- `gateway_id`: The unique identifier of the gateway.
- `link_id`: The unique identifier of the link.
- `modules`: The configuration of a module.
"""
function update_link_module_flow end

function update_link_module_flow(
    clientToken,
    gatewayId,
    linkId,
    modules;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return rtbfabric(
        "POST",
        "/gateway/$(gatewayId)/link/$(linkId)/module-flow",
        Dict{String,Any}("clientToken" => clientToken, "modules" => modules);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_link_module_flow(
    clientToken,
    gatewayId,
    linkId,
    modules,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return rtbfabric(
        "POST",
        "/gateway/$(gatewayId)/link/$(linkId)/module-flow",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("clientToken" => clientToken, "modules" => modules),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_requester_gateway(client_token, gateway_id)
    update_requester_gateway(client_token, gateway_id, params::Dict{String,<:Any})

Updates a requester gateway.

# Arguments

- `client_token`: The unique client token.
- `gateway_id`: The unique identifier of the gateway.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"description"`: An optional description for the requester gateway.
"""
function update_requester_gateway end

function update_requester_gateway(
    clientToken, gatewayId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return rtbfabric(
        "POST",
        "/requester-gateway/$(gatewayId)/update",
        Dict{String,Any}("clientToken" => clientToken);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_requester_gateway(
    clientToken,
    gatewayId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return rtbfabric(
        "POST",
        "/requester-gateway/$(gatewayId)/update",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("clientToken" => clientToken), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_responder_gateway(client_token, gateway_id, port, protocol)
    update_responder_gateway(client_token, gateway_id, port, protocol, params::Dict{String,<:Any})

Updates a responder gateway.

# Arguments

- `client_token`: The unique client token.
- `gateway_id`: The unique identifier of the gateway.
- `port`: The networking port to use.
- `protocol`: The networking protocol to use.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"description"`: An optional description for the responder gateway.
- `"domainName"`: The domain name for the responder gateway.
- `"listenerConfig"`: The listener configuration for the responder gateway.
- `"managedEndpointConfiguration"`: The configuration for the managed endpoint.
- `"trustStoreConfiguration"`: The configuration of the trust store.
"""
function update_responder_gateway end

function update_responder_gateway(
    clientToken,
    gatewayId,
    port,
    protocol;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return rtbfabric(
        "POST",
        "/responder-gateway/$(gatewayId)/update",
        Dict{String,Any}(
            "clientToken" => clientToken, "port" => port, "protocol" => protocol
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_responder_gateway(
    clientToken,
    gatewayId,
    port,
    protocol,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return rtbfabric(
        "POST",
        "/responder-gateway/$(gatewayId)/update",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "clientToken" => clientToken, "port" => port, "protocol" => protocol
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end
