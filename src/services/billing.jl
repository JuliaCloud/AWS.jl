# This file is auto-generated by AWS.jl
# Note: `SERVICE_FEATURE_SET` is embedded by `@service`
using AWS: AbstractAWSConfig, current_aws_config, _merge
using AWS.AWSServices: billing
using AWS.UUIDs: uuid4

"""
    associate_source_views(arn, source_views)
    associate_source_views(arn, source_views, params::Dict{String,<:Any})

Associates one or more source billing views with an existing billing view. This allows
creating aggregate billing views that combine data from multiple sources.

# Arguments

- `arn`: The Amazon Resource Name (ARN) of the billing view to associate source views with.
- `source_views`: A list of ARNs of the source billing views to associate.
"""
function associate_source_views end

function associate_source_views(
    arn, sourceViews; aws_config::AbstractAWSConfig=current_aws_config()
)
    return billing(
        "AssociateSourceViews",
        Dict{String,Any}("arn" => arn, "sourceViews" => sourceViews);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function associate_source_views(
    arn,
    sourceViews,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return billing(
        "AssociateSourceViews",
        Dict{String,Any}(
            mergewith(
                _merge, Dict{String,Any}("arn" => arn, "sourceViews" => sourceViews), params
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_billing_view(name, source_views)
    create_billing_view(name, source_views, params::Dict{String,<:Any})

Creates a billing view with the specified billing view attributes.

# Arguments

- `name`: The name of the billing view.
- `source_views`: A list of billing views used as the data source for the custom billing
  view.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"X-Amzn-Client-Token"`: A unique, case-sensitive identifier you specify to ensure
  idempotency of the request. Idempotency ensures that an API request completes no more than
  one time. If the original request completes successfully, any subsequent retries complete
  successfully without performing any further actions with an idempotent request.
- `"dataFilterExpression"`: See [Expression](https://docs.aws.amazon.com/aws-cost-management/latest/APIReference/API_billing_Expression.html).
  Billing view only supports `LINKED_ACCOUNT`, `Tags`, and `CostCategories`.
- `"description"`: The description of the billing view.
- `"resourceTags"`: A list of key value map specifying tags associated to the billing view
  being created.
"""
function create_billing_view end

function create_billing_view(
    name, sourceViews; aws_config::AbstractAWSConfig=current_aws_config()
)
    return billing(
        "CreateBillingView",
        Dict{String,Any}(
            "name" => name,
            "sourceViews" => sourceViews,
            "X-Amzn-Client-Token" => string(uuid4()),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_billing_view(
    name,
    sourceViews,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return billing(
        "CreateBillingView",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "name" => name,
                    "sourceViews" => sourceViews,
                    "X-Amzn-Client-Token" => string(uuid4()),
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_billing_view(arn)
    delete_billing_view(arn, params::Dict{String,<:Any})

Deletes the specified billing view.

# Arguments

- `arn`: The Amazon Resource Name (ARN) that can be used to uniquely identify the billing
  view.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"force"`: If set to true, forces deletion of the billing view even if it has derived
  resources (e.g. other billing views or budgets). Use with caution as this may break
  dependent resources.
"""
function delete_billing_view end

function delete_billing_view(arn; aws_config::AbstractAWSConfig=current_aws_config())
    return billing(
        "DeleteBillingView",
        Dict{String,Any}("arn" => arn);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_billing_view(
    arn, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return billing(
        "DeleteBillingView",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("arn" => arn), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    disassociate_source_views(arn, source_views)
    disassociate_source_views(arn, source_views, params::Dict{String,<:Any})

Removes the association between one or more source billing views and an existing billing
view. This allows modifying the composition of aggregate billing views.

# Arguments

- `arn`: The Amazon Resource Name (ARN) of the billing view to disassociate source views
  from.
- `source_views`: A list of ARNs of the source billing views to disassociate.
"""
function disassociate_source_views end

function disassociate_source_views(
    arn, sourceViews; aws_config::AbstractAWSConfig=current_aws_config()
)
    return billing(
        "DisassociateSourceViews",
        Dict{String,Any}("arn" => arn, "sourceViews" => sourceViews);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function disassociate_source_views(
    arn,
    sourceViews,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return billing(
        "DisassociateSourceViews",
        Dict{String,Any}(
            mergewith(
                _merge, Dict{String,Any}("arn" => arn, "sourceViews" => sourceViews), params
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_billing_view(arn)
    get_billing_view(arn, params::Dict{String,<:Any})

Returns the metadata associated to the specified billing view ARN.

# Arguments

- `arn`: The Amazon Resource Name (ARN) that can be used to uniquely identify the billing
  view.
"""
function get_billing_view end

function get_billing_view(arn; aws_config::AbstractAWSConfig=current_aws_config())
    return billing(
        "GetBillingView",
        Dict{String,Any}("arn" => arn);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_billing_view(
    arn, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return billing(
        "GetBillingView",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("arn" => arn), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_resource_policy(resource_arn)
    get_resource_policy(resource_arn, params::Dict{String,<:Any})

Returns the resource-based policy document attached to the resource in `JSON` format.

# Arguments

- `resource_arn`: The Amazon Resource Name (ARN) of the billing view resource to which the
  policy is attached to.
"""
function get_resource_policy end

function get_resource_policy(
    resourceArn; aws_config::AbstractAWSConfig=current_aws_config()
)
    return billing(
        "GetResourcePolicy",
        Dict{String,Any}("resourceArn" => resourceArn);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_resource_policy(
    resourceArn,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return billing(
        "GetResourcePolicy",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("resourceArn" => resourceArn), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_billing_views()
    list_billing_views(params::Dict{String,<:Any})

Lists the billing views available for a given time period.

Every Amazon Web Services account has a unique `PRIMARY` billing view that represents the
billing data available by default. Accounts that use Billing Conductor also have
`BILLING_GROUP` billing views representing pro forma costs associated with each created
billing group.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"activeTimeRange"`: The time range for the billing views listed. `PRIMARY` billing view
  is always listed. `BILLING_GROUP` billing views are listed for time ranges when the
  associated billing group resource in Billing Conductor is active. The time range must be
  within one calendar month.
- `"arns"`: The Amazon Resource Name (ARN) that can be used to uniquely identify the billing
  view.
- `"billingViewTypes"`: The type of billing view.
- `"maxResults"`: The maximum number of billing views to retrieve. Default is 100.
- `"names"`: Filters the list of billing views by name. You can specify search criteria to
  match billing view names based on the search option provided.
- `"nextToken"`: The pagination token that is used on subsequent calls to list billing
  views.
- `"ownerAccountId"`: The list of owners of the billing view.
- `"sourceAccountId"`: Filters the results to include only billing views that use the
  specified account as a source.
"""
function list_billing_views end

function list_billing_views(; aws_config::AbstractAWSConfig=current_aws_config())
    return billing("ListBillingViews"; aws_config, feature_set=SERVICE_FEATURE_SET)
end

function list_billing_views(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return billing("ListBillingViews", params; aws_config, feature_set=SERVICE_FEATURE_SET)
end

"""
    list_source_views_for_billing_view(arn)
    list_source_views_for_billing_view(arn, params::Dict{String,<:Any})

Lists the source views (managed Amazon Web Services billing views) associated with the
billing view.

# Arguments

- `arn`: The Amazon Resource Name (ARN) that can be used to uniquely identify the billing
  view.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"maxResults"`: The number of entries a paginated response contains.
- `"nextToken"`: The pagination token that is used on subsequent calls to list billing
  views.
"""
function list_source_views_for_billing_view end

function list_source_views_for_billing_view(
    arn; aws_config::AbstractAWSConfig=current_aws_config()
)
    return billing(
        "ListSourceViewsForBillingView",
        Dict{String,Any}("arn" => arn);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_source_views_for_billing_view(
    arn, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return billing(
        "ListSourceViewsForBillingView",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("arn" => arn), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_tags_for_resource(resource_arn)
    list_tags_for_resource(resource_arn, params::Dict{String,<:Any})

Lists tags associated with the billing view resource.

# Arguments

- `resource_arn`: The Amazon Resource Name (ARN) of the resource.
"""
function list_tags_for_resource end

function list_tags_for_resource(
    resourceArn; aws_config::AbstractAWSConfig=current_aws_config()
)
    return billing(
        "ListTagsForResource",
        Dict{String,Any}("resourceArn" => resourceArn);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_tags_for_resource(
    resourceArn,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return billing(
        "ListTagsForResource",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("resourceArn" => resourceArn), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    tag_resource(resource_arn, resource_tags)
    tag_resource(resource_arn, resource_tags, params::Dict{String,<:Any})

An API operation for adding one or more tags (key-value pairs) to a resource.

# Arguments

- `resource_arn`: The Amazon Resource Name (ARN) of the resource.
- `resource_tags`: A list of tag key value pairs that are associated with the resource.
"""
function tag_resource end

function tag_resource(
    resourceArn, resourceTags; aws_config::AbstractAWSConfig=current_aws_config()
)
    return billing(
        "TagResource",
        Dict{String,Any}("resourceArn" => resourceArn, "resourceTags" => resourceTags);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function tag_resource(
    resourceArn,
    resourceTags,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return billing(
        "TagResource",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "resourceArn" => resourceArn, "resourceTags" => resourceTags
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    untag_resource(resource_arn, resource_tag_keys)
    untag_resource(resource_arn, resource_tag_keys, params::Dict{String,<:Any})

Removes one or more tags from a resource. Specify only tag keys in your request. Don't
specify the value.

# Arguments

- `resource_arn`: The Amazon Resource Name (ARN) of the resource.
- `resource_tag_keys`: A list of tag key value pairs that are associated with the resource.
"""
function untag_resource end

function untag_resource(
    resourceArn, resourceTagKeys; aws_config::AbstractAWSConfig=current_aws_config()
)
    return billing(
        "UntagResource",
        Dict{String,Any}(
            "resourceArn" => resourceArn, "resourceTagKeys" => resourceTagKeys
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function untag_resource(
    resourceArn,
    resourceTagKeys,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return billing(
        "UntagResource",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "resourceArn" => resourceArn, "resourceTagKeys" => resourceTagKeys
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_billing_view(arn)
    update_billing_view(arn, params::Dict{String,<:Any})

An API to update the attributes of the billing view.

# Arguments

- `arn`: The Amazon Resource Name (ARN) that can be used to uniquely identify the billing
  view.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"dataFilterExpression"`: See [Expression](https://docs.aws.amazon.com/aws-cost-management/latest/APIReference/API_billing_Expression.html).
  Billing view only supports `LINKED_ACCOUNT`, `Tags`, and `CostCategories`.
- `"description"`: The description of the billing view.
- `"name"`: The name of the billing view.
"""
function update_billing_view end

function update_billing_view(arn; aws_config::AbstractAWSConfig=current_aws_config())
    return billing(
        "UpdateBillingView",
        Dict{String,Any}("arn" => arn);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_billing_view(
    arn, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return billing(
        "UpdateBillingView",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("arn" => arn), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end
