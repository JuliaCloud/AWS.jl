# This file is auto-generated by AWS.jl
# Note: `SERVICE_FEATURE_SET` is embedded by `@service`
using AWS: AbstractAWSConfig, current_aws_config, _merge
using AWS.AWSServices: partnercentral_benefits
using AWS.UUIDs: uuid4

"""
    amend_benefit_application(amendment_reason, amendments, catalog, client_token, identifier, revision)
    amend_benefit_application(amendment_reason, amendments, catalog, client_token, identifier, revision, params::Dict{String,<:Any})

Modifies an existing benefit application by applying amendments to specific fields while
maintaining revision control.

# Arguments

- `amendment_reason`: A descriptive reason explaining why the benefit application is being
  amended.
- `amendments`: A list of specific field amendments to apply to the benefit application.
- `catalog`: The catalog identifier that specifies which benefit catalog the application
  belongs to.
- `client_token`: A unique, case-sensitive identifier to ensure idempotent processing of the
  amendment request.
- `identifier`: The unique identifier of the benefit application to be amended.
- `revision`: The current revision number of the benefit application to ensure optimistic
  concurrency control.
"""
function amend_benefit_application end

function amend_benefit_application(
    AmendmentReason,
    Amendments,
    Catalog,
    ClientToken,
    Identifier,
    Revision;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_benefits(
        "AmendBenefitApplication",
        Dict{String,Any}(
            "AmendmentReason" => AmendmentReason,
            "Amendments" => Amendments,
            "Catalog" => Catalog,
            "ClientToken" => ClientToken,
            "Identifier" => Identifier,
            "Revision" => Revision,
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function amend_benefit_application(
    AmendmentReason,
    Amendments,
    Catalog,
    ClientToken,
    Identifier,
    Revision,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_benefits(
        "AmendBenefitApplication",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "AmendmentReason" => AmendmentReason,
                    "Amendments" => Amendments,
                    "Catalog" => Catalog,
                    "ClientToken" => ClientToken,
                    "Identifier" => Identifier,
                    "Revision" => Revision,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    associate_benefit_application_resource(benefit_application_identifier, catalog, resource_arn)
    associate_benefit_application_resource(benefit_application_identifier, catalog, resource_arn, params::Dict{String,<:Any})

Links an AWS resource to an existing benefit application for tracking and management
purposes.

# Arguments

- `benefit_application_identifier`: The unique identifier of the benefit application to
  associate the resource with.
- `catalog`: The catalog identifier that specifies which benefit catalog the application
  belongs to.
- `resource_arn`: The Amazon Resource Name (ARN) of the AWS resource to associate with the
  benefit application.
"""
function associate_benefit_application_resource end

function associate_benefit_application_resource(
    BenefitApplicationIdentifier,
    Catalog,
    ResourceArn;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_benefits(
        "AssociateBenefitApplicationResource",
        Dict{String,Any}(
            "BenefitApplicationIdentifier" => BenefitApplicationIdentifier,
            "Catalog" => Catalog,
            "ResourceArn" => ResourceArn,
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function associate_benefit_application_resource(
    BenefitApplicationIdentifier,
    Catalog,
    ResourceArn,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_benefits(
        "AssociateBenefitApplicationResource",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "BenefitApplicationIdentifier" => BenefitApplicationIdentifier,
                    "Catalog" => Catalog,
                    "ResourceArn" => ResourceArn,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    cancel_benefit_application(catalog, client_token, identifier)
    cancel_benefit_application(catalog, client_token, identifier, params::Dict{String,<:Any})

Cancels a benefit application that is currently in progress, preventing further processing.

# Arguments

- `catalog`: The catalog identifier that specifies which benefit catalog the application
  belongs to.
- `client_token`: A unique, case-sensitive identifier to ensure idempotent processing of the
  cancellation request.
- `identifier`: The unique identifier of the benefit application to cancel.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Reason"`: A descriptive reason explaining why the benefit application is being
  cancelled.
"""
function cancel_benefit_application end

function cancel_benefit_application(
    Catalog, ClientToken, Identifier; aws_config::AbstractAWSConfig=current_aws_config()
)
    return partnercentral_benefits(
        "CancelBenefitApplication",
        Dict{String,Any}(
            "Catalog" => Catalog, "ClientToken" => ClientToken, "Identifier" => Identifier
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function cancel_benefit_application(
    Catalog,
    ClientToken,
    Identifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_benefits(
        "CancelBenefitApplication",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "Catalog" => Catalog,
                    "ClientToken" => ClientToken,
                    "Identifier" => Identifier,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_benefit_application(benefit_identifier, catalog, client_token)
    create_benefit_application(benefit_identifier, catalog, client_token, params::Dict{String,<:Any})

Creates a new benefit application for a partner to request access to AWS benefits and
programs.

# Arguments

- `benefit_identifier`: The unique identifier of the benefit being requested in this
  application.
- `catalog`: The catalog identifier that specifies which benefit catalog to create the
  application in.
- `client_token`: A unique, case-sensitive identifier to ensure idempotent processing of the
  creation request.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"AssociatedResources"`: AWS resources that are associated with this benefit application.
- `"BenefitApplicationDetails"`: Detailed information and requirements specific to the
  benefit being requested.
- `"Description"`: A detailed description of the benefit application and its intended use.
- `"FileDetails"`: Supporting documents and files attached to the benefit application.
- `"FulfillmentTypes"`: The types of fulfillment requested for this benefit application
  (e.g., credits, access, disbursement).
- `"Name"`: A human-readable name for the benefit application.
- `"PartnerContacts"`: Contact information for partner representatives responsible for this
  benefit application.
- `"Tags"`: Key-value pairs to categorize and organize the benefit application.
"""
function create_benefit_application end

function create_benefit_application(
    BenefitIdentifier,
    Catalog,
    ClientToken;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_benefits(
        "CreateBenefitApplication",
        Dict{String,Any}(
            "BenefitIdentifier" => BenefitIdentifier,
            "Catalog" => Catalog,
            "ClientToken" => ClientToken,
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_benefit_application(
    BenefitIdentifier,
    Catalog,
    ClientToken,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_benefits(
        "CreateBenefitApplication",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "BenefitIdentifier" => BenefitIdentifier,
                    "Catalog" => Catalog,
                    "ClientToken" => ClientToken,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    disassociate_benefit_application_resource(benefit_application_identifier, catalog, resource_arn)
    disassociate_benefit_application_resource(benefit_application_identifier, catalog, resource_arn, params::Dict{String,<:Any})

Removes the association between an AWS resource and a benefit application.

# Arguments

- `benefit_application_identifier`: The unique identifier of the benefit application to
  disassociate the resource from.
- `catalog`: The catalog identifier that specifies which benefit catalog the application
  belongs to.
- `resource_arn`: The Amazon Resource Name (ARN) of the AWS resource to disassociate from
  the benefit application.
"""
function disassociate_benefit_application_resource end

function disassociate_benefit_application_resource(
    BenefitApplicationIdentifier,
    Catalog,
    ResourceArn;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_benefits(
        "DisassociateBenefitApplicationResource",
        Dict{String,Any}(
            "BenefitApplicationIdentifier" => BenefitApplicationIdentifier,
            "Catalog" => Catalog,
            "ResourceArn" => ResourceArn,
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function disassociate_benefit_application_resource(
    BenefitApplicationIdentifier,
    Catalog,
    ResourceArn,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_benefits(
        "DisassociateBenefitApplicationResource",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "BenefitApplicationIdentifier" => BenefitApplicationIdentifier,
                    "Catalog" => Catalog,
                    "ResourceArn" => ResourceArn,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_benefit(catalog, identifier)
    get_benefit(catalog, identifier, params::Dict{String,<:Any})

Retrieves detailed information about a specific benefit available in the partner catalog.

# Arguments

- `catalog`: The catalog identifier that specifies which benefit catalog to query.
- `identifier`: The unique identifier of the benefit to retrieve.
"""
function get_benefit end

function get_benefit(
    Catalog, Identifier; aws_config::AbstractAWSConfig=current_aws_config()
)
    return partnercentral_benefits(
        "GetBenefit",
        Dict{String,Any}("Catalog" => Catalog, "Identifier" => Identifier);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_benefit(
    Catalog,
    Identifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_benefits(
        "GetBenefit",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("Catalog" => Catalog, "Identifier" => Identifier),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_benefit_allocation(catalog, identifier)
    get_benefit_allocation(catalog, identifier, params::Dict{String,<:Any})

Retrieves detailed information about a specific benefit allocation that has been granted to
a partner.

# Arguments

- `catalog`: The catalog identifier that specifies which benefit catalog to query.
- `identifier`: The unique identifier of the benefit allocation to retrieve.
"""
function get_benefit_allocation end

function get_benefit_allocation(
    Catalog, Identifier; aws_config::AbstractAWSConfig=current_aws_config()
)
    return partnercentral_benefits(
        "GetBenefitAllocation",
        Dict{String,Any}("Catalog" => Catalog, "Identifier" => Identifier);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_benefit_allocation(
    Catalog,
    Identifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_benefits(
        "GetBenefitAllocation",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("Catalog" => Catalog, "Identifier" => Identifier),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_benefit_application(catalog, identifier)
    get_benefit_application(catalog, identifier, params::Dict{String,<:Any})

Retrieves detailed information about a specific benefit application.

# Arguments

- `catalog`: The catalog identifier that specifies which benefit catalog to query.
- `identifier`: The unique identifier of the benefit application to retrieve.
"""
function get_benefit_application end

function get_benefit_application(
    Catalog, Identifier; aws_config::AbstractAWSConfig=current_aws_config()
)
    return partnercentral_benefits(
        "GetBenefitApplication",
        Dict{String,Any}("Catalog" => Catalog, "Identifier" => Identifier);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_benefit_application(
    Catalog,
    Identifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_benefits(
        "GetBenefitApplication",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("Catalog" => Catalog, "Identifier" => Identifier),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_benefit_allocations(catalog)
    list_benefit_allocations(catalog, params::Dict{String,<:Any})

Retrieves a paginated list of benefit allocations based on specified filter criteria.

# Arguments

- `catalog`: The catalog identifier to filter benefit allocations by catalog.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"BenefitApplicationIdentifiers"`: Filter benefit allocations by specific benefit
  application identifiers.
- `"BenefitIdentifiers"`: Filter benefit allocations by specific benefit identifiers.
- `"FulfillmentTypes"`: Filter benefit allocations by specific fulfillment types.
- `"MaxResults"`: The maximum number of benefit allocations to return in a single response.
- `"NextToken"`: A pagination token to retrieve the next set of results from a previous
  request.
- `"Status"`: Filter benefit allocations by their current status.
"""
function list_benefit_allocations end

function list_benefit_allocations(
    Catalog; aws_config::AbstractAWSConfig=current_aws_config()
)
    return partnercentral_benefits(
        "ListBenefitAllocations",
        Dict{String,Any}("Catalog" => Catalog);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_benefit_allocations(
    Catalog,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_benefits(
        "ListBenefitAllocations",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("Catalog" => Catalog), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_benefit_applications(catalog)
    list_benefit_applications(catalog, params::Dict{String,<:Any})

Retrieves a paginated list of benefit applications based on specified filter criteria.

# Arguments

- `catalog`: The catalog identifier to filter benefit applications by catalog.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"AssociatedResourceArns"`: Filter benefit applications by specific AWS resource ARNs.
- `"AssociatedResources"`: Filter benefit applications by associated AWS resources.
- `"BenefitIdentifiers"`: Filter benefit applications by specific benefit identifiers.
- `"FulfillmentTypes"`: Filter benefit applications by specific fulfillment types.
- `"MaxResults"`: The maximum number of benefit applications to return in a single response.
- `"NextToken"`: A pagination token to retrieve the next set of results from a previous
  request.
- `"Programs"`: Filter benefit applications by specific AWS partner programs.
- `"Stages"`: Filter benefit applications by their current processing stage.
- `"Status"`: Filter benefit applications by their current processing status.
"""
function list_benefit_applications end

function list_benefit_applications(
    Catalog; aws_config::AbstractAWSConfig=current_aws_config()
)
    return partnercentral_benefits(
        "ListBenefitApplications",
        Dict{String,Any}("Catalog" => Catalog);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_benefit_applications(
    Catalog,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_benefits(
        "ListBenefitApplications",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("Catalog" => Catalog), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_benefits(catalog)
    list_benefits(catalog, params::Dict{String,<:Any})

Retrieves a paginated list of available benefits based on specified filter criteria.

# Arguments

- `catalog`: The catalog identifier to filter benefits by catalog.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"FulfillmentTypes"`: Filter benefits by specific fulfillment types.
- `"MaxResults"`: The maximum number of benefits to return in a single response.
- `"NextToken"`: A pagination token to retrieve the next set of results from a previous
  request.
- `"Programs"`: Filter benefits by specific AWS partner programs.
- `"Status"`: Filter benefits by their current status.
"""
function list_benefits end

function list_benefits(Catalog; aws_config::AbstractAWSConfig=current_aws_config())
    return partnercentral_benefits(
        "ListBenefits",
        Dict{String,Any}("Catalog" => Catalog);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_benefits(
    Catalog,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_benefits(
        "ListBenefits",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("Catalog" => Catalog), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_tags_for_resource(resource_arn)
    list_tags_for_resource(resource_arn, params::Dict{String,<:Any})

Retrieves all tags associated with a specific resource.

# Arguments

- `resource_arn`: The Amazon Resource Name (ARN) of the resource to list tags for.
"""
function list_tags_for_resource end

function list_tags_for_resource(
    resourceArn; aws_config::AbstractAWSConfig=current_aws_config()
)
    return partnercentral_benefits(
        "ListTagsForResource",
        Dict{String,Any}("resourceArn" => resourceArn);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_tags_for_resource(
    resourceArn,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_benefits(
        "ListTagsForResource",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("resourceArn" => resourceArn), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    recall_benefit_application(catalog, identifier, reason)
    recall_benefit_application(catalog, identifier, reason, params::Dict{String,<:Any})

Recalls a submitted benefit application, returning it to draft status for further
modifications.

# Arguments

- `catalog`: The catalog identifier that specifies which benefit catalog the application
  belongs to.
- `identifier`: The unique identifier of the benefit application to recall.
- `reason`: A descriptive reason explaining why the benefit application is being recalled.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"ClientToken"`: A unique, case-sensitive identifier to ensure idempotent processing of
  the recall request.
"""
function recall_benefit_application end

function recall_benefit_application(
    Catalog, Identifier, Reason; aws_config::AbstractAWSConfig=current_aws_config()
)
    return partnercentral_benefits(
        "RecallBenefitApplication",
        Dict{String,Any}(
            "Catalog" => Catalog, "Identifier" => Identifier, "Reason" => Reason
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function recall_benefit_application(
    Catalog,
    Identifier,
    Reason,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_benefits(
        "RecallBenefitApplication",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "Catalog" => Catalog, "Identifier" => Identifier, "Reason" => Reason
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    submit_benefit_application(catalog, identifier)
    submit_benefit_application(catalog, identifier, params::Dict{String,<:Any})

Submits a benefit application for review and processing by AWS.

# Arguments

- `catalog`: The catalog identifier that specifies which benefit catalog the application
  belongs to.
- `identifier`: The unique identifier of the benefit application to submit.
"""
function submit_benefit_application end

function submit_benefit_application(
    Catalog, Identifier; aws_config::AbstractAWSConfig=current_aws_config()
)
    return partnercentral_benefits(
        "SubmitBenefitApplication",
        Dict{String,Any}("Catalog" => Catalog, "Identifier" => Identifier);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function submit_benefit_application(
    Catalog,
    Identifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_benefits(
        "SubmitBenefitApplication",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("Catalog" => Catalog, "Identifier" => Identifier),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    tag_resource(resource_arn, tags)
    tag_resource(resource_arn, tags, params::Dict{String,<:Any})

Adds or updates tags for a specified resource.

# Arguments

- `resource_arn`: The Amazon Resource Name (ARN) of the resource to add tags to.
- `tags`: A list of key-value pairs to add as tags to the resource.
"""
function tag_resource end

function tag_resource(resourceArn, tags; aws_config::AbstractAWSConfig=current_aws_config())
    return partnercentral_benefits(
        "TagResource",
        Dict{String,Any}("resourceArn" => resourceArn, "tags" => tags);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function tag_resource(
    resourceArn,
    tags,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_benefits(
        "TagResource",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("resourceArn" => resourceArn, "tags" => tags),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    untag_resource(resource_arn, tag_keys)
    untag_resource(resource_arn, tag_keys, params::Dict{String,<:Any})

Removes specified tags from a resource.

# Arguments

- `resource_arn`: The Amazon Resource Name (ARN) of the resource to remove tags from.
- `tag_keys`: A list of tag keys to remove from the resource.
"""
function untag_resource end

function untag_resource(
    resourceArn, tagKeys; aws_config::AbstractAWSConfig=current_aws_config()
)
    return partnercentral_benefits(
        "UntagResource",
        Dict{String,Any}("resourceArn" => resourceArn, "tagKeys" => tagKeys);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function untag_resource(
    resourceArn,
    tagKeys,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_benefits(
        "UntagResource",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("resourceArn" => resourceArn, "tagKeys" => tagKeys),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_benefit_application(catalog, client_token, identifier, revision)
    update_benefit_application(catalog, client_token, identifier, revision, params::Dict{String,<:Any})

Updates an existing benefit application with new information while maintaining revision
control.

# Arguments

- `catalog`: The catalog identifier that specifies which benefit catalog the application
  belongs to.
- `client_token`: A unique, case-sensitive identifier to ensure idempotent processing of the
  update request.
- `identifier`: The unique identifier of the benefit application to update.
- `revision`: The current revision number of the benefit application to ensure optimistic
  concurrency control.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"BenefitApplicationDetails"`: Updated detailed information and requirements specific to
  the benefit being requested.
- `"Description"`: The updated detailed description of the benefit application.
- `"FileDetails"`: Updated supporting documents and files attached to the benefit
  application.
- `"Name"`: The updated human-readable name for the benefit application.
- `"PartnerContacts"`: Updated contact information for partner representatives responsible
  for this benefit application.
"""
function update_benefit_application end

function update_benefit_application(
    Catalog,
    ClientToken,
    Identifier,
    Revision;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_benefits(
        "UpdateBenefitApplication",
        Dict{String,Any}(
            "Catalog" => Catalog,
            "ClientToken" => ClientToken,
            "Identifier" => Identifier,
            "Revision" => Revision,
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_benefit_application(
    Catalog,
    ClientToken,
    Identifier,
    Revision,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return partnercentral_benefits(
        "UpdateBenefitApplication",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "Catalog" => Catalog,
                    "ClientToken" => ClientToken,
                    "Identifier" => Identifier,
                    "Revision" => Revision,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end
