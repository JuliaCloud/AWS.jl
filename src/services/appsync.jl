# This file is auto-generated by AWSMetadata.jl
include("../AWSServices.jl")
using Compat
using .AWSServices: appsync

"""
    CreateApiCache()

Creates a cache for the GraphQL API.

Required Parameters
apiCachingBehavior => Caching behavior.    FULL_REQUEST_CACHING: All requests are fully cached.    PER_RESOLVER_CACHING: Individual resovlers that you specify are cached.  
apiId => The GraphQL API Id.
ttl => TTL in seconds for cache entries. Valid values are between 1 and 3600 seconds.
type => The cache instance type.    T2_SMALL: A t2.small instance type.    T2_MEDIUM: A t2.medium instance type.    R4_LARGE: A r4.large instance type.    R4_XLARGE: A r4.xlarge instance type.    R4_2XLARGE: A r4.2xlarge instance type.    R4_4XLARGE: A r4.4xlarge instance type.    R4_8XLARGE: A r4.8xlarge instance type.  

Optional Parameters
atRestEncryptionEnabled => At rest encryption flag for cache. This setting cannot be updated after creation.
transitEncryptionEnabled => Transit encryption flag when connecting to cache. This setting cannot be updated after creation.
"""
CreateApiCache(apiCachingBehavior, apiId, ttl, type) = appsync("POST", "/v1/apis/$(apiId)/ApiCaches", Dict{String, Any}("apiCachingBehavior"=>apiCachingBehavior, "ttl"=>ttl, "type"=>type))
CreateApiCache(apiCachingBehavior, apiId, ttl, type, args::AbstractDict{String, <: Any}) = appsync("POST", "/v1/apis/$(apiId)/ApiCaches", Dict{String, Any}("apiCachingBehavior"=>apiCachingBehavior, "ttl"=>ttl, "type"=>type, args...))
CreateApiCache(a...; b...) = CreateApiCache(a..., b)

"""
    CreateApiKey()

Creates a unique key that you can distribute to clients who are executing your API.

Required Parameters
apiId => The ID for your GraphQL API.

Optional Parameters
description => A description of the purpose of the API key.
expires => The time from creation time after which the API key expires. The date is represented as seconds since the epoch, rounded down to the nearest hour. The default value for this parameter is 7 days from creation time. For more information, see .
"""
CreateApiKey(apiId) = appsync("POST", "/v1/apis/$(apiId)/apikeys")
CreateApiKey(apiId, args::AbstractDict{String, <: Any}) = appsync("POST", "/v1/apis/$(apiId)/apikeys", args)
CreateApiKey(a...; b...) = CreateApiKey(a..., b)

"""
    CreateDataSource()

Creates a DataSource object.

Required Parameters
apiId => The API ID for the GraphQL API for the DataSource.
name => A user-supplied name for the DataSource.
type => The type of the DataSource.

Optional Parameters
description => A description of the DataSource.
dynamodbConfig => Amazon DynamoDB settings.
elasticsearchConfig => Amazon Elasticsearch Service settings.
httpConfig => HTTP endpoint settings.
lambdaConfig => AWS Lambda settings.
relationalDatabaseConfig => Relational database settings.
serviceRoleArn => The AWS IAM service role ARN for the data source. The system assumes this role when accessing the data source.
"""
CreateDataSource(apiId, name, type) = appsync("POST", "/v1/apis/$(apiId)/datasources", Dict{String, Any}("name"=>name, "type"=>type))
CreateDataSource(apiId, name, type, args::AbstractDict{String, <: Any}) = appsync("POST", "/v1/apis/$(apiId)/datasources", Dict{String, Any}("name"=>name, "type"=>type, args...))
CreateDataSource(a...; b...) = CreateDataSource(a..., b)

"""
    CreateFunction()

Creates a Function object. A function is a reusable entity. Multiple functions can be used to compose the resolver logic.

Required Parameters
apiId => The GraphQL API ID.
dataSourceName => The Function DataSource name.
functionVersion => The version of the request mapping template. Currently the supported value is 2018-05-29. 
name => The Function name. The function name does not have to be unique.
requestMappingTemplate => The Function request mapping template. Functions support only the 2018-05-29 version of the request mapping template.

Optional Parameters
description => The Function description.
responseMappingTemplate => The Function response mapping template. 
"""
CreateFunction(apiId, dataSourceName, functionVersion, name, requestMappingTemplate) = appsync("POST", "/v1/apis/$(apiId)/functions", Dict{String, Any}("dataSourceName"=>dataSourceName, "functionVersion"=>functionVersion, "name"=>name, "requestMappingTemplate"=>requestMappingTemplate))
CreateFunction(apiId, dataSourceName, functionVersion, name, requestMappingTemplate, args::AbstractDict{String, <: Any}) = appsync("POST", "/v1/apis/$(apiId)/functions", Dict{String, Any}("dataSourceName"=>dataSourceName, "functionVersion"=>functionVersion, "name"=>name, "requestMappingTemplate"=>requestMappingTemplate, args...))
CreateFunction(a...; b...) = CreateFunction(a..., b)

"""
    CreateGraphqlApi()

Creates a GraphqlApi object.

Required Parameters
authenticationType => The authentication type: API key, AWS IAM, OIDC, or Amazon Cognito user pools.
name => A user-supplied name for the GraphqlApi.

Optional Parameters
additionalAuthenticationProviders => A list of additional authentication providers for the GraphqlApi API.
logConfig => The Amazon CloudWatch Logs configuration.
openIDConnectConfig => The OpenID Connect configuration.
tags => A TagMap object.
userPoolConfig => The Amazon Cognito user pool configuration.
xrayEnabled => A flag indicating whether to enable X-Ray tracing for the GraphqlApi.
"""
CreateGraphqlApi(authenticationType, name) = appsync("POST", "/v1/apis", Dict{String, Any}("authenticationType"=>authenticationType, "name"=>name))
CreateGraphqlApi(authenticationType, name, args::AbstractDict{String, <: Any}) = appsync("POST", "/v1/apis", Dict{String, Any}("authenticationType"=>authenticationType, "name"=>name, args...))
CreateGraphqlApi(a...; b...) = CreateGraphqlApi(a..., b)

"""
    CreateResolver()

Creates a Resolver object. A resolver converts incoming requests into a format that a data source can understand and converts the data source's responses into GraphQL.

Required Parameters
apiId => The ID for the GraphQL API for which the resolver is being created.
fieldName => The name of the field to attach the resolver to.
requestMappingTemplate => The mapping template to be used for requests. A resolver uses a request mapping template to convert a GraphQL expression into a format that a data source can understand. Mapping templates are written in Apache Velocity Template Language (VTL).
typeName => The name of the Type.

Optional Parameters
cachingConfig => The caching configuration for the resolver.
dataSourceName => The name of the data source for which the resolver is being created.
kind => The resolver type.    UNIT: A UNIT resolver type. A UNIT resolver is the default resolver type. A UNIT resolver enables you to execute a GraphQL query against a single data source.    PIPELINE: A PIPELINE resolver type. A PIPELINE resolver enables you to execute a series of Function in a serial manner. You can use a pipeline resolver to execute a GraphQL query against multiple data sources.  
pipelineConfig => The PipelineConfig.
responseMappingTemplate => The mapping template to be used for responses from the data source.
syncConfig => The SyncConfig for a resolver attached to a versioned datasource.
"""
CreateResolver(apiId, fieldName, requestMappingTemplate, typeName) = appsync("POST", "/v1/apis/$(apiId)/types/$(typeName)/resolvers", Dict{String, Any}("fieldName"=>fieldName, "requestMappingTemplate"=>requestMappingTemplate))
CreateResolver(apiId, fieldName, requestMappingTemplate, typeName, args::AbstractDict{String, <: Any}) = appsync("POST", "/v1/apis/$(apiId)/types/$(typeName)/resolvers", Dict{String, Any}("fieldName"=>fieldName, "requestMappingTemplate"=>requestMappingTemplate, args...))
CreateResolver(a...; b...) = CreateResolver(a..., b)

"""
    CreateType()

Creates a Type object.

Required Parameters
apiId => The API ID.
definition => The type definition, in GraphQL Schema Definition Language (SDL) format. For more information, see the GraphQL SDL documentation.
format => The type format: SDL or JSON.

"""
CreateType(apiId, definition, format) = appsync("POST", "/v1/apis/$(apiId)/types", Dict{String, Any}("definition"=>definition, "format"=>format))
CreateType(apiId, definition, format, args::AbstractDict{String, <: Any}) = appsync("POST", "/v1/apis/$(apiId)/types", Dict{String, Any}("definition"=>definition, "format"=>format, args...))
CreateType(a...; b...) = CreateType(a..., b)

"""
    DeleteApiCache()

Deletes an ApiCache object.

Required Parameters
apiId => The API ID.

"""
DeleteApiCache(apiId) = appsync("DELETE", "/v1/apis/$(apiId)/ApiCaches")
DeleteApiCache(apiId, args::AbstractDict{String, <: Any}) = appsync("DELETE", "/v1/apis/$(apiId)/ApiCaches", args)
DeleteApiCache(a...; b...) = DeleteApiCache(a..., b)

"""
    DeleteApiKey()

Deletes an API key.

Required Parameters
apiId => The API ID.
id => The ID for the API key.

"""
DeleteApiKey(apiId, id) = appsync("DELETE", "/v1/apis/$(apiId)/apikeys/$(id)")
DeleteApiKey(apiId, id, args::AbstractDict{String, <: Any}) = appsync("DELETE", "/v1/apis/$(apiId)/apikeys/$(id)", args)
DeleteApiKey(a...; b...) = DeleteApiKey(a..., b)

"""
    DeleteDataSource()

Deletes a DataSource object.

Required Parameters
apiId => The API ID.
name => The name of the data source.

"""
DeleteDataSource(apiId, name) = appsync("DELETE", "/v1/apis/$(apiId)/datasources/$(name)")
DeleteDataSource(apiId, name, args::AbstractDict{String, <: Any}) = appsync("DELETE", "/v1/apis/$(apiId)/datasources/$(name)", args)
DeleteDataSource(a...; b...) = DeleteDataSource(a..., b)

"""
    DeleteFunction()

Deletes a Function.

Required Parameters
apiId => The GraphQL API ID.
functionId => The Function ID.

"""
DeleteFunction(apiId, functionId) = appsync("DELETE", "/v1/apis/$(apiId)/functions/$(functionId)")
DeleteFunction(apiId, functionId, args::AbstractDict{String, <: Any}) = appsync("DELETE", "/v1/apis/$(apiId)/functions/$(functionId)", args)
DeleteFunction(a...; b...) = DeleteFunction(a..., b)

"""
    DeleteGraphqlApi()

Deletes a GraphqlApi object.

Required Parameters
apiId => The API ID.

"""
DeleteGraphqlApi(apiId) = appsync("DELETE", "/v1/apis/$(apiId)")
DeleteGraphqlApi(apiId, args::AbstractDict{String, <: Any}) = appsync("DELETE", "/v1/apis/$(apiId)", args)
DeleteGraphqlApi(a...; b...) = DeleteGraphqlApi(a..., b)

"""
    DeleteResolver()

Deletes a Resolver object.

Required Parameters
apiId => The API ID.
fieldName => The resolver field name.
typeName => The name of the resolver type.

"""
DeleteResolver(apiId, fieldName, typeName) = appsync("DELETE", "/v1/apis/$(apiId)/types/$(typeName)/resolvers/$(fieldName)")
DeleteResolver(apiId, fieldName, typeName, args::AbstractDict{String, <: Any}) = appsync("DELETE", "/v1/apis/$(apiId)/types/$(typeName)/resolvers/$(fieldName)", args)
DeleteResolver(a...; b...) = DeleteResolver(a..., b)

"""
    DeleteType()

Deletes a Type object.

Required Parameters
apiId => The API ID.
typeName => The type name.

"""
DeleteType(apiId, typeName) = appsync("DELETE", "/v1/apis/$(apiId)/types/$(typeName)")
DeleteType(apiId, typeName, args::AbstractDict{String, <: Any}) = appsync("DELETE", "/v1/apis/$(apiId)/types/$(typeName)", args)
DeleteType(a...; b...) = DeleteType(a..., b)

"""
    FlushApiCache()

Flushes an ApiCache object.

Required Parameters
apiId => The API ID.

"""
FlushApiCache(apiId) = appsync("DELETE", "/v1/apis/$(apiId)/FlushCache")
FlushApiCache(apiId, args::AbstractDict{String, <: Any}) = appsync("DELETE", "/v1/apis/$(apiId)/FlushCache", args)
FlushApiCache(a...; b...) = FlushApiCache(a..., b)

"""
    GetApiCache()

Retrieves an ApiCache object.

Required Parameters
apiId => The API ID.

"""
GetApiCache(apiId) = appsync("GET", "/v1/apis/$(apiId)/ApiCaches")
GetApiCache(apiId, args::AbstractDict{String, <: Any}) = appsync("GET", "/v1/apis/$(apiId)/ApiCaches", args)
GetApiCache(a...; b...) = GetApiCache(a..., b)

"""
    GetDataSource()

Retrieves a DataSource object.

Required Parameters
apiId => The API ID.
name => The name of the data source.

"""
GetDataSource(apiId, name) = appsync("GET", "/v1/apis/$(apiId)/datasources/$(name)")
GetDataSource(apiId, name, args::AbstractDict{String, <: Any}) = appsync("GET", "/v1/apis/$(apiId)/datasources/$(name)", args)
GetDataSource(a...; b...) = GetDataSource(a..., b)

"""
    GetFunction()

Get a Function.

Required Parameters
apiId => The GraphQL API ID.
functionId => The Function ID.

"""
GetFunction(apiId, functionId) = appsync("GET", "/v1/apis/$(apiId)/functions/$(functionId)")
GetFunction(apiId, functionId, args::AbstractDict{String, <: Any}) = appsync("GET", "/v1/apis/$(apiId)/functions/$(functionId)", args)
GetFunction(a...; b...) = GetFunction(a..., b)

"""
    GetGraphqlApi()

Retrieves a GraphqlApi object.

Required Parameters
apiId => The API ID for the GraphQL API.

"""
GetGraphqlApi(apiId) = appsync("GET", "/v1/apis/$(apiId)")
GetGraphqlApi(apiId, args::AbstractDict{String, <: Any}) = appsync("GET", "/v1/apis/$(apiId)", args)
GetGraphqlApi(a...; b...) = GetGraphqlApi(a..., b)

"""
    GetIntrospectionSchema()

Retrieves the introspection schema for a GraphQL API.

Required Parameters
apiId => The API ID.
format => The schema format: SDL or JSON.

Optional Parameters
includeDirectives => A flag that specifies whether the schema introspection should contain directives.
"""
GetIntrospectionSchema(apiId, format) = appsync("GET", "/v1/apis/$(apiId)/schema", Dict{String, Any}("format"=>format))
GetIntrospectionSchema(apiId, format, args::AbstractDict{String, <: Any}) = appsync("GET", "/v1/apis/$(apiId)/schema", Dict{String, Any}("format"=>format, args...))
GetIntrospectionSchema(a...; b...) = GetIntrospectionSchema(a..., b)

"""
    GetResolver()

Retrieves a Resolver object.

Required Parameters
apiId => The API ID.
fieldName => The resolver field name.
typeName => The resolver type name.

"""
GetResolver(apiId, fieldName, typeName) = appsync("GET", "/v1/apis/$(apiId)/types/$(typeName)/resolvers/$(fieldName)")
GetResolver(apiId, fieldName, typeName, args::AbstractDict{String, <: Any}) = appsync("GET", "/v1/apis/$(apiId)/types/$(typeName)/resolvers/$(fieldName)", args)
GetResolver(a...; b...) = GetResolver(a..., b)

"""
    GetSchemaCreationStatus()

Retrieves the current status of a schema creation operation.

Required Parameters
apiId => The API ID.

"""
GetSchemaCreationStatus(apiId) = appsync("GET", "/v1/apis/$(apiId)/schemacreation")
GetSchemaCreationStatus(apiId, args::AbstractDict{String, <: Any}) = appsync("GET", "/v1/apis/$(apiId)/schemacreation", args)
GetSchemaCreationStatus(a...; b...) = GetSchemaCreationStatus(a..., b)

"""
    GetType()

Retrieves a Type object.

Required Parameters
apiId => The API ID.
format => The type format: SDL or JSON.
typeName => The type name.

"""
GetType(apiId, format, typeName) = appsync("GET", "/v1/apis/$(apiId)/types/$(typeName)", Dict{String, Any}("format"=>format))
GetType(apiId, format, typeName, args::AbstractDict{String, <: Any}) = appsync("GET", "/v1/apis/$(apiId)/types/$(typeName)", Dict{String, Any}("format"=>format, args...))
GetType(a...; b...) = GetType(a..., b)

"""
    ListApiKeys()

Lists the API keys for a given API.  API keys are deleted automatically sometime after they expire. However, they may still be included in the response until they have actually been deleted. You can safely call DeleteApiKey to manually delete a key before it's automatically deleted. 

Required Parameters
apiId => The API ID.

Optional Parameters
maxResults => The maximum number of results you want the request to return.
nextToken => An identifier that was returned from the previous call to this operation, which can be used to return the next set of items in the list.
"""
ListApiKeys(apiId) = appsync("GET", "/v1/apis/$(apiId)/apikeys")
ListApiKeys(apiId, args::AbstractDict{String, <: Any}) = appsync("GET", "/v1/apis/$(apiId)/apikeys", args)
ListApiKeys(a...; b...) = ListApiKeys(a..., b)

"""
    ListDataSources()

Lists the data sources for a given API.

Required Parameters
apiId => The API ID.

Optional Parameters
maxResults => The maximum number of results you want the request to return.
nextToken => An identifier that was returned from the previous call to this operation, which can be used to return the next set of items in the list. 
"""
ListDataSources(apiId) = appsync("GET", "/v1/apis/$(apiId)/datasources")
ListDataSources(apiId, args::AbstractDict{String, <: Any}) = appsync("GET", "/v1/apis/$(apiId)/datasources", args)
ListDataSources(a...; b...) = ListDataSources(a..., b)

"""
    ListFunctions()

List multiple functions.

Required Parameters
apiId => The GraphQL API ID.

Optional Parameters
maxResults => The maximum number of results you want the request to return.
nextToken => An identifier that was returned from the previous call to this operation, which can be used to return the next set of items in the list.
"""
ListFunctions(apiId) = appsync("GET", "/v1/apis/$(apiId)/functions")
ListFunctions(apiId, args::AbstractDict{String, <: Any}) = appsync("GET", "/v1/apis/$(apiId)/functions", args)
ListFunctions(a...; b...) = ListFunctions(a..., b)

"""
    ListGraphqlApis()

Lists your GraphQL APIs.

Optional Parameters
maxResults => The maximum number of results you want the request to return.
nextToken => An identifier that was returned from the previous call to this operation, which can be used to return the next set of items in the list. 
"""
ListGraphqlApis() = appsync("GET", "/v1/apis")
ListGraphqlApis(args::AbstractDict{String, Any}) = appsync("GET", "/v1/apis", args)
ListGraphqlApis(a...; b...) = ListGraphqlApis(a..., b)

"""
    ListResolvers()

Lists the resolvers for a given API and type.

Required Parameters
apiId => The API ID.
typeName => The type name.

Optional Parameters
maxResults => The maximum number of results you want the request to return.
nextToken => An identifier that was returned from the previous call to this operation, which can be used to return the next set of items in the list. 
"""
ListResolvers(apiId, typeName) = appsync("GET", "/v1/apis/$(apiId)/types/$(typeName)/resolvers")
ListResolvers(apiId, typeName, args::AbstractDict{String, <: Any}) = appsync("GET", "/v1/apis/$(apiId)/types/$(typeName)/resolvers", args)
ListResolvers(a...; b...) = ListResolvers(a..., b)

"""
    ListResolversByFunction()

List the resolvers that are associated with a specific function.

Required Parameters
apiId => The API ID.
functionId => The Function ID.

Optional Parameters
maxResults => The maximum number of results you want the request to return.
nextToken => An identifier that was returned from the previous call to this operation, which you can use to return the next set of items in the list.
"""
ListResolversByFunction(apiId, functionId) = appsync("GET", "/v1/apis/$(apiId)/functions/$(functionId)/resolvers")
ListResolversByFunction(apiId, functionId, args::AbstractDict{String, <: Any}) = appsync("GET", "/v1/apis/$(apiId)/functions/$(functionId)/resolvers", args)
ListResolversByFunction(a...; b...) = ListResolversByFunction(a..., b)

"""
    ListTagsForResource()

Lists the tags for a resource.

Required Parameters
resourceArn => The GraphqlApi ARN.

"""
ListTagsForResource(resourceArn) = appsync("GET", "/v1/tags/$(resourceArn)")
ListTagsForResource(resourceArn, args::AbstractDict{String, <: Any}) = appsync("GET", "/v1/tags/$(resourceArn)", args)
ListTagsForResource(a...; b...) = ListTagsForResource(a..., b)

"""
    ListTypes()

Lists the types for a given API.

Required Parameters
apiId => The API ID.
format => The type format: SDL or JSON.

Optional Parameters
maxResults => The maximum number of results you want the request to return.
nextToken => An identifier that was returned from the previous call to this operation, which can be used to return the next set of items in the list. 
"""
ListTypes(apiId, format) = appsync("GET", "/v1/apis/$(apiId)/types", Dict{String, Any}("format"=>format))
ListTypes(apiId, format, args::AbstractDict{String, <: Any}) = appsync("GET", "/v1/apis/$(apiId)/types", Dict{String, Any}("format"=>format, args...))
ListTypes(a...; b...) = ListTypes(a..., b)

"""
    StartSchemaCreation()

Adds a new schema to your GraphQL API. This operation is asynchronous. Use to determine when it has completed.

Required Parameters
apiId => The API ID.
definition => The schema definition, in GraphQL schema language format.

"""
StartSchemaCreation(apiId, definition) = appsync("POST", "/v1/apis/$(apiId)/schemacreation", Dict{String, Any}("definition"=>definition))
StartSchemaCreation(apiId, definition, args::AbstractDict{String, <: Any}) = appsync("POST", "/v1/apis/$(apiId)/schemacreation", Dict{String, Any}("definition"=>definition, args...))
StartSchemaCreation(a...; b...) = StartSchemaCreation(a..., b)

"""
    TagResource()

Tags a resource with user-supplied tags.

Required Parameters
resourceArn => The GraphqlApi ARN.
tags => A TagMap object.

"""
TagResource(resourceArn, tags) = appsync("POST", "/v1/tags/$(resourceArn)", Dict{String, Any}("tags"=>tags))
TagResource(resourceArn, tags, args::AbstractDict{String, <: Any}) = appsync("POST", "/v1/tags/$(resourceArn)", Dict{String, Any}("tags"=>tags, args...))
TagResource(a...; b...) = TagResource(a..., b)

"""
    UntagResource()

Untags a resource.

Required Parameters
resourceArn => The GraphqlApi ARN.
tagKeys => A list of TagKey objects.

"""
UntagResource(resourceArn, tagKeys) = appsync("DELETE", "/v1/tags/$(resourceArn)", Dict{String, Any}("tagKeys"=>tagKeys))
UntagResource(resourceArn, tagKeys, args::AbstractDict{String, <: Any}) = appsync("DELETE", "/v1/tags/$(resourceArn)", Dict{String, Any}("tagKeys"=>tagKeys, args...))
UntagResource(a...; b...) = UntagResource(a..., b)

"""
    UpdateApiCache()

Updates the cache for the GraphQL API.

Required Parameters
apiCachingBehavior => Caching behavior.    FULL_REQUEST_CACHING: All requests are fully cached.    PER_RESOLVER_CACHING: Individual resovlers that you specify are cached.  
apiId => The GraphQL API Id.
ttl => TTL in seconds for cache entries. Valid values are between 1 and 3600 seconds.
type => The cache instance type.    T2_SMALL: A t2.small instance type.    T2_MEDIUM: A t2.medium instance type.    R4_LARGE: A r4.large instance type.    R4_XLARGE: A r4.xlarge instance type.    R4_2XLARGE: A r4.2xlarge instance type.    R4_4XLARGE: A r4.4xlarge instance type.    R4_8XLARGE: A r4.8xlarge instance type.  

"""
UpdateApiCache(apiCachingBehavior, apiId, ttl, type) = appsync("POST", "/v1/apis/$(apiId)/ApiCaches/update", Dict{String, Any}("apiCachingBehavior"=>apiCachingBehavior, "ttl"=>ttl, "type"=>type))
UpdateApiCache(apiCachingBehavior, apiId, ttl, type, args::AbstractDict{String, <: Any}) = appsync("POST", "/v1/apis/$(apiId)/ApiCaches/update", Dict{String, Any}("apiCachingBehavior"=>apiCachingBehavior, "ttl"=>ttl, "type"=>type, args...))
UpdateApiCache(a...; b...) = UpdateApiCache(a..., b)

"""
    UpdateApiKey()

Updates an API key.

Required Parameters
apiId => The ID for the GraphQL API.
id => The API key ID.

Optional Parameters
description => A description of the purpose of the API key.
expires => The time from update time after which the API key expires. The date is represented as seconds since the epoch. For more information, see .
"""
UpdateApiKey(apiId, id) = appsync("POST", "/v1/apis/$(apiId)/apikeys/$(id)")
UpdateApiKey(apiId, id, args::AbstractDict{String, <: Any}) = appsync("POST", "/v1/apis/$(apiId)/apikeys/$(id)", args)
UpdateApiKey(a...; b...) = UpdateApiKey(a..., b)

"""
    UpdateDataSource()

Updates a DataSource object.

Required Parameters
apiId => The API ID.
name => The new name for the data source.
type => The new data source type.

Optional Parameters
description => The new description for the data source.
dynamodbConfig => The new Amazon DynamoDB configuration.
elasticsearchConfig => The new Elasticsearch Service configuration.
httpConfig => The new HTTP endpoint configuration.
lambdaConfig => The new AWS Lambda configuration.
relationalDatabaseConfig => The new relational database configuration.
serviceRoleArn => The new service role ARN for the data source.
"""
UpdateDataSource(apiId, name, type) = appsync("POST", "/v1/apis/$(apiId)/datasources/$(name)", Dict{String, Any}("type"=>type))
UpdateDataSource(apiId, name, type, args::AbstractDict{String, <: Any}) = appsync("POST", "/v1/apis/$(apiId)/datasources/$(name)", Dict{String, Any}("type"=>type, args...))
UpdateDataSource(a...; b...) = UpdateDataSource(a..., b)

"""
    UpdateFunction()

Updates a Function object.

Required Parameters
apiId => The GraphQL API ID.
dataSourceName => The Function DataSource name.
functionId => The function ID.
functionVersion => The version of the request mapping template. Currently the supported value is 2018-05-29. 
name => The Function name.
requestMappingTemplate => The Function request mapping template. Functions support only the 2018-05-29 version of the request mapping template.

Optional Parameters
description => The Function description.
responseMappingTemplate => The Function request mapping template. 
"""
UpdateFunction(apiId, dataSourceName, functionId, functionVersion, name, requestMappingTemplate) = appsync("POST", "/v1/apis/$(apiId)/functions/$(functionId)", Dict{String, Any}("dataSourceName"=>dataSourceName, "functionVersion"=>functionVersion, "name"=>name, "requestMappingTemplate"=>requestMappingTemplate))
UpdateFunction(apiId, dataSourceName, functionId, functionVersion, name, requestMappingTemplate, args::AbstractDict{String, <: Any}) = appsync("POST", "/v1/apis/$(apiId)/functions/$(functionId)", Dict{String, Any}("dataSourceName"=>dataSourceName, "functionVersion"=>functionVersion, "name"=>name, "requestMappingTemplate"=>requestMappingTemplate, args...))
UpdateFunction(a...; b...) = UpdateFunction(a..., b)

"""
    UpdateGraphqlApi()

Updates a GraphqlApi object.

Required Parameters
apiId => The API ID.
name => The new name for the GraphqlApi object.

Optional Parameters
additionalAuthenticationProviders => A list of additional authentication providers for the GraphqlApi API.
authenticationType => The new authentication type for the GraphqlApi object.
logConfig => The Amazon CloudWatch Logs configuration for the GraphqlApi object.
openIDConnectConfig => The OpenID Connect configuration for the GraphqlApi object.
userPoolConfig => The new Amazon Cognito user pool configuration for the GraphqlApi object.
xrayEnabled => A flag indicating whether to enable X-Ray tracing for the GraphqlApi.
"""
UpdateGraphqlApi(apiId, name) = appsync("POST", "/v1/apis/$(apiId)", Dict{String, Any}("name"=>name))
UpdateGraphqlApi(apiId, name, args::AbstractDict{String, <: Any}) = appsync("POST", "/v1/apis/$(apiId)", Dict{String, Any}("name"=>name, args...))
UpdateGraphqlApi(a...; b...) = UpdateGraphqlApi(a..., b)

"""
    UpdateResolver()

Updates a Resolver object.

Required Parameters
apiId => The API ID.
fieldName => The new field name.
requestMappingTemplate => The new request mapping template.
typeName => The new type name.

Optional Parameters
cachingConfig => The caching configuration for the resolver.
dataSourceName => The new data source name.
kind => The resolver type.    UNIT: A UNIT resolver type. A UNIT resolver is the default resolver type. A UNIT resolver enables you to execute a GraphQL query against a single data source.    PIPELINE: A PIPELINE resolver type. A PIPELINE resolver enables you to execute a series of Function in a serial manner. You can use a pipeline resolver to execute a GraphQL query against multiple data sources.  
pipelineConfig => The PipelineConfig.
responseMappingTemplate => The new response mapping template.
syncConfig => The SyncConfig for a resolver attached to a versioned datasource.
"""
UpdateResolver(apiId, fieldName, requestMappingTemplate, typeName) = appsync("POST", "/v1/apis/$(apiId)/types/$(typeName)/resolvers/$(fieldName)", Dict{String, Any}("requestMappingTemplate"=>requestMappingTemplate))
UpdateResolver(apiId, fieldName, requestMappingTemplate, typeName, args::AbstractDict{String, <: Any}) = appsync("POST", "/v1/apis/$(apiId)/types/$(typeName)/resolvers/$(fieldName)", Dict{String, Any}("requestMappingTemplate"=>requestMappingTemplate, args...))
UpdateResolver(a...; b...) = UpdateResolver(a..., b)

"""
    UpdateType()

Updates a Type object.

Required Parameters
apiId => The API ID.
format => The new type format: SDL or JSON.
typeName => The new type name.

Optional Parameters
definition => The new definition.
"""
UpdateType(apiId, format, typeName) = appsync("POST", "/v1/apis/$(apiId)/types/$(typeName)", Dict{String, Any}("format"=>format))
UpdateType(apiId, format, typeName, args::AbstractDict{String, <: Any}) = appsync("POST", "/v1/apis/$(apiId)/types/$(typeName)", Dict{String, Any}("format"=>format, args...))
UpdateType(a...; b...) = UpdateType(a..., b)
