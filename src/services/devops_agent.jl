# This file is auto-generated by AWS.jl
# Note: `SERVICE_FEATURE_SET` is embedded by `@service`
using AWS: AbstractAWSConfig, current_aws_config, _merge
using AWS.AWSServices: devops_agent
using AWS.UUIDs: uuid4

"""
    allow_vended_log_delivery_for_resource(delivery_source_arn, resource_arn_being_authorized)
    allow_vended_log_delivery_for_resource(delivery_source_arn, resource_arn_being_authorized, params::Dict{String,<:Any})

Authorize Ingestion Hub subscription operation.

# Arguments

- `delivery_source_arn`: The ARN of the delivery source for vended log delivery.
- `resource_arn_being_authorized`: The ARN of the resource being authorized for vended log
  delivery.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"logType"`: The type of log to be delivered.
"""
function allow_vended_log_delivery_for_resource end

function allow_vended_log_delivery_for_resource(
    deliverySourceArn,
    resourceArnBeingAuthorized;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return devops_agent(
        "POST",
        "/allow-vended-log-delivery-for-resource",
        Dict{String,Any}(
            "deliverySourceArn" => deliverySourceArn,
            "resourceArnBeingAuthorized" => resourceArnBeingAuthorized,
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function allow_vended_log_delivery_for_resource(
    deliverySourceArn,
    resourceArnBeingAuthorized,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return devops_agent(
        "POST",
        "/allow-vended-log-delivery-for-resource",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "deliverySourceArn" => deliverySourceArn,
                    "resourceArnBeingAuthorized" => resourceArnBeingAuthorized,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    associate_service(agent_space_id, configuration, service_id)
    associate_service(agent_space_id, configuration, service_id, params::Dict{String,<:Any})

Adds a specific service association to an AgentSpace. It overwrites the existing association
of the same service. Returns 201 Created on success.

# Arguments

- `agent_space_id`: The unique identifier of the AgentSpace
- `configuration`: The configuration that directs how AgentSpace interacts with the given
  service.
- `service_id`: The unique identifier of the service.
"""
function associate_service end

function associate_service(
    agentSpaceId,
    configuration,
    serviceId;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return devops_agent(
        "POST",
        "/v1/agentspaces/$(agentSpaceId)/associations",
        Dict{String,Any}("configuration" => configuration, "serviceId" => serviceId);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function associate_service(
    agentSpaceId,
    configuration,
    serviceId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return devops_agent(
        "POST",
        "/v1/agentspaces/$(agentSpaceId)/associations",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "configuration" => configuration, "serviceId" => serviceId
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_agent_space(name)
    create_agent_space(name, params::Dict{String,<:Any})

Creates a new AgentSpace with the specified name and description. Duplicate space names are
allowed.

# Arguments

- `name`: The name of the AgentSpace.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"clientToken"`: Client-provided token to ensure request idempotency. When the same token
  is provided in subsequent calls, the same response is returned within a 8-hour window.
- `"description"`: The description of the AgentSpace.
- `"kmsKeyArn"`: The ARN of the AWS Key Management Service (AWS KMS) customer managed key
  that's used to encrypt resources.
- `"locale"`: The locale for the AgentSpace, which determines the language used in agent
  responses.
- `"tags"`: Tags to add to the AgentSpace at creation time.
"""
function create_agent_space end

function create_agent_space(name; aws_config::AbstractAWSConfig=current_aws_config())
    return devops_agent(
        "POST",
        "/v1/agentspaces",
        Dict{String,Any}("name" => name, "clientToken" => string(uuid4()));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_agent_space(
    name, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return devops_agent(
        "POST",
        "/v1/agentspaces",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("name" => name, "clientToken" => string(uuid4())),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_backlog_task(agent_space_id, priority, task_type, title)
    create_backlog_task(agent_space_id, priority, task_type, title, params::Dict{String,<:Any})

Creates a new backlog task in the specified agent space

# Arguments

- `agent_space_id`: The unique identifier for the agent space where the task will be created
- `priority`: The priority level of the task
- `task_type`: The type of task being created
- `title`: The title of the backlog task

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"clientToken"`: Client-provided token for idempotent operations
- `"description"`: Optional detailed description of the task
- `"reference"`: Optional reference information for the task
"""
function create_backlog_task end

function create_backlog_task(
    agentSpaceId,
    priority,
    taskType,
    title;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return devops_agent(
        "POST",
        "/backlog/agent-space/$(agentSpaceId)/tasks",
        Dict{String,Any}(
            "priority" => priority,
            "taskType" => taskType,
            "title" => title,
            "clientToken" => string(uuid4()),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_backlog_task(
    agentSpaceId,
    priority,
    taskType,
    title,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return devops_agent(
        "POST",
        "/backlog/agent-space/$(agentSpaceId)/tasks",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "priority" => priority,
                    "taskType" => taskType,
                    "title" => title,
                    "clientToken" => string(uuid4()),
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_chat(agent_space_id, user_id)
    create_chat(agent_space_id, user_id, params::Dict{String,<:Any})

Creates a new chat execution in the specified agent space

# Arguments

- `agent_space_id`:
- `user_id`: The user identifier for the chat

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"userType"`: The authentication type of the user
"""
function create_chat end

function create_chat(
    agentSpaceId, userId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return devops_agent(
        "POST",
        "/agents/agent-space/$(agentSpaceId)/chat/create",
        Dict{String,Any}("userId" => userId);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_chat(
    agentSpaceId,
    userId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return devops_agent(
        "POST",
        "/agents/agent-space/$(agentSpaceId)/chat/create",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("userId" => userId), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_private_connection(mode, name)
    create_private_connection(mode, name, params::Dict{String,<:Any})

Creates a Private Connection to a target resource.

# Arguments

- `mode`: Private Connection mode configuration.
- `name`: Unique name for this Private Connection within the account.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"tags"`: Tags to add to the Private Connection at creation time.
"""
function create_private_connection end

function create_private_connection(
    mode, name; aws_config::AbstractAWSConfig=current_aws_config()
)
    return devops_agent(
        "POST",
        "/v1/private-connections",
        Dict{String,Any}("mode" => mode, "name" => name);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_private_connection(
    mode,
    name,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return devops_agent(
        "POST",
        "/v1/private-connections",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("mode" => mode, "name" => name), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_agent_space(agent_space_id)
    delete_agent_space(agent_space_id, params::Dict{String,<:Any})

Deletes an AgentSpace. This operation is idempotent and returns a 204 No Content response on
success.

# Arguments

- `agent_space_id`: The unique identifier of the AgentSpace
"""
function delete_agent_space end

function delete_agent_space(
    agentSpaceId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return devops_agent(
        "DELETE",
        "/v1/agentspaces/$(agentSpaceId)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_agent_space(
    agentSpaceId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return devops_agent(
        "DELETE",
        "/v1/agentspaces/$(agentSpaceId)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_private_connection(name)
    delete_private_connection(name, params::Dict{String,<:Any})

Deletes a Private Connection. The deletion is asynchronous and returns DELETE_IN_PROGRESS
status.

# Arguments

- `name`: The name of the Private Connection.
"""
function delete_private_connection end

function delete_private_connection(name; aws_config::AbstractAWSConfig=current_aws_config())
    return devops_agent(
        "DELETE",
        "/v1/private-connections/$(name)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_private_connection(
    name, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return devops_agent(
        "DELETE",
        "/v1/private-connections/$(name)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    deregister_service(service_id)
    deregister_service(service_id, params::Dict{String,<:Any})

Deregister a service

# Arguments

- `service_id`: The service id to deregister. A service can only be deregistered if it is
  not associated with any AgentSpace.
"""
function deregister_service end

function deregister_service(serviceId; aws_config::AbstractAWSConfig=current_aws_config())
    return devops_agent(
        "DELETE", "/v1/services/$(serviceId)"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function deregister_service(
    serviceId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return devops_agent(
        "DELETE",
        "/v1/services/$(serviceId)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    describe_private_connection(name)
    describe_private_connection(name, params::Dict{String,<:Any})

Retrieves details of an existing Private Connection.

# Arguments

- `name`: The name of the Private Connection.
"""
function describe_private_connection end

function describe_private_connection(
    name; aws_config::AbstractAWSConfig=current_aws_config()
)
    return devops_agent(
        "GET",
        "/v1/private-connections/$(name)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function describe_private_connection(
    name, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return devops_agent(
        "GET",
        "/v1/private-connections/$(name)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    disable_operator_app(agent_space_id)
    disable_operator_app(agent_space_id, params::Dict{String,<:Any})

Disable the Operator App for the specified AgentSpace

# Arguments

- `agent_space_id`: The unique identifier of the AgentSpace

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"x-amzn-app-auth-flow"`: The authentication flow configured for the operator App. e.g.
  idc
"""
function disable_operator_app end

function disable_operator_app(
    agentSpaceId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return devops_agent(
        "DELETE",
        "/v1/agentspaces/$(agentSpaceId)/operator";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function disable_operator_app(
    agentSpaceId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return devops_agent(
        "DELETE",
        "/v1/agentspaces/$(agentSpaceId)/operator",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    disassociate_service(agent_space_id, association_id)
    disassociate_service(agent_space_id, association_id, params::Dict{String,<:Any})

Deletes a specific service association from an AgentSpace. This operation is idempotent and
returns a 204 No Content response on success.

# Arguments

- `agent_space_id`: The unique identifier of the AgentSpace
- `association_id`: The unique identifier of the given association.
"""
function disassociate_service end

function disassociate_service(
    agentSpaceId, associationId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return devops_agent(
        "DELETE",
        "/v1/agentspaces/$(agentSpaceId)/associations/$(associationId)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function disassociate_service(
    agentSpaceId,
    associationId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return devops_agent(
        "DELETE",
        "/v1/agentspaces/$(agentSpaceId)/associations/$(associationId)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    enable_operator_app(agent_space_id, auth_flow, operator_app_role_arn)
    enable_operator_app(agent_space_id, auth_flow, operator_app_role_arn, params::Dict{String,<:Any})

Enable the Operator App to access the given AgentSpace

# Arguments

- `agent_space_id`: The unique identifier of the AgentSpace
- `auth_flow`: The authentication flow configured for the operator App. e.g. iam or idc
- `operator_app_role_arn`: The IAM role end users assume to access AIDevOps APIs

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"idcInstanceArn"`: The IdC instance Arn used to create an IdC auth application
- `"idpClientId"`: The OIDC client ID for the IdP application
- `"idpClientSecret"`: The OIDC client secret for the IdP application
- `"issuerUrl"`: The OIDC issuer URL of the external Identity Provider
- `"provider"`: The Identity Provider name (e.g., Entra, Okta, Google)
"""
function enable_operator_app end

function enable_operator_app(
    agentSpaceId,
    authFlow,
    operatorAppRoleArn;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return devops_agent(
        "POST",
        "/v1/agentspaces/$(agentSpaceId)/operator",
        Dict{String,Any}(
            "authFlow" => authFlow, "operatorAppRoleArn" => operatorAppRoleArn
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function enable_operator_app(
    agentSpaceId,
    authFlow,
    operatorAppRoleArn,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return devops_agent(
        "POST",
        "/v1/agentspaces/$(agentSpaceId)/operator",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "authFlow" => authFlow, "operatorAppRoleArn" => operatorAppRoleArn
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_account_usage()
    get_account_usage(params::Dict{String,<:Any})

Retrieves monthly account usage metrics and limits for the AWS account.
"""
function get_account_usage end

function get_account_usage(; aws_config::AbstractAWSConfig=current_aws_config())
    return devops_agent(
        "GET", "/usage/account"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function get_account_usage(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return devops_agent(
        "GET", "/usage/account", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    get_agent_space(agent_space_id)
    get_agent_space(agent_space_id, params::Dict{String,<:Any})

Retrieves detailed information about a specific AgentSpace.

# Arguments

- `agent_space_id`: The unique identifier of the AgentSpace
"""
function get_agent_space end

function get_agent_space(agentSpaceId; aws_config::AbstractAWSConfig=current_aws_config())
    return devops_agent(
        "GET",
        "/v1/agentspaces/$(agentSpaceId)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_agent_space(
    agentSpaceId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return devops_agent(
        "GET",
        "/v1/agentspaces/$(agentSpaceId)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_association(agent_space_id, association_id)
    get_association(agent_space_id, association_id, params::Dict{String,<:Any})

Retrieves given associations configured for a specific AgentSpace.

# Arguments

- `agent_space_id`: The unique identifier of the AgentSpace
- `association_id`: The unique identifier of the given association.
"""
function get_association end

function get_association(
    agentSpaceId, associationId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return devops_agent(
        "GET",
        "/v1/agentspaces/$(agentSpaceId)/associations/$(associationId)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_association(
    agentSpaceId,
    associationId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return devops_agent(
        "GET",
        "/v1/agentspaces/$(agentSpaceId)/associations/$(associationId)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_backlog_task(agent_space_id, task_id)
    get_backlog_task(agent_space_id, task_id, params::Dict{String,<:Any})

Gets a backlog task for the specified agent space and task id

# Arguments

- `agent_space_id`: The unique identifier for the agent space containing the task
- `task_id`: The unique identifier of the task to retrieve
"""
function get_backlog_task end

function get_backlog_task(
    agentSpaceId, taskId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return devops_agent(
        "GET",
        "/backlog/agent-space/$(agentSpaceId)/tasks/$(taskId)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_backlog_task(
    agentSpaceId,
    taskId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return devops_agent(
        "GET",
        "/backlog/agent-space/$(agentSpaceId)/tasks/$(taskId)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_operator_app(agent_space_id)
    get_operator_app(agent_space_id, params::Dict{String,<:Any})

Get the full auth configuration of operator including any enabled auth flow

# Arguments

- `agent_space_id`: The unique identifier of the AgentSpace
"""
function get_operator_app end

function get_operator_app(agentSpaceId; aws_config::AbstractAWSConfig=current_aws_config())
    return devops_agent(
        "GET",
        "/v2/agentspaces/$(agentSpaceId)/operator";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_operator_app(
    agentSpaceId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return devops_agent(
        "GET",
        "/v2/agentspaces/$(agentSpaceId)/operator",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_recommendation(agent_space_id, recommendation_id)
    get_recommendation(agent_space_id, recommendation_id, params::Dict{String,<:Any})

Retrieves a specific recommendation by its ID

# Arguments

- `agent_space_id`: The unique identifier for the agent space containing the recommendation
- `recommendation_id`: The unique identifier for the recommendation to retrieve

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"recommendationVersion"`: Specific version of the recommendation to retrieve. If not
  specified, returns the latest version.
"""
function get_recommendation end

function get_recommendation(
    agentSpaceId, recommendationId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return devops_agent(
        "GET",
        "/backlog/agent-space/$(agentSpaceId)/recommendations/$(recommendationId)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_recommendation(
    agentSpaceId,
    recommendationId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return devops_agent(
        "GET",
        "/backlog/agent-space/$(agentSpaceId)/recommendations/$(recommendationId)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_service(service_id)
    get_service(service_id, params::Dict{String,<:Any})

Retrieves given service by it's unique identifier

# Arguments

- `service_id`: The unique identifier of the given service.
"""
function get_service end

function get_service(serviceId; aws_config::AbstractAWSConfig=current_aws_config())
    return devops_agent(
        "GET", "/v1/services/$(serviceId)"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function get_service(
    serviceId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return devops_agent(
        "GET",
        "/v1/services/$(serviceId)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_agent_spaces()
    list_agent_spaces(params::Dict{String,<:Any})

Lists all AgentSpaces with optional pagination.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"maxResults"`: Maximum number of results to return in a single call.
- `"nextToken"`: Token for the next page of results.
"""
function list_agent_spaces end

function list_agent_spaces(; aws_config::AbstractAWSConfig=current_aws_config())
    return devops_agent(
        "POST", "/v1/agentspaces/list"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_agent_spaces(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return devops_agent(
        "POST", "/v1/agentspaces/list", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    list_associations(agent_space_id)
    list_associations(agent_space_id, params::Dict{String,<:Any})

List all associations for given AgentSpace

# Arguments

- `agent_space_id`: The unique identifier of the AgentSpace

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"filterServiceTypes"`: A comma-separated list of service types to filter list
  associations output
- `"maxResults"`: Maximum number of results to return in a single call.
- `"nextToken"`: Token for the next page of results.
"""
function list_associations end

function list_associations(agentSpaceId; aws_config::AbstractAWSConfig=current_aws_config())
    return devops_agent(
        "POST",
        "/v1/agentspaces/$(agentSpaceId)/associations/list";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_associations(
    agentSpaceId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return devops_agent(
        "POST",
        "/v1/agentspaces/$(agentSpaceId)/associations/list",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_backlog_tasks(agent_space_id)
    list_backlog_tasks(agent_space_id, params::Dict{String,<:Any})

Lists backlog tasks in the specified agent space with optional filtering and sorting

# Arguments

- `agent_space_id`: The unique identifier for the agent space containing the tasks

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"filter"`: Filter criteria to apply when listing tasks Filtering restrictions: - Each
  filter field list is limited to a single value - Filtering by Priority and Status at the
  same time when not filtering by Type is not permitted - Timestamp filters (createdAfter,
  createdBefore) can be combined with other filters when not sorting by priority
- `"limit"`: Maximum number of tasks to return in a single response (1-1000, default: 100)
- `"nextToken"`: Token for retrieving the next page of results
- `"order"`: Sort order for the tasks based on sortField (default: DESC)
- `"sortField"`: Field to sort by Sorting restrictions: - Only sorting on createdAt is
  supported when using priority or status filters alone. - Sorting by priority is not
  supported when using Timestamp filters (createdAfter, createdBefore)
"""
function list_backlog_tasks end

function list_backlog_tasks(
    agentSpaceId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return devops_agent(
        "POST",
        "/backlog/agent-space/$(agentSpaceId)/tasks/list";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_backlog_tasks(
    agentSpaceId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return devops_agent(
        "POST",
        "/backlog/agent-space/$(agentSpaceId)/tasks/list",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_chats(agent_space_id, user_id)
    list_chats(agent_space_id, user_id, params::Dict{String,<:Any})

Retrieves a paginated list of the user's recent chat executions

# Arguments

- `agent_space_id`:
- `user_id`: The user identifier to list chats for

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"maxResults"`: Maximum number of results to return
- `"nextToken"`: Token for pagination
"""
function list_chats end

function list_chats(
    agentSpaceId, userId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return devops_agent(
        "GET",
        "/agents/agent-space/$(agentSpaceId)/chat/list",
        Dict{String,Any}("userId" => userId);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_chats(
    agentSpaceId,
    userId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return devops_agent(
        "GET",
        "/agents/agent-space/$(agentSpaceId)/chat/list",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("userId" => userId), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_executions(agent_space_id, task_id)
    list_executions(agent_space_id, task_id, params::Dict{String,<:Any})

List executions

# Arguments

- `agent_space_id`: The unique identifier for the agent space
- `task_id`: The unique identifier of the task whose executions to retrieve

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"limit"`: Maximum number of executions to return
- `"nextToken"`: Token for pagination to retrieve the next set of results
"""
function list_executions end

function list_executions(
    agentSpaceId, taskId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return devops_agent(
        "POST",
        "/journal/agent-space/$(agentSpaceId)/executions",
        Dict{String,Any}("taskId" => taskId);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_executions(
    agentSpaceId,
    taskId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return devops_agent(
        "POST",
        "/journal/agent-space/$(agentSpaceId)/executions",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("taskId" => taskId), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_goals(agent_space_id)
    list_goals(agent_space_id, params::Dict{String,<:Any})

Lists goals in the specified agent space with optional filtering

# Arguments

- `agent_space_id`: The unique identifier for the agent space

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"goalType"`: Filter goals by goal type
- `"limit"`: Maximum number of goals to return
- `"nextToken"`: Pagination token for the next set of results
- `"status"`: Filter goals by goal status
"""
function list_goals end

function list_goals(agentSpaceId; aws_config::AbstractAWSConfig=current_aws_config())
    return devops_agent(
        "POST",
        "/backlog/agent-space/$(agentSpaceId)/goals/list";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_goals(
    agentSpaceId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return devops_agent(
        "POST",
        "/backlog/agent-space/$(agentSpaceId)/goals/list",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_journal_records(agent_space_id, execution_id)
    list_journal_records(agent_space_id, execution_id, params::Dict{String,<:Any})

List journal records for a specific execution

# Arguments

- `agent_space_id`: The unique identifier for the agent space containing the execution
- `execution_id`: The unique identifier of the execution whose journal records to retrieve

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"limit"`: Maximum number of records to return in a single response (1-100, default: 100)
- `"nextToken"`: Token for retrieving the next page of results
- `"order"`: Sort order for the records based on timestamp (default: DESC)
- `"recordType"`: Filter records by type (empty string returns all types)
"""
function list_journal_records end

function list_journal_records(
    agentSpaceId, executionId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return devops_agent(
        "POST",
        "/journal/agent-space/$(agentSpaceId)/journalRecords",
        Dict{String,Any}("executionId" => executionId);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_journal_records(
    agentSpaceId,
    executionId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return devops_agent(
        "POST",
        "/journal/agent-space/$(agentSpaceId)/journalRecords",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("executionId" => executionId), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_pending_messages(agent_space_id, execution_id)
    list_pending_messages(agent_space_id, execution_id, params::Dict{String,<:Any})

List pending messages for a specific execution.

# Arguments

- `agent_space_id`:
- `execution_id`: The unique identifier of the execution whose journal records to retrieve
"""
function list_pending_messages end

function list_pending_messages(
    agentSpaceId, executionId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return devops_agent(
        "POST",
        "/agents/agent-space/$(agentSpaceId)/pendingMessages",
        Dict{String,Any}("executionId" => executionId);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_pending_messages(
    agentSpaceId,
    executionId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return devops_agent(
        "POST",
        "/agents/agent-space/$(agentSpaceId)/pendingMessages",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("executionId" => executionId), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_private_connections()
    list_private_connections(params::Dict{String,<:Any})

Lists all Private Connections in the caller's account.
"""
function list_private_connections end

function list_private_connections(; aws_config::AbstractAWSConfig=current_aws_config())
    return devops_agent(
        "GET", "/v1/private-connections"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_private_connections(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return devops_agent(
        "GET",
        "/v1/private-connections",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_recommendations(agent_space_id)
    list_recommendations(agent_space_id, params::Dict{String,<:Any})

Lists recommendations for the specified agent space

# Arguments

- `agent_space_id`: The unique identifier for the agent space containing the recommendations

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"goalId"`: Optional goal ID to filter recommendations by specific goal
- `"limit"`: Maximum number of recommendations to return in a single response
- `"nextToken"`: Token for retrieving the next page of results
- `"priority"`: Optional priority to filter recommendations by priority level
- `"status"`: Optional status to filter recommendations by their current status
- `"taskId"`: Optional task ID to filter recommendations by specific task
"""
function list_recommendations end

function list_recommendations(
    agentSpaceId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return devops_agent(
        "POST",
        "/backlog/agent-space/$(agentSpaceId)/recommendations/list";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_recommendations(
    agentSpaceId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return devops_agent(
        "POST",
        "/backlog/agent-space/$(agentSpaceId)/recommendations/list",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_services()
    list_services(params::Dict{String,<:Any})

List a list of registered service on the account level.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"filterServiceType"`: Optional filter to list only services of a specific type.
- `"maxResults"`: Maximum number of results to return in a single call.
- `"nextToken"`: Token for the next page of results.
"""
function list_services end

function list_services(; aws_config::AbstractAWSConfig=current_aws_config())
    return devops_agent(
        "POST", "/v1/services/list"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_services(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return devops_agent(
        "POST", "/v1/services/list", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    list_tags_for_resource(resource_arn)
    list_tags_for_resource(resource_arn, params::Dict{String,<:Any})

Lists tags for the specified AWS DevOps Agent resource.

# Arguments

- `resource_arn`: The ARN of the resource.
"""
function list_tags_for_resource end

function list_tags_for_resource(
    resourceArn; aws_config::AbstractAWSConfig=current_aws_config()
)
    return devops_agent(
        "GET", "/tags/$(resourceArn)"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_tags_for_resource(
    resourceArn,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return devops_agent(
        "GET", "/tags/$(resourceArn)", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    list_webhooks(agent_space_id, association_id)
    list_webhooks(agent_space_id, association_id, params::Dict{String,<:Any})

List all webhooks for given Association

# Arguments

- `agent_space_id`: The unique identifier of the AgentSpace
- `association_id`: The unique identifier of the given association.
"""
function list_webhooks end

function list_webhooks(
    agentSpaceId, associationId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return devops_agent(
        "POST",
        "/v1/agentspaces/$(agentSpaceId)/associations/$(associationId)/webhooks/list";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_webhooks(
    agentSpaceId,
    associationId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return devops_agent(
        "POST",
        "/v1/agentspaces/$(agentSpaceId)/associations/$(associationId)/webhooks/list",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    register_service(service, service_details)
    register_service(service, service_details, params::Dict{String,<:Any})

This operation registers the specified service

# Arguments

- `service`:
- `service_details`: Service-specific authorization configuration parameters

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"kmsKeyArn"`: The ARN of the AWS Key Management Service (AWS KMS) customer managed key
  that's used to encrypt resources.
- `"name"`: The display name for the service registration.
- `"privateConnectionName"`: The name of the private connection to use for VPC connectivity.
- `"tags"`: Tags to add to the Service at registration time.
"""
function register_service end

function register_service(
    service, serviceDetails; aws_config::AbstractAWSConfig=current_aws_config()
)
    return devops_agent(
        "POST",
        "/v1/register/$(service)",
        Dict{String,Any}("serviceDetails" => serviceDetails);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function register_service(
    service,
    serviceDetails,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return devops_agent(
        "POST",
        "/v1/register/$(service)",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("serviceDetails" => serviceDetails), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    send_message(agent_space_id, content, execution_id, user_id)
    send_message(agent_space_id, content, execution_id, user_id, params::Dict{String,<:Any})

Sends a chat message and streams the response for the specified agent space execution

# Arguments

- `agent_space_id`: The agent space identifier
- `content`: The user message content
- `execution_id`: The execution identifier for the chat session
- `user_id`: Required user identifier

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"context"`: Optional context for the message
"""
function send_message end

function send_message(
    agentSpaceId,
    content,
    executionId,
    userId;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return devops_agent(
        "POST",
        "/agents/agent-space/$(agentSpaceId)/chat/sendMessage",
        Dict{String,Any}(
            "content" => content, "executionId" => executionId, "userId" => userId
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function send_message(
    agentSpaceId,
    content,
    executionId,
    userId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return devops_agent(
        "POST",
        "/agents/agent-space/$(agentSpaceId)/chat/sendMessage",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "content" => content, "executionId" => executionId, "userId" => userId
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    tag_resource(resource_arn, tags)
    tag_resource(resource_arn, tags, params::Dict{String,<:Any})

Adds or overwrites tags for the specified AWS DevOps Agent resource.

# Arguments

- `resource_arn`: The ARN of the resource to tag.
- `tags`: Tags to add to the resource.
"""
function tag_resource end

function tag_resource(resourceArn, tags; aws_config::AbstractAWSConfig=current_aws_config())
    return devops_agent(
        "POST",
        "/tags/$(resourceArn)",
        Dict{String,Any}("tags" => tags);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function tag_resource(
    resourceArn,
    tags,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return devops_agent(
        "POST",
        "/tags/$(resourceArn)",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("tags" => tags), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    untag_resource(resource_arn, tag_keys)
    untag_resource(resource_arn, tag_keys, params::Dict{String,<:Any})

Removes tags from the specified AWS DevOps Agent resource.

# Arguments

- `resource_arn`: The ARN of the resource to untag.
- `tag_keys`: Tag keys to remove.
"""
function untag_resource end

function untag_resource(
    resourceArn, tagKeys; aws_config::AbstractAWSConfig=current_aws_config()
)
    return devops_agent(
        "DELETE",
        "/tags/$(resourceArn)",
        Dict{String,Any}("tagKeys" => tagKeys);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function untag_resource(
    resourceArn,
    tagKeys,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return devops_agent(
        "DELETE",
        "/tags/$(resourceArn)",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("tagKeys" => tagKeys), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_agent_space(agent_space_id)
    update_agent_space(agent_space_id, params::Dict{String,<:Any})

Updates the information of an existing AgentSpace.

# Arguments

- `agent_space_id`: The unique identifier of the AgentSpace

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"description"`: The updated description of the AgentSpace.
- `"locale"`: The updated locale for the AgentSpace, which determines the language used in
  agent responses.
- `"name"`: The updated name of the AgentSpace.
"""
function update_agent_space end

function update_agent_space(
    agentSpaceId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return devops_agent(
        "PATCH",
        "/v1/agentspaces/$(agentSpaceId)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_agent_space(
    agentSpaceId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return devops_agent(
        "PATCH",
        "/v1/agentspaces/$(agentSpaceId)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_association(agent_space_id, association_id, configuration)
    update_association(agent_space_id, association_id, configuration, params::Dict{String,<:Any})

Partially updates the configuration of an existing service association for an AgentSpace.
Present fields are fully replaced; absent fields are left unchanged. Returns 200 OK on
success.

# Arguments

- `agent_space_id`: The unique identifier of the AgentSpace
- `association_id`: The unique identifier of the given association.
- `configuration`: The configuration that directs how AgentSpace interacts with the given
  service. The entire configuration is replaced on update.
"""
function update_association end

function update_association(
    agentSpaceId,
    associationId,
    configuration;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return devops_agent(
        "PATCH",
        "/v1/agentspaces/$(agentSpaceId)/associations/$(associationId)",
        Dict{String,Any}("configuration" => configuration);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_association(
    agentSpaceId,
    associationId,
    configuration,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return devops_agent(
        "PATCH",
        "/v1/agentspaces/$(agentSpaceId)/associations/$(associationId)",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("configuration" => configuration), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_backlog_task(agent_space_id, task_id)
    update_backlog_task(agent_space_id, task_id, params::Dict{String,<:Any})

Update an existing backlog task.

# Arguments

- `agent_space_id`: The unique identifier for the agent space containing the task
- `task_id`: The unique identifier of the task to update

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"clientToken"`: Client-provided token for idempotent operations
- `"taskStatus"`: Updated task status
"""
function update_backlog_task end

function update_backlog_task(
    agentSpaceId, taskId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return devops_agent(
        "PATCH",
        "/backlog/agent-space/$(agentSpaceId)/tasks/$(taskId)",
        Dict{String,Any}("clientToken" => string(uuid4()));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_backlog_task(
    agentSpaceId,
    taskId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return devops_agent(
        "PATCH",
        "/backlog/agent-space/$(agentSpaceId)/tasks/$(taskId)",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("clientToken" => string(uuid4())), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_goal(agent_space_id, goal_id)
    update_goal(agent_space_id, goal_id, params::Dict{String,<:Any})

Update an existing goal

# Arguments

- `agent_space_id`: The unique identifier for the agent space containing the goal
- `goal_id`: The unique identifier of the goal to update

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"clientToken"`: Client-provided token for idempotent operations
- `"evaluationSchedule"`: Update goal schedule state
"""
function update_goal end

function update_goal(
    agentSpaceId, goalId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return devops_agent(
        "PATCH",
        "/backlog/agent-space/$(agentSpaceId)/goals/$(goalId)",
        Dict{String,Any}("clientToken" => string(uuid4()));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_goal(
    agentSpaceId,
    goalId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return devops_agent(
        "PATCH",
        "/backlog/agent-space/$(agentSpaceId)/goals/$(goalId)",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("clientToken" => string(uuid4())), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_operator_app_idp_config(agent_space_id)
    update_operator_app_idp_config(agent_space_id, params::Dict{String,<:Any})

Update the external Identity Provider configuration for the Operator App

# Arguments

- `agent_space_id`: The unique identifier of the AgentSpace

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"idpClientSecret"`: The OIDC client secret for the IdP application
"""
function update_operator_app_idp_config end

function update_operator_app_idp_config(
    agentSpaceId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return devops_agent(
        "PATCH",
        "/v1/agentspaces/$(agentSpaceId)/operator/idp";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_operator_app_idp_config(
    agentSpaceId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return devops_agent(
        "PATCH",
        "/v1/agentspaces/$(agentSpaceId)/operator/idp",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_private_connection_certificate(certificate, name)
    update_private_connection_certificate(certificate, name, params::Dict{String,<:Any})

Updates the certificate associated with a Private Connection.

# Arguments

- `certificate`: The new certificate for the Private Connection.
- `name`: The name of the Private Connection.
"""
function update_private_connection_certificate end

function update_private_connection_certificate(
    certificate, name; aws_config::AbstractAWSConfig=current_aws_config()
)
    return devops_agent(
        "POST",
        "/v1/private-connections/$(name)/certificate",
        Dict{String,Any}("certificate" => certificate);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_private_connection_certificate(
    certificate,
    name,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return devops_agent(
        "POST",
        "/v1/private-connections/$(name)/certificate",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("certificate" => certificate), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_recommendation(agent_space_id, recommendation_id)
    update_recommendation(agent_space_id, recommendation_id, params::Dict{String,<:Any})

Updates an existing recommendation with new content, status, or metadata

# Arguments

- `agent_space_id`: The unique identifier for the agent space containing the recommendation
- `recommendation_id`: The unique identifier for the recommendation to update

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"additionalContext"`: Additional context for recommendation
- `"clientToken"`: A unique token that ensures idempotency of the request
- `"status"`: Current status of the recommendation
"""
function update_recommendation end

function update_recommendation(
    agentSpaceId, recommendationId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return devops_agent(
        "PATCH",
        "/backlog/agent-space/$(agentSpaceId)/recommendations/$(recommendationId)",
        Dict{String,Any}("clientToken" => string(uuid4()));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_recommendation(
    agentSpaceId,
    recommendationId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return devops_agent(
        "PATCH",
        "/backlog/agent-space/$(agentSpaceId)/recommendations/$(recommendationId)",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("clientToken" => string(uuid4())), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    validate_aws_associations(agent_space_id)
    validate_aws_associations(agent_space_id, params::Dict{String,<:Any})

Validates an aws association and set status and returns a 204 No Content response on
success.

# Arguments

- `agent_space_id`: The unique identifier of the AgentSpace
"""
function validate_aws_associations end

function validate_aws_associations(
    agentSpaceId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return devops_agent(
        "POST",
        "/v1/agentspaces/$(agentSpaceId)/associations/validate";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function validate_aws_associations(
    agentSpaceId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return devops_agent(
        "POST",
        "/v1/agentspaces/$(agentSpaceId)/associations/validate",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end
