# This file is auto-generated by AWS.jl
# Note: `SERVICE_FEATURE_SET` is embedded by `@service`
using AWS: AbstractAWSConfig, current_aws_config, _merge
using AWS.AWSServices: backupsearch
using AWS.UUIDs: uuid4

"""
    get_search_job(search_job_identifier)
    get_search_job(search_job_identifier, params::Dict{String,<:Any})

This operation retrieves metadata of a search job, including its progress.

# Arguments

- `search_job_identifier`: Required unique string that specifies the search job.
"""
function get_search_job end

function get_search_job(
    SearchJobIdentifier; aws_config::AbstractAWSConfig=current_aws_config()
)
    return backupsearch(
        "GET",
        "/search-jobs/$(SearchJobIdentifier)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_search_job(
    SearchJobIdentifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return backupsearch(
        "GET",
        "/search-jobs/$(SearchJobIdentifier)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_search_result_export_job(export_job_identifier)
    get_search_result_export_job(export_job_identifier, params::Dict{String,<:Any})

This operation retrieves the metadata of an export job.

An export job is an operation that transmits the results of a search job to a specified S3
bucket in a .csv file.

An export job allows you to retain results of a search beyond the search job's scheduled
retention of 7 days.

# Arguments

- `export_job_identifier`: This is the unique string that identifies a specific export job.

  Required for this operation.
"""
function get_search_result_export_job end

function get_search_result_export_job(
    ExportJobIdentifier; aws_config::AbstractAWSConfig=current_aws_config()
)
    return backupsearch(
        "GET",
        "/export-search-jobs/$(ExportJobIdentifier)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_search_result_export_job(
    ExportJobIdentifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return backupsearch(
        "GET",
        "/export-search-jobs/$(ExportJobIdentifier)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_search_job_backups(search_job_identifier)
    list_search_job_backups(search_job_identifier, params::Dict{String,<:Any})

This operation returns a list of all backups (recovery points) in a paginated format that
were included in the search job.

If a search does not display an expected backup in the results, you can call this operation
to display each backup included in the search. Any backups that were not included because
they have a `FAILED` status from a permissions issue will be displayed, along with a status
message.

Only recovery points with a backup index that has a status of `ACTIVE` will be included in
search results. If the index has any other status, its status will be displayed along with a
status message.

# Arguments

- `search_job_identifier`: The unique string that specifies the search job.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"maxResults"`: The maximum number of resource list items to be returned.

- `"nextToken"`: The next item following a partial list of returned backups included in a
  search job.

  For example, if a request is made to return `MaxResults` number of backups, `NextToken`
  allows you to return more items in your list starting at the location pointed to by the
  next token.
"""
function list_search_job_backups end

function list_search_job_backups(
    SearchJobIdentifier; aws_config::AbstractAWSConfig=current_aws_config()
)
    return backupsearch(
        "GET",
        "/search-jobs/$(SearchJobIdentifier)/backups";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_search_job_backups(
    SearchJobIdentifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return backupsearch(
        "GET",
        "/search-jobs/$(SearchJobIdentifier)/backups",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_search_job_results(search_job_identifier)
    list_search_job_results(search_job_identifier, params::Dict{String,<:Any})

This operation returns a list of a specified search job.

# Arguments

- `search_job_identifier`: The unique string that specifies the search job.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"maxResults"`: The maximum number of resource list items to be returned.

- `"nextToken"`: The next item following a partial list of returned search job results.

  For example, if a request is made to return `MaxResults` number of search job results,
  `NextToken` allows you to return more items in your list starting at the location pointed
  to by the next token.
"""
function list_search_job_results end

function list_search_job_results(
    SearchJobIdentifier; aws_config::AbstractAWSConfig=current_aws_config()
)
    return backupsearch(
        "GET",
        "/search-jobs/$(SearchJobIdentifier)/search-results";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_search_job_results(
    SearchJobIdentifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return backupsearch(
        "GET",
        "/search-jobs/$(SearchJobIdentifier)/search-results",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_search_jobs()
    list_search_jobs(params::Dict{String,<:Any})

This operation returns a list of search jobs belonging to an account.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"MaxResults"`: The maximum number of resource list items to be returned.

- `"NextToken"`: The next item following a partial list of returned search jobs.

  For example, if a request is made to return `MaxResults` number of backups, `NextToken`
  allows you to return more items in your list starting at the location pointed to by the
  next token.

- `"Status"`: Include this parameter to filter list by search job status.
"""
function list_search_jobs end

function list_search_jobs(; aws_config::AbstractAWSConfig=current_aws_config())
    return backupsearch("GET", "/search-jobs"; aws_config, feature_set=SERVICE_FEATURE_SET)
end

function list_search_jobs(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return backupsearch(
        "GET", "/search-jobs", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    list_search_result_export_jobs()
    list_search_result_export_jobs(params::Dict{String,<:Any})

This operation exports search results of a search job to a specified destination S3 bucket.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"MaxResults"`: The maximum number of resource list items to be returned.

- `"NextToken"`: The next item following a partial list of returned backups included in a
  search job.

  For example, if a request is made to return `MaxResults` number of backups, `NextToken`
  allows you to return more items in your list starting at the location pointed to by the
  next token.

- `"SearchJobIdentifier"`: The unique string that specifies the search job.

- `"Status"`: The search jobs to be included in the export job can be filtered by including
  this parameter.
"""
function list_search_result_export_jobs end

function list_search_result_export_jobs(;
    aws_config::AbstractAWSConfig=current_aws_config()
)
    return backupsearch(
        "GET", "/export-search-jobs"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_search_result_export_jobs(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return backupsearch(
        "GET", "/export-search-jobs", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    list_tags_for_resource(resource_arn)
    list_tags_for_resource(resource_arn, params::Dict{String,<:Any})

This operation returns the tags for a resource type.

# Arguments

- `resource_arn`: The Amazon Resource Name (ARN) that uniquely identifies the resource.&gt;
"""
function list_tags_for_resource end

function list_tags_for_resource(
    ResourceArn; aws_config::AbstractAWSConfig=current_aws_config()
)
    return backupsearch(
        "GET", "/tags/$(ResourceArn)"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_tags_for_resource(
    ResourceArn,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return backupsearch(
        "GET", "/tags/$(ResourceArn)", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    start_search_job(search_scope)
    start_search_job(search_scope, params::Dict{String,<:Any})

This operation creates a search job which returns recovery points filtered by SearchScope
and items filtered by ItemFilters.

You can optionally include ClientToken, EncryptionKeyArn, Name, and/or Tags.

# Arguments

- `search_scope`: This object can contain BackupResourceTypes, BackupResourceArns,
  BackupResourceCreationTime, BackupResourceTags, and SourceResourceArns to filter the
  recovery points returned by the search job.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"ClientToken"`: Include this parameter to allow multiple identical calls for idempotency.

  A client token is valid for 8 hours after the first request that uses it is completed.
  After this time, any request with the same token is treated as a new request.

- `"EncryptionKeyArn"`: The encryption key for the specified search job.

- `"ItemFilters"`: Item Filters represent all input item properties specified when the
  search was created.

  Contains either EBSItemFilters or S3ItemFilters

- `"Name"`: Include alphanumeric characters to create a name for this search job.

- `"Tags"`: List of tags returned by the operation.
"""
function start_search_job end

function start_search_job(SearchScope; aws_config::AbstractAWSConfig=current_aws_config())
    return backupsearch(
        "PUT",
        "/search-jobs",
        Dict{String,Any}("SearchScope" => SearchScope);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function start_search_job(
    SearchScope,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return backupsearch(
        "PUT",
        "/search-jobs",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("SearchScope" => SearchScope), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    start_search_result_export_job(export_specification, search_job_identifier)
    start_search_result_export_job(export_specification, search_job_identifier, params::Dict{String,<:Any})

This operations starts a job to export the results of search job to a designated S3 bucket.

# Arguments

- `export_specification`: This specification contains a required string of the destination
  bucket; optionally, you can include the destination prefix.
- `search_job_identifier`: The unique string that specifies the search job.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"ClientToken"`: Include this parameter to allow multiple identical calls for idempotency.

  A client token is valid for 8 hours after the first request that uses it is completed.
  After this time, any request with the same token is treated as a new request.

- `"RoleArn"`: This parameter specifies the role ARN used to start the search results export
  jobs.

- `"Tags"`: Optional tags to include. A tag is a key-value pair you can use to manage,
  filter, and search for your resources. Allowed characters include UTF-8 letters, numbers,
  spaces, and the following characters: + - = . _ : /.
"""
function start_search_result_export_job end

function start_search_result_export_job(
    ExportSpecification,
    SearchJobIdentifier;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return backupsearch(
        "PUT",
        "/export-search-jobs",
        Dict{String,Any}(
            "ExportSpecification" => ExportSpecification,
            "SearchJobIdentifier" => SearchJobIdentifier,
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function start_search_result_export_job(
    ExportSpecification,
    SearchJobIdentifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return backupsearch(
        "PUT",
        "/export-search-jobs",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "ExportSpecification" => ExportSpecification,
                    "SearchJobIdentifier" => SearchJobIdentifier,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    stop_search_job(search_job_identifier)
    stop_search_job(search_job_identifier, params::Dict{String,<:Any})

This operations ends a search job.

Only a search job with a status of `RUNNING` can be stopped.

# Arguments

- `search_job_identifier`: The unique string that specifies the search job.
"""
function stop_search_job end

function stop_search_job(
    SearchJobIdentifier; aws_config::AbstractAWSConfig=current_aws_config()
)
    return backupsearch(
        "PUT",
        "/search-jobs/$(SearchJobIdentifier)/actions/cancel";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function stop_search_job(
    SearchJobIdentifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return backupsearch(
        "PUT",
        "/search-jobs/$(SearchJobIdentifier)/actions/cancel",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    tag_resource(resource_arn, tags)
    tag_resource(resource_arn, tags, params::Dict{String,<:Any})

This operation puts tags on the resource you indicate.

# Arguments

- `resource_arn`: The Amazon Resource Name (ARN) that uniquely identifies the resource.

  This is the resource that will have the indicated tags.

- `tags`: Required tags to include. A tag is a key-value pair you can use to manage, filter,
  and search for your resources. Allowed characters include UTF-8 letters, numbers, spaces,
  and the following characters: + - = . _ : /.
"""
function tag_resource end

function tag_resource(ResourceArn, Tags; aws_config::AbstractAWSConfig=current_aws_config())
    return backupsearch(
        "POST",
        "/tags/$(ResourceArn)",
        Dict{String,Any}("Tags" => Tags);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function tag_resource(
    ResourceArn,
    Tags,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return backupsearch(
        "POST",
        "/tags/$(ResourceArn)",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("Tags" => Tags), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    untag_resource(resource_arn, tag_keys)
    untag_resource(resource_arn, tag_keys, params::Dict{String,<:Any})

This operation removes tags from the specified resource.

# Arguments

- `resource_arn`: The Amazon Resource Name (ARN) that uniquely identifies the resource where
  you want to remove tags.
- `tag_keys`: This required parameter contains the tag keys you want to remove from the
  source.
"""
function untag_resource end

function untag_resource(
    ResourceArn, tagKeys; aws_config::AbstractAWSConfig=current_aws_config()
)
    return backupsearch(
        "DELETE",
        "/tags/$(ResourceArn)",
        Dict{String,Any}("tagKeys" => tagKeys);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function untag_resource(
    ResourceArn,
    tagKeys,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return backupsearch(
        "DELETE",
        "/tags/$(ResourceArn)",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("tagKeys" => tagKeys), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end
