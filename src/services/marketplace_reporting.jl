# This file is auto-generated by AWS.jl
# Note: `SERVICE_FEATURE_SET` is embedded by `@service`
using AWS: AbstractAWSConfig, current_aws_config, _merge
using AWS.AWSServices: marketplace_reporting
using AWS.UUIDs: uuid4

"""
    get_buyer_dashboard(dashboard_identifier, embedding_domains)
    get_buyer_dashboard(dashboard_identifier, embedding_domains, params::Dict{String,<:Any})

Generates an embedding URL for an Amazon QuickSight dashboard for an anonymous user.

!!! note
    This API is available only to Amazon Web Services Organization management accounts or
    delegated administrators registered for the procurement insights
    (`procurement-insights.marketplace.amazonaws.com`) feature.

The following rules apply to a generated URL:

- It contains a temporary bearer token, valid for 5 minutes after it is generated. Once
  redeemed within that period, it cannot be re-used again.
- It has a session lifetime of one hour. The 5-minute validity period runs separately from
  the session lifetime.

# Arguments

- `dashboard_identifier`: The ARN of the requested dashboard.
- `embedding_domains`: Fully qualified domains that you add to the allow list for access to
  the generated URL that is then embedded. You can list up to two domains or subdomains in
  each API call. To include all subdomains under a specific domain, use `*`. For example,
  `https://*.amazon.com` includes all subdomains under `https://aws.amazon.com`.
"""
function get_buyer_dashboard end

function get_buyer_dashboard(
    dashboardIdentifier,
    embeddingDomains;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return marketplace_reporting(
        "POST",
        "/getBuyerDashboard",
        Dict{String,Any}(
            "dashboardIdentifier" => dashboardIdentifier,
            "embeddingDomains" => embeddingDomains,
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_buyer_dashboard(
    dashboardIdentifier,
    embeddingDomains,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return marketplace_reporting(
        "POST",
        "/getBuyerDashboard",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "dashboardIdentifier" => dashboardIdentifier,
                    "embeddingDomains" => embeddingDomains,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end
