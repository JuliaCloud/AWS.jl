# This file is auto-generated by AWS.jl
# Note: `SERVICE_FEATURE_SET` is embedded by `@service`
using AWS: AbstractAWSConfig, current_aws_config, _merge
using AWS.AWSServices: dsql
using AWS.UUIDs: uuid4

"""
    create_cluster()
    create_cluster(params::Dict{String,<:Any})

The CreateCluster API allows you to create both single-Region clusters and multi-Region
clusters. With the addition of the *multiRegionProperties* parameter, you can create a
cluster with witness Region support and establish peer relationships with clusters in other
Regions during creation.

!!! note
    Creating multi-Region clusters requires additional IAM permissions beyond those needed
    for single-Region clusters, as detailed in the **Required permissions** section below.

**Required permissions**

### dsql:CreateCluster

Required to create a cluster.

Resources: `arn:aws:dsql:region:account-id:cluster/*`

### dsql:TagResource

Permission to add tags to a resource.

Resources: `arn:aws:dsql:region:account-id:cluster/*`

### dsql:PutMultiRegionProperties

Permission to configure multi-Region properties for a cluster.

Resources: `arn:aws:dsql:region:account-id:cluster/*`

### dsql:AddPeerCluster

When specifying `multiRegionProperties.clusters`, permission to add peer clusters.

Resources:

- Local cluster: `arn:aws:dsql:region:account-id:cluster/*`
- Each peer cluster: exact ARN of each specified peer cluster

### dsql:PutWitnessRegion

When specifying `multiRegionProperties.witnessRegion`, permission to set a witness Region.
This permission is checked both in the cluster Region and in the witness Region.

Resources: `arn:aws:dsql:region:account-id:cluster/*`

Condition Keys: `dsql:WitnessRegion` (matching the specified witness region)

!!! important
    - The witness Region specified in `multiRegionProperties.witnessRegion` cannot be the
      same as the cluster's Region.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"bypassPolicyLockoutSafetyCheck"`: An optional field that controls whether to bypass the
  lockout prevention check. When set to true, this parameter allows you to apply a policy
  that might lock you out of the cluster. Use with caution.

- `"clientToken"`: A unique, case-sensitive identifier that you provide to ensure the
  idempotency of the request. Idempotency ensures that an API request completes only once.
  With an idempotent request, if the original request completes successfully, the subsequent
  retries with the same client token return the result from the original successful request
  and they have no additional effect.

  If you don't specify a client token, the Amazon Web Services SDK automatically generates
  one.

- `"deletionProtectionEnabled"`: If enabled, you can't delete your cluster. You must first
  disable this property before you can delete your cluster.

- `"kmsEncryptionKey"`: The KMS key that encrypts and protects the data on your cluster. You
  can specify the ARN, ID, or alias of an existing key or have Amazon Web Services create a
  default key for you.

- `"multiRegionProperties"`: The configuration settings when creating a multi-Region
  cluster, including the witness region and linked cluster properties.

- `"policy"`: An optional resource-based policy document in JSON format that defines access
  permissions for the cluster.

- `"tags"`: A map of key and value pairs to use to tag your cluster.
"""
function create_cluster end

function create_cluster(; aws_config::AbstractAWSConfig=current_aws_config())
    return dsql(
        "POST",
        "/cluster",
        Dict{String,Any}("clientToken" => string(uuid4()));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_cluster(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return dsql(
        "POST",
        "/cluster",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("clientToken" => string(uuid4())), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_cluster(identifier)
    delete_cluster(identifier, params::Dict{String,<:Any})

Deletes a cluster in Amazon Aurora DSQL.

# Arguments

- `identifier`: The ID of the cluster to delete.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"client-token"`: A unique, case-sensitive identifier that you provide to ensure the
  idempotency of the request. Idempotency ensures that an API request completes only once.
  With an idempotent request, if the original request completes successfully. The subsequent
  retries with the same client token return the result from the original successful request
  and they have no additional effect.

  If you don't specify a client token, the Amazon Web Services SDK automatically generates
  one.
"""
function delete_cluster end

function delete_cluster(identifier; aws_config::AbstractAWSConfig=current_aws_config())
    return dsql(
        "DELETE",
        "/cluster/$(identifier)",
        Dict{String,Any}("client-token" => string(uuid4()));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_cluster(
    identifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return dsql(
        "DELETE",
        "/cluster/$(identifier)",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("client-token" => string(uuid4())), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_cluster_policy(identifier)
    delete_cluster_policy(identifier, params::Dict{String,<:Any})

Deletes the resource-based policy attached to a cluster. This removes all access permissions
defined by the policy, reverting to default access controls.

# Arguments

- `identifier`:

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"client-token"`:
- `"expected-policy-version"`: The expected version of the policy to delete. This parameter
  ensures that you're deleting the correct version of the policy and helps prevent
  accidental deletions.
"""
function delete_cluster_policy end

function delete_cluster_policy(
    identifier; aws_config::AbstractAWSConfig=current_aws_config()
)
    return dsql(
        "DELETE",
        "/cluster/$(identifier)/policy",
        Dict{String,Any}("client-token" => string(uuid4()));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_cluster_policy(
    identifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return dsql(
        "DELETE",
        "/cluster/$(identifier)/policy",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("client-token" => string(uuid4())), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_cluster(identifier)
    get_cluster(identifier, params::Dict{String,<:Any})

Retrieves information about a cluster.

# Arguments

- `identifier`: The ID of the cluster to retrieve.
"""
function get_cluster end

function get_cluster(identifier; aws_config::AbstractAWSConfig=current_aws_config())
    return dsql(
        "GET", "/cluster/$(identifier)"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function get_cluster(
    identifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return dsql(
        "GET", "/cluster/$(identifier)", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    get_cluster_policy(identifier)
    get_cluster_policy(identifier, params::Dict{String,<:Any})

Retrieves the resource-based policy document attached to a cluster. This policy defines the
access permissions and conditions for the cluster.

# Arguments

- `identifier`: The ID of the cluster to retrieve the policy from.
"""
function get_cluster_policy end

function get_cluster_policy(identifier; aws_config::AbstractAWSConfig=current_aws_config())
    return dsql(
        "GET", "/cluster/$(identifier)/policy"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function get_cluster_policy(
    identifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return dsql(
        "GET",
        "/cluster/$(identifier)/policy",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_vpc_endpoint_service_name(identifier)
    get_vpc_endpoint_service_name(identifier, params::Dict{String,<:Any})

Retrieves the VPC endpoint service name.

# Arguments

- `identifier`: The ID of the cluster to retrieve.
"""
function get_vpc_endpoint_service_name end

function get_vpc_endpoint_service_name(
    identifier; aws_config::AbstractAWSConfig=current_aws_config()
)
    return dsql(
        "GET",
        "/clusters/$(identifier)/vpc-endpoint-service-name";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_vpc_endpoint_service_name(
    identifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return dsql(
        "GET",
        "/clusters/$(identifier)/vpc-endpoint-service-name",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_clusters()
    list_clusters(params::Dict{String,<:Any})

Retrieves information about a list of clusters.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"max-results"`: An optional parameter that specifies the maximum number of results to
  return. You can use nextToken to display the next page of results.
- `"next-token"`: If your initial ListClusters operation returns a nextToken, you can
  include the returned nextToken in following ListClusters operations, which returns results
  in the next page.
"""
function list_clusters end

function list_clusters(; aws_config::AbstractAWSConfig=current_aws_config())
    return dsql("GET", "/cluster"; aws_config, feature_set=SERVICE_FEATURE_SET)
end

function list_clusters(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return dsql("GET", "/cluster", params; aws_config, feature_set=SERVICE_FEATURE_SET)
end

"""
    list_tags_for_resource(resource_arn)
    list_tags_for_resource(resource_arn, params::Dict{String,<:Any})

Lists all of the tags for a resource.

# Arguments

- `resource_arn`: The ARN of the resource for which you want to list the tags.
"""
function list_tags_for_resource end

function list_tags_for_resource(
    resourceArn; aws_config::AbstractAWSConfig=current_aws_config()
)
    return dsql("GET", "/tags/$(resourceArn)"; aws_config, feature_set=SERVICE_FEATURE_SET)
end

function list_tags_for_resource(
    resourceArn,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return dsql(
        "GET", "/tags/$(resourceArn)", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    put_cluster_policy(identifier, policy)
    put_cluster_policy(identifier, policy, params::Dict{String,<:Any})

Attaches a resource-based policy to a cluster. This policy defines access permissions and
conditions for the cluster, allowing you to control which principals can perform actions on
the cluster.

# Arguments

- `identifier`:
- `policy`: The resource-based policy document to attach to the cluster. This should be a
  valid JSON policy document that defines permissions and conditions.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"bypassPolicyLockoutSafetyCheck"`: A flag that allows you to bypass the policy lockout
  safety check. When set to true, this parameter allows you to apply a policy that might
  lock you out of the cluster. Use with caution.
- `"clientToken"`:
- `"expectedPolicyVersion"`: The expected version of the current policy. This parameter
  ensures that you're updating the correct version of the policy and helps prevent
  concurrent modification conflicts.
"""
function put_cluster_policy end

function put_cluster_policy(
    identifier, policy; aws_config::AbstractAWSConfig=current_aws_config()
)
    return dsql(
        "POST",
        "/cluster/$(identifier)/policy",
        Dict{String,Any}("policy" => policy, "clientToken" => string(uuid4()));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function put_cluster_policy(
    identifier,
    policy,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return dsql(
        "POST",
        "/cluster/$(identifier)/policy",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("policy" => policy, "clientToken" => string(uuid4())),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    tag_resource(resource_arn, tags)
    tag_resource(resource_arn, tags, params::Dict{String,<:Any})

Tags a resource with a map of key and value pairs.

# Arguments

- `resource_arn`: The ARN of the resource that you want to tag.
- `tags`: A map of key and value pairs to use to tag your resource.
"""
function tag_resource end

function tag_resource(resourceArn, tags; aws_config::AbstractAWSConfig=current_aws_config())
    return dsql(
        "POST",
        "/tags/$(resourceArn)",
        Dict{String,Any}("tags" => tags);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function tag_resource(
    resourceArn,
    tags,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return dsql(
        "POST",
        "/tags/$(resourceArn)",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("tags" => tags), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    untag_resource(resource_arn, tag_keys)
    untag_resource(resource_arn, tag_keys, params::Dict{String,<:Any})

Removes a tag from a resource.

# Arguments

- `resource_arn`: The ARN of the resource from which to remove tags.
- `tag_keys`: The array of keys of the tags that you want to remove.
"""
function untag_resource end

function untag_resource(
    resourceArn, tagKeys; aws_config::AbstractAWSConfig=current_aws_config()
)
    return dsql(
        "DELETE",
        "/tags/$(resourceArn)",
        Dict{String,Any}("tagKeys" => tagKeys);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function untag_resource(
    resourceArn,
    tagKeys,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return dsql(
        "DELETE",
        "/tags/$(resourceArn)",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("tagKeys" => tagKeys), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_cluster(identifier)
    update_cluster(identifier, params::Dict{String,<:Any})

The *UpdateCluster* API allows you to modify both single-Region and multi-Region cluster
configurations. With the *multiRegionProperties* parameter, you can add or modify witness
Region support and manage peer relationships with clusters in other Regions.

!!! note
    Note that updating multi-Region clusters requires additional IAM permissions beyond
    those needed for standard cluster updates, as detailed in the Permissions section.

**Required permissions**

### dsql:UpdateCluster

Permission to update a DSQL cluster.

Resources: `arn:aws:dsql:*region*:*account-id*:cluster/*cluster-id*`

### dsql:PutMultiRegionProperties

Permission to configure multi-Region properties for a cluster.

Resources: `arn:aws:dsql:*region*:*account-id*:cluster/*cluster-id*`

### dsql:GetCluster

Permission to retrieve cluster information.

Resources: `arn:aws:dsql:*region*:*account-id*:cluster/*cluster-id*`

### dsql:AddPeerCluster

Permission to add peer clusters.

Resources:

- Local cluster: `arn:aws:dsql:*region*:*account-id*:cluster/*cluster-id*`
- Each peer cluster: exact ARN of each specified peer cluster

### dsql:RemovePeerCluster

Permission to remove peer clusters. The *dsql:RemovePeerCluster* permission uses a wildcard
ARN pattern to simplify permission management during updates.

Resources: `arn:aws:dsql:*:*account-id*:cluster/*`

### dsql:PutWitnessRegion

Permission to set a witness Region.

Resources: `arn:aws:dsql:*region*:*account-id*:cluster/*cluster-id*`

Condition Keys: dsql:WitnessRegion (matching the specified witness Region)

**This permission is checked both in the cluster Region and in the witness Region.**

!!! important
    - The witness region specified in `multiRegionProperties.witnessRegion` cannot be the
      same as the cluster's Region.
    - When updating clusters with peer relationships, permissions are checked for both
      adding and removing peers.
    - The `dsql:RemovePeerCluster` permission uses a wildcard ARN pattern to simplify
      permission management during updates.

# Arguments

- `identifier`: The ID of the cluster you want to update.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"clientToken"`: A unique, case-sensitive identifier that you provide to ensure the
  idempotency of the request. Idempotency ensures that an API request completes only once.
  With an idempotent request, if the original request completes successfully. The subsequent
  retries with the same client token return the result from the original successful request
  and they have no additional effect.

  If you don't specify a client token, the Amazon Web Services SDK automatically generates
  one.

- `"deletionProtectionEnabled"`: Specifies whether to enable deletion protection in your
  cluster.

- `"kmsEncryptionKey"`: The KMS key that encrypts and protects the data on your cluster. You
  can specify the ARN, ID, or alias of an existing key or have Amazon Web Services create a
  default key for you.

- `"multiRegionProperties"`: The new multi-Region cluster configuration settings to be
  applied during an update operation.
"""
function update_cluster end

function update_cluster(identifier; aws_config::AbstractAWSConfig=current_aws_config())
    return dsql(
        "POST",
        "/cluster/$(identifier)",
        Dict{String,Any}("clientToken" => string(uuid4()));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_cluster(
    identifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return dsql(
        "POST",
        "/cluster/$(identifier)",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("clientToken" => string(uuid4())), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end
