# This file is auto-generated by AWS.jl
# Note: `SERVICE_FEATURE_SET` is embedded by `@service`
using AWS: AbstractAWSConfig, current_aws_config, _merge
using AWS.AWSServices: signer_data
using AWS.UUIDs: uuid4

"""
    get_revocation_status(certificate_hashes, job_arn, platform_id, profile_version_arn, signature_timestamp)
    get_revocation_status(certificate_hashes, job_arn, platform_id, profile_version_arn, signature_timestamp, params::Dict{String,<:Any})

Retrieves the revocation status for a signed artifact by checking if the signing profile,
job, or certificate has been revoked.

# Arguments

- `certificate_hashes`: List of certificate hashes to check for revocation.
- `job_arn`: The ARN of the signing job that produced the signature.
- `platform_id`: The platform identifier for the signing platform used.
- `profile_version_arn`: The ARN of the signing profile version used to sign the artifact.
- `signature_timestamp`: The timestamp when the artifact was signed, in ISO 8601 format.
"""
function get_revocation_status end

function get_revocation_status(
    certificateHashes,
    jobArn,
    platformId,
    profileVersionArn,
    signatureTimestamp;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return signer_data(
        "GET",
        "/revocations",
        Dict{String,Any}(
            "certificateHashes" => certificateHashes,
            "jobArn" => jobArn,
            "platformId" => platformId,
            "profileVersionArn" => profileVersionArn,
            "signatureTimestamp" => signatureTimestamp,
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_revocation_status(
    certificateHashes,
    jobArn,
    platformId,
    profileVersionArn,
    signatureTimestamp,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return signer_data(
        "GET",
        "/revocations",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "certificateHashes" => certificateHashes,
                    "jobArn" => jobArn,
                    "platformId" => platformId,
                    "profileVersionArn" => profileVersionArn,
                    "signatureTimestamp" => signatureTimestamp,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end
