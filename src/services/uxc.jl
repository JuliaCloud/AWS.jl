# This file is auto-generated by AWS.jl
# Note: `SERVICE_FEATURE_SET` is embedded by `@service`
using AWS: AbstractAWSConfig, current_aws_config, _merge
using AWS.AWSServices: uxc
using AWS.UUIDs: uuid4

"""
    get_account_customizations()
    get_account_customizations(params::Dict{String,<:Any})

Returns the current account customization settings, including account color, visible
services, and visible Regions. Settings that you have not configured return their default
values: visible Regions and visible services return `null`, and account color returns
`none`.

!!! note
    The `visibleServices` and `visibleRegions` settings control only the appearance of
    services and Regions in the Amazon Web Services Management Console. They do not restrict
    access through the CLI, SDKs, or other APIs.
"""
function get_account_customizations end

function get_account_customizations(; aws_config::AbstractAWSConfig=current_aws_config())
    return uxc(
        "GET", "/v1/account-customizations"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function get_account_customizations(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return uxc(
        "GET",
        "/v1/account-customizations",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_services()
    list_services(params::Dict{String,<:Any})

Returns a paginated list of Amazon Web Services service identifiers that you can use as
values for the `visibleServices` setting in [UpdateAccountCustomizations](https://docs.aws.amazon.com/awsconsolehelpdocs/latest/APIReference/API_UpdateAccountCustomizations.html).
The available services vary by Amazon Web Services partition. Use pagination to retrieve all
results.

!!! note
    The `visibleServices` setting controls only the appearance of services in the Amazon Web
    Services Management Console. It does not restrict access through the CLI, SDKs, or other
    APIs.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"maxResults"`: The maximum number of results to return per page.
- `"nextToken"`: The token for retrieving the next page of results. Use the `nextToken`
  value from a previous response.
"""
function list_services end

function list_services(; aws_config::AbstractAWSConfig=current_aws_config())
    return uxc("GET", "/v1/services"; aws_config, feature_set=SERVICE_FEATURE_SET)
end

function list_services(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return uxc("GET", "/v1/services", params; aws_config, feature_set=SERVICE_FEATURE_SET)
end

"""
    update_account_customizations()
    update_account_customizations(params::Dict{String,<:Any})

Updates one or more account customization settings. You can update account color, visible
services, and visible Regions in a single request. Only the settings that you include in the
request body are modified. Omitted settings remain unchanged. To reset a setting to its
default behavior, set the value to `null` for visible Regions and visible services, or
`none` for account color. This operation is idempotent.

!!! note
    The `visibleServices` and `visibleRegions` settings control only the appearance of
    services and Regions in the Amazon Web Services Management Console. They do not restrict
    access through the CLI, SDKs, or other APIs.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"accountColor"`: The account color preference to set. Set to `none` to reset to the
  default (no color).
- `"visibleRegions"`: The list of Amazon Web Services Region codes to make visible in the
  Amazon Web Services Management Console. Set to `null` to reset to the default, which makes
  all Regions visible. For a list of valid Region codes, see [Amazon Web Services Regions](https://docs.aws.amazon.com/global-infrastructure/latest/regions/aws-regions.html).
- `"visibleServices"`: The list of Amazon Web Services service identifiers to make visible
  in the Amazon Web Services Management Console. Set to `null` to reset to the default,
  which makes all services visible. For valid service identifiers, call [ListServices](https://docs.aws.amazon.com/awsconsolehelpdocs/latest/APIReference/API_ListServices.html).
"""
function update_account_customizations end

function update_account_customizations(; aws_config::AbstractAWSConfig=current_aws_config())
    return uxc(
        "PATCH", "/v1/account-customizations"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function update_account_customizations(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return uxc(
        "PATCH",
        "/v1/account-customizations",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end
