# This file is auto-generated by AWS.jl
# Note: `SERVICE_FEATURE_SET` is embedded by `@service`
using AWS: AbstractAWSConfig, current_aws_config, _merge
using AWS.AWSServices: mpa
using AWS.UUIDs: uuid4

"""
    cancel_session(session_arn)
    cancel_session(session_arn, params::Dict{String,<:Any})

Cancels an approval session. For more information, see [Session](https://docs.aws.amazon.com/mpa/latest/userguide/mpa-concepts.html)
in the *Multi-party approval User Guide*.

# Arguments

- `session_arn`: Amazon Resource Name (ARN) for the session.
"""
function cancel_session end

function cancel_session(SessionArn; aws_config::AbstractAWSConfig=current_aws_config())
    return mpa(
        "PUT", "/sessions/$(SessionArn)"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function cancel_session(
    SessionArn,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return mpa(
        "PUT",
        "/sessions/$(SessionArn)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_approval_team(approval_strategy, approvers, description, name, policies)
    create_approval_team(approval_strategy, approvers, description, name, policies, params::Dict{String,<:Any})

Creates a new approval team. For more information, see [Approval team](https://docs.aws.amazon.com/mpa/latest/userguide/mpa-concepts.html)
in the *Multi-party approval User Guide*.

# Arguments

- `approval_strategy`: An `ApprovalStrategy` object. Contains details for how the team
  grants approval.
- `approvers`: An array of `ApprovalTeamRequesterApprovers` objects. Contains details for
  the approvers in the team.
- `description`: Description for the team.
- `name`: Name of the team.
- `policies`: An array of `PolicyReference` objects. Contains a list of policies that define
  the permissions for team resources.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"ClientToken"`: Unique, case-sensitive identifier that you provide to ensure the
  idempotency of the request. If not provided, the Amazon Web Services populates this field.

  !!! note
      **What is idempotency?**

      When you make a mutating API request, the request typically returns a result before
      the operation's asynchronous workflows have completed. Operations might also time out
      or encounter other server issues before they complete, even though the request has
      already returned a result. This could make it difficult to determine whether the
      request succeeded or not, and could lead to multiple retries to ensure that the
      operation completes successfully. However, if the original request and the subsequent
      retries are successful, the operation is completed multiple times. This means that you
      might create more resources than you intended.

      *Idempotency* ensures that an API request completes no more than one time. With an
      idempotent request, if the original request completes successfully, any subsequent
      retries complete successfully without performing any further actions.

- `"Tags"`: Tags you want to attach to the team.
"""
function create_approval_team end

function create_approval_team(
    ApprovalStrategy,
    Approvers,
    Description,
    Name,
    Policies;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return mpa(
        "POST",
        "/approval-teams",
        Dict{String,Any}(
            "ApprovalStrategy" => ApprovalStrategy,
            "Approvers" => Approvers,
            "Description" => Description,
            "Name" => Name,
            "Policies" => Policies,
            "ClientToken" => string(uuid4()),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_approval_team(
    ApprovalStrategy,
    Approvers,
    Description,
    Name,
    Policies,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return mpa(
        "POST",
        "/approval-teams",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "ApprovalStrategy" => ApprovalStrategy,
                    "Approvers" => Approvers,
                    "Description" => Description,
                    "Name" => Name,
                    "Policies" => Policies,
                    "ClientToken" => string(uuid4()),
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_identity_source(identity_source_parameters)
    create_identity_source(identity_source_parameters, params::Dict{String,<:Any})

Creates a new identity source. For more information, see [Identity Source](https://docs.aws.amazon.com/mpa/latest/userguide/mpa-concepts.html)
in the *Multi-party approval User Guide*.

# Arguments

- `identity_source_parameters`: A `IdentitySourceParameters` object. Contains details for
  the resource that provides identities to the identity source. For example, an IAM Identity
  Center instance.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"ClientToken"`: Unique, case-sensitive identifier that you provide to ensure the
  idempotency of the request. If not provided, the Amazon Web Services populates this field.

  !!! note
      **What is idempotency?**

      When you make a mutating API request, the request typically returns a result before
      the operation's asynchronous workflows have completed. Operations might also time out
      or encounter other server issues before they complete, even though the request has
      already returned a result. This could make it difficult to determine whether the
      request succeeded or not, and could lead to multiple retries to ensure that the
      operation completes successfully. However, if the original request and the subsequent
      retries are successful, the operation is completed multiple times. This means that you
      might create more resources than you intended.

      *Idempotency* ensures that an API request completes no more than one time. With an
      idempotent request, if the original request completes successfully, any subsequent
      retries complete successfully without performing any further actions.

- `"Tags"`: Tag you want to attach to the identity source.
"""
function create_identity_source end

function create_identity_source(
    IdentitySourceParameters; aws_config::AbstractAWSConfig=current_aws_config()
)
    return mpa(
        "POST",
        "/identity-sources",
        Dict{String,Any}(
            "IdentitySourceParameters" => IdentitySourceParameters,
            "ClientToken" => string(uuid4()),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_identity_source(
    IdentitySourceParameters,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return mpa(
        "POST",
        "/identity-sources",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "IdentitySourceParameters" => IdentitySourceParameters,
                    "ClientToken" => string(uuid4()),
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_identity_source(identity_source_arn)
    delete_identity_source(identity_source_arn, params::Dict{String,<:Any})

Deletes an identity source. For more information, see [Identity Source](https://docs.aws.amazon.com/mpa/latest/userguide/mpa-concepts.html)
in the *Multi-party approval User Guide*.

# Arguments

- `identity_source_arn`: Amazon Resource Name (ARN) for identity source.
"""
function delete_identity_source end

function delete_identity_source(
    IdentitySourceArn; aws_config::AbstractAWSConfig=current_aws_config()
)
    return mpa(
        "DELETE",
        "/identity-sources/$(IdentitySourceArn)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_identity_source(
    IdentitySourceArn,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return mpa(
        "DELETE",
        "/identity-sources/$(IdentitySourceArn)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_inactive_approval_team_version(arn, version_id)
    delete_inactive_approval_team_version(arn, version_id, params::Dict{String,<:Any})

Deletes an inactive approval team. For more information, see [Team health](https://docs.aws.amazon.com/mpa/latest/userguide/mpa-health.html)
in the *Multi-party approval User Guide*.

You can also use this operation to delete a team draft. For more information, see [Interacting with drafts](https://docs.aws.amazon.com/mpa/latest/userguide/update-team.html#update-team-draft-status)
in the *Multi-party approval User Guide*.

# Arguments

- `arn`: Amaazon Resource Name (ARN) for the team.
- `version_id`: Version ID for the team.
"""
function delete_inactive_approval_team_version end

function delete_inactive_approval_team_version(
    Arn, VersionId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return mpa(
        "DELETE",
        "/approval-teams/$(Arn)/$(VersionId)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_inactive_approval_team_version(
    Arn,
    VersionId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return mpa(
        "DELETE",
        "/approval-teams/$(Arn)/$(VersionId)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_approval_team(arn)
    get_approval_team(arn, params::Dict{String,<:Any})

Returns details for an approval team.

# Arguments

- `arn`: Amazon Resource Name (ARN) for the team.
"""
function get_approval_team end

function get_approval_team(Arn; aws_config::AbstractAWSConfig=current_aws_config())
    return mpa("GET", "/approval-teams/$(Arn)"; aws_config, feature_set=SERVICE_FEATURE_SET)
end

function get_approval_team(
    Arn, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return mpa(
        "GET", "/approval-teams/$(Arn)", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    get_identity_source(identity_source_arn)
    get_identity_source(identity_source_arn, params::Dict{String,<:Any})

Returns details for an identity source. For more information, see [Identity Source](https://docs.aws.amazon.com/mpa/latest/userguide/mpa-concepts.html)
in the *Multi-party approval User Guide*.

# Arguments

- `identity_source_arn`: Amazon Resource Name (ARN) for the identity source.
"""
function get_identity_source end

function get_identity_source(
    IdentitySourceArn; aws_config::AbstractAWSConfig=current_aws_config()
)
    return mpa(
        "GET",
        "/identity-sources/$(IdentitySourceArn)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_identity_source(
    IdentitySourceArn,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return mpa(
        "GET",
        "/identity-sources/$(IdentitySourceArn)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_policy_version(policy_version_arn)
    get_policy_version(policy_version_arn, params::Dict{String,<:Any})

Returns details for the version of a policy. Policies define the permissions for team
resources.

# Arguments

- `policy_version_arn`: Amazon Resource Name (ARN) for the policy.
"""
function get_policy_version end

function get_policy_version(
    PolicyVersionArn; aws_config::AbstractAWSConfig=current_aws_config()
)
    return mpa(
        "GET",
        "/policy-versions/$(PolicyVersionArn)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_policy_version(
    PolicyVersionArn,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return mpa(
        "GET",
        "/policy-versions/$(PolicyVersionArn)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_resource_policy(policy_name, policy_type, resource_arn)
    get_resource_policy(policy_name, policy_type, resource_arn, params::Dict{String,<:Any})

Returns details about a policy for a resource.

# Arguments

- `policy_name`: Name of the policy.
- `policy_type`: The type of policy.
- `resource_arn`: Amazon Resource Name (ARN) for the resource.
"""
function get_resource_policy end

function get_resource_policy(
    PolicyName, PolicyType, ResourceArn; aws_config::AbstractAWSConfig=current_aws_config()
)
    return mpa(
        "POST",
        "/GetResourcePolicy",
        Dict{String,Any}(
            "PolicyName" => PolicyName,
            "PolicyType" => PolicyType,
            "ResourceArn" => ResourceArn,
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_resource_policy(
    PolicyName,
    PolicyType,
    ResourceArn,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return mpa(
        "POST",
        "/GetResourcePolicy",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "PolicyName" => PolicyName,
                    "PolicyType" => PolicyType,
                    "ResourceArn" => ResourceArn,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_session(session_arn)
    get_session(session_arn, params::Dict{String,<:Any})

Returns details for an approval session. For more information, see [Session](https://docs.aws.amazon.com/mpa/latest/userguide/mpa-concepts.html)
in the *Multi-party approval User Guide*.

# Arguments

- `session_arn`: Amazon Resource Name (ARN) for the session.
"""
function get_session end

function get_session(SessionArn; aws_config::AbstractAWSConfig=current_aws_config())
    return mpa(
        "GET", "/sessions/$(SessionArn)"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function get_session(
    SessionArn,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return mpa(
        "GET",
        "/sessions/$(SessionArn)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_approval_teams()
    list_approval_teams(params::Dict{String,<:Any})

Returns a list of approval teams.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"MaxResults"`: The maximum number of items to return in the response. If more results
  exist than the specified `MaxResults` value, a token is included in the response so that
  you can retrieve the remaining results.
- `"NextToken"`: If present, indicates that more output is available than is included in the
  current response. Use this value in the `next_token` request parameter in a next call to
  the operation to get more output. You can repeat this until the `NextToken` response
  element returns `null`.
"""
function list_approval_teams end

function list_approval_teams(; aws_config::AbstractAWSConfig=current_aws_config())
    return mpa("POST", "/approval-teams/?List"; aws_config, feature_set=SERVICE_FEATURE_SET)
end

function list_approval_teams(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return mpa(
        "POST", "/approval-teams/?List", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    list_identity_sources()
    list_identity_sources(params::Dict{String,<:Any})

Returns a list of identity sources. For more information, see [Identity Source](https://docs.aws.amazon.com/mpa/latest/userguide/mpa-concepts.html)
in the *Multi-party approval User Guide*.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"MaxResults"`: The maximum number of items to return in the response. If more results
  exist than the specified `MaxResults` value, a token is included in the response so that
  you can retrieve the remaining results.
- `"NextToken"`: If present, indicates that more output is available than is included in the
  current response. Use this value in the `next_token` request parameter in a next call to
  the operation to get more output. You can repeat this until the `NextToken` response
  element returns `null`.
"""
function list_identity_sources end

function list_identity_sources(; aws_config::AbstractAWSConfig=current_aws_config())
    return mpa(
        "POST", "/identity-sources/?List"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_identity_sources(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return mpa(
        "POST",
        "/identity-sources/?List",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_policies()
    list_policies(params::Dict{String,<:Any})

Returns a list of policies. Policies define the permissions for team resources.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"MaxResults"`: The maximum number of items to return in the response. If more results
  exist than the specified `MaxResults` value, a token is included in the response so that
  you can retrieve the remaining results.
- `"NextToken"`: If present, indicates that more output is available than is included in the
  current response. Use this value in the `next_token` request parameter in a next call to
  the operation to get more output. You can repeat this until the `NextToken` response
  element returns `null`.
"""
function list_policies end

function list_policies(; aws_config::AbstractAWSConfig=current_aws_config())
    return mpa("POST", "/policies/?List"; aws_config, feature_set=SERVICE_FEATURE_SET)
end

function list_policies(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return mpa(
        "POST", "/policies/?List", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    list_policy_versions(policy_arn)
    list_policy_versions(policy_arn, params::Dict{String,<:Any})

Returns a list of the versions for policies. Policies define the permissions for team
resources.

# Arguments

- `policy_arn`: Amazon Resource Name (ARN) for the policy.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"MaxResults"`: The maximum number of items to return in the response. If more results
  exist than the specified `MaxResults` value, a token is included in the response so that
  you can retrieve the remaining results.
- `"NextToken"`: If present, indicates that more output is available than is included in the
  current response. Use this value in the `next_token` request parameter in a next call to
  the operation to get more output. You can repeat this until the `NextToken` response
  element returns `null`.
"""
function list_policy_versions end

function list_policy_versions(PolicyArn; aws_config::AbstractAWSConfig=current_aws_config())
    return mpa(
        "POST", "/policies/$(PolicyArn)/?List"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_policy_versions(
    PolicyArn,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return mpa(
        "POST",
        "/policies/$(PolicyArn)/?List",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_resource_policies(resource_arn)
    list_resource_policies(resource_arn, params::Dict{String,<:Any})

Returns a list of policies for a resource.

# Arguments

- `resource_arn`: Amazon Resource Name (ARN) for the resource.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"MaxResults"`: The maximum number of items to return in the response. If more results
  exist than the specified `MaxResults` value, a token is included in the response so that
  you can retrieve the remaining results.
- `"NextToken"`: If present, indicates that more output is available than is included in the
  current response. Use this value in the `next_token` request parameter in a next call to
  the operation to get more output. You can repeat this until the `NextToken` response
  element returns `null`.
"""
function list_resource_policies end

function list_resource_policies(
    ResourceArn; aws_config::AbstractAWSConfig=current_aws_config()
)
    return mpa(
        "POST",
        "/resource-policies/$(ResourceArn)/?List";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_resource_policies(
    ResourceArn,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return mpa(
        "POST",
        "/resource-policies/$(ResourceArn)/?List",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_sessions(approval_team_arn)
    list_sessions(approval_team_arn, params::Dict{String,<:Any})

Returns a list of approval sessions. For more information, see [Session](https://docs.aws.amazon.com/mpa/latest/userguide/mpa-concepts.html)
in the *Multi-party approval User Guide*.

# Arguments

- `approval_team_arn`: Amazon Resource Name (ARN) for the approval team.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Filters"`: An array of `Filter` objects. Contains the filter to apply when listing
  sessions.
- `"MaxResults"`: The maximum number of items to return in the response. If more results
  exist than the specified `MaxResults` value, a token is included in the response so that
  you can retrieve the remaining results.
- `"NextToken"`: If present, indicates that more output is available than is included in the
  current response. Use this value in the `next_token` request parameter in a next call to
  the operation to get more output. You can repeat this until the `NextToken` response
  element returns `null`.
"""
function list_sessions end

function list_sessions(ApprovalTeamArn; aws_config::AbstractAWSConfig=current_aws_config())
    return mpa(
        "POST",
        "/approval-teams/$(ApprovalTeamArn)/sessions/?List";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_sessions(
    ApprovalTeamArn,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return mpa(
        "POST",
        "/approval-teams/$(ApprovalTeamArn)/sessions/?List",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_tags_for_resource(resource_arn)
    list_tags_for_resource(resource_arn, params::Dict{String,<:Any})

Returns a list of the tags for a resource.

# Arguments

- `resource_arn`: Amazon Resource Name (ARN) for the resource.
"""
function list_tags_for_resource end

function list_tags_for_resource(
    ResourceArn; aws_config::AbstractAWSConfig=current_aws_config()
)
    return mpa("GET", "/tags/$(ResourceArn)"; aws_config, feature_set=SERVICE_FEATURE_SET)
end

function list_tags_for_resource(
    ResourceArn,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return mpa(
        "GET", "/tags/$(ResourceArn)", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    start_active_approval_team_deletion(arn)
    start_active_approval_team_deletion(arn, params::Dict{String,<:Any})

Starts the deletion process for an active approval team.

!!! note
    **Deletions require team approval**

    Requests to delete an active team must be approved by the team.

# Arguments

- `arn`: Amazon Resource Name (ARN) for the team.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"PendingWindowDays"`: Number of days between when the team approves the delete request
  and when the team is deleted.
"""
function start_active_approval_team_deletion end

function start_active_approval_team_deletion(
    Arn; aws_config::AbstractAWSConfig=current_aws_config()
)
    return mpa(
        "POST", "/approval-teams/$(Arn)?Delete"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function start_active_approval_team_deletion(
    Arn, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return mpa(
        "POST",
        "/approval-teams/$(Arn)?Delete",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    start_approval_team_baseline(arn)
    start_approval_team_baseline(arn, params::Dict{String,<:Any})

Starts a baseline session for specified approvers on an `ACTIVE` approval team.

# Arguments

- `arn`: Amazon Resource Name (ARN) for the approval team.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"ApproverIds"`: Array of approver IDs.
"""
function start_approval_team_baseline end

function start_approval_team_baseline(
    Arn; aws_config::AbstractAWSConfig=current_aws_config()
)
    return mpa(
        "POST",
        "/approval-teams/$(Arn)/baseline";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function start_approval_team_baseline(
    Arn, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return mpa(
        "POST",
        "/approval-teams/$(Arn)/baseline",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    tag_resource(resource_arn, tags)
    tag_resource(resource_arn, tags, params::Dict{String,<:Any})

Creates or updates a resource tag. Each tag is a label consisting of a user-defined key and
value. Tags can help you manage, identify, organize, search for, and filter resources.

# Arguments

- `resource_arn`: Amazon Resource Name (ARN) for the resource you want to tag.
- `tags`: Tags that you have added to the specified resource.
"""
function tag_resource end

function tag_resource(ResourceArn, Tags; aws_config::AbstractAWSConfig=current_aws_config())
    return mpa(
        "PUT",
        "/tags/$(ResourceArn)",
        Dict{String,Any}("Tags" => Tags);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function tag_resource(
    ResourceArn,
    Tags,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return mpa(
        "PUT",
        "/tags/$(ResourceArn)",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("Tags" => Tags), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    untag_resource(resource_arn, tag_keys)
    untag_resource(resource_arn, tag_keys, params::Dict{String,<:Any})

Removes a resource tag. Each tag is a label consisting of a user-defined key and value. Tags
can help you manage, identify, organize, search for, and filter resources.

# Arguments

- `resource_arn`: Amazon Resource Name (ARN) for the resource you want to untag.
- `tag_keys`: Array of tag key-value pairs that you want to untag.
"""
function untag_resource end

function untag_resource(
    ResourceArn, TagKeys; aws_config::AbstractAWSConfig=current_aws_config()
)
    return mpa(
        "POST",
        "/tags/$(ResourceArn)",
        Dict{String,Any}("TagKeys" => TagKeys);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function untag_resource(
    ResourceArn,
    TagKeys,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return mpa(
        "POST",
        "/tags/$(ResourceArn)",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("TagKeys" => TagKeys), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_approval_team(arn)
    update_approval_team(arn, params::Dict{String,<:Any})

Updates an approval team. You can request to update the team description, approval
threshold, and approvers in the team.

!!! note
    **Updates require team approval**

    Updates to an active team must be approved by the team.

# Arguments

- `arn`: Amazon Resource Name (ARN) for the team.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"ApprovalStrategy"`: An `ApprovalStrategy` object. Contains details for how the team
  grants approval.
- `"Approvers"`: An array of `ApprovalTeamRequestApprover` objects. Contains details for the
  approvers in the team.
- `"Description"`: Description for the team.
- `"UpdateActions"`: A list of `UpdateAction` to perform when updating the team.
"""
function update_approval_team end

function update_approval_team(Arn; aws_config::AbstractAWSConfig=current_aws_config())
    return mpa(
        "PATCH", "/approval-teams/$(Arn)"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function update_approval_team(
    Arn, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return mpa(
        "PATCH",
        "/approval-teams/$(Arn)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end
