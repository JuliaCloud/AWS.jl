# This file is auto-generated by AWS.jl
# Note: `SERVICE_FEATURE_SET` is embedded by `@service`
using AWS: AbstractAWSConfig, current_aws_config, _merge
using AWS.AWSServices: s3tables
using AWS.UUIDs: uuid4

"""
    create_namespace(namespace, table_bucket_arn)
    create_namespace(namespace, table_bucket_arn, params::Dict{String,<:Any})

Creates a namespace. A namespace is a logical grouping of tables within your table bucket,
which you can use to organize tables. For more information, see [Create a namespace](https://docs.aws.amazon.com/AmazonS3/latest/userguide/s3-tables-namespace-create.html)
in the *Amazon Simple Storage Service User Guide*.

### Permissions

You must have the `s3tables:CreateNamespace` permission to use this operation.

# Arguments

- `namespace`: A name for the namespace.
- `table_bucket_arn`: The Amazon Resource Name (ARN) of the table bucket to create the
  namespace in.
"""
function create_namespace end

function create_namespace(
    namespace, tableBucketARN; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3tables(
        "PUT",
        "/namespaces/$(tableBucketARN)",
        Dict{String,Any}("namespace" => namespace);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_namespace(
    namespace,
    tableBucketARN,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "PUT",
        "/namespaces/$(tableBucketARN)",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("namespace" => namespace), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_table(format, name, namespace, table_bucket_arn)
    create_table(format, name, namespace, table_bucket_arn, params::Dict{String,<:Any})

Creates a new table associated with the given namespace in a table bucket. For more
information, see [Creating an Amazon S3 table](https://docs.aws.amazon.com/AmazonS3/latest/userguide/s3-tables-create.html)
in the *Amazon Simple Storage Service User Guide*.

### Permissions

- You must have the `s3tables:CreateTable` permission to use this operation.
- If you use this operation with the optional `metadata` request parameter you must have the
  `s3tables:PutTableData` permission.
- If you use this operation with the optional `encryption_configuration` request parameter
  you must have the `s3tables:PutTableEncryption` permission.
- If you use this operation with the `storage_class_configuration` request parameter, you
  must have the `s3tables:PutTableStorageClass` permission.
- To create a table with tags, you must have the `s3tables:TagResource` permission in
  addition to `s3tables:CreateTable` permission.

!!! note
    Additionally, If you choose SSE-KMS encryption you must grant the S3 Tables maintenance
    principal access to your KMS key. For more information, see [Permissions requirements for S3 Tables SSE-KMS encryption](https://docs.aws.amazon.com/AmazonS3/latest/userguide/s3-tables-kms-permissions.html).

# Arguments

- `format`: The format for the table.
- `name`: The name for the table.
- `namespace`: The namespace to associated with the table.
- `table_bucket_arn`: The Amazon Resource Name (ARN) of the table bucket to create the table
  in.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"encryptionConfiguration"`: The encryption configuration to use for the table. This
  configuration specifies the encryption algorithm and, if using SSE-KMS, the KMS key to use
  for encrypting the table.

  !!! note
      If you choose SSE-KMS encryption you must grant the S3 Tables maintenance principal
      access to your KMS key. For more information, see [Permissions requirements for S3 Tables SSE-KMS encryption](https://docs.aws.amazon.com/AmazonS3/latest/userguide/s3-tables-kms-permissions.html).

- `"metadata"`: The metadata for the table.

- `"storageClassConfiguration"`: The storage class configuration for the table. If not
  specified, the table inherits the storage class configuration from its table bucket.
  Specify this parameter to override the bucket's default storage class for this table.

- `"tags"`: A map of user-defined tags that you would like to apply to the table that you
  are creating. A tag is a key-value pair that you apply to your resources. Tags can help
  you organize, track costs for, and control access to resources. For more information, see [Tagging for cost allocation or attribute-based access control (ABAC)](https://docs.aws.amazon.com/AmazonS3/latest/userguide/tagging.html).

  !!! note
      You must have the `s3tables:TagResource` permission in addition to
      `s3tables:CreateTable` permission to create a table with tags.
"""
function create_table end

function create_table(
    format,
    name,
    namespace,
    tableBucketARN;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "PUT",
        "/tables/$(tableBucketARN)/$(namespace)",
        Dict{String,Any}("format" => format, "name" => name);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_table(
    format,
    name,
    namespace,
    tableBucketARN,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "PUT",
        "/tables/$(tableBucketARN)/$(namespace)",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("format" => format, "name" => name), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_table_bucket(name)
    create_table_bucket(name, params::Dict{String,<:Any})

Creates a table bucket. For more information, see [Creating a table bucket](https://docs.aws.amazon.com/AmazonS3/latest/userguide/s3-tables-buckets-create.html)
in the *Amazon Simple Storage Service User Guide*.

### Permissions

- You must have the `s3tables:CreateTableBucket` permission to use this operation.
- If you use this operation with the optional `encryptionConfiguration` parameter you must
  have the `s3tables:PutTableBucketEncryption` permission.
- If you use this operation with the `storage_class_configuration` request parameter, you
  must have the `s3tables:PutTableBucketStorageClass` permission.
- To create a table bucket with tags, you must have the `s3tables:TagResource` permission in
  addition to `s3tables:CreateTableBucket` permission.

# Arguments

- `name`: The name for the table bucket.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"encryptionConfiguration"`: The encryption configuration to use for the table bucket.
  This configuration specifies the default encryption settings that will be applied to all
  tables created in this bucket unless overridden at the table level. The configuration
  includes the encryption algorithm and, if using SSE-KMS, the KMS key to use.

- `"storageClassConfiguration"`: The default storage class configuration for the table
  bucket. This configuration will be applied to all new tables created in this bucket unless
  overridden at the table level. If not specified, the service default storage class will be
  used.

- `"tags"`: A map of user-defined tags that you would like to apply to the table bucket that
  you are creating. A tag is a key-value pair that you apply to your resources. Tags can
  help you organize and control access to resources. For more information, see [Tagging for cost allocation or attribute-based access control (ABAC)](https://docs.aws.amazon.com/AmazonS3/latest/userguide/tagging.html).

  !!! note
      You must have the `s3tables:TagResource` permission in addition to
      `s3tables:CreateTableBucket` permisson to create a table bucket with tags.
"""
function create_table_bucket end

function create_table_bucket(name; aws_config::AbstractAWSConfig=current_aws_config())
    return s3tables(
        "PUT",
        "/buckets",
        Dict{String,Any}("name" => name);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_table_bucket(
    name, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3tables(
        "PUT",
        "/buckets",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("name" => name), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_namespace(namespace, table_bucket_arn)
    delete_namespace(namespace, table_bucket_arn, params::Dict{String,<:Any})

Deletes a namespace. For more information, see [Delete a namespace](https://docs.aws.amazon.com/AmazonS3/latest/userguide/s3-tables-namespace-delete.html)
in the *Amazon Simple Storage Service User Guide*.

### Permissions

You must have the `s3tables:DeleteNamespace` permission to use this operation.

# Arguments

- `namespace`: The name of the namespace.
- `table_bucket_arn`: The Amazon Resource Name (ARN) of the table bucket associated with the
  namespace.
"""
function delete_namespace end

function delete_namespace(
    namespace, tableBucketARN; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3tables(
        "DELETE",
        "/namespaces/$(tableBucketARN)/$(namespace)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_namespace(
    namespace,
    tableBucketARN,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "DELETE",
        "/namespaces/$(tableBucketARN)/$(namespace)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_table(name, namespace, table_bucket_arn)
    delete_table(name, namespace, table_bucket_arn, params::Dict{String,<:Any})

Deletes a table. For more information, see [Deleting an Amazon S3 table](https://docs.aws.amazon.com/AmazonS3/latest/userguide/s3-tables-delete.html)
in the *Amazon Simple Storage Service User Guide*.

### Permissions

You must have the `s3tables:DeleteTable` permission to use this operation.

# Arguments

- `name`: The name of the table.
- `namespace`: The namespace associated with the table.
- `table_bucket_arn`: The Amazon Resource Name (ARN) of the table bucket that contains the
  table.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"versionToken"`: The version token of the table.
"""
function delete_table end

function delete_table(
    name, namespace, tableBucketARN; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3tables(
        "DELETE",
        "/tables/$(tableBucketARN)/$(namespace)/$(name)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_table(
    name,
    namespace,
    tableBucketARN,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "DELETE",
        "/tables/$(tableBucketARN)/$(namespace)/$(name)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_table_bucket(table_bucket_arn)
    delete_table_bucket(table_bucket_arn, params::Dict{String,<:Any})

Deletes a table bucket. For more information, see [Deleting a table bucket](https://docs.aws.amazon.com/AmazonS3/latest/userguide/s3-tables-buckets-delete.html)
in the *Amazon Simple Storage Service User Guide*.

### Permissions

You must have the `s3tables:DeleteTableBucket` permission to use this operation.

# Arguments

- `table_bucket_arn`: The Amazon Resource Name (ARN) of the table bucket.
"""
function delete_table_bucket end

function delete_table_bucket(
    tableBucketARN; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3tables(
        "DELETE", "/buckets/$(tableBucketARN)"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function delete_table_bucket(
    tableBucketARN,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "DELETE",
        "/buckets/$(tableBucketARN)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_table_bucket_encryption(table_bucket_arn)
    delete_table_bucket_encryption(table_bucket_arn, params::Dict{String,<:Any})

Deletes the encryption configuration for a table bucket.

### Permissions

You must have the `s3tables:DeleteTableBucketEncryption` permission to use this operation.

# Arguments

- `table_bucket_arn`: The Amazon Resource Name (ARN) of the table bucket.
"""
function delete_table_bucket_encryption end

function delete_table_bucket_encryption(
    tableBucketARN; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3tables(
        "DELETE",
        "/buckets/$(tableBucketARN)/encryption";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_table_bucket_encryption(
    tableBucketARN,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "DELETE",
        "/buckets/$(tableBucketARN)/encryption",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_table_bucket_metrics_configuration(table_bucket_arn)
    delete_table_bucket_metrics_configuration(table_bucket_arn, params::Dict{String,<:Any})

Deletes the metrics configuration for a table bucket.

### Permissions

You must have the `s3tables:DeleteTableBucketMetricsConfiguration` permission to use this
operation.

# Arguments

- `table_bucket_arn`: The Amazon Resource Name (ARN) of the table bucket.
"""
function delete_table_bucket_metrics_configuration end

function delete_table_bucket_metrics_configuration(
    tableBucketARN; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3tables(
        "DELETE",
        "/buckets/$(tableBucketARN)/metrics";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_table_bucket_metrics_configuration(
    tableBucketARN,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "DELETE",
        "/buckets/$(tableBucketARN)/metrics",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_table_bucket_policy(table_bucket_arn)
    delete_table_bucket_policy(table_bucket_arn, params::Dict{String,<:Any})

Deletes a table bucket policy. For more information, see [Deleting a table bucket policy](https://docs.aws.amazon.com/AmazonS3/latest/userguide/s3-tables-bucket-policy.html#table-bucket-policy-delete)
in the *Amazon Simple Storage Service User Guide*.

### Permissions

You must have the `s3tables:DeleteTableBucketPolicy` permission to use this operation.

# Arguments

- `table_bucket_arn`: The Amazon Resource Name (ARN) of the table bucket.
"""
function delete_table_bucket_policy end

function delete_table_bucket_policy(
    tableBucketARN; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3tables(
        "DELETE",
        "/buckets/$(tableBucketARN)/policy";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_table_bucket_policy(
    tableBucketARN,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "DELETE",
        "/buckets/$(tableBucketARN)/policy",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_table_bucket_replication(table_bucket_arn)
    delete_table_bucket_replication(table_bucket_arn, params::Dict{String,<:Any})

Deletes the replication configuration for a table bucket. After deletion, new table updates
will no longer be replicated to destination buckets, though existing replicated tables will
remain in destination buckets.

### Permissions

You must have the `s3tables:DeleteTableBucketReplication` permission to use this operation.

# Arguments

- `table_bucket_arn`: The Amazon Resource Name (ARN) of the table bucket.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"versionToken"`: A version token from a previous GetTableBucketReplication call. Use this
  token to ensure you're deleting the expected version of the configuration.
"""
function delete_table_bucket_replication end

function delete_table_bucket_replication(
    tableBucketARN; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3tables(
        "DELETE",
        "/table-bucket-replication",
        Dict{String,Any}("tableBucketARN" => tableBucketARN);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_table_bucket_replication(
    tableBucketARN,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "DELETE",
        "/table-bucket-replication",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("tableBucketARN" => tableBucketARN), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_table_policy(name, namespace, table_bucket_arn)
    delete_table_policy(name, namespace, table_bucket_arn, params::Dict{String,<:Any})

Deletes a table policy. For more information, see [Deleting a table policy](https://docs.aws.amazon.com/AmazonS3/latest/userguide/s3-tables-table-policy.html#table-policy-delete)
in the *Amazon Simple Storage Service User Guide*.

### Permissions

You must have the `s3tables:DeleteTablePolicy` permission to use this operation.

# Arguments

- `name`: The table name.
- `namespace`: The namespace associated with the table.
- `table_bucket_arn`: The Amazon Resource Name (ARN) of the table bucket that contains the
  table.
"""
function delete_table_policy end

function delete_table_policy(
    name, namespace, tableBucketARN; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3tables(
        "DELETE",
        "/tables/$(tableBucketARN)/$(namespace)/$(name)/policy";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_table_policy(
    name,
    namespace,
    tableBucketARN,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "DELETE",
        "/tables/$(tableBucketARN)/$(namespace)/$(name)/policy",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_table_replication(table_arn, version_token)
    delete_table_replication(table_arn, version_token, params::Dict{String,<:Any})

Deletes the replication configuration for a specific table. After deletion, new updates to
this table will no longer be replicated to destination tables, though existing replicated
copies will remain in destination buckets.

### Permissions

You must have the `s3tables:DeleteTableReplication` permission to use this operation.

# Arguments

- `table_arn`: The Amazon Resource Name (ARN) of the table.
- `version_token`: A version token from a previous GetTableReplication call. Use this token
  to ensure you're deleting the expected version of the configuration.
"""
function delete_table_replication end

function delete_table_replication(
    tableArn, versionToken; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3tables(
        "DELETE",
        "/table-replication",
        Dict{String,Any}("tableArn" => tableArn, "versionToken" => versionToken);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_table_replication(
    tableArn,
    versionToken,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "DELETE",
        "/table-replication",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("tableArn" => tableArn, "versionToken" => versionToken),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_namespace(namespace, table_bucket_arn)
    get_namespace(namespace, table_bucket_arn, params::Dict{String,<:Any})

Gets details about a namespace. For more information, see [Table namespaces](https://docs.aws.amazon.com/AmazonS3/latest/userguide/s3-tables-namespace.html)
in the *Amazon Simple Storage Service User Guide*.

### Permissions

You must have the `s3tables:GetNamespace` permission to use this operation.

# Arguments

- `namespace`: The name of the namespace.
- `table_bucket_arn`: The Amazon Resource Name (ARN) of the table bucket.
"""
function get_namespace end

function get_namespace(
    namespace, tableBucketARN; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3tables(
        "GET",
        "/namespaces/$(tableBucketARN)/$(namespace)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_namespace(
    namespace,
    tableBucketARN,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "GET",
        "/namespaces/$(tableBucketARN)/$(namespace)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_table()
    get_table(params::Dict{String,<:Any})

Gets details about a table. For more information, see [S3 Tables](https://docs.aws.amazon.com/AmazonS3/latest/userguide/s3-tables-tables.html)
in the *Amazon Simple Storage Service User Guide*.

### Permissions

You must have the `s3tables:GetTable` permission to use this operation.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"name"`: The name of the table.
- `"namespace"`: The name of the namespace the table is associated with.
- `"tableArn"`: The Amazon Resource Name (ARN) of the table.
- `"tableBucketARN"`: The Amazon Resource Name (ARN) of the table bucket associated with the
  table.
"""
function get_table end

function get_table(; aws_config::AbstractAWSConfig=current_aws_config())
    return s3tables("GET", "/get-table"; aws_config, feature_set=SERVICE_FEATURE_SET)
end

function get_table(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3tables(
        "GET", "/get-table", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    get_table_bucket(table_bucket_arn)
    get_table_bucket(table_bucket_arn, params::Dict{String,<:Any})

Gets details on a table bucket. For more information, see [Viewing details about an Amazon S3 table bucket](https://docs.aws.amazon.com/AmazonS3/latest/userguide/s3-tables-buckets-details.html)
in the *Amazon Simple Storage Service User Guide*.

### Permissions

You must have the `s3tables:GetTableBucket` permission to use this operation.

# Arguments

- `table_bucket_arn`: The Amazon Resource Name (ARN) of the table bucket.
"""
function get_table_bucket end

function get_table_bucket(
    tableBucketARN; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3tables(
        "GET", "/buckets/$(tableBucketARN)"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function get_table_bucket(
    tableBucketARN,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "GET",
        "/buckets/$(tableBucketARN)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_table_bucket_encryption(table_bucket_arn)
    get_table_bucket_encryption(table_bucket_arn, params::Dict{String,<:Any})

Gets the encryption configuration for a table bucket.

### Permissions

You must have the `s3tables:GetTableBucketEncryption` permission to use this operation.

# Arguments

- `table_bucket_arn`: The Amazon Resource Name (ARN) of the table bucket.
"""
function get_table_bucket_encryption end

function get_table_bucket_encryption(
    tableBucketARN; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3tables(
        "GET",
        "/buckets/$(tableBucketARN)/encryption";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_table_bucket_encryption(
    tableBucketARN,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "GET",
        "/buckets/$(tableBucketARN)/encryption",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_table_bucket_maintenance_configuration(table_bucket_arn)
    get_table_bucket_maintenance_configuration(table_bucket_arn, params::Dict{String,<:Any})

Gets details about a maintenance configuration for a given table bucket. For more
information, see [Amazon S3 table bucket maintenance](https://docs.aws.amazon.com/AmazonS3/latest/userguide/s3-table-buckets-maintenance.html)
in the *Amazon Simple Storage Service User Guide*.

### Permissions

You must have the `s3tables:GetTableBucketMaintenanceConfiguration` permission to use this
operation.

# Arguments

- `table_bucket_arn`: The Amazon Resource Name (ARN) of the table bucket associated with the
  maintenance configuration.
"""
function get_table_bucket_maintenance_configuration end

function get_table_bucket_maintenance_configuration(
    tableBucketARN; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3tables(
        "GET",
        "/buckets/$(tableBucketARN)/maintenance";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_table_bucket_maintenance_configuration(
    tableBucketARN,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "GET",
        "/buckets/$(tableBucketARN)/maintenance",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_table_bucket_metrics_configuration(table_bucket_arn)
    get_table_bucket_metrics_configuration(table_bucket_arn, params::Dict{String,<:Any})

Gets the metrics configuration for a table bucket.

### Permissions

You must have the `s3tables:GetTableBucketMetricsConfiguration` permission to use this
operation.

# Arguments

- `table_bucket_arn`: The Amazon Resource Name (ARN) of the table bucket.
"""
function get_table_bucket_metrics_configuration end

function get_table_bucket_metrics_configuration(
    tableBucketARN; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3tables(
        "GET",
        "/buckets/$(tableBucketARN)/metrics";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_table_bucket_metrics_configuration(
    tableBucketARN,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "GET",
        "/buckets/$(tableBucketARN)/metrics",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_table_bucket_policy(table_bucket_arn)
    get_table_bucket_policy(table_bucket_arn, params::Dict{String,<:Any})

Gets details about a table bucket policy. For more information, see [Viewing a table bucket policy](https://docs.aws.amazon.com/AmazonS3/latest/userguide/s3-tables-bucket-policy.html#table-bucket-policy-get)
in the *Amazon Simple Storage Service User Guide*.

### Permissions

You must have the `s3tables:GetTableBucketPolicy` permission to use this operation.

# Arguments

- `table_bucket_arn`: The Amazon Resource Name (ARN) of the table bucket.
"""
function get_table_bucket_policy end

function get_table_bucket_policy(
    tableBucketARN; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3tables(
        "GET",
        "/buckets/$(tableBucketARN)/policy";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_table_bucket_policy(
    tableBucketARN,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "GET",
        "/buckets/$(tableBucketARN)/policy",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_table_bucket_replication(table_bucket_arn)
    get_table_bucket_replication(table_bucket_arn, params::Dict{String,<:Any})

Retrieves the replication configuration for a table bucket.This operation returns the IAM
role, `versionToken`, and replication rules that define how tables in this bucket are
replicated to other buckets.

### Permissions

You must have the `s3tables:GetTableBucketReplication` permission to use this operation.

# Arguments

- `table_bucket_arn`: The Amazon Resource Name (ARN) of the table bucket.
"""
function get_table_bucket_replication end

function get_table_bucket_replication(
    tableBucketARN; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3tables(
        "GET",
        "/table-bucket-replication",
        Dict{String,Any}("tableBucketARN" => tableBucketARN);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_table_bucket_replication(
    tableBucketARN,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "GET",
        "/table-bucket-replication",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("tableBucketARN" => tableBucketARN), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_table_bucket_storage_class(table_bucket_arn)
    get_table_bucket_storage_class(table_bucket_arn, params::Dict{String,<:Any})

Retrieves the storage class configuration for a specific table. This allows you to view the
storage class settings that apply to an individual table, which may differ from the table
bucket's default configuration.

### Permissions

You must have the `s3tables:GetTableBucketStorageClass` permission to use this operation.

# Arguments

- `table_bucket_arn`: The Amazon Resource Name (ARN) of the table bucket.
"""
function get_table_bucket_storage_class end

function get_table_bucket_storage_class(
    tableBucketARN; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3tables(
        "GET",
        "/buckets/$(tableBucketARN)/storage-class";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_table_bucket_storage_class(
    tableBucketARN,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "GET",
        "/buckets/$(tableBucketARN)/storage-class",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_table_encryption(name, namespace, table_bucket_arn)
    get_table_encryption(name, namespace, table_bucket_arn, params::Dict{String,<:Any})

Gets the encryption configuration for a table.

### Permissions

You must have the `s3tables:GetTableEncryption` permission to use this operation.

# Arguments

- `name`: The name of the table.
- `namespace`: The namespace associated with the table.
- `table_bucket_arn`: The Amazon Resource Name (ARN) of the table bucket containing the
  table.
"""
function get_table_encryption end

function get_table_encryption(
    name, namespace, tableBucketARN; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3tables(
        "GET",
        "/tables/$(tableBucketARN)/$(namespace)/$(name)/encryption";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_table_encryption(
    name,
    namespace,
    tableBucketARN,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "GET",
        "/tables/$(tableBucketARN)/$(namespace)/$(name)/encryption",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_table_maintenance_configuration(name, namespace, table_bucket_arn)
    get_table_maintenance_configuration(name, namespace, table_bucket_arn, params::Dict{String,<:Any})

Gets details about the maintenance configuration of a table. For more information, see [S3 Tables maintenance](https://docs.aws.amazon.com/AmazonS3/latest/userguide/s3-tables-maintenance.html)
in the *Amazon Simple Storage Service User Guide*.

### Permissions

- You must have the `s3tables:GetTableMaintenanceConfiguration` permission to use this
  operation.
- You must have the `s3tables:GetTableData` permission to use set the compaction strategy to
  `sort` or `zorder`.

# Arguments

- `name`: The name of the table.
- `namespace`: The namespace associated with the table.
- `table_bucket_arn`: The Amazon Resource Name (ARN) of the table bucket.
"""
function get_table_maintenance_configuration end

function get_table_maintenance_configuration(
    name, namespace, tableBucketARN; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3tables(
        "GET",
        "/tables/$(tableBucketARN)/$(namespace)/$(name)/maintenance";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_table_maintenance_configuration(
    name,
    namespace,
    tableBucketARN,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "GET",
        "/tables/$(tableBucketARN)/$(namespace)/$(name)/maintenance",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_table_maintenance_job_status(name, namespace, table_bucket_arn)
    get_table_maintenance_job_status(name, namespace, table_bucket_arn, params::Dict{String,<:Any})

Gets the status of a maintenance job for a table. For more information, see [S3 Tables maintenance](https://docs.aws.amazon.com/AmazonS3/latest/userguide/s3-tables-maintenance.html)
in the *Amazon Simple Storage Service User Guide*.

### Permissions

You must have the `s3tables:GetTableMaintenanceJobStatus` permission to use this operation.

# Arguments

- `name`: The name of the table containing the maintenance job status you want to check.
- `namespace`: The name of the namespace the table is associated with.
- `table_bucket_arn`: The Amazon Resource Name (ARN) of the table bucket.
"""
function get_table_maintenance_job_status end

function get_table_maintenance_job_status(
    name, namespace, tableBucketARN; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3tables(
        "GET",
        "/tables/$(tableBucketARN)/$(namespace)/$(name)/maintenance-job-status";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_table_maintenance_job_status(
    name,
    namespace,
    tableBucketARN,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "GET",
        "/tables/$(tableBucketARN)/$(namespace)/$(name)/maintenance-job-status",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_table_metadata_location(name, namespace, table_bucket_arn)
    get_table_metadata_location(name, namespace, table_bucket_arn, params::Dict{String,<:Any})

Gets the location of the table metadata.

### Permissions

You must have the `s3tables:GetTableMetadataLocation` permission to use this operation.

# Arguments

- `name`: The name of the table.
- `namespace`: The namespace of the table.
- `table_bucket_arn`: The Amazon Resource Name (ARN) of the table bucket.
"""
function get_table_metadata_location end

function get_table_metadata_location(
    name, namespace, tableBucketARN; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3tables(
        "GET",
        "/tables/$(tableBucketARN)/$(namespace)/$(name)/metadata-location";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_table_metadata_location(
    name,
    namespace,
    tableBucketARN,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "GET",
        "/tables/$(tableBucketARN)/$(namespace)/$(name)/metadata-location",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_table_policy(name, namespace, table_bucket_arn)
    get_table_policy(name, namespace, table_bucket_arn, params::Dict{String,<:Any})

Gets details about a table policy. For more information, see [Viewing a table policy](https://docs.aws.amazon.com/AmazonS3/latest/userguide/s3-tables-table-policy.html#table-policy-get)
in the *Amazon Simple Storage Service User Guide*.

### Permissions

You must have the `s3tables:GetTablePolicy` permission to use this operation.

# Arguments

- `name`: The name of the table.
- `namespace`: The namespace associated with the table.
- `table_bucket_arn`: The Amazon Resource Name (ARN) of the table bucket that contains the
  table.
"""
function get_table_policy end

function get_table_policy(
    name, namespace, tableBucketARN; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3tables(
        "GET",
        "/tables/$(tableBucketARN)/$(namespace)/$(name)/policy";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_table_policy(
    name,
    namespace,
    tableBucketARN,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "GET",
        "/tables/$(tableBucketARN)/$(namespace)/$(name)/policy",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_table_record_expiration_configuration(table_arn)
    get_table_record_expiration_configuration(table_arn, params::Dict{String,<:Any})

Retrieves the expiration configuration settings for records in a table, and the status of
the configuration. If the status of the configuration is `enabled`, records expire and are
automatically removed from the table after the specified number of days.

### Permissions

You must have the `s3tables:GetTableRecordExpirationConfiguration` permission to use this
operation.

# Arguments

- `table_arn`: The Amazon Resource Name (ARN) of the table.
"""
function get_table_record_expiration_configuration end

function get_table_record_expiration_configuration(
    tableArn; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3tables(
        "GET",
        "/table-record-expiration",
        Dict{String,Any}("tableArn" => tableArn);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_table_record_expiration_configuration(
    tableArn,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "GET",
        "/table-record-expiration",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("tableArn" => tableArn), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_table_record_expiration_job_status(table_arn)
    get_table_record_expiration_job_status(table_arn, params::Dict{String,<:Any})

Retrieves the status, metrics, and details of the latest record expiration job for a table.
This includes when the job ran, and whether it succeeded or failed. If the job ran
successfully, this also includes statistics about the records that were removed.

### Permissions

You must have the `s3tables:GetTableRecordExpirationJobStatus` permission to use this
operation.

# Arguments

- `table_arn`: The Amazon Resource Name (ARN) of the table.
"""
function get_table_record_expiration_job_status end

function get_table_record_expiration_job_status(
    tableArn; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3tables(
        "GET",
        "/table-record-expiration-job-status",
        Dict{String,Any}("tableArn" => tableArn);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_table_record_expiration_job_status(
    tableArn,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "GET",
        "/table-record-expiration-job-status",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("tableArn" => tableArn), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_table_replication(table_arn)
    get_table_replication(table_arn, params::Dict{String,<:Any})

Retrieves the replication configuration for a specific table.

### Permissions

You must have the `s3tables:GetTableReplication` permission to use this operation.

# Arguments

- `table_arn`: The Amazon Resource Name (ARN) of the table.
"""
function get_table_replication end

function get_table_replication(tableArn; aws_config::AbstractAWSConfig=current_aws_config())
    return s3tables(
        "GET",
        "/table-replication",
        Dict{String,Any}("tableArn" => tableArn);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_table_replication(
    tableArn,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "GET",
        "/table-replication",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("tableArn" => tableArn), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_table_replication_status(table_arn)
    get_table_replication_status(table_arn, params::Dict{String,<:Any})

Retrieves the replication status for a table, including the status of replication to each
destination. This operation provides visibility into replication health and progress.

### Permissions

You must have the `s3tables:GetTableReplicationStatus` permission to use this operation.

# Arguments

- `table_arn`: The Amazon Resource Name (ARN) of the table.
"""
function get_table_replication_status end

function get_table_replication_status(
    tableArn; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3tables(
        "GET",
        "/replication-status",
        Dict{String,Any}("tableArn" => tableArn);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_table_replication_status(
    tableArn,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "GET",
        "/replication-status",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("tableArn" => tableArn), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_table_storage_class(name, namespace, table_bucket_arn)
    get_table_storage_class(name, namespace, table_bucket_arn, params::Dict{String,<:Any})

Retrieves the storage class configuration for a specific table. This allows you to view the
storage class settings that apply to an individual table, which may differ from the table
bucket's default configuration.

### Permissions

You must have the `s3tables:GetTableStorageClass` permission to use this operation.

# Arguments

- `name`: The name of the table.
- `namespace`: The namespace associated with the table.
- `table_bucket_arn`: The Amazon Resource Name (ARN) of the table bucket that contains the
  table.
"""
function get_table_storage_class end

function get_table_storage_class(
    name, namespace, tableBucketARN; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3tables(
        "GET",
        "/tables/$(tableBucketARN)/$(namespace)/$(name)/storage-class";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_table_storage_class(
    name,
    namespace,
    tableBucketARN,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "GET",
        "/tables/$(tableBucketARN)/$(namespace)/$(name)/storage-class",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_namespaces(table_bucket_arn)
    list_namespaces(table_bucket_arn, params::Dict{String,<:Any})

Lists the namespaces within a table bucket. For more information, see [Table namespaces](https://docs.aws.amazon.com/AmazonS3/latest/userguide/s3-tables-namespace.html)
in the *Amazon Simple Storage Service User Guide*.

### Permissions

You must have the `s3tables:ListNamespaces` permission to use this operation.

# Arguments

- `table_bucket_arn`: The Amazon Resource Name (ARN) of the table bucket.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"continuationToken"`: `ContinuationToken` indicates to Amazon S3 that the list is being
  continued on this bucket with a token. `ContinuationToken` is obfuscated and is not a real
  key. You can use this `ContinuationToken` for pagination of the list results.
- `"maxNamespaces"`: The maximum number of namespaces to return in the list.
- `"prefix"`: The prefix of the namespaces.
"""
function list_namespaces end

function list_namespaces(tableBucketARN; aws_config::AbstractAWSConfig=current_aws_config())
    return s3tables(
        "GET", "/namespaces/$(tableBucketARN)"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_namespaces(
    tableBucketARN,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "GET",
        "/namespaces/$(tableBucketARN)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_table_buckets()
    list_table_buckets(params::Dict{String,<:Any})

Lists table buckets for your account. For more information, see [S3 Table buckets](https://docs.aws.amazon.com/AmazonS3/latest/userguide/s3-tables-buckets.html)
in the *Amazon Simple Storage Service User Guide*.

### Permissions

You must have the `s3tables:ListTableBuckets` permission to use this operation.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"continuationToken"`: `ContinuationToken` indicates to Amazon S3 that the list is being
  continued on this bucket with a token. `ContinuationToken` is obfuscated and is not a real
  key. You can use this `ContinuationToken` for pagination of the list results.
- `"maxBuckets"`: The maximum number of table buckets to return in the list.
- `"prefix"`: The prefix of the table buckets.
- `"type"`: The type of table buckets to filter by in the list.
"""
function list_table_buckets end

function list_table_buckets(; aws_config::AbstractAWSConfig=current_aws_config())
    return s3tables("GET", "/buckets"; aws_config, feature_set=SERVICE_FEATURE_SET)
end

function list_table_buckets(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3tables("GET", "/buckets", params; aws_config, feature_set=SERVICE_FEATURE_SET)
end

"""
    list_tables(table_bucket_arn)
    list_tables(table_bucket_arn, params::Dict{String,<:Any})

List tables in the given table bucket. For more information, see [S3 Tables](https://docs.aws.amazon.com/AmazonS3/latest/userguide/s3-tables-tables.html)
in the *Amazon Simple Storage Service User Guide*.

### Permissions

You must have the `s3tables:ListTables` permission to use this operation.

# Arguments

- `table_bucket_arn`: The Amazon resource Name (ARN) of the table bucket.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"continuationToken"`: `ContinuationToken` indicates to Amazon S3 that the list is being
  continued on this bucket with a token. `ContinuationToken` is obfuscated and is not a real
  key. You can use this `ContinuationToken` for pagination of the list results.
- `"maxTables"`: The maximum number of tables to return.
- `"namespace"`: The namespace of the tables.
- `"prefix"`: The prefix of the tables.
"""
function list_tables end

function list_tables(tableBucketARN; aws_config::AbstractAWSConfig=current_aws_config())
    return s3tables(
        "GET", "/tables/$(tableBucketARN)"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_tables(
    tableBucketARN,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "GET",
        "/tables/$(tableBucketARN)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_tags_for_resource(resource_arn)
    list_tags_for_resource(resource_arn, params::Dict{String,<:Any})

Lists all of the tags applied to a specified Amazon S3 Tables resource. Each tag is a label
consisting of a key and value pair. Tags can help you organize, track costs for, and control
access to resources.

!!! note
    For a list of S3 resources that support tagging, see [Managing tags for Amazon S3 resources](https://docs.aws.amazon.com/AmazonS3/latest/userguide/tagging.html#manage-tags).

### Permissions

For tables and table buckets, you must have the `s3tables:ListTagsForResource` permission to
use this operation.

# Arguments

- `resource_arn`: The Amazon Resource Name (ARN) of the Amazon S3 Tables resource that you
  want to list tags for. The tagged resource can be a table bucket or a table. For a list of
  all S3 resources that support tagging, see [Managing tags for Amazon S3 resources](https://docs.aws.amazon.com/AmazonS3/latest/userguide/tagging.html#manage-tags).
"""
function list_tags_for_resource end

function list_tags_for_resource(
    resourceArn; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3tables(
        "GET", "/tag/$(resourceArn)"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_tags_for_resource(
    resourceArn,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "GET", "/tag/$(resourceArn)", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    put_table_bucket_encryption(encryption_configuration, table_bucket_arn)
    put_table_bucket_encryption(encryption_configuration, table_bucket_arn, params::Dict{String,<:Any})

Sets the encryption configuration for a table bucket.

### Permissions

You must have the `s3tables:PutTableBucketEncryption` permission to use this operation.

!!! note
    If you choose SSE-KMS encryption you must grant the S3 Tables maintenance principal
    access to your KMS key. For more information, see [Permissions requirements for S3 Tables SSE-KMS encryption](https://docs.aws.amazon.com/AmazonS3/latest/userguide/s3-tables-kms-permissions.html)
    in the *Amazon Simple Storage Service User Guide*.

# Arguments

- `encryption_configuration`: The encryption configuration to apply to the table bucket.
- `table_bucket_arn`: The Amazon Resource Name (ARN) of the table bucket.
"""
function put_table_bucket_encryption end

function put_table_bucket_encryption(
    encryptionConfiguration,
    tableBucketARN;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "PUT",
        "/buckets/$(tableBucketARN)/encryption",
        Dict{String,Any}("encryptionConfiguration" => encryptionConfiguration);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function put_table_bucket_encryption(
    encryptionConfiguration,
    tableBucketARN,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "PUT",
        "/buckets/$(tableBucketARN)/encryption",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("encryptionConfiguration" => encryptionConfiguration),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    put_table_bucket_maintenance_configuration(table_bucket_arn, type, value)
    put_table_bucket_maintenance_configuration(table_bucket_arn, type, value, params::Dict{String,<:Any})

Creates a new maintenance configuration or replaces an existing maintenance configuration
for a table bucket. For more information, see [Amazon S3 table bucket maintenance](https://docs.aws.amazon.com/AmazonS3/latest/userguide/s3-table-buckets-maintenance.html)
in the *Amazon Simple Storage Service User Guide*.

### Permissions

You must have the `s3tables:PutTableBucketMaintenanceConfiguration` permission to use this
operation.

# Arguments

- `table_bucket_arn`: The Amazon Resource Name (ARN) of the table bucket associated with the
  maintenance configuration.
- `type`: The type of the maintenance configuration.
- `value`: Defines the values of the maintenance configuration for the table bucket.
"""
function put_table_bucket_maintenance_configuration end

function put_table_bucket_maintenance_configuration(
    tableBucketARN, type, value; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3tables(
        "PUT",
        "/buckets/$(tableBucketARN)/maintenance/$(type)",
        Dict{String,Any}("value" => value);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function put_table_bucket_maintenance_configuration(
    tableBucketARN,
    type,
    value,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "PUT",
        "/buckets/$(tableBucketARN)/maintenance/$(type)",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("value" => value), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    put_table_bucket_metrics_configuration(table_bucket_arn)
    put_table_bucket_metrics_configuration(table_bucket_arn, params::Dict{String,<:Any})

Sets the metrics configuration for a table bucket.

### Permissions

You must have the `s3tables:PutTableBucketMetricsConfiguration` permission to use this
operation.

# Arguments

- `table_bucket_arn`: The Amazon Resource Name (ARN) of the table bucket.
"""
function put_table_bucket_metrics_configuration end

function put_table_bucket_metrics_configuration(
    tableBucketARN; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3tables(
        "PUT",
        "/buckets/$(tableBucketARN)/metrics";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function put_table_bucket_metrics_configuration(
    tableBucketARN,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "PUT",
        "/buckets/$(tableBucketARN)/metrics",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    put_table_bucket_policy(resource_policy, table_bucket_arn)
    put_table_bucket_policy(resource_policy, table_bucket_arn, params::Dict{String,<:Any})

Creates a new table bucket policy or replaces an existing table bucket policy for a table
bucket. For more information, see [Adding a table bucket policy](https://docs.aws.amazon.com/AmazonS3/latest/userguide/s3-tables-bucket-policy.html#table-bucket-policy-add)
in the *Amazon Simple Storage Service User Guide*.

### Permissions

You must have the `s3tables:PutTableBucketPolicy` permission to use this operation.

# Arguments

- `resource_policy`: The `JSON` that defines the policy.
- `table_bucket_arn`: The Amazon Resource Name (ARN) of the table bucket.
"""
function put_table_bucket_policy end

function put_table_bucket_policy(
    resourcePolicy, tableBucketARN; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3tables(
        "PUT",
        "/buckets/$(tableBucketARN)/policy",
        Dict{String,Any}("resourcePolicy" => resourcePolicy);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function put_table_bucket_policy(
    resourcePolicy,
    tableBucketARN,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "PUT",
        "/buckets/$(tableBucketARN)/policy",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("resourcePolicy" => resourcePolicy), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    put_table_bucket_replication(configuration, table_bucket_arn)
    put_table_bucket_replication(configuration, table_bucket_arn, params::Dict{String,<:Any})

Creates or updates the replication configuration for a table bucket. This operation defines
how tables in the source bucket are replicated to destination buckets. Replication helps
ensure data availability and disaster recovery across regions or accounts.

### Permissions

- You must have the `s3tables:PutTableBucketReplication` permission to use this operation.
  The IAM role specified in the configuration must have permissions to read from the source
  bucket and write permissions to all destination buckets.
- You must also have the following permissions:
  - `s3tables:GetTable` permission on the source table.
  - `s3tables:ListTables` permission on the bucket containing the table.
  - `s3tables:CreateTable` permission for the destination.
  - `s3tables:CreateNamespace` permission for the destination.
  - `s3tables:GetTableMaintenanceConfig` permission for the source bucket.
  - `s3tables:PutTableMaintenanceConfig` permission for the destination bucket.
- You must have `iam:PassRole` permission with condition allowing roles to be passed to
  `replication.s3tables.amazonaws.com`.

# Arguments

- `configuration`: The replication configuration to apply, including the IAM role and
  replication rules.
- `table_bucket_arn`: The Amazon Resource Name (ARN) of the source table bucket.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"versionToken"`: A version token from a previous GetTableBucketReplication call. Use this
  token to ensure you're updating the expected version of the configuration.
"""
function put_table_bucket_replication end

function put_table_bucket_replication(
    configuration, tableBucketARN; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3tables(
        "PUT",
        "/table-bucket-replication",
        Dict{String,Any}(
            "configuration" => configuration, "tableBucketARN" => tableBucketARN
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function put_table_bucket_replication(
    configuration,
    tableBucketARN,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "PUT",
        "/table-bucket-replication",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "configuration" => configuration, "tableBucketARN" => tableBucketARN
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    put_table_bucket_storage_class(storage_class_configuration, table_bucket_arn)
    put_table_bucket_storage_class(storage_class_configuration, table_bucket_arn, params::Dict{String,<:Any})

Sets or updates the storage class configuration for a table bucket. This configuration
serves as the default storage class for all new tables created in the bucket, allowing you
to optimize storage costs at the bucket level.

### Permissions

You must have the `s3tables:PutTableBucketStorageClass` permission to use this operation.

# Arguments

- `storage_class_configuration`: The storage class configuration to apply to the table
  bucket. This configuration will serve as the default for new tables created in this
  bucket.
- `table_bucket_arn`: The Amazon Resource Name (ARN) of the table bucket.
"""
function put_table_bucket_storage_class end

function put_table_bucket_storage_class(
    storageClassConfiguration,
    tableBucketARN;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "PUT",
        "/buckets/$(tableBucketARN)/storage-class",
        Dict{String,Any}("storageClassConfiguration" => storageClassConfiguration);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function put_table_bucket_storage_class(
    storageClassConfiguration,
    tableBucketARN,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "PUT",
        "/buckets/$(tableBucketARN)/storage-class",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("storageClassConfiguration" => storageClassConfiguration),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    put_table_maintenance_configuration(name, namespace, table_bucket_arn, type, value)
    put_table_maintenance_configuration(name, namespace, table_bucket_arn, type, value, params::Dict{String,<:Any})

Creates a new maintenance configuration or replaces an existing maintenance configuration
for a table. For more information, see [S3 Tables maintenance](https://docs.aws.amazon.com/AmazonS3/latest/userguide/s3-tables-maintenance.html)
in the *Amazon Simple Storage Service User Guide*.

### Permissions

You must have the `s3tables:PutTableMaintenanceConfiguration` permission to use this
operation.

# Arguments

- `name`: The name of the table.
- `namespace`: The namespace of the table.
- `table_bucket_arn`: The Amazon Resource Name (ARN) of the table associated with the
  maintenance configuration.
- `type`: The type of the maintenance configuration.
- `value`: Defines the values of the maintenance configuration for the table.
"""
function put_table_maintenance_configuration end

function put_table_maintenance_configuration(
    name,
    namespace,
    tableBucketARN,
    type,
    value;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "PUT",
        "/tables/$(tableBucketARN)/$(namespace)/$(name)/maintenance/$(type)",
        Dict{String,Any}("value" => value);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function put_table_maintenance_configuration(
    name,
    namespace,
    tableBucketARN,
    type,
    value,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "PUT",
        "/tables/$(tableBucketARN)/$(namespace)/$(name)/maintenance/$(type)",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("value" => value), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    put_table_policy(name, namespace, resource_policy, table_bucket_arn)
    put_table_policy(name, namespace, resource_policy, table_bucket_arn, params::Dict{String,<:Any})

Creates a new table policy or replaces an existing table policy for a table. For more
information, see [Adding a table policy](https://docs.aws.amazon.com/AmazonS3/latest/userguide/s3-tables-table-policy.html#table-policy-add)
in the *Amazon Simple Storage Service User Guide*.

### Permissions

You must have the `s3tables:PutTablePolicy` permission to use this operation.

# Arguments

- `name`: The name of the table.
- `namespace`: The namespace associated with the table.
- `resource_policy`: The `JSON` that defines the policy.
- `table_bucket_arn`: The Amazon Resource Name (ARN) of the table bucket that contains the
  table.
"""
function put_table_policy end

function put_table_policy(
    name,
    namespace,
    resourcePolicy,
    tableBucketARN;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "PUT",
        "/tables/$(tableBucketARN)/$(namespace)/$(name)/policy",
        Dict{String,Any}("resourcePolicy" => resourcePolicy);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function put_table_policy(
    name,
    namespace,
    resourcePolicy,
    tableBucketARN,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "PUT",
        "/tables/$(tableBucketARN)/$(namespace)/$(name)/policy",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("resourcePolicy" => resourcePolicy), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    put_table_record_expiration_configuration(table_arn, value)
    put_table_record_expiration_configuration(table_arn, value, params::Dict{String,<:Any})

Creates or updates the expiration configuration settings for records in a table, including
the status of the configuration. If you enable record expiration for a table, records expire
and are automatically removed from the table after the number of days that you specify.

### Permissions

You must have the `s3tables:PutTableRecordExpirationConfiguration` permission to use this
operation.

# Arguments

- `table_arn`: The Amazon Resource Name (ARN) of the table.
- `value`: The record expiration configuration to apply to the table, including the status
  (`enabled` or `disabled`) and retention period in days.
"""
function put_table_record_expiration_configuration end

function put_table_record_expiration_configuration(
    tableArn, value; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3tables(
        "PUT",
        "/table-record-expiration",
        Dict{String,Any}("tableArn" => tableArn, "value" => value);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function put_table_record_expiration_configuration(
    tableArn,
    value,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "PUT",
        "/table-record-expiration",
        Dict{String,Any}(
            mergewith(
                _merge, Dict{String,Any}("tableArn" => tableArn, "value" => value), params
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    put_table_replication(configuration, table_arn)
    put_table_replication(configuration, table_arn, params::Dict{String,<:Any})

Creates or updates the replication configuration for a specific table. This operation allows
you to define table-level replication independently of bucket-level replication, providing
granular control over which tables are replicated and where.

### Permissions

- You must have the `s3tables:PutTableReplication` permission to use this operation. The IAM
  role specified in the configuration must have permissions to read from the source table
  and write to all destination tables.
- You must also have the following permissions:
  - `s3tables:GetTable` permission on the source table being replicated.
  - `s3tables:CreateTable` permission for the destination.
  - `s3tables:CreateNamespace` permission for the destination.
  - `s3tables:GetTableMaintenanceConfig` permission for the source table.
  - `s3tables:PutTableMaintenanceConfig` permission for the destination table.
- You must have `iam:PassRole` permission with condition allowing roles to be passed to
  `replication.s3tables.amazonaws.com`.

# Arguments

- `configuration`: The replication configuration to apply to the table, including the IAM
  role and replication rules.
- `table_arn`: The Amazon Resource Name (ARN) of the source table.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"versionToken"`: A version token from a previous GetTableReplication call. Use this token
  to ensure you're updating the expected version of the configuration.
"""
function put_table_replication end

function put_table_replication(
    configuration, tableArn; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3tables(
        "PUT",
        "/table-replication",
        Dict{String,Any}("configuration" => configuration, "tableArn" => tableArn);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function put_table_replication(
    configuration,
    tableArn,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "PUT",
        "/table-replication",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("configuration" => configuration, "tableArn" => tableArn),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    rename_table(name, namespace, table_bucket_arn)
    rename_table(name, namespace, table_bucket_arn, params::Dict{String,<:Any})

Renames a table or a namespace. For more information, see [S3 Tables](https://docs.aws.amazon.com/AmazonS3/latest/userguide/s3-tables-tables.html)
in the *Amazon Simple Storage Service User Guide*.

### Permissions

You must have the `s3tables:RenameTable` permission to use this operation.

# Arguments

- `name`: The current name of the table.
- `namespace`: The namespace associated with the table.
- `table_bucket_arn`: The Amazon Resource Name (ARN) of the table bucket.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"newName"`: The new name for the table.
- `"newNamespaceName"`: The new name for the namespace.
- `"versionToken"`: The version token of the table.
"""
function rename_table end

function rename_table(
    name, namespace, tableBucketARN; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3tables(
        "PUT",
        "/tables/$(tableBucketARN)/$(namespace)/$(name)/rename";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function rename_table(
    name,
    namespace,
    tableBucketARN,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "PUT",
        "/tables/$(tableBucketARN)/$(namespace)/$(name)/rename",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    tag_resource(resource_arn, tags)
    tag_resource(resource_arn, tags, params::Dict{String,<:Any})

Applies one or more user-defined tags to an Amazon S3 Tables resource or updates existing
tags. Each tag is a label consisting of a key and value pair. Tags can help you organize,
track costs for, and control access to your resources. You can add up to 50 tags for each S3
resource.

!!! note
    For a list of S3 resources that support tagging, see [Managing tags for Amazon S3 resources](https://docs.aws.amazon.com/AmazonS3/latest/userguide/tagging.html#manage-tags).

### Permissions

For tables and table buckets, you must have the `s3tables:TagResource` permission to use
this operation.

# Arguments

- `resource_arn`: The Amazon Resource Name (ARN) of the Amazon S3 Tables resource that
  you're applying tags to. The tagged resource can be a table bucket or a table. For a list
  of all S3 resources that support tagging, see [Managing tags for Amazon S3 resources](https://docs.aws.amazon.com/AmazonS3/latest/userguide/tagging.html#manage-tags).
- `tags`: The user-defined tag that you want to add to the specified S3 Tables resource. For
  more information, see [Tagging for cost allocation or attribute-based access control (ABAC)](https://docs.aws.amazon.com/AmazonS3/latest/userguide/tagging.html).
"""
function tag_resource end

function tag_resource(resourceArn, tags; aws_config::AbstractAWSConfig=current_aws_config())
    return s3tables(
        "POST",
        "/tag/$(resourceArn)",
        Dict{String,Any}("tags" => tags);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function tag_resource(
    resourceArn,
    tags,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "POST",
        "/tag/$(resourceArn)",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("tags" => tags), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    untag_resource(resource_arn, tag_keys)
    untag_resource(resource_arn, tag_keys, params::Dict{String,<:Any})

Removes the specified user-defined tags from an Amazon S3 Tables resource. You can pass one
or more tag keys.

!!! note
    For a list of S3 resources that support tagging, see [Managing tags for Amazon S3 resources](https://docs.aws.amazon.com/AmazonS3/latest/userguide/tagging.html#manage-tags).

### Permissions

For tables and table buckets, you must have the `s3tables:UntagResource` permission to use
this operation.

# Arguments

- `resource_arn`: The Amazon Resource Name (ARN) of the Amazon S3 Tables resource that
  you're removing tags from. The tagged resource can be a table bucket or a table. For a
  list of all S3 resources that support tagging, see [Managing tags for Amazon S3 resources](https://docs.aws.amazon.com/AmazonS3/latest/userguide/tagging.html#manage-tags).
- `tag_keys`: The array of tag keys that you're removing from the S3 Tables resource. For
  more information, see [Tagging for cost allocation or attribute-based access control (ABAC)](https://docs.aws.amazon.com/AmazonS3/latest/userguide/tagging.html).
"""
function untag_resource end

function untag_resource(
    resourceArn, tagKeys; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3tables(
        "DELETE",
        "/tag/$(resourceArn)",
        Dict{String,Any}("tagKeys" => tagKeys);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function untag_resource(
    resourceArn,
    tagKeys,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "DELETE",
        "/tag/$(resourceArn)",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("tagKeys" => tagKeys), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_table_metadata_location(metadata_location, name, namespace, table_bucket_arn, version_token)
    update_table_metadata_location(metadata_location, name, namespace, table_bucket_arn, version_token, params::Dict{String,<:Any})

Updates the metadata location for a table. The metadata location of a table must be an S3
URI that begins with the table's warehouse location. The metadata location for an Apache
Iceberg table must end with `.metadata.json`, or if the metadata file is Gzip-compressed,
`.metadata.json.gz`.

### Permissions

You must have the `s3tables:UpdateTableMetadataLocation` permission to use this operation.

# Arguments

- `metadata_location`: The new metadata location for the table.
- `name`: The name of the table.
- `namespace`: The namespace of the table.
- `table_bucket_arn`: The Amazon Resource Name (ARN) of the table bucket.
- `version_token`: The version token of the table.
"""
function update_table_metadata_location end

function update_table_metadata_location(
    metadataLocation,
    name,
    namespace,
    tableBucketARN,
    versionToken;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "PUT",
        "/tables/$(tableBucketARN)/$(namespace)/$(name)/metadata-location",
        Dict{String,Any}(
            "metadataLocation" => metadataLocation, "versionToken" => versionToken
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_table_metadata_location(
    metadataLocation,
    name,
    namespace,
    tableBucketARN,
    versionToken,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3tables(
        "PUT",
        "/tables/$(tableBucketARN)/$(namespace)/$(name)/metadata-location",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "metadataLocation" => metadataLocation, "versionToken" => versionToken
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end
