# This file is auto-generated by AWS.jl
# Note: `SERVICE_FEATURE_SET` is embedded by `@service`
using AWS: AbstractAWSConfig, current_aws_config, _merge
using AWS.AWSServices: simpledbv2
using AWS.UUIDs: uuid4

"""
    get_export(export_arn)
    get_export(export_arn, params::Dict{String,<:Any})

Returns information for an existing domain export.

# Arguments

- `export_arn`: Unique ARN identifier of the export.
"""
function get_export end

function get_export(exportArn; aws_config::AbstractAWSConfig=current_aws_config())
    return simpledbv2(
        "POST",
        "/v2/GetExport",
        Dict{String,Any}("exportArn" => exportArn);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_export(
    exportArn,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return simpledbv2(
        "POST",
        "/v2/GetExport",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("exportArn" => exportArn), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_exports()
    list_exports(params::Dict{String,<:Any})

Lists all exports that were created. The results are paginated and can be filtered by domain
name.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"domainName"`: The name of the domain to filter exports. If not provided, exports for all
  the domains will be listed.
- `"maxResults"`: The maximum number of exports to return in a single response.
- `"nextToken"`: A pagination token used to retrieve the next page of results. This token is
  obtained from the nextToken field in the previous ListExportsResponse. Leave empty for the
  first request.
"""
function list_exports end

function list_exports(; aws_config::AbstractAWSConfig=current_aws_config())
    return simpledbv2(
        "POST", "/v2/ListExports"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_exports(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return simpledbv2(
        "POST", "/v2/ListExports", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    start_domain_export(domain_name, s3_bucket)
    start_domain_export(domain_name, s3_bucket, params::Dict{String,<:Any})

Initiates the export of a SimpleDB domain to an S3 bucket.

# Arguments

- `domain_name`: The name of the domain to export.
- `s3_bucket`: The name of the S3 bucket where the domain data will be exported.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"clientToken"`: Providing a ClientToken makes the call to StartDomainExport API
  idempotent, meaning that multiple identical calls have the same effect as one single call.
  A client token is valid for 8 hours after the first request that uses it is completed.
  After 8 hours, any request with the same client token is treated as a new request. Do not
  resubmit the same request with the same client token for more than 8 hours, or the result
  might not be idempotent. If you submit a request with the same client token but a change
  in other parameters within the 8-hour idempotency window, a ConflictException will be
  returned.

- `"s3BucketOwner"`: The ID of the AWS account that owns the bucket the export will be
  stored in.

- `"s3KeyPrefix"`: The prefix string to be used to generate the S3 object keys for export
  artifacts.

- `"s3SseAlgorithm"`: The server-side encryption algorithm to use for the exported data in
  S3. Valid values are: AES256 (SSE-S3) and KMS (SSE-KMS). If not specified, bucket's
  default encryption will apply.

- `"s3SseKmsKeyId"`: The KMS key ID to use for server-side encryption with AWS KMS-managed
  keys (SSE-KMS). This parameter is only expected with KMS as the S3 SSE algorithm.
"""
function start_domain_export end

function start_domain_export(
    domainName, s3Bucket; aws_config::AbstractAWSConfig=current_aws_config()
)
    return simpledbv2(
        "POST",
        "/v2/StartDomainExport",
        Dict{String,Any}(
            "domainName" => domainName,
            "s3Bucket" => s3Bucket,
            "clientToken" => string(uuid4()),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function start_domain_export(
    domainName,
    s3Bucket,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return simpledbv2(
        "POST",
        "/v2/StartDomainExport",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "domainName" => domainName,
                    "s3Bucket" => s3Bucket,
                    "clientToken" => string(uuid4()),
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end
