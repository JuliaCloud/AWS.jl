# This file is auto-generated by AWS.jl
# Note: `SERVICE_FEATURE_SET` is embedded by `@service`
using AWS: AbstractAWSConfig, current_aws_config, _merge
using AWS.AWSServices: connecthealth
using AWS.UUIDs: uuid4

"""
    activate_subscription(domain_id, subscription_id)
    activate_subscription(domain_id, subscription_id, params::Dict{String,<:Any})

Activates a Subscription to enable billing for a user.

# Arguments

- `domain_id`: The unique identifier of the parent Domain.
- `subscription_id`: The unique identifier of the Subscription.
"""
function activate_subscription end

function activate_subscription(
    domainId, subscriptionId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return connecthealth(
        "POST",
        "/domains/$(domainId)/subscriptions/$(subscriptionId)/activate";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function activate_subscription(
    domainId,
    subscriptionId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return connecthealth(
        "POST",
        "/domains/$(domainId)/subscriptions/$(subscriptionId)/activate",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_domain(name)
    create_domain(name, params::Dict{String,<:Any})

Creates a new Domain for managing HealthAgent resources.

# Arguments

- `name`: The name for the new Domain.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"kmsKeyArn"`: The ARN of the KMS key to use for encrypting data in this Domain.
- `"tags"`: Tags to associate with the Domain.
- `"webAppSetupConfiguration"`: Configuration for the Domain web application. Optional, but
  if provided all fields are required.
"""
function create_domain end

function create_domain(name; aws_config::AbstractAWSConfig=current_aws_config())
    return connecthealth(
        "POST",
        "/domain",
        Dict{String,Any}("name" => name);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_domain(
    name, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return connecthealth(
        "POST",
        "/domain",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("name" => name), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_subscription(domain_id)
    create_subscription(domain_id, params::Dict{String,<:Any})

Creates a new Subscription within a Domain for billing and user management.

# Arguments

- `domain_id`: The unique identifier of the parent Domain.
"""
function create_subscription end

function create_subscription(domainId; aws_config::AbstractAWSConfig=current_aws_config())
    return connecthealth(
        "POST",
        "/domains/$(domainId)/subscriptions";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_subscription(
    domainId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return connecthealth(
        "POST",
        "/domains/$(domainId)/subscriptions",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    deactivate_subscription(domain_id, subscription_id)
    deactivate_subscription(domain_id, subscription_id, params::Dict{String,<:Any})

Deactivates a Subscription to stop billing for a user.

# Arguments

- `domain_id`: The unique identifier of the parent Domain.
- `subscription_id`: The unique identifier of the Subscription.
"""
function deactivate_subscription end

function deactivate_subscription(
    domainId, subscriptionId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return connecthealth(
        "POST",
        "/domains/$(domainId)/subscriptions/$(subscriptionId)/deactivate";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function deactivate_subscription(
    domainId,
    subscriptionId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return connecthealth(
        "POST",
        "/domains/$(domainId)/subscriptions/$(subscriptionId)/deactivate",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_domain(domain_id)
    delete_domain(domain_id, params::Dict{String,<:Any})

Deletes a Domain and all associated resources.

# Arguments

- `domain_id`: The id of the Domain to delete
"""
function delete_domain end

function delete_domain(domainId; aws_config::AbstractAWSConfig=current_aws_config())
    return connecthealth(
        "DELETE", "/domain/$(domainId)"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function delete_domain(
    domainId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return connecthealth(
        "DELETE", "/domain/$(domainId)", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    get_domain(domain_id)
    get_domain(domain_id, params::Dict{String,<:Any})

Retrieves information about a Domain.

# Arguments

- `domain_id`: The id of the Domain to get
"""
function get_domain end

function get_domain(domainId; aws_config::AbstractAWSConfig=current_aws_config())
    return connecthealth(
        "GET", "/domain/$(domainId)"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function get_domain(
    domainId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return connecthealth(
        "GET", "/domain/$(domainId)", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    get_medical_scribe_listening_session(domain_id, session_id, subscription_id)
    get_medical_scribe_listening_session(domain_id, session_id, subscription_id, params::Dict{String,<:Any})

Retrieves details about an existing Medical Scribe listening session

# Arguments

- `domain_id`: The Domain identifier
- `session_id`: The Session identifier
- `subscription_id`: The Subscription identifier
"""
function get_medical_scribe_listening_session end

function get_medical_scribe_listening_session(
    domainId, sessionId, subscriptionId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return connecthealth(
        "GET",
        "/medical-scribe-stream/domain/$(domainId)/subscription/$(subscriptionId)/session/$(sessionId)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_medical_scribe_listening_session(
    domainId,
    sessionId,
    subscriptionId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return connecthealth(
        "GET",
        "/medical-scribe-stream/domain/$(domainId)/subscription/$(subscriptionId)/session/$(sessionId)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_patient_insights_job(domain_id, job_id)
    get_patient_insights_job(domain_id, job_id, params::Dict{String,<:Any})

Get details of a started patient insights job.

# Arguments

- `domain_id`:
- `job_id`:
"""
function get_patient_insights_job end

function get_patient_insights_job(
    domainId, jobId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return connecthealth(
        "GET",
        "/domain/$(domainId)/patient-insights-job/$(jobId)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_patient_insights_job(
    domainId,
    jobId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return connecthealth(
        "GET",
        "/domain/$(domainId)/patient-insights-job/$(jobId)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_subscription(domain_id, subscription_id)
    get_subscription(domain_id, subscription_id, params::Dict{String,<:Any})

Retrieves information about a Subscription.

# Arguments

- `domain_id`: The unique identifier of the parent Domain.
- `subscription_id`: The unique identifier of the Subscription.
"""
function get_subscription end

function get_subscription(
    domainId, subscriptionId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return connecthealth(
        "GET",
        "/domains/$(domainId)/subscriptions/$(subscriptionId)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_subscription(
    domainId,
    subscriptionId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return connecthealth(
        "GET",
        "/domains/$(domainId)/subscriptions/$(subscriptionId)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_domains()
    list_domains(params::Dict{String,<:Any})

Lists Domains for a given account.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"maxResults"`: Maximum number of results to return.
- `"nextToken"`: Token for pagination.
- `"status"`: Filter by Domain status.
"""
function list_domains end

function list_domains(; aws_config::AbstractAWSConfig=current_aws_config())
    return connecthealth("GET", "/domain"; aws_config, feature_set=SERVICE_FEATURE_SET)
end

function list_domains(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return connecthealth(
        "GET", "/domain", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    list_subscriptions(domain_id)
    list_subscriptions(domain_id, params::Dict{String,<:Any})

Lists all Subscriptions within a Domain.

# Arguments

- `domain_id`: The unique identifier of the parent Domain.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"maxResults"`: Maximum number of results to return.
- `"nextToken"`: Token for pagination.
"""
function list_subscriptions end

function list_subscriptions(domainId; aws_config::AbstractAWSConfig=current_aws_config())
    return connecthealth(
        "GET",
        "/domains/$(domainId)/subscriptions";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_subscriptions(
    domainId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return connecthealth(
        "GET",
        "/domains/$(domainId)/subscriptions",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_tags_for_resource(resource_arn)
    list_tags_for_resource(resource_arn, params::Dict{String,<:Any})

Lists the tags associated with the specified resource

# Arguments

- `resource_arn`: The ARN of the resource to list tags for
"""
function list_tags_for_resource end

function list_tags_for_resource(
    resourceArn; aws_config::AbstractAWSConfig=current_aws_config()
)
    return connecthealth(
        "GET", "/tags/$(resourceArn)"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_tags_for_resource(
    resourceArn,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return connecthealth(
        "GET", "/tags/$(resourceArn)", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    start_medical_scribe_listening_session(x-amzn-medscribe-domain-id, x-amzn-medscribe-language-code, x-amzn-medscribe-media-encoding, x-amzn-medscribe-sample-rate, x-amzn-medscribe-session-id, x-amzn-medscribe-subscription-id)
    start_medical_scribe_listening_session(x-amzn-medscribe-domain-id, x-amzn-medscribe-language-code, x-amzn-medscribe-media-encoding, x-amzn-medscribe-sample-rate, x-amzn-medscribe-session-id, x-amzn-medscribe-subscription-id, params::Dict{String,<:Any})

Starts a new Medical Scribe listening session for real-time audio transcription

# Arguments

- `x-amzn-medscribe-domain-id`: The Domain identifier
- `x-amzn-medscribe-language-code`: The Language Code for the audio in the session
- `x-amzn-medscribe-media-encoding`: The encoding for the input audio
- `x-amzn-medscribe-sample-rate`: The sample rate of the input audio
- `x-amzn-medscribe-session-id`: The Session identifier
- `x-amzn-medscribe-subscription-id`: The Subscription identifier

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"inputStream"`:
"""
function start_medical_scribe_listening_session end

function start_medical_scribe_listening_session(
    x_amzn_medscribe_domain_id,
    x_amzn_medscribe_language_code,
    x_amzn_medscribe_media_encoding,
    x_amzn_medscribe_sample_rate,
    x_amzn_medscribe_session_id,
    x_amzn_medscribe_subscription_id;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return connecthealth(
        "POST",
        "/medical-scribe-stream/",
        Dict{String,Any}(
            "headers" => Dict{String,Any}(
                "x-amzn-medscribe-domain-id" => x_amzn_medscribe_domain_id,
                "x-amzn-medscribe-language-code" => x_amzn_medscribe_language_code,
                "x-amzn-medscribe-media-encoding" => x_amzn_medscribe_media_encoding,
                "x-amzn-medscribe-sample-rate" => x_amzn_medscribe_sample_rate,
                "x-amzn-medscribe-session-id" => x_amzn_medscribe_session_id,
                "x-amzn-medscribe-subscription-id" => x_amzn_medscribe_subscription_id,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function start_medical_scribe_listening_session(
    x_amzn_medscribe_domain_id,
    x_amzn_medscribe_language_code,
    x_amzn_medscribe_media_encoding,
    x_amzn_medscribe_sample_rate,
    x_amzn_medscribe_session_id,
    x_amzn_medscribe_subscription_id,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return connecthealth(
        "POST",
        "/medical-scribe-stream/",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "headers" => Dict{String,Any}(
                        "x-amzn-medscribe-domain-id" => x_amzn_medscribe_domain_id,
                        "x-amzn-medscribe-language-code" =>
                            x_amzn_medscribe_language_code,
                        "x-amzn-medscribe-media-encoding" =>
                            x_amzn_medscribe_media_encoding,
                        "x-amzn-medscribe-sample-rate" => x_amzn_medscribe_sample_rate,
                        "x-amzn-medscribe-session-id" => x_amzn_medscribe_session_id,
                        "x-amzn-medscribe-subscription-id" =>
                            x_amzn_medscribe_subscription_id,
                    ),
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    start_patient_insights_job(domain_id, encounter_context, input_data_config, insights_context, output_data_config, patient_context, user_context)
    start_patient_insights_job(domain_id, encounter_context, input_data_config, insights_context, output_data_config, patient_context, user_context, params::Dict{String,<:Any})

Starts a new patient insights job.

# Arguments

- `domain_id`:
- `encounter_context`:
- `input_data_config`:
- `insights_context`:
- `output_data_config`:
- `patient_context`:
- `user_context`:

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"clientToken"`: Unique, case-sensitive identifier that you provide to ensure the
  idempotency of the request.
"""
function start_patient_insights_job end

function start_patient_insights_job(
    domainId,
    encounterContext,
    inputDataConfig,
    insightsContext,
    outputDataConfig,
    patientContext,
    userContext;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return connecthealth(
        "POST",
        "/domain/$(domainId)/patient-insights-job",
        Dict{String,Any}(
            "encounterContext" => encounterContext,
            "inputDataConfig" => inputDataConfig,
            "insightsContext" => insightsContext,
            "outputDataConfig" => outputDataConfig,
            "patientContext" => patientContext,
            "userContext" => userContext,
            "clientToken" => string(uuid4()),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function start_patient_insights_job(
    domainId,
    encounterContext,
    inputDataConfig,
    insightsContext,
    outputDataConfig,
    patientContext,
    userContext,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return connecthealth(
        "POST",
        "/domain/$(domainId)/patient-insights-job",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "encounterContext" => encounterContext,
                    "inputDataConfig" => inputDataConfig,
                    "insightsContext" => insightsContext,
                    "outputDataConfig" => outputDataConfig,
                    "patientContext" => patientContext,
                    "userContext" => userContext,
                    "clientToken" => string(uuid4()),
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    tag_resource(resource_arn, tags)
    tag_resource(resource_arn, tags, params::Dict{String,<:Any})

Associates the specified tags with the specified resource

# Arguments

- `resource_arn`: The ARN of the resource to tag
- `tags`: The tags to add to the resource
"""
function tag_resource end

function tag_resource(resourceArn, tags; aws_config::AbstractAWSConfig=current_aws_config())
    return connecthealth(
        "POST",
        "/tags/$(resourceArn)",
        Dict{String,Any}("tags" => tags);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function tag_resource(
    resourceArn,
    tags,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return connecthealth(
        "POST",
        "/tags/$(resourceArn)",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("tags" => tags), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    untag_resource(resource_arn, tag_keys)
    untag_resource(resource_arn, tag_keys, params::Dict{String,<:Any})

Removes the specified tags from the specified resource

# Arguments

- `resource_arn`: The ARN of the resource to untag
- `tag_keys`: The tag keys to remove from the resource
"""
function untag_resource end

function untag_resource(
    resourceArn, tagKeys; aws_config::AbstractAWSConfig=current_aws_config()
)
    return connecthealth(
        "DELETE",
        "/tags/$(resourceArn)",
        Dict{String,Any}("tagKeys" => tagKeys);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function untag_resource(
    resourceArn,
    tagKeys,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return connecthealth(
        "DELETE",
        "/tags/$(resourceArn)",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("tagKeys" => tagKeys), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end
