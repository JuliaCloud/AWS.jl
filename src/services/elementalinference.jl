# This file is auto-generated by AWS.jl
# Note: `SERVICE_FEATURE_SET` is embedded by `@service`
using AWS: AbstractAWSConfig, current_aws_config, _merge
using AWS.AWSServices: elementalinference
using AWS.UUIDs: uuid4

"""
    associate_feed(associated_resource_name, id, outputs)
    associate_feed(associated_resource_name, id, outputs, params::Dict{String,<:Any})

Associates a resource with the feed. The resource provides the input that Elemental
Inference needs needs in order to perform an Elemental Inference feature, such as cropping
video. You always provide the resource by associating it with a feed. You can associate only
one resource with each feed.

# Arguments

- `associated_resource_name`: An identifier for the resource. If the resource is from an AWS
  service, this identifier must be the full ARN of that resource. Otherwise, the identifier
  is a name that you assign and that is appropriate for the application that owns the
  resource. This name must not resemble an ARN.
- `id`: The ID of the feed.
- `outputs`: The outputs to add to this feed. You must specify at least one output. You can
  later use the UpdateFeed action to change the list of outputs.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"dryRun"`: Set to true if you want to do a dry run of the associate action.
"""
function associate_feed end

function associate_feed(
    associatedResourceName, id, outputs; aws_config::AbstractAWSConfig=current_aws_config()
)
    return elementalinference(
        "POST",
        "/v1/feed/$(id)/associate",
        Dict{String,Any}(
            "associatedResourceName" => associatedResourceName, "outputs" => outputs
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function associate_feed(
    associatedResourceName,
    id,
    outputs,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return elementalinference(
        "POST",
        "/v1/feed/$(id)/associate",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "associatedResourceName" => associatedResourceName, "outputs" => outputs
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_feed(name, outputs)
    create_feed(name, outputs, params::Dict{String,<:Any})

Creates a feed. The feed is the target for live streams being sent by the calling
application. An example of a calling application is AWS Elemental MediaLive. After you
create the feed, you can associate a resource with the feed.

# Arguments

- `name`: A name for this feed.
- `outputs`: An array of outputs for this feed. Each output represents a specific Elemental
  Inference feature. For example, an output might represent the crop feature.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"tags"`: If you want to include tags, add them now. You won't be able to add them later.
"""
function create_feed end

function create_feed(name, outputs; aws_config::AbstractAWSConfig=current_aws_config())
    return elementalinference(
        "POST",
        "/v1/feed",
        Dict{String,Any}("name" => name, "outputs" => outputs);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_feed(
    name,
    outputs,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return elementalinference(
        "POST",
        "/v1/feed",
        Dict{String,Any}(
            mergewith(
                _merge, Dict{String,Any}("name" => name, "outputs" => outputs), params
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_feed(id)
    delete_feed(id, params::Dict{String,<:Any})

Deletes the specified feed. The feed can be deleted at any time.

# Arguments

- `id`: The ID of the feed.
"""
function delete_feed end

function delete_feed(id; aws_config::AbstractAWSConfig=current_aws_config())
    return elementalinference(
        "DELETE", "/v1/feed/$(id)"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function delete_feed(
    id, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return elementalinference(
        "DELETE", "/v1/feed/$(id)", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    disassociate_feed(associated_resource_name, id)
    disassociate_feed(associated_resource_name, id, params::Dict{String,<:Any})

Releases the resource (for example, an MediaLive channel) that is associated with this feed.
The outputs in the feed become disabled.

# Arguments

- `associated_resource_name`: The name of the resource currently associated with the feed'.
- `id`: The ID of the feed where you want to release the resource.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"dryRun"`: Set to true if you want to do a dry run of the disassociate action.
"""
function disassociate_feed end

function disassociate_feed(
    associatedResourceName, id; aws_config::AbstractAWSConfig=current_aws_config()
)
    return elementalinference(
        "POST",
        "/v1/feed/$(id)/disassociate",
        Dict{String,Any}("associatedResourceName" => associatedResourceName);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function disassociate_feed(
    associatedResourceName,
    id,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return elementalinference(
        "POST",
        "/v1/feed/$(id)/disassociate",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("associatedResourceName" => associatedResourceName),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_feed(id)
    get_feed(id, params::Dict{String,<:Any})

Retrieves information about the specified feed.

# Arguments

- `id`: The ID of the feed to query.
"""
function get_feed end

function get_feed(id; aws_config::AbstractAWSConfig=current_aws_config())
    return elementalinference(
        "GET", "/v1/feed/$(id)"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function get_feed(
    id, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return elementalinference(
        "GET", "/v1/feed/$(id)", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    list_feeds()
    list_feeds(params::Dict{String,<:Any})

Displays a list of feeds that belong to this AWS account.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"maxResults"`: The maximum number of results to return per API request.

  For example, you submit a list request with MaxResults set at 5. Although 20 items match
  your request, the service returns no more than the first 5 items. (The service also
  returns a NextToken value that you can use to fetch the next batch of results.)

  The service might return fewer results than the MaxResults value. If MaxResults is not
  included in the request, the service defaults to pagination with a maximum of 10 results
  per page.

  Valid Range: Minimum value of 1. Maximum value of 1000.

- `"nextToken"`: The token that identifies the batch of results that you want to see.

  For example, you submit a ListBridges request with MaxResults set at 5. The service
  returns the first batch of results (up to 5) and a NextToken value. To see the next batch
  of results, you can submit the ListBridges request a second time and specify the NextToken
  value.
"""
function list_feeds end

function list_feeds(; aws_config::AbstractAWSConfig=current_aws_config())
    return elementalinference(
        "GET", "/v1/feeds"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_feeds(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return elementalinference(
        "GET", "/v1/feeds", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    list_tags_for_resource(resource_arn)
    list_tags_for_resource(resource_arn, params::Dict{String,<:Any})

List all tags that are on an Elemental Inference resource in the current region.

# Arguments

- `resource_arn`: The ARN of the resource whose tags you want to query.
"""
function list_tags_for_resource end

function list_tags_for_resource(
    resourceArn; aws_config::AbstractAWSConfig=current_aws_config()
)
    return elementalinference(
        "GET", "/v1/tags/$(resourceArn)"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_tags_for_resource(
    resourceArn,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return elementalinference(
        "GET",
        "/v1/tags/$(resourceArn)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    tag_resource(resource_arn, tags)
    tag_resource(resource_arn, tags, params::Dict{String,<:Any})

Associates the specified tags to the resource identified by the specified resourceArn in the
current region. If existing tags on a resource are not specified in the request parameters,
they are not changed. When a resource is deleted, the tags associated with that resource are
also deleted.

# Arguments

- `resource_arn`: The ARN of the resource where you want to add tags.
- `tags`: A list of tags to add to the resource.
"""
function tag_resource end

function tag_resource(resourceArn, tags; aws_config::AbstractAWSConfig=current_aws_config())
    return elementalinference(
        "POST",
        "/v1/tags/$(resourceArn)",
        Dict{String,Any}("tags" => tags);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function tag_resource(
    resourceArn,
    tags,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return elementalinference(
        "POST",
        "/v1/tags/$(resourceArn)",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("tags" => tags), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    untag_resource(resource_arn, tag_keys)
    untag_resource(resource_arn, tag_keys, params::Dict{String,<:Any})

Deletes specified tags from the specified resource in the current region.

# Arguments

- `resource_arn`: The ARN of the resource where you want to delete one or more tags.
- `tag_keys`: The keys of the tags to delete.
"""
function untag_resource end

function untag_resource(
    resourceArn, tagKeys; aws_config::AbstractAWSConfig=current_aws_config()
)
    return elementalinference(
        "DELETE",
        "/v1/tags/$(resourceArn)",
        Dict{String,Any}("tagKeys" => tagKeys);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function untag_resource(
    resourceArn,
    tagKeys,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return elementalinference(
        "DELETE",
        "/v1/tags/$(resourceArn)",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("tagKeys" => tagKeys), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_feed(id, name, outputs)
    update_feed(id, name, outputs, params::Dict{String,<:Any})

Updates the name and/or outputs in a feed.

# Arguments

- `id`: The ID of the feed to update.
- `name`: Required. You can specify the existing name (to leave it unchanged) or a new name.
- `outputs`: Required. You can specify the existing array of outputs (to leave outputs
  unchanged) or you can specify a new array.
"""
function update_feed end

function update_feed(id, name, outputs; aws_config::AbstractAWSConfig=current_aws_config())
    return elementalinference(
        "PUT",
        "/v1/feed/$(id)",
        Dict{String,Any}("name" => name, "outputs" => outputs);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_feed(
    id,
    name,
    outputs,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return elementalinference(
        "PUT",
        "/v1/feed/$(id)",
        Dict{String,Any}(
            mergewith(
                _merge, Dict{String,Any}("name" => name, "outputs" => outputs), params
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end
