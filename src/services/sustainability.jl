# This file is auto-generated by AWS.jl
# Note: `SERVICE_FEATURE_SET` is embedded by `@service`
using AWS: AbstractAWSConfig, current_aws_config, _merge
using AWS.AWSServices: sustainability
using AWS.UUIDs: uuid4

"""
    get_estimated_carbon_emissions(time_period)
    get_estimated_carbon_emissions(time_period, params::Dict{String,<:Any})

Returns estimated carbon emission values based on customer grouping and filtering
parameters. We recommend using pagination to ensure that the operation returns quickly and
successfully.

# Arguments

- `time_period`: The date range for fetching estimated carbon emissions.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"EmissionsTypes"`: The emission types to include in the results. If absent, returns
  `TOTAL_LBM_CARBON_EMISSIONS` and `TOTAL_MBM_CARBON_EMISSIONS` emissions types.
- `"FilterBy"`: The criteria for filtering estimated carbon emissions.
- `"Granularity"`: The time granularity for the results. If absent, uses `MONTHLY` time
  granularity.
- `"GranularityConfiguration"`: Configuration for fiscal year calculations when using
  `YEARLY_FISCAL` or `QUARTERLY_FISCAL` granularity.
- `"GroupBy"`: The dimensions available for grouping estimated carbon emissions.
- `"MaxResults"`: The maximum number of results to return in a single call. Default is 40.
- `"NextToken"`: The pagination token specifying which page of results to return in the
  response. If no token is provided, the default page is the first page.
"""
function get_estimated_carbon_emissions end

function get_estimated_carbon_emissions(
    TimePeriod; aws_config::AbstractAWSConfig=current_aws_config()
)
    return sustainability(
        "POST",
        "/v1/estimated-carbon-emissions",
        Dict{String,Any}("TimePeriod" => TimePeriod);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_estimated_carbon_emissions(
    TimePeriod,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return sustainability(
        "POST",
        "/v1/estimated-carbon-emissions",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("TimePeriod" => TimePeriod), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_estimated_carbon_emissions_dimension_values(dimensions, time_period)
    get_estimated_carbon_emissions_dimension_values(dimensions, time_period, params::Dict{String,<:Any})

Returns the possible dimension values available for a customer's account. We recommend using
pagination to ensure that the operation returns quickly and successfully.

# Arguments

- `dimensions`: The dimensions available for grouping estimated carbon emissions.
- `time_period`: The date range for fetching the dimension values.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"MaxResults"`: The maximum number of results to return in a single call. Default is 40.
- `"NextToken"`: The pagination token specifying which page of results to return in the
  response. If no token is provided, the default page is the first page.
"""
function get_estimated_carbon_emissions_dimension_values end

function get_estimated_carbon_emissions_dimension_values(
    Dimensions, TimePeriod; aws_config::AbstractAWSConfig=current_aws_config()
)
    return sustainability(
        "POST",
        "/v1/estimated-carbon-emissions-dimension-values",
        Dict{String,Any}("Dimensions" => Dimensions, "TimePeriod" => TimePeriod);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_estimated_carbon_emissions_dimension_values(
    Dimensions,
    TimePeriod,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return sustainability(
        "POST",
        "/v1/estimated-carbon-emissions-dimension-values",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("Dimensions" => Dimensions, "TimePeriod" => TimePeriod),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end
