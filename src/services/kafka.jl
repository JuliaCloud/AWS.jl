# This file is auto-generated by AWSMetadata.jl
include("../AWSServices.jl")
using Compat
using .AWSServices: kafka

"""
    CreateCluster()


            Creates a new MSK cluster.
         

Required Parameters
brokerNodeGroupInfo => 
            Information about the broker nodes in the cluster.
         
clusterName => 
            The name of the cluster.
         
kafkaVersion => 
            The version of Apache Kafka.
         
numberOfBrokerNodes => 
            The number of broker nodes in the cluster.
         

Optional Parameters
clientAuthentication => 
            Includes all client authentication related information.
         
configurationInfo => 
            Represents the configuration that you want MSK to use for the brokers in a cluster.
         
encryptionInfo => 
            Includes all encryption-related information.
         
enhancedMonitoring => 
            Specifies the level of monitoring for the MSK cluster. The possible values are DEFAULT, PER_BROKER, and PER_TOPIC_PER_BROKER.
         
loggingInfo => 
openMonitoring => 
            The settings for open monitoring.
         
tags => 
            Create tags when creating the cluster.
         
"""
CreateCluster(brokerNodeGroupInfo, clusterName, kafkaVersion, numberOfBrokerNodes) = kafka("POST", "/v1/clusters", Dict{String, Any}("brokerNodeGroupInfo"=>brokerNodeGroupInfo, "clusterName"=>clusterName, "kafkaVersion"=>kafkaVersion, "numberOfBrokerNodes"=>numberOfBrokerNodes))
CreateCluster(brokerNodeGroupInfo, clusterName, kafkaVersion, numberOfBrokerNodes, args::AbstractDict{String, <: Any}) = kafka("POST", "/v1/clusters", Dict{String, Any}("brokerNodeGroupInfo"=>brokerNodeGroupInfo, "clusterName"=>clusterName, "kafkaVersion"=>kafkaVersion, "numberOfBrokerNodes"=>numberOfBrokerNodes, args...))
CreateCluster(a...; b...) = CreateCluster(a..., b)

"""
    CreateConfiguration()


            Creates a new MSK configuration.
         

Required Parameters
name => 
            The name of the configuration.
         
serverProperties => 
            Contents of the server.properties file. When using the API, you must ensure that the contents of the file are base64 encoded. 
               When using the AWS Management Console, the SDK, or the AWS CLI, the contents of server.properties can be in plaintext.
         

Optional Parameters
description => 
            The description of the configuration.
         
kafkaVersions => 
            The versions of Apache Kafka with which you can use this MSK configuration.
         
"""
CreateConfiguration(name, serverProperties) = kafka("POST", "/v1/configurations", Dict{String, Any}("name"=>name, "serverProperties"=>serverProperties))
CreateConfiguration(name, serverProperties, args::AbstractDict{String, <: Any}) = kafka("POST", "/v1/configurations", Dict{String, Any}("name"=>name, "serverProperties"=>serverProperties, args...))
CreateConfiguration(a...; b...) = CreateConfiguration(a..., b)

"""
    DeleteCluster()


            Deletes the MSK cluster specified by the Amazon Resource Name (ARN) in the request.
         

Required Parameters
clusterArn => 
            The Amazon Resource Name (ARN) that uniquely identifies the cluster.
         

Optional Parameters
currentVersion => 
            The current version of the MSK cluster.
         
"""
DeleteCluster(clusterArn) = kafka("DELETE", "/v1/clusters/$(clusterArn)")
DeleteCluster(clusterArn, args::AbstractDict{String, <: Any}) = kafka("DELETE", "/v1/clusters/$(clusterArn)", args)
DeleteCluster(a...; b...) = DeleteCluster(a..., b)

"""
    DescribeCluster()


            Returns a description of the MSK cluster whose Amazon Resource Name (ARN) is specified in the request.
         

Required Parameters
clusterArn => 
            The Amazon Resource Name (ARN) that uniquely identifies the cluster.
         

"""
DescribeCluster(clusterArn) = kafka("GET", "/v1/clusters/$(clusterArn)")
DescribeCluster(clusterArn, args::AbstractDict{String, <: Any}) = kafka("GET", "/v1/clusters/$(clusterArn)", args)
DescribeCluster(a...; b...) = DescribeCluster(a..., b)

"""
    DescribeClusterOperation()


            Returns a description of the cluster operation specified by the ARN.
         

Required Parameters
clusterOperationArn => 
            The Amazon Resource Name (ARN) that uniquely identifies the MSK cluster operation.
         

"""
DescribeClusterOperation(clusterOperationArn) = kafka("GET", "/v1/operations/$(clusterOperationArn)")
DescribeClusterOperation(clusterOperationArn, args::AbstractDict{String, <: Any}) = kafka("GET", "/v1/operations/$(clusterOperationArn)", args)
DescribeClusterOperation(a...; b...) = DescribeClusterOperation(a..., b)

"""
    DescribeConfiguration()


            Returns a description of this MSK configuration.
         

Required Parameters
arn => 
            The Amazon Resource Name (ARN) that uniquely identifies an MSK configuration and all of its revisions.
         

"""
DescribeConfiguration(arn) = kafka("GET", "/v1/configurations/$(arn)")
DescribeConfiguration(arn, args::AbstractDict{String, <: Any}) = kafka("GET", "/v1/configurations/$(arn)", args)
DescribeConfiguration(a...; b...) = DescribeConfiguration(a..., b)

"""
    DescribeConfigurationRevision()


            Returns a description of this revision of the configuration.
         

Required Parameters
arn => 
            The Amazon Resource Name (ARN) that uniquely identifies an MSK configuration and all of its revisions.
         
revision => 
            A string that uniquely identifies a revision of an MSK configuration.
         

"""
DescribeConfigurationRevision(arn, revision) = kafka("GET", "/v1/configurations/$(arn)/revisions/$(revision)")
DescribeConfigurationRevision(arn, revision, args::AbstractDict{String, <: Any}) = kafka("GET", "/v1/configurations/$(arn)/revisions/$(revision)", args)
DescribeConfigurationRevision(a...; b...) = DescribeConfigurationRevision(a..., b)

"""
    GetBootstrapBrokers()


            A list of brokers that a client application can use to bootstrap.
         

Required Parameters
clusterArn => 
            The Amazon Resource Name (ARN) that uniquely identifies the cluster.
         

"""
GetBootstrapBrokers(clusterArn) = kafka("GET", "/v1/clusters/$(clusterArn)/bootstrap-brokers")
GetBootstrapBrokers(clusterArn, args::AbstractDict{String, <: Any}) = kafka("GET", "/v1/clusters/$(clusterArn)/bootstrap-brokers", args)
GetBootstrapBrokers(a...; b...) = GetBootstrapBrokers(a..., b)

"""
    GetCompatibleKafkaVersions()


            Gets the Apache Kafka versions to which you can update the MSK cluster.
         

Optional Parameters
clusterArn => 
            The Amazon Resource Name (ARN) of the cluster check.
            
"""
GetCompatibleKafkaVersions() = kafka("GET", "/v1/compatible-kafka-versions")
GetCompatibleKafkaVersions(args::AbstractDict{String, Any}) = kafka("GET", "/v1/compatible-kafka-versions", args)
GetCompatibleKafkaVersions(a...; b...) = GetCompatibleKafkaVersions(a..., b)

"""
    ListClusterOperations()


            Returns a list of all the operations that have been performed on the specified MSK cluster.
         

Required Parameters
clusterArn => 
            The Amazon Resource Name (ARN) that uniquely identifies the cluster.
         

Optional Parameters
maxResults => 
            The maximum number of results to return in the response. If there are more results, the response includes a NextToken parameter.
         
nextToken => 
            The paginated results marker. When the result of the operation is truncated, the call returns NextToken in the response. 
            To get the next batch, provide this token in your next request.
         
"""
ListClusterOperations(clusterArn) = kafka("GET", "/v1/clusters/$(clusterArn)/operations")
ListClusterOperations(clusterArn, args::AbstractDict{String, <: Any}) = kafka("GET", "/v1/clusters/$(clusterArn)/operations", args)
ListClusterOperations(a...; b...) = ListClusterOperations(a..., b)

"""
    ListClusters()


            Returns a list of all the MSK clusters in the current Region.
         

Optional Parameters
clusterNameFilter => 
            Specify a prefix of the name of the clusters that you want to list. The service lists all the clusters whose names start with this prefix.
         
maxResults => 
            The maximum number of results to return in the response. If there are more results, the response includes a NextToken parameter.
         
nextToken => 
            The paginated results marker. When the result of the operation is truncated, the call returns NextToken in the response. 
            To get the next batch, provide this token in your next request.
         
"""
ListClusters() = kafka("GET", "/v1/clusters")
ListClusters(args::AbstractDict{String, Any}) = kafka("GET", "/v1/clusters", args)
ListClusters(a...; b...) = ListClusters(a..., b)

"""
    ListConfigurationRevisions()


            Returns a list of all the MSK configurations in this Region.
         

Required Parameters
arn => 
            The Amazon Resource Name (ARN) that uniquely identifies an MSK configuration and all of its revisions.
         

Optional Parameters
maxResults => 
            The maximum number of results to return in the response. If there are more results, the response includes a NextToken parameter.
         
nextToken => 
            The paginated results marker. When the result of the operation is truncated, the call returns NextToken in the response. 
            To get the next batch, provide this token in your next request.
         
"""
ListConfigurationRevisions(arn) = kafka("GET", "/v1/configurations/$(arn)/revisions")
ListConfigurationRevisions(arn, args::AbstractDict{String, <: Any}) = kafka("GET", "/v1/configurations/$(arn)/revisions", args)
ListConfigurationRevisions(a...; b...) = ListConfigurationRevisions(a..., b)

"""
    ListConfigurations()


            Returns a list of all the MSK configurations in this Region.
         

Optional Parameters
maxResults => 
            The maximum number of results to return in the response. If there are more results, the response includes a NextToken parameter.
         
nextToken => 
            The paginated results marker. When the result of the operation is truncated, the call returns NextToken in the response. 
            To get the next batch, provide this token in your next request.
         
"""
ListConfigurations() = kafka("GET", "/v1/configurations")
ListConfigurations(args::AbstractDict{String, Any}) = kafka("GET", "/v1/configurations", args)
ListConfigurations(a...; b...) = ListConfigurations(a..., b)

"""
    ListKafkaVersions()


            Returns a list of Kafka versions.
         

Optional Parameters
maxResults => 
            The maximum number of results to return in the response. If there are more results, the response includes a NextToken parameter.
nextToken => 
            The paginated results marker. When the result of the operation is truncated, the call returns NextToken in the response. To get the next batch, provide this token in your next request.
"""
ListKafkaVersions() = kafka("GET", "/v1/kafka-versions")
ListKafkaVersions(args::AbstractDict{String, Any}) = kafka("GET", "/v1/kafka-versions", args)
ListKafkaVersions(a...; b...) = ListKafkaVersions(a..., b)

"""
    ListNodes()


            Returns a list of the broker nodes in the cluster.
         

Required Parameters
clusterArn => 
            The Amazon Resource Name (ARN) that uniquely identifies the cluster.
         

Optional Parameters
maxResults => 
            The maximum number of results to return in the response. If there are more results, the response includes a NextToken parameter.
         
nextToken => 
            The paginated results marker. When the result of the operation is truncated, the call returns NextToken in the response. 
            To get the next batch, provide this token in your next request.
         
"""
ListNodes(clusterArn) = kafka("GET", "/v1/clusters/$(clusterArn)/nodes")
ListNodes(clusterArn, args::AbstractDict{String, <: Any}) = kafka("GET", "/v1/clusters/$(clusterArn)/nodes", args)
ListNodes(a...; b...) = ListNodes(a..., b)

"""
    ListTagsForResource()


            Returns a list of the tags associated with the specified resource.
         

Required Parameters
resourceArn => 
            The Amazon Resource Name (ARN) that uniquely identifies the resource that's associated with the tags.
         

"""
ListTagsForResource(resourceArn) = kafka("GET", "/v1/tags/$(resourceArn)")
ListTagsForResource(resourceArn, args::AbstractDict{String, <: Any}) = kafka("GET", "/v1/tags/$(resourceArn)", args)
ListTagsForResource(a...; b...) = ListTagsForResource(a..., b)

"""
    TagResource()


            Adds tags to the specified MSK resource.
         

Required Parameters
resourceArn => 
            The Amazon Resource Name (ARN) that uniquely identifies the resource that's associated with the tags.
         
tags => 
            The key-value pair for the resource tag.
         

"""
TagResource(resourceArn, tags) = kafka("POST", "/v1/tags/$(resourceArn)", Dict{String, Any}("tags"=>tags))
TagResource(resourceArn, tags, args::AbstractDict{String, <: Any}) = kafka("POST", "/v1/tags/$(resourceArn)", Dict{String, Any}("tags"=>tags, args...))
TagResource(a...; b...) = TagResource(a..., b)

"""
    UntagResource()


            Removes the tags associated with the keys that are provided in the query.
         

Required Parameters
resourceArn => 
            The Amazon Resource Name (ARN) that uniquely identifies the resource that's associated with the tags.
         
tagKeys => 
            Tag keys must be unique for a given cluster. In addition, the following restrictions apply:
            
               
                  Each tag key must be unique. If you add a tag with a key that's already in
                  use, your new tag overwrites the existing key-value pair. 
               
               
                  You can't start a tag key with aws: because this prefix is reserved for use
                  by  AWS.  AWS creates tags that begin with this prefix on your behalf, but
                  you can't edit or delete them.
               
               
                  Tag keys must be between 1 and 128 Unicode characters in length.
               
               
                  Tag keys must consist of the following characters: Unicode letters, digits,
                  white space, and the following special characters: _ . / = + -
                     @.
               
            
         

"""
UntagResource(resourceArn, tagKeys) = kafka("DELETE", "/v1/tags/$(resourceArn)", Dict{String, Any}("tagKeys"=>tagKeys))
UntagResource(resourceArn, tagKeys, args::AbstractDict{String, <: Any}) = kafka("DELETE", "/v1/tags/$(resourceArn)", Dict{String, Any}("tagKeys"=>tagKeys, args...))
UntagResource(a...; b...) = UntagResource(a..., b)

"""
    UpdateBrokerCount()


            Updates the number of broker nodes in the cluster.
         

Required Parameters
clusterArn => 
            The Amazon Resource Name (ARN) that uniquely identifies the cluster.
         
currentVersion => 
            The version of cluster to update from. A successful operation will then generate a new version.
         
targetNumberOfBrokerNodes => 
            The number of broker nodes that you want the cluster to have after this operation completes successfully.
         

"""
UpdateBrokerCount(clusterArn, currentVersion, targetNumberOfBrokerNodes) = kafka("PUT", "/v1/clusters/$(clusterArn)/nodes/count", Dict{String, Any}("currentVersion"=>currentVersion, "targetNumberOfBrokerNodes"=>targetNumberOfBrokerNodes))
UpdateBrokerCount(clusterArn, currentVersion, targetNumberOfBrokerNodes, args::AbstractDict{String, <: Any}) = kafka("PUT", "/v1/clusters/$(clusterArn)/nodes/count", Dict{String, Any}("currentVersion"=>currentVersion, "targetNumberOfBrokerNodes"=>targetNumberOfBrokerNodes, args...))
UpdateBrokerCount(a...; b...) = UpdateBrokerCount(a..., b)

"""
    UpdateBrokerStorage()


            Updates the EBS storage associated with MSK brokers.
         

Required Parameters
clusterArn => 
            The Amazon Resource Name (ARN) that uniquely identifies the cluster.
         
currentVersion => 
            The version of cluster to update from. A successful operation will then generate a new version.
         
targetBrokerEBSVolumeInfo => 
            Describes the target volume size and the ID of the broker to apply the update to.
         

"""
UpdateBrokerStorage(clusterArn, currentVersion, targetBrokerEBSVolumeInfo) = kafka("PUT", "/v1/clusters/$(clusterArn)/nodes/storage", Dict{String, Any}("currentVersion"=>currentVersion, "targetBrokerEBSVolumeInfo"=>targetBrokerEBSVolumeInfo))
UpdateBrokerStorage(clusterArn, currentVersion, targetBrokerEBSVolumeInfo, args::AbstractDict{String, <: Any}) = kafka("PUT", "/v1/clusters/$(clusterArn)/nodes/storage", Dict{String, Any}("currentVersion"=>currentVersion, "targetBrokerEBSVolumeInfo"=>targetBrokerEBSVolumeInfo, args...))
UpdateBrokerStorage(a...; b...) = UpdateBrokerStorage(a..., b)

"""
    UpdateClusterConfiguration()


            Updates the cluster with the configuration that is specified in the request body.
         

Required Parameters
clusterArn => 
            The Amazon Resource Name (ARN) that uniquely identifies the cluster.
         
configurationInfo => 
            Represents the configuration that you want MSK to use for the brokers in a cluster.
         
currentVersion => 
            The version of the cluster that needs to be updated.
         

"""
UpdateClusterConfiguration(clusterArn, configurationInfo, currentVersion) = kafka("PUT", "/v1/clusters/$(clusterArn)/configuration", Dict{String, Any}("configurationInfo"=>configurationInfo, "currentVersion"=>currentVersion))
UpdateClusterConfiguration(clusterArn, configurationInfo, currentVersion, args::AbstractDict{String, <: Any}) = kafka("PUT", "/v1/clusters/$(clusterArn)/configuration", Dict{String, Any}("configurationInfo"=>configurationInfo, "currentVersion"=>currentVersion, args...))
UpdateClusterConfiguration(a...; b...) = UpdateClusterConfiguration(a..., b)

"""
    UpdateClusterKafkaVersion()


            Updates the Apache Kafka version for the cluster.
         

Required Parameters
clusterArn => 
            The Amazon Resource Name (ARN) of the cluster to be updated.
            
currentVersion => 
            Current cluster version.
            
targetKafkaVersion => 
            Target Kafka version.
            

Optional Parameters
configurationInfo => 
            The custom configuration that should be applied on the new version of cluster.
            
"""
UpdateClusterKafkaVersion(clusterArn, currentVersion, targetKafkaVersion) = kafka("PUT", "/v1/clusters/$(clusterArn)/version", Dict{String, Any}("currentVersion"=>currentVersion, "targetKafkaVersion"=>targetKafkaVersion))
UpdateClusterKafkaVersion(clusterArn, currentVersion, targetKafkaVersion, args::AbstractDict{String, <: Any}) = kafka("PUT", "/v1/clusters/$(clusterArn)/version", Dict{String, Any}("currentVersion"=>currentVersion, "targetKafkaVersion"=>targetKafkaVersion, args...))
UpdateClusterKafkaVersion(a...; b...) = UpdateClusterKafkaVersion(a..., b)

"""
    UpdateMonitoring()


            Updates the monitoring settings for the cluster. You can use this operation to specify which Apache Kafka metrics you want Amazon MSK to send to Amazon CloudWatch. You can also specify settings for open monitoring with Prometheus.
         

Required Parameters
clusterArn => 
            The Amazon Resource Name (ARN) that uniquely identifies the cluster.
         
currentVersion => 
            The version of the MSK cluster to update. Cluster versions aren't simple numbers. You can describe an MSK cluster to find its version. When this update operation is successful, it generates a new cluster version.
         

Optional Parameters
enhancedMonitoring => 
            Specifies which Apache Kafka metrics Amazon MSK gathers and sends to Amazon CloudWatch for this cluster.
         
loggingInfo => 
openMonitoring => 
            The settings for open monitoring.
         
"""
UpdateMonitoring(clusterArn, currentVersion) = kafka("PUT", "/v1/clusters/$(clusterArn)/monitoring", Dict{String, Any}("currentVersion"=>currentVersion))
UpdateMonitoring(clusterArn, currentVersion, args::AbstractDict{String, <: Any}) = kafka("PUT", "/v1/clusters/$(clusterArn)/monitoring", Dict{String, Any}("currentVersion"=>currentVersion, args...))
UpdateMonitoring(a...; b...) = UpdateMonitoring(a..., b)
