# This file is auto-generated by AWS.jl
# Note: `SERVICE_FEATURE_SET` is embedded by `@service`
using AWS: AbstractAWSConfig, current_aws_config, _merge
using AWS.AWSServices: directory_service_data
using AWS.UUIDs: uuid4

"""
    add_group_member(directory_id, group_name, member_name)
    add_group_member(directory_id, group_name, member_name, params::Dict{String,<:Any})

Adds an existing user, group, or computer as a group member.

# Arguments

- `directory_id`: The identifier (ID) of the directory that's associated with the group.
- `group_name`: The name of the group.
- `member_name`: The `SAMAccountName` of the user, group, or computer to add as a group
  member.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"ClientToken"`: A unique and case-sensitive identifier that you provide to make sure the
  idempotency of the request, so multiple identical calls have the same effect as one single
  call.

  A client token is valid for 8 hours after the first request that uses it completes. After
  8 hours, any request with the same client token is treated as a new request. If the
request succeeds, any future uses of that token will be idempotent for another 8 hours.

  If you submit a request with the same client token but change one of the other parameters
  within the 8-hour idempotency window, Directory Service Data returns an
  `ConflictException`.

  !!! note
      This parameter is optional when using the CLI or SDK.

- `"MemberRealm"`: The domain name that's associated with the group member. This parameter
  is required only when adding a member outside of your Managed Microsoft AD domain to a
  group inside of your Managed Microsoft AD domain. This parameter defaults to the Managed
  Microsoft AD domain.

  !!! note
      This parameter is case insensitive.
"""
function add_group_member end

function add_group_member(
    DirectoryId, GroupName, MemberName; aws_config::AbstractAWSConfig=current_aws_config()
)
    return directory_service_data(
        "POST",
        "/GroupMemberships/AddGroupMember",
        Dict{String,Any}(
            "DirectoryId" => DirectoryId,
            "GroupName" => GroupName,
            "MemberName" => MemberName,
            "ClientToken" => string(uuid4()),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function add_group_member(
    DirectoryId,
    GroupName,
    MemberName,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return directory_service_data(
        "POST",
        "/GroupMemberships/AddGroupMember",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "DirectoryId" => DirectoryId,
                    "GroupName" => GroupName,
                    "MemberName" => MemberName,
                    "ClientToken" => string(uuid4()),
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_group(directory_id, samaccount_name)
    create_group(directory_id, samaccount_name, params::Dict{String,<:Any})

Creates a new group.

# Arguments

- `directory_id`: The identifier (ID) of the directory that's associated with the group.
- `samaccount_name`: The name of the group.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"ClientToken"`: A unique and case-sensitive identifier that you provide to make sure the
  idempotency of the request, so multiple identical calls have the same effect as one single
  call.

  A client token is valid for 8 hours after the first request that uses it completes. After
  8 hours, any request with the same client token is treated as a new request. If the
request succeeds, any future uses of that token will be idempotent for another 8 hours.

  If you submit a request with the same client token but change one of the other parameters
  within the 8-hour idempotency window, Directory Service Data returns an
  `ConflictException`.

  !!! note
      This parameter is optional when using the CLI or SDK.

- `"GroupScope"`: The scope of the AD group. For details, see [Active Directory security group scope](https://learn.microsoft.com/en-us/windows-server/identity/ad-ds/manage/understand-security-groups#group-scope).

- `"GroupType"`: The AD group type. For details, see [Active Directory security group type](https://learn.microsoft.com/en-us/windows-server/identity/ad-ds/manage/understand-security-groups#how-active-directory-security-groups-work).

- `"OtherAttributes"`: An expression that defines one or more attributes with the data type
  and value of each attribute.
"""
function create_group end

function create_group(
    DirectoryId, SAMAccountName; aws_config::AbstractAWSConfig=current_aws_config()
)
    return directory_service_data(
        "POST",
        "/Groups/CreateGroup",
        Dict{String,Any}(
            "DirectoryId" => DirectoryId,
            "SAMAccountName" => SAMAccountName,
            "ClientToken" => string(uuid4()),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_group(
    DirectoryId,
    SAMAccountName,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return directory_service_data(
        "POST",
        "/Groups/CreateGroup",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "DirectoryId" => DirectoryId,
                    "SAMAccountName" => SAMAccountName,
                    "ClientToken" => string(uuid4()),
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_user(directory_id, samaccount_name)
    create_user(directory_id, samaccount_name, params::Dict{String,<:Any})

Creates a new user.

# Arguments

- `directory_id`: The identifier (ID) of the directory that’s associated with the user.
- `samaccount_name`: The name of the user.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"ClientToken"`: A unique and case-sensitive identifier that you provide to make sure the
  idempotency of the request, so multiple identical calls have the same effect as one single
  call.

  A client token is valid for 8 hours after the first request that uses it completes. After
  8 hours, any request with the same client token is treated as a new request. If the
request succeeds, any future uses of that token will be idempotent for another 8 hours.

  If you submit a request with the same client token but change one of the other parameters
  within the 8-hour idempotency window, Directory Service Data returns an
  `ConflictException`.

  !!! note
      This parameter is optional when using the CLI or SDK.

- `"EmailAddress"`: The email address of the user.

- `"GivenName"`: The first name of the user.

- `"OtherAttributes"`: An expression that defines one or more attribute names with the data
  type and value of each attribute. A key is an attribute name, and the value is a list of
  maps. For a list of supported attributes, see [Directory Service Data Attributes](https://docs.aws.amazon.com/directoryservice/latest/admin-guide/ad_data_attributes.html).

  !!! note
      Attribute names are case insensitive.

- `"Surname"`: The last name of the user.
"""
function create_user end

function create_user(
    DirectoryId, SAMAccountName; aws_config::AbstractAWSConfig=current_aws_config()
)
    return directory_service_data(
        "POST",
        "/Users/CreateUser",
        Dict{String,Any}(
            "DirectoryId" => DirectoryId,
            "SAMAccountName" => SAMAccountName,
            "ClientToken" => string(uuid4()),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_user(
    DirectoryId,
    SAMAccountName,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return directory_service_data(
        "POST",
        "/Users/CreateUser",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "DirectoryId" => DirectoryId,
                    "SAMAccountName" => SAMAccountName,
                    "ClientToken" => string(uuid4()),
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_group(directory_id, samaccount_name)
    delete_group(directory_id, samaccount_name, params::Dict{String,<:Any})

Deletes a group.

# Arguments

- `directory_id`: The identifier (ID) of the directory that's associated with the group.
- `samaccount_name`: The name of the group.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"ClientToken"`: A unique and case-sensitive identifier that you provide to make sure the
  idempotency of the request, so multiple identical calls have the same effect as one single
  call.

  A client token is valid for 8 hours after the first request that uses it completes. After
  8 hours, any request with the same client token is treated as a new request. If the
request succeeds, any future uses of that token will be idempotent for another 8 hours.

  If you submit a request with the same client token but change one of the other parameters
  within the 8-hour idempotency window, Directory Service Data returns an
  `ConflictException`.

  !!! note
      This parameter is optional when using the CLI or SDK.
"""
function delete_group end

function delete_group(
    DirectoryId, SAMAccountName; aws_config::AbstractAWSConfig=current_aws_config()
)
    return directory_service_data(
        "POST",
        "/Groups/DeleteGroup",
        Dict{String,Any}(
            "DirectoryId" => DirectoryId,
            "SAMAccountName" => SAMAccountName,
            "ClientToken" => string(uuid4()),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_group(
    DirectoryId,
    SAMAccountName,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return directory_service_data(
        "POST",
        "/Groups/DeleteGroup",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "DirectoryId" => DirectoryId,
                    "SAMAccountName" => SAMAccountName,
                    "ClientToken" => string(uuid4()),
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_user(directory_id, samaccount_name)
    delete_user(directory_id, samaccount_name, params::Dict{String,<:Any})

Deletes a user.

# Arguments

- `directory_id`: The identifier (ID) of the directory that's associated with the user.
- `samaccount_name`: The name of the user.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"ClientToken"`: A unique and case-sensitive identifier that you provide to make sure the
  idempotency of the request, so multiple identical calls have the same effect as one single
  call.

  A client token is valid for 8 hours after the first request that uses it completes. After
  8 hours, any request with the same client token is treated as a new request. If the
request succeeds, any future uses of that token will be idempotent for another 8 hours.

  If you submit a request with the same client token but change one of the other parameters
  within the 8-hour idempotency window, Directory Service Data returns an
  `ConflictException`.

  !!! note
      This parameter is optional when using the CLI or SDK.
"""
function delete_user end

function delete_user(
    DirectoryId, SAMAccountName; aws_config::AbstractAWSConfig=current_aws_config()
)
    return directory_service_data(
        "POST",
        "/Users/DeleteUser",
        Dict{String,Any}(
            "DirectoryId" => DirectoryId,
            "SAMAccountName" => SAMAccountName,
            "ClientToken" => string(uuid4()),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_user(
    DirectoryId,
    SAMAccountName,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return directory_service_data(
        "POST",
        "/Users/DeleteUser",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "DirectoryId" => DirectoryId,
                    "SAMAccountName" => SAMAccountName,
                    "ClientToken" => string(uuid4()),
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    describe_group(directory_id, samaccount_name)
    describe_group(directory_id, samaccount_name, params::Dict{String,<:Any})

Returns information about a specific group.

# Arguments

- `directory_id`: The Identifier (ID) of the directory associated with the group.
- `samaccount_name`: The name of the group.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"OtherAttributes"`: One or more attributes to be returned for the group. For a list of
  supported attributes, see [Directory Service Data Attributes](https://docs.aws.amazon.com/directoryservice/latest/admin-guide/ad_data_attributes.html).

- `"Realm"`: The domain name that's associated with the group.

  !!! note
      This parameter is optional, so you can return groups outside of your Managed Microsoft
      AD domain. When no value is defined, only your Managed Microsoft AD groups are
      returned.

      This value is case insensitive.
"""
function describe_group end

function describe_group(
    DirectoryId, SAMAccountName; aws_config::AbstractAWSConfig=current_aws_config()
)
    return directory_service_data(
        "POST",
        "/Groups/DescribeGroup",
        Dict{String,Any}("DirectoryId" => DirectoryId, "SAMAccountName" => SAMAccountName);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function describe_group(
    DirectoryId,
    SAMAccountName,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return directory_service_data(
        "POST",
        "/Groups/DescribeGroup",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "DirectoryId" => DirectoryId, "SAMAccountName" => SAMAccountName
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    describe_user(directory_id, samaccount_name)
    describe_user(directory_id, samaccount_name, params::Dict{String,<:Any})

Returns information about a specific user.

# Arguments

- `directory_id`: The identifier (ID) of the directory that's associated with the user.
- `samaccount_name`: The name of the user.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"OtherAttributes"`: One or more attribute names to be returned for the user. A key is an
  attribute name, and the value is a list of maps. For a list of supported attributes, see [Directory Service Data Attributes](https://docs.aws.amazon.com/directoryservice/latest/admin-guide/ad_data_attributes.html).

- `"Realm"`: The domain name that's associated with the user.

  !!! note
      This parameter is optional, so you can return users outside your Managed Microsoft AD
      domain. When no value is defined, only your Managed Microsoft AD users are returned.

      This value is case insensitive.
"""
function describe_user end

function describe_user(
    DirectoryId, SAMAccountName; aws_config::AbstractAWSConfig=current_aws_config()
)
    return directory_service_data(
        "POST",
        "/Users/DescribeUser",
        Dict{String,Any}("DirectoryId" => DirectoryId, "SAMAccountName" => SAMAccountName);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function describe_user(
    DirectoryId,
    SAMAccountName,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return directory_service_data(
        "POST",
        "/Users/DescribeUser",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "DirectoryId" => DirectoryId, "SAMAccountName" => SAMAccountName
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    disable_user(directory_id, samaccount_name)
    disable_user(directory_id, samaccount_name, params::Dict{String,<:Any})

Deactivates an active user account. For information about how to enable an inactive user
account, see [ResetUserPassword](https://docs.aws.amazon.com/directoryservice/latest/devguide/API_ResetUserPassword.html)
in the *Directory Service API Reference*.

# Arguments

- `directory_id`: The identifier (ID) of the directory that's associated with the user.
- `samaccount_name`: The name of the user.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"ClientToken"`: A unique and case-sensitive identifier that you provide to make sure the
  idempotency of the request, so multiple identical calls have the same effect as one single
  call.

  A client token is valid for 8 hours after the first request that uses it completes. After
  8 hours, any request with the same client token is treated as a new request. If the
request succeeds, any future uses of that token will be idempotent for another 8 hours.

  If you submit a request with the same client token but change one of the other parameters
  within the 8-hour idempotency window, Directory Service Data returns an
  `ConflictException`.

  !!! note
      This parameter is optional when using the CLI or SDK.
"""
function disable_user end

function disable_user(
    DirectoryId, SAMAccountName; aws_config::AbstractAWSConfig=current_aws_config()
)
    return directory_service_data(
        "POST",
        "/Users/DisableUser",
        Dict{String,Any}(
            "DirectoryId" => DirectoryId,
            "SAMAccountName" => SAMAccountName,
            "ClientToken" => string(uuid4()),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function disable_user(
    DirectoryId,
    SAMAccountName,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return directory_service_data(
        "POST",
        "/Users/DisableUser",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "DirectoryId" => DirectoryId,
                    "SAMAccountName" => SAMAccountName,
                    "ClientToken" => string(uuid4()),
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_group_members(directory_id, samaccount_name)
    list_group_members(directory_id, samaccount_name, params::Dict{String,<:Any})

Returns member information for the specified group.

This operation supports pagination with the use of the `NextToken` request and response
parameters. If more results are available, the `ListGroupMembers.NextToken` member contains
a token that you pass in the next call to `ListGroupMembers`. This retrieves the next set of
items.

You can also specify a maximum number of return results with the `MaxResults` parameter.

# Arguments

- `directory_id`: The identifier (ID) of the directory that's associated with the group.
- `samaccount_name`: The name of the group.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"MaxResults"`: The maximum number of results to be returned per request.

- `"MemberRealm"`: The domain name that's associated with the group member. This parameter
  defaults to the Managed Microsoft AD domain.

  !!! note
      This parameter is optional and case insensitive.

- `"NextToken"`: An encoded paging token for paginated calls that can be passed back to
  retrieve the next page.

- `"Realm"`: The domain name that's associated with the group.

  !!! note
      This parameter is optional, so you can return members from a group outside of your
      Managed Microsoft AD domain. When no value is defined, only members of your Managed
      Microsoft AD groups are returned.

      This value is case insensitive.
"""
function list_group_members end

function list_group_members(
    DirectoryId, SAMAccountName; aws_config::AbstractAWSConfig=current_aws_config()
)
    return directory_service_data(
        "POST",
        "/GroupMemberships/ListGroupMembers",
        Dict{String,Any}("DirectoryId" => DirectoryId, "SAMAccountName" => SAMAccountName);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_group_members(
    DirectoryId,
    SAMAccountName,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return directory_service_data(
        "POST",
        "/GroupMemberships/ListGroupMembers",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "DirectoryId" => DirectoryId, "SAMAccountName" => SAMAccountName
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_groups(directory_id)
    list_groups(directory_id, params::Dict{String,<:Any})

Returns group information for the specified directory.

This operation supports pagination with the use of the `NextToken` request and response
parameters. If more results are available, the `ListGroups.NextToken` member contains a
token that you pass in the next call to `ListGroups`. This retrieves the next set of items.

You can also specify a maximum number of return results with the `MaxResults` parameter.

# Arguments

- `directory_id`: The identifier (ID) of the directory that's associated with the group.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"MaxResults"`: The maximum number of results to be returned per request.

- `"NextToken"`: An encoded paging token for paginated calls that can be passed back to
  retrieve the next page.

- `"Realm"`: The domain name associated with the directory.

  !!! note
      This parameter is optional, so you can return groups outside of your Managed Microsoft
      AD domain. When no value is defined, only your Managed Microsoft AD groups are
      returned.

      This value is case insensitive.
"""
function list_groups end

function list_groups(DirectoryId; aws_config::AbstractAWSConfig=current_aws_config())
    return directory_service_data(
        "POST",
        "/Groups/ListGroups",
        Dict{String,Any}("DirectoryId" => DirectoryId);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_groups(
    DirectoryId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return directory_service_data(
        "POST",
        "/Groups/ListGroups",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("DirectoryId" => DirectoryId), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_groups_for_member(directory_id, samaccount_name)
    list_groups_for_member(directory_id, samaccount_name, params::Dict{String,<:Any})

Returns group information for the specified member.

This operation supports pagination with the use of the `NextToken` request and response
parameters. If more results are available, the `ListGroupsForMember.NextToken` member
contains a token that you pass in the next call to `ListGroupsForMember`. This retrieves the
next set of items.

You can also specify a maximum number of return results with the `MaxResults` parameter.

# Arguments

- `directory_id`: The identifier (ID) of the directory that's associated with the member.
- `samaccount_name`: The `SAMAccountName` of the user, group, or computer that's a member of
  the group.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"MaxResults"`: The maximum number of results to be returned per request.

- `"MemberRealm"`: The domain name that's associated with the group member.

  !!! note
      This parameter is optional, so you can limit your results to the group members in a
      specific domain.

      This parameter is case insensitive and defaults to `Realm`

- `"NextToken"`: An encoded paging token for paginated calls that can be passed back to
  retrieve the next page.

- `"Realm"`: The domain name that's associated with the group.

  !!! note
      This parameter is optional, so you can return groups outside of your Managed Microsoft
      AD domain. When no value is defined, only your Managed Microsoft AD groups are
      returned.

      This value is case insensitive and defaults to your Managed Microsoft AD domain.
"""
function list_groups_for_member end

function list_groups_for_member(
    DirectoryId, SAMAccountName; aws_config::AbstractAWSConfig=current_aws_config()
)
    return directory_service_data(
        "POST",
        "/GroupMemberships/ListGroupsForMember",
        Dict{String,Any}("DirectoryId" => DirectoryId, "SAMAccountName" => SAMAccountName);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_groups_for_member(
    DirectoryId,
    SAMAccountName,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return directory_service_data(
        "POST",
        "/GroupMemberships/ListGroupsForMember",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "DirectoryId" => DirectoryId, "SAMAccountName" => SAMAccountName
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_users(directory_id)
    list_users(directory_id, params::Dict{String,<:Any})

Returns user information for the specified directory.

This operation supports pagination with the use of the `NextToken` request and response
parameters. If more results are available, the `ListUsers.NextToken` member contains a token
that you pass in the next call to `ListUsers`. This retrieves the next set of items.

You can also specify a maximum number of return results with the `MaxResults` parameter.

# Arguments

- `directory_id`: The identifier (ID) of the directory that's associated with the user.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"MaxResults"`: The maximum number of results to be returned per request.

- `"NextToken"`: An encoded paging token for paginated calls that can be passed back to
  retrieve the next page.

- `"Realm"`: The domain name that's associated with the user.

  !!! note
      This parameter is optional, so you can return users outside of your Managed Microsoft
      AD domain. When no value is defined, only your Managed Microsoft AD users are
      returned.

      This value is case insensitive.
"""
function list_users end

function list_users(DirectoryId; aws_config::AbstractAWSConfig=current_aws_config())
    return directory_service_data(
        "POST",
        "/Users/ListUsers",
        Dict{String,Any}("DirectoryId" => DirectoryId);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_users(
    DirectoryId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return directory_service_data(
        "POST",
        "/Users/ListUsers",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("DirectoryId" => DirectoryId), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    remove_group_member(directory_id, group_name, member_name)
    remove_group_member(directory_id, group_name, member_name, params::Dict{String,<:Any})

Removes a member from a group.

# Arguments

- `directory_id`: The identifier (ID) of the directory that's associated with the member.
- `group_name`: The name of the group.
- `member_name`: The `SAMAccountName` of the user, group, or computer to remove from the
  group.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"ClientToken"`: A unique and case-sensitive identifier that you provide to make sure the
  idempotency of the request, so multiple identical calls have the same effect as one single
  call.

  A client token is valid for 8 hours after the first request that uses it completes. After
  8 hours, any request with the same client token is treated as a new request. If the
request succeeds, any future uses of that token will be idempotent for another 8 hours.

  If you submit a request with the same client token but change one of the other parameters
  within the 8-hour idempotency window, Directory Service Data returns an
  `ConflictException`.

  !!! note
      This parameter is optional when using the CLI or SDK.

- `"MemberRealm"`: The domain name that's associated with the group member. This parameter
  defaults to the Managed Microsoft AD domain.

  !!! note
      This parameter is optional and case insensitive.
"""
function remove_group_member end

function remove_group_member(
    DirectoryId, GroupName, MemberName; aws_config::AbstractAWSConfig=current_aws_config()
)
    return directory_service_data(
        "POST",
        "/GroupMemberships/RemoveGroupMember",
        Dict{String,Any}(
            "DirectoryId" => DirectoryId,
            "GroupName" => GroupName,
            "MemberName" => MemberName,
            "ClientToken" => string(uuid4()),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function remove_group_member(
    DirectoryId,
    GroupName,
    MemberName,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return directory_service_data(
        "POST",
        "/GroupMemberships/RemoveGroupMember",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "DirectoryId" => DirectoryId,
                    "GroupName" => GroupName,
                    "MemberName" => MemberName,
                    "ClientToken" => string(uuid4()),
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    search_groups(directory_id, search_attributes, search_string)
    search_groups(directory_id, search_attributes, search_string, params::Dict{String,<:Any})

Searches the specified directory for a group. You can find groups that match the
`SearchString` parameter with the value of their attributes included in the `SearchString`
parameter.

This operation supports pagination with the use of the `NextToken` request and response
parameters. If more results are available, the `SearchGroups.NextToken` member contains a
token that you pass in the next call to `SearchGroups`. This retrieves the next set of
items.

You can also specify a maximum number of return results with the `MaxResults` parameter.

# Arguments

- `directory_id`: The identifier (ID) of the directory that's associated with the group.

- `search_attributes`: One or more data attributes that are used to search for a group. For
  a list of supported attributes, see [Directory Service Data Attributes](https://docs.aws.amazon.com/directoryservice/latest/admin-guide/ad_data_attributes.html).

- `search_string`: The attribute value that you want to search for.

  !!! note
      Wildcard `(*)` searches aren't supported. For a list of supported attributes, see [Directory Service Data Attributes](https://docs.aws.amazon.com/directoryservice/latest/admin-guide/ad_data_attributes.html).

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"MaxResults"`: The maximum number of results to be returned per request.

- `"NextToken"`: An encoded paging token for paginated calls that can be passed back to
  retrieve the next page.

- `"Realm"`: The domain name that's associated with the group.

  !!! note
      This parameter is optional, so you can return groups outside of your Managed Microsoft
      AD domain. When no value is defined, only your Managed Microsoft AD groups are
      returned.

      This value is case insensitive.
"""
function search_groups end

function search_groups(
    DirectoryId,
    SearchAttributes,
    SearchString;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return directory_service_data(
        "POST",
        "/Groups/SearchGroups",
        Dict{String,Any}(
            "DirectoryId" => DirectoryId,
            "SearchAttributes" => SearchAttributes,
            "SearchString" => SearchString,
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function search_groups(
    DirectoryId,
    SearchAttributes,
    SearchString,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return directory_service_data(
        "POST",
        "/Groups/SearchGroups",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "DirectoryId" => DirectoryId,
                    "SearchAttributes" => SearchAttributes,
                    "SearchString" => SearchString,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    search_users(directory_id, search_attributes, search_string)
    search_users(directory_id, search_attributes, search_string, params::Dict{String,<:Any})

Searches the specified directory for a user. You can find users that match the
`SearchString` parameter with the value of their attributes included in the `SearchString`
parameter.

This operation supports pagination with the use of the `NextToken` request and response
parameters. If more results are available, the `SearchUsers.NextToken` member contains a
token that you pass in the next call to `SearchUsers`. This retrieves the next set of items.

You can also specify a maximum number of return results with the `MaxResults` parameter.

# Arguments

- `directory_id`: The identifier (ID) of the directory that's associated with the user.

- `search_attributes`: One or more data attributes that are used to search for a user. For a
  list of supported attributes, see [Directory Service Data Attributes](https://docs.aws.amazon.com/directoryservice/latest/admin-guide/ad_data_attributes.html).

- `search_string`: The attribute value that you want to search for.

  !!! note
      Wildcard `(*)` searches aren't supported. For a list of supported attributes, see [Directory Service Data Attributes](https://docs.aws.amazon.com/directoryservice/latest/admin-guide/ad_data_attributes.html).

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"MaxResults"`: The maximum number of results to be returned per request.

- `"NextToken"`: An encoded paging token for paginated calls that can be passed back to
  retrieve the next page.

- `"Realm"`: The domain name that's associated with the user.

  !!! note
      This parameter is optional, so you can return users outside of your Managed Microsoft
      AD domain. When no value is defined, only your Managed Microsoft AD users are
      returned.

      This value is case insensitive.
"""
function search_users end

function search_users(
    DirectoryId,
    SearchAttributes,
    SearchString;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return directory_service_data(
        "POST",
        "/Users/SearchUsers",
        Dict{String,Any}(
            "DirectoryId" => DirectoryId,
            "SearchAttributes" => SearchAttributes,
            "SearchString" => SearchString,
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function search_users(
    DirectoryId,
    SearchAttributes,
    SearchString,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return directory_service_data(
        "POST",
        "/Users/SearchUsers",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "DirectoryId" => DirectoryId,
                    "SearchAttributes" => SearchAttributes,
                    "SearchString" => SearchString,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_group(directory_id, samaccount_name)
    update_group(directory_id, samaccount_name, params::Dict{String,<:Any})

Updates group information.

# Arguments

- `directory_id`: The identifier (ID) of the directory that's associated with the group.
- `samaccount_name`: The name of the group.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"ClientToken"`: A unique and case-sensitive identifier that you provide to make sure the
  idempotency of the request, so multiple identical calls have the same effect as one single
  call.

  A client token is valid for 8 hours after the first request that uses it completes. After
  8 hours, any request with the same client token is treated as a new request. If the
request succeeds, any future uses of that token will be idempotent for another 8 hours.

  If you submit a request with the same client token but change one of the other parameters
  within the 8-hour idempotency window, Directory Service Data returns an
  `ConflictException`.

  !!! note
      This parameter is optional when using the CLI or SDK.

- `"GroupScope"`: The scope of the AD group. For details, see [Active Directory security groups](https://learn.microsoft.com/en-us/windows-server/identity/ad-ds/manage/understand-security-groups#group-scope).

- `"GroupType"`: The AD group type. For details, see [Active Directory security group type](https://learn.microsoft.com/en-us/windows-server/identity/ad-ds/manage/understand-security-groups#how-active-directory-security-groups-work).

- `"OtherAttributes"`: An expression that defines one or more attributes with the data type
  and the value of each attribute.

- `"UpdateType"`: The type of update to be performed. If no value exists for the attribute,
  use `ADD`. Otherwise, use `REPLACE` to change an attribute value or `REMOVE` to clear the
  attribute value.
"""
function update_group end

function update_group(
    DirectoryId, SAMAccountName; aws_config::AbstractAWSConfig=current_aws_config()
)
    return directory_service_data(
        "POST",
        "/Groups/UpdateGroup",
        Dict{String,Any}(
            "DirectoryId" => DirectoryId,
            "SAMAccountName" => SAMAccountName,
            "ClientToken" => string(uuid4()),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_group(
    DirectoryId,
    SAMAccountName,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return directory_service_data(
        "POST",
        "/Groups/UpdateGroup",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "DirectoryId" => DirectoryId,
                    "SAMAccountName" => SAMAccountName,
                    "ClientToken" => string(uuid4()),
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_user(directory_id, samaccount_name)
    update_user(directory_id, samaccount_name, params::Dict{String,<:Any})

Updates user information.

# Arguments

- `directory_id`: The identifier (ID) of the directory that's associated with the user.
- `samaccount_name`: The name of the user.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"ClientToken"`: A unique and case-sensitive identifier that you provide to make sure the
  idempotency of the request, so multiple identical calls have the same effect as one single
  call.

  A client token is valid for 8 hours after the first request that uses it completes. After
  8 hours, any request with the same client token is treated as a new request. If the
request succeeds, any future uses of that token will be idempotent for another 8 hours.

  If you submit a request with the same client token but change one of the other parameters
  within the 8-hour idempotency window, Directory Service Data returns an
  `ConflictException`.

  !!! note
      This parameter is optional when using the CLI or SDK.

- `"EmailAddress"`: The email address of the user.

- `"GivenName"`: The first name of the user.

- `"OtherAttributes"`: An expression that defines one or more attribute names with the data
  type and value of each attribute. A key is an attribute name, and the value is a list of
  maps. For a list of supported attributes, see [Directory Service Data Attributes](https://docs.aws.amazon.com/directoryservice/latest/admin-guide/ad_data_attributes.html).

  !!! note
      Attribute names are case insensitive.

- `"Surname"`: The last name of the user.

- `"UpdateType"`: The type of update to be performed. If no value exists for the attribute,
  use `ADD`. Otherwise, use `REPLACE` to change an attribute value or `REMOVE` to clear the
  attribute value.
"""
function update_user end

function update_user(
    DirectoryId, SAMAccountName; aws_config::AbstractAWSConfig=current_aws_config()
)
    return directory_service_data(
        "POST",
        "/Users/UpdateUser",
        Dict{String,Any}(
            "DirectoryId" => DirectoryId,
            "SAMAccountName" => SAMAccountName,
            "ClientToken" => string(uuid4()),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_user(
    DirectoryId,
    SAMAccountName,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return directory_service_data(
        "POST",
        "/Users/UpdateUser",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "DirectoryId" => DirectoryId,
                    "SAMAccountName" => SAMAccountName,
                    "ClientToken" => string(uuid4()),
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end
