# This file is auto-generated by AWS.jl
# Note: `SERVICE_FEATURE_SET` is embedded by `@service`
using AWS: AbstractAWSConfig, current_aws_config, _merge
using AWS.AWSServices: s3files
using AWS.UUIDs: uuid4

"""
    create_access_point(file_system_id)
    create_access_point(file_system_id, params::Dict{String,<:Any})

Creates an S3 File System Access Point for application-specific access with POSIX user
identity and root directory enforcement. Access points provide a way to manage access to
shared datasets in multi-tenant scenarios.

# Arguments

- `file_system_id`: The ID or Amazon Resource Name (ARN) of the S3 File System.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"clientToken"`: A unique, case-sensitive identifier to ensure that the operation
  completes no more than one time. If this token matches a previous request, Amazon Web
  Services ignores the request, but does not return an error.
- `"posixUser"`: The POSIX identity with uid, gid, and secondary group IDs for user
  enforcement when accessing the file system through this access point.
- `"rootDirectory"`: The root directory path for the access point, with optional creation
  permissions for newly created directories.
- `"tags"`: An array of key-value pairs to apply to the access point for resource tagging.
"""
function create_access_point end

function create_access_point(
    fileSystemId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3files(
        "PUT",
        "/access-points",
        Dict{String,Any}("fileSystemId" => fileSystemId, "clientToken" => string(uuid4()));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_access_point(
    fileSystemId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3files(
        "PUT",
        "/access-points",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "fileSystemId" => fileSystemId, "clientToken" => string(uuid4())
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_file_system(bucket, role_arn)
    create_file_system(bucket, role_arn, params::Dict{String,<:Any})

Creates an S3 File System resource scoped to a bucket or prefix within a bucket, enabling
file system access to S3 data. To create a file system, you need an S3 bucket and an IAM
role that grants the service permission to access the bucket.

# Arguments

- `bucket`: The Amazon Resource Name (ARN) of the S3 bucket that will be accessible through
  the file system. The bucket must exist and be in the same Amazon Web Services Region as
  the file system.
- `role_arn`: The ARN of the IAM role that grants the S3 Files service permission to read
  and write data between the file system and the S3 bucket. This role must have the
  necessary permissions to access the specified bucket and prefix.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"acceptBucketWarning"`: Set to true to acknowledge and accept any warnings about the
  bucket configuration. If not specified, the operation may fail if there are bucket
  configuration warnings.
- `"clientToken"`: A unique, case-sensitive identifier that you provide to ensure idempotent
  creation. Up to 64 ASCII characters are allowed. If you don't specify a client token, the
  Amazon Web Services SDK automatically generates one.
- `"kmsKeyId"`: The ARN, key ID, or alias of the KMS key to use for encryption. If not
  specified, the service uses a service-owned key for encryption. You can specify a KMS key
  using the following formats: key ID, ARN, key alias, or key alias ARN. If you use
  `KmsKeyId`, the file system will be encrypted.
- `"prefix"`: An optional prefix within the S3 bucket to scope the file system access. If
  specified, the file system provides access only to objects with keys that begin with this
  prefix. If not specified, the file system provides access to the entire bucket.
- `"tags"`: An array of key-value pairs to apply as tags to the file system resource. Each
  tag is a user-defined key-value pair. You can use tags to categorize and manage your file
  systems. Each key must be unique for the resource.
"""
function create_file_system end

function create_file_system(
    bucket, roleArn; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3files(
        "PUT",
        "/file-systems",
        Dict{String,Any}(
            "bucket" => bucket, "roleArn" => roleArn, "clientToken" => string(uuid4())
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_file_system(
    bucket,
    roleArn,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3files(
        "PUT",
        "/file-systems",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "bucket" => bucket,
                    "roleArn" => roleArn,
                    "clientToken" => string(uuid4()),
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_mount_target(file_system_id, subnet_id)
    create_mount_target(file_system_id, subnet_id, params::Dict{String,<:Any})

Creates a mount target resource as an endpoint for mounting the S3 File System from compute
resources in a specific Availability Zone and VPC. Mount targets provide network access to
the file system.

# Arguments

- `file_system_id`: The ID or Amazon Resource Name (ARN) of the S3 File System to create the
  mount target for.
- `subnet_id`: The ID of the subnet where the mount target will be created. The subnet must
  be in the same Amazon Web Services Region as the file system. For file systems with
  regional availability, you can create mount targets in any subnet within the Region. The
  subnet determines the Availability Zone where the mount target will be located.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"ipAddressType"`: The IP address type for the mount target. If not specified, `IPV4_ONLY`
  is used. The IP address type must match the IP configuration of the specified subnet.
- `"ipv4Address"`: A specific IPv4 address to assign to the mount target. If not specified
  and the IP address type supports IPv4, an address is automatically assigned from the
  subnet's available IPv4 address range. The address must be within the subnet's CIDR block
  and not already in use.
- `"ipv6Address"`: A specific IPv6 address to assign to the mount target. If not specified
  and the IP address type supports IPv6, an address is automatically assigned from the
  subnet's available IPv6 address range. The address must be within the subnet's IPv6 CIDR
  block and not already in use.
- `"securityGroups"`: An array of VPC security group IDs to associate with the mount
  target's network interface. These security groups control network access to the mount
  target. If not specified, the default security group for the subnet's VPC is used. All
  security groups must belong to the same VPC as the subnet.
"""
function create_mount_target end

function create_mount_target(
    fileSystemId, subnetId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3files(
        "PUT",
        "/mount-targets",
        Dict{String,Any}("fileSystemId" => fileSystemId, "subnetId" => subnetId);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_mount_target(
    fileSystemId,
    subnetId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3files(
        "PUT",
        "/mount-targets",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("fileSystemId" => fileSystemId, "subnetId" => subnetId),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_access_point(access_point_id)
    delete_access_point(access_point_id, params::Dict{String,<:Any})

Deletes an S3 File System Access Point. This operation is irreversible.

# Arguments

- `access_point_id`: The ID or Amazon Resource Name (ARN) of the access point to delete.
"""
function delete_access_point end

function delete_access_point(
    accessPointId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3files(
        "DELETE",
        "/access-points/$(accessPointId)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_access_point(
    accessPointId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3files(
        "DELETE",
        "/access-points/$(accessPointId)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_file_system(file_system_id)
    delete_file_system(file_system_id, params::Dict{String,<:Any})

Deletes an S3 File System. You can optionally force deletion of a file system that has
pending export data.

# Arguments

- `file_system_id`: The ID or Amazon Resource Name (ARN) of the S3 File System to delete.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"forceDelete"`: If true, allows deletion of a file system that contains data pending
  export to S3. If false (the default), the deletion will fail if there is data that has not
  yet been exported to the S3 bucket. Use this parameter with caution as it may result in
  data loss.
"""
function delete_file_system end

function delete_file_system(
    fileSystemId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3files(
        "DELETE",
        "/file-systems/$(fileSystemId)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_file_system(
    fileSystemId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3files(
        "DELETE",
        "/file-systems/$(fileSystemId)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_file_system_policy(file_system_id)
    delete_file_system_policy(file_system_id, params::Dict{String,<:Any})

Deletes the IAM resource policy of an S3 File System.

# Arguments

- `file_system_id`: The ID or Amazon Resource Name (ARN) of the S3 File System whose
  resource policy to delete.
"""
function delete_file_system_policy end

function delete_file_system_policy(
    fileSystemId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3files(
        "DELETE",
        "/file-systems/$(fileSystemId)/policy";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_file_system_policy(
    fileSystemId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3files(
        "DELETE",
        "/file-systems/$(fileSystemId)/policy",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_mount_target(mount_target_id)
    delete_mount_target(mount_target_id, params::Dict{String,<:Any})

Deletes the specified mount target. This operation is irreversible.

# Arguments

- `mount_target_id`: The ID of the mount target to delete.
"""
function delete_mount_target end

function delete_mount_target(
    mountTargetId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3files(
        "DELETE",
        "/mount-targets/$(mountTargetId)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_mount_target(
    mountTargetId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3files(
        "DELETE",
        "/mount-targets/$(mountTargetId)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_access_point(access_point_id)
    get_access_point(access_point_id, params::Dict{String,<:Any})

Returns resource information for an S3 File System Access Point.

# Arguments

- `access_point_id`: The ID or Amazon Resource Name (ARN) of the access point to retrieve
  information for.
"""
function get_access_point end

function get_access_point(accessPointId; aws_config::AbstractAWSConfig=current_aws_config())
    return s3files(
        "GET",
        "/access-points/$(accessPointId)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_access_point(
    accessPointId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3files(
        "GET",
        "/access-points/$(accessPointId)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_file_system(file_system_id)
    get_file_system(file_system_id, params::Dict{String,<:Any})

Returns resource information for the specified S3 File System including status,
configuration, and metadata.

# Arguments

- `file_system_id`: The ID or Amazon Resource Name (ARN) of the S3 File System to retrieve
  information for.
"""
function get_file_system end

function get_file_system(fileSystemId; aws_config::AbstractAWSConfig=current_aws_config())
    return s3files(
        "GET", "/file-systems/$(fileSystemId)"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function get_file_system(
    fileSystemId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3files(
        "GET",
        "/file-systems/$(fileSystemId)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_file_system_policy(file_system_id)
    get_file_system_policy(file_system_id, params::Dict{String,<:Any})

Returns the IAM resource policy of an S3 File System.

# Arguments

- `file_system_id`: The ID or Amazon Resource Name (ARN) of the S3 File System whose
  resource policy to retrieve.
"""
function get_file_system_policy end

function get_file_system_policy(
    fileSystemId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3files(
        "GET",
        "/file-systems/$(fileSystemId)/policy";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_file_system_policy(
    fileSystemId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3files(
        "GET",
        "/file-systems/$(fileSystemId)/policy",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_mount_target(mount_target_id)
    get_mount_target(mount_target_id, params::Dict{String,<:Any})

Returns detailed resource information for the specified mount target including network
configuration.

# Arguments

- `mount_target_id`: The ID of the mount target to retrieve information for.
"""
function get_mount_target end

function get_mount_target(mountTargetId; aws_config::AbstractAWSConfig=current_aws_config())
    return s3files(
        "GET",
        "/mount-targets/$(mountTargetId)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_mount_target(
    mountTargetId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3files(
        "GET",
        "/mount-targets/$(mountTargetId)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_synchronization_configuration(file_system_id)
    get_synchronization_configuration(file_system_id, params::Dict{String,<:Any})

Returns the synchronization configuration for the specified S3 File System, including import
data rules and expiration data rules.

# Arguments

- `file_system_id`: The ID or Amazon Resource Name (ARN) of the S3 File System to retrieve
  the synchronization configuration for.
"""
function get_synchronization_configuration end

function get_synchronization_configuration(
    fileSystemId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3files(
        "GET",
        "/file-systems/$(fileSystemId)/synchronization-configuration";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_synchronization_configuration(
    fileSystemId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3files(
        "GET",
        "/file-systems/$(fileSystemId)/synchronization-configuration",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_access_points(file_system_id)
    list_access_points(file_system_id, params::Dict{String,<:Any})

Returns resource information for all S3 File System Access Points associated with the
specified S3 File System.

# Arguments

- `file_system_id`: The ID or Amazon Resource Name (ARN) of the S3 File System to list
  access points for.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"maxResults"`: The maximum number of access points to return in a single response.
- `"nextToken"`: A pagination token returned from a previous call to continue listing access
  points.
"""
function list_access_points end

function list_access_points(
    fileSystemId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3files(
        "GET",
        "/access-points",
        Dict{String,Any}("fileSystemId" => fileSystemId);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_access_points(
    fileSystemId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3files(
        "GET",
        "/access-points",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("fileSystemId" => fileSystemId), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_file_systems()
    list_file_systems(params::Dict{String,<:Any})

Returns a list of all S3 File Systems owned by the account with optional filtering by
bucket.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"bucket"`: Optional filter to list only file systems associated with the specified S3
  bucket Amazon Resource Name (ARN). If provided, only file systems that provide access to
  this bucket will be returned in the response.
- `"maxResults"`: The maximum number of file systems to return in a single response. If not
  specified, up to 100 file systems are returned.
- `"nextToken"`: A pagination token returned from a previous call to continue listing file
  systems.
"""
function list_file_systems end

function list_file_systems(; aws_config::AbstractAWSConfig=current_aws_config())
    return s3files("GET", "/file-systems"; aws_config, feature_set=SERVICE_FEATURE_SET)
end

function list_file_systems(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3files(
        "GET", "/file-systems", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    list_mount_targets()
    list_mount_targets(params::Dict{String,<:Any})

Returns resource information for all mount targets with optional filtering by file system,
access point, and VPC.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"accessPointId"`: Optional filter to list only mount targets associated with the
  specified access point ID or Amazon Resource Name (ARN).
- `"fileSystemId"`: Optional filter to list only mount targets associated with the specified
  S3 File System ID or Amazon Resource Name (ARN). If provided, only mount targets for this
  file system will be returned in the response.
- `"maxResults"`: The maximum number of mount targets to return in a single response.
- `"nextToken"`: A pagination token returned from a previous call to continue listing mount
  targets.
"""
function list_mount_targets end

function list_mount_targets(; aws_config::AbstractAWSConfig=current_aws_config())
    return s3files("GET", "/mount-targets"; aws_config, feature_set=SERVICE_FEATURE_SET)
end

function list_mount_targets(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3files(
        "GET", "/mount-targets", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    list_tags_for_resource(resource_id)
    list_tags_for_resource(resource_id, params::Dict{String,<:Any})

Lists all tags for S3 Files resources.

# Arguments

- `resource_id`: The ID or Amazon Resource Name (ARN) of the resource to list tags for.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"MaxResults"`: The maximum number of tags to return in a single response.
- `"NextToken"`: A pagination token returned from a previous call to continue listing tags.
"""
function list_tags_for_resource end

function list_tags_for_resource(
    resourceId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3files(
        "GET", "/resource-tags/$(resourceId)"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_tags_for_resource(
    resourceId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3files(
        "GET",
        "/resource-tags/$(resourceId)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    put_file_system_policy(file_system_id, policy)
    put_file_system_policy(file_system_id, policy, params::Dict{String,<:Any})

Creates or replaces the IAM resource policy for an S3 File System to control access
permissions.

# Arguments

- `file_system_id`: The ID or Amazon Resource Name (ARN) of the S3 File System to apply the
  resource policy to.
- `policy`: The JSON-formatted resource policy to apply to the file system. The policy
  defines the permissions for accessing the file system. The policy must be a valid JSON
  document that follows IAM policy syntax.
"""
function put_file_system_policy end

function put_file_system_policy(
    fileSystemId, policy; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3files(
        "PUT",
        "/file-systems/$(fileSystemId)/policy",
        Dict{String,Any}("policy" => policy);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function put_file_system_policy(
    fileSystemId,
    policy,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3files(
        "PUT",
        "/file-systems/$(fileSystemId)/policy",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("policy" => policy), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    put_synchronization_configuration(expiration_data_rules, file_system_id, import_data_rules)
    put_synchronization_configuration(expiration_data_rules, file_system_id, import_data_rules, params::Dict{String,<:Any})

Creates or updates the synchronization configuration for the specified S3 File System,
including import data rules and expiration data rules.

# Arguments

- `expiration_data_rules`: An array of expiration data rules that control when cached data
  expires from the file system.
- `file_system_id`: The ID or Amazon Resource Name (ARN) of the S3 File System to configure
  synchronization for.
- `import_data_rules`: An array of import data rules that control how data is imported from
  S3 into the file system.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"latestVersionNumber"`: The version number of the current synchronization configuration.
  Omit this value when creating a synchronization configuration for the first time. For
  subsequent updates, provide this value for optimistic concurrency control. If the version
  number does not match the current configuration, the request fails with a
  `ConflictException`.
"""
function put_synchronization_configuration end

function put_synchronization_configuration(
    expirationDataRules,
    fileSystemId,
    importDataRules;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3files(
        "PUT",
        "/file-systems/$(fileSystemId)/synchronization-configuration",
        Dict{String,Any}(
            "expirationDataRules" => expirationDataRules,
            "importDataRules" => importDataRules,
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function put_synchronization_configuration(
    expirationDataRules,
    fileSystemId,
    importDataRules,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3files(
        "PUT",
        "/file-systems/$(fileSystemId)/synchronization-configuration",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "expirationDataRules" => expirationDataRules,
                    "importDataRules" => importDataRules,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    tag_resource(resource_id, tags)
    tag_resource(resource_id, tags, params::Dict{String,<:Any})

Creates tags for S3 Files resources using standard Amazon Web Services tagging APIs.

# Arguments

- `resource_id`: The ID or Amazon Resource Name (ARN) of the resource to add tags to.
- `tags`: An array of key-value pairs to add as tags to the resource.
"""
function tag_resource end

function tag_resource(resourceId, tags; aws_config::AbstractAWSConfig=current_aws_config())
    return s3files(
        "POST",
        "/resource-tags/$(resourceId)",
        Dict{String,Any}("tags" => tags);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function tag_resource(
    resourceId,
    tags,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3files(
        "POST",
        "/resource-tags/$(resourceId)",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("tags" => tags), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    untag_resource(resource_id, tag_keys)
    untag_resource(resource_id, tag_keys, params::Dict{String,<:Any})

Removes tags from S3 Files resources.

# Arguments

- `resource_id`: The ID or Amazon Resource Name (ARN) of the resource to remove tags from.
- `tag_keys`: An array of tag keys to remove from the resource.
"""
function untag_resource end

function untag_resource(
    resourceId, tagKeys; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3files(
        "DELETE",
        "/resource-tags/$(resourceId)",
        Dict{String,Any}("tagKeys" => tagKeys);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function untag_resource(
    resourceId,
    tagKeys,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3files(
        "DELETE",
        "/resource-tags/$(resourceId)",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("tagKeys" => tagKeys), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_mount_target(mount_target_id, security_groups)
    update_mount_target(mount_target_id, security_groups, params::Dict{String,<:Any})

Updates the mount target resource, specifically security group configurations.

# Arguments

- `mount_target_id`: The ID of the mount target to update.
- `security_groups`: An array of VPC security group IDs to associate with the mount target's
  network interface. This replaces the existing security groups. All security groups must
  belong to the same VPC as the mount target's subnet.
"""
function update_mount_target end

function update_mount_target(
    mountTargetId, securityGroups; aws_config::AbstractAWSConfig=current_aws_config()
)
    return s3files(
        "PUT",
        "/mount-targets/$(mountTargetId)",
        Dict{String,Any}("securityGroups" => securityGroups);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_mount_target(
    mountTargetId,
    securityGroups,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return s3files(
        "PUT",
        "/mount-targets/$(mountTargetId)",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("securityGroups" => securityGroups), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end
