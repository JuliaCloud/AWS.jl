# This file is auto-generated by AWS.jl
# Note: `SERVICE_FEATURE_SET` is embedded by `@service`
using AWS: AbstractAWSConfig, current_aws_config, _merge
using AWS.AWSServices: arc_zonal_shift
using AWS.UUIDs: uuid4

"""
    cancel_practice_run(zonal_shift_id)
    cancel_practice_run(zonal_shift_id, params::Dict{String,<:Any})

Cancel an in-progress practice run zonal shift in Amazon Application Recovery Controller.

# Arguments

- `zonal_shift_id`: The identifier of a practice run zonal shift in Amazon Application
  Recovery Controller that you want to cancel.
"""
function cancel_practice_run end

function cancel_practice_run(
    zonalShiftId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return arc_zonal_shift(
        "DELETE",
        "/practiceruns/$(zonalShiftId)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function cancel_practice_run(
    zonalShiftId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return arc_zonal_shift(
        "DELETE",
        "/practiceruns/$(zonalShiftId)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    cancel_zonal_shift(zonal_shift_id)
    cancel_zonal_shift(zonal_shift_id, params::Dict{String,<:Any})

Cancel a zonal shift in Amazon Application Recovery Controller. To cancel the zonal shift,
specify the zonal shift ID.

A zonal shift can be one that you've started for a resource in your Amazon Web Services
account in an Amazon Web Services Region, or it can be a zonal shift started by a practice
run with zonal autoshift.

# Arguments

- `zonal_shift_id`: The internally-generated identifier of a zonal shift.
"""
function cancel_zonal_shift end

function cancel_zonal_shift(
    zonalShiftId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return arc_zonal_shift(
        "DELETE",
        "/zonalshifts/$(zonalShiftId)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function cancel_zonal_shift(
    zonalShiftId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return arc_zonal_shift(
        "DELETE",
        "/zonalshifts/$(zonalShiftId)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_practice_run_configuration(outcome_alarms, resource_identifier)
    create_practice_run_configuration(outcome_alarms, resource_identifier, params::Dict{String,<:Any})

A practice run configuration for zonal autoshift is required when you enable zonal
autoshift. A practice run configuration includes specifications for blocked dates and
blocked time windows, and for Amazon CloudWatch alarms that you create to use with practice
runs. The alarms that you specify are an *outcome alarm*, to monitor application health
during practice runs and, optionally, a *blocking alarm*, to block practice runs from
starting.

When a resource has a practice run configuration, ARC starts zonal shifts for the resource
weekly, to shift traffic for practice runs. Practice runs help you to ensure that shifting
away traffic from an Availability Zone during an autoshift is safe for your application.

For more information, see [Considerations when you configure zonal autoshift](https://docs.aws.amazon.com/r53recovery/latest/dg/arc-zonal-autoshift.considerations.html)
in the Amazon Application Recovery Controller Developer Guide.

# Arguments

- `outcome_alarms`: *Outcome alarms* for practice runs are alarms that you specify that end
  a practice run when one or more of the alarms is in an `ALARM` state.

  Configure one or more of these alarms to monitor the health of your application when
  traffic is shifted away from an Availability Zone during each practice run. You should
  configure these alarms to go into an `ALARM` state if you want to stop a zonal shift, to
  let traffic for the resource return to the original Availability Zone.

- `resource_identifier`: The identifier of the resource that Amazon Web Services shifts
  traffic for with a practice run zonal shift. The identifier is the Amazon Resource Name
  (ARN) for the resource.

  Amazon Application Recovery Controller currently supports enabling the following resources
  for zonal shift and zonal autoshift:

  - [Amazon EC2 Auto Scaling groups](https://docs.aws.amazon.com/r53recovery/latest/dg/arc-zonal-shift.resource-types.ec2-auto-scaling-groups.html)
  - [Amazon Elastic Kubernetes Service](https://docs.aws.amazon.com/r53recovery/latest/dg/arc-zonal-shift.resource-types.eks.html)
  - [Application Load Balancer](https://docs.aws.amazon.com/r53recovery/latest/dg/arc-zonal-shift.resource-types.app-load-balancers.html)
  - [Network Load Balancer](https://docs.aws.amazon.com/r53recovery/latest/dg/arc-zonal-shift.resource-types.network-load-balancers.html)

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"allowedWindows"`: Optionally, you can allow ARC to start practice runs for specific
  windows of days and times.

  The format for allowed windows is: DAY:HH:SS-DAY:HH:SS. Keep in mind, when you specify
  dates, that dates and times for practice runs are in UTC. Also, be aware of potential time
  adjustments that might be required for daylight saving time differences. Separate multiple
  allowed windows with spaces.

  For example, say you want to allow practice runs only on Wednesdays and Fridays from noon
  to 5 p.m. For this scenario, you could set the following recurring days and times as
  allowed windows, for example: `Wed-12:00-Wed:17:00 Fri-12:00-Fri:17:00`.

  !!! important
      The `allowedWindows` have to start and end on the same day. Windows that span multiple
      days aren't supported.

- `"blockedDates"`: Optionally, you can block ARC from starting practice runs for a resource
  on specific calendar dates.

  The format for blocked dates is: YYYY-MM-DD. Keep in mind, when you specify dates, that
  dates and times for practice runs are in UTC. Separate multiple blocked dates with spaces.

  For example, if you have an application update scheduled to launch on May 1, 2024, and you
  don't want practice runs to shift traffic away at that time, you could set a blocked date
  for `2024-05-01`.

- `"blockedWindows"`: Optionally, you can block ARC from starting practice runs for specific
  windows of days and times.

  The format for blocked windows is: DAY:HH:SS-DAY:HH:SS. Keep in mind, when you specify
  dates, that dates and times for practice runs are in UTC. Also, be aware of potential time
  adjustments that might be required for daylight saving time differences. Separate multiple
  blocked windows with spaces.

  For example, say you run business report summaries three days a week. For this scenario,
  you could set the following recurring days and times as blocked windows, for example:
  `Mon:00:00-Mon:10:00 Wed-20:30-Wed:21:30 Fri-20:30-Fri:21:30`.

  !!! important
      The `blockedWindows` have to start and end on the same day. Windows that span multiple
      days aren't supported.

- `"blockingAlarms"`: *Blocking alarms* for practice runs are optional alarms that you can
  specify that block practice runs when one or more of the alarms is in an `ALARM` state.
"""
function create_practice_run_configuration end

function create_practice_run_configuration(
    outcomeAlarms, resourceIdentifier; aws_config::AbstractAWSConfig=current_aws_config()
)
    return arc_zonal_shift(
        "POST",
        "/configuration",
        Dict{String,Any}(
            "outcomeAlarms" => outcomeAlarms, "resourceIdentifier" => resourceIdentifier
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_practice_run_configuration(
    outcomeAlarms,
    resourceIdentifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return arc_zonal_shift(
        "POST",
        "/configuration",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "outcomeAlarms" => outcomeAlarms,
                    "resourceIdentifier" => resourceIdentifier,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_practice_run_configuration(resource_identifier)
    delete_practice_run_configuration(resource_identifier, params::Dict{String,<:Any})

Deletes the practice run configuration for a resource. Before you can delete a practice run
configuration for a resource., you must disable zonal autoshift for the resource. Practice
runs must be configured for zonal autoshift to be enabled.

# Arguments

- `resource_identifier`: The identifier for the resource that you want to delete the
  practice run configuration for. The identifier is the Amazon Resource Name (ARN) for the
  resource.
"""
function delete_practice_run_configuration end

function delete_practice_run_configuration(
    resourceIdentifier; aws_config::AbstractAWSConfig=current_aws_config()
)
    return arc_zonal_shift(
        "DELETE",
        "/configuration/$(resourceIdentifier)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_practice_run_configuration(
    resourceIdentifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return arc_zonal_shift(
        "DELETE",
        "/configuration/$(resourceIdentifier)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_autoshift_observer_notification_status()
    get_autoshift_observer_notification_status(params::Dict{String,<:Any})

Returns the status of the autoshift observer notification. Autoshift observer notifications
notify you through Amazon EventBridge when there is an autoshift event for zonal autoshift.
The status can be `ENABLED` or `DISABLED`. When `ENABLED`, a notification is sent when an
autoshift is triggered. When `DISABLED`, notifications are not sent.
"""
function get_autoshift_observer_notification_status end

function get_autoshift_observer_notification_status(;
    aws_config::AbstractAWSConfig=current_aws_config()
)
    return arc_zonal_shift(
        "GET",
        "/autoshift-observer-notification";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_autoshift_observer_notification_status(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return arc_zonal_shift(
        "GET",
        "/autoshift-observer-notification",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_managed_resource(resource_identifier)
    get_managed_resource(resource_identifier, params::Dict{String,<:Any})

Get information about a resource that's been registered for zonal shifts with Amazon
Application Recovery Controller in this Amazon Web Services Region. Resources that are
registered for zonal shifts are managed resources in ARC. You can start zonal shifts and
configure zonal autoshift for managed resources.

# Arguments

- `resource_identifier`: The identifier for the resource that Amazon Web Services shifts
  traffic for. The identifier is the Amazon Resource Name (ARN) for the resource.

  Amazon Application Recovery Controller currently supports enabling the following resources
  for zonal shift and zonal autoshift:

  - [Amazon EC2 Auto Scaling groups](https://docs.aws.amazon.com/r53recovery/latest/dg/arc-zonal-shift.resource-types.ec2-auto-scaling-groups.html)
  - [Amazon Elastic Kubernetes Service](https://docs.aws.amazon.com/r53recovery/latest/dg/arc-zonal-shift.resource-types.eks.html)
  - [Application Load Balancer](https://docs.aws.amazon.com/r53recovery/latest/dg/arc-zonal-shift.resource-types.app-load-balancers.html)
  - [Network Load Balancer](https://docs.aws.amazon.com/r53recovery/latest/dg/arc-zonal-shift.resource-types.network-load-balancers.html)
"""
function get_managed_resource end

function get_managed_resource(
    resourceIdentifier; aws_config::AbstractAWSConfig=current_aws_config()
)
    return arc_zonal_shift(
        "GET",
        "/managedresources/$(resourceIdentifier)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_managed_resource(
    resourceIdentifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return arc_zonal_shift(
        "GET",
        "/managedresources/$(resourceIdentifier)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_autoshifts()
    list_autoshifts(params::Dict{String,<:Any})

Returns the autoshifts for an Amazon Web Services Region. By default, the call returns only
`ACTIVE` autoshifts. Optionally, you can specify the `status` parameter to return
`COMPLETED` autoshifts.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"maxResults"`: The number of objects that you want to return with this call.
- `"nextToken"`: Specifies that you want to receive the next page of results. Valid only if
  you received a `nextToken` response in the previous request. If you did, it indicates that
  more output is available. Set this parameter to the value provided by the previous call's
  `nextToken` response to request the next page of results.
- `"status"`: The status of the autoshift.
"""
function list_autoshifts end

function list_autoshifts(; aws_config::AbstractAWSConfig=current_aws_config())
    return arc_zonal_shift(
        "GET", "/autoshifts"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_autoshifts(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return arc_zonal_shift(
        "GET", "/autoshifts", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    list_managed_resources()
    list_managed_resources(params::Dict{String,<:Any})

Lists all the resources in your Amazon Web Services account in this Amazon Web Services
Region that are managed for zonal shifts in Amazon Application Recovery Controller, and
information about them. The information includes the zonal autoshift status for the
resource, as well as the Amazon Resource Name (ARN), the Availability Zones that each
resource is deployed in, and the resource name.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"maxResults"`: The number of objects that you want to return with this call.
- `"nextToken"`: Specifies that you want to receive the next page of results. Valid only if
  you received a `nextToken` response in the previous request. If you did, it indicates that
  more output is available. Set this parameter to the value provided by the previous call's
  `nextToken` response to request the next page of results.
"""
function list_managed_resources end

function list_managed_resources(; aws_config::AbstractAWSConfig=current_aws_config())
    return arc_zonal_shift(
        "GET", "/managedresources"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_managed_resources(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return arc_zonal_shift(
        "GET", "/managedresources", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    list_zonal_shifts()
    list_zonal_shifts(params::Dict{String,<:Any})

Lists all active and completed zonal shifts in Amazon Application Recovery Controller in
your Amazon Web Services account in this Amazon Web Services Region. `ListZonalShifts`
returns customer-initiated zonal shifts, as well as practice run zonal shifts that ARC
started on your behalf for zonal autoshift.

For more information about listing autoshifts, see ["&gt;ListAutoshifts](https://docs.aws.amazon.com/arc-zonal-shift/latest/api/API_ListAutoshifts.html).

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"maxResults"`: The number of objects that you want to return with this call.

- `"nextToken"`: Specifies that you want to receive the next page of results. Valid only if
  you received a `nextToken` response in the previous request. If you did, it indicates that
  more output is available. Set this parameter to the value provided by the previous call's
  `nextToken` response to request the next page of results.

- `"resourceIdentifier"`: The identifier for the resource that you want to list zonal shifts
  for. The identifier is the Amazon Resource Name (ARN) for the resource.

- `"status"`: A status for a zonal shift.

  The `Status` for a zonal shift can have one of the following values:

  - **ACTIVE**: The zonal shift has been started and is active.
  - **EXPIRED**: The zonal shift has expired (the expiry time was exceeded).
  - **CANCELED**: The zonal shift was canceled.
"""
function list_zonal_shifts end

function list_zonal_shifts(; aws_config::AbstractAWSConfig=current_aws_config())
    return arc_zonal_shift(
        "GET", "/zonalshifts"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_zonal_shifts(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return arc_zonal_shift(
        "GET", "/zonalshifts", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    start_practice_run(away_from, comment, resource_identifier)
    start_practice_run(away_from, comment, resource_identifier, params::Dict{String,<:Any})

Start an on-demand practice run zonal shift in Amazon Application Recovery Controller. With
zonal autoshift enabled, you can start an on-demand practice run to verify preparedness at
any time. Amazon Web Services also runs automated practice runs about weekly when you have
enabled zonal autoshift.

For more information, see [Considerations when you configure zonal autoshift](https://docs.aws.amazon.com/r53recovery/latest/dg/arc-zonal-autoshift.considerations.html)
in the Amazon Application Recovery Controller Developer Guide.

# Arguments

- `away_from`: The Availability Zone (for example, `use1-az1`) that traffic is shifted away
  from for the resource that you specify for the practice run.
- `comment`: The initial comment that you enter about the practice run. Be aware that this
  comment can be overwritten by Amazon Web Services if the automatic check for balanced
  capacity fails. For more information, see [Capacity checks for practice runs](https://docs.aws.amazon.com/r53recovery/latest/dg/arc-zonal-autoshift.how-it-works.capacity-check.html)
  in the Amazon Application Recovery Controller Developer Guide.
- `resource_identifier`: The identifier for the resource that you want to start a practice
  run zonal shift for. The identifier is the Amazon Resource Name (ARN) for the resource.
"""
function start_practice_run end

function start_practice_run(
    awayFrom,
    comment,
    resourceIdentifier;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return arc_zonal_shift(
        "POST",
        "/practiceruns",
        Dict{String,Any}(
            "awayFrom" => awayFrom,
            "comment" => comment,
            "resourceIdentifier" => resourceIdentifier,
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function start_practice_run(
    awayFrom,
    comment,
    resourceIdentifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return arc_zonal_shift(
        "POST",
        "/practiceruns",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "awayFrom" => awayFrom,
                    "comment" => comment,
                    "resourceIdentifier" => resourceIdentifier,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    start_zonal_shift(away_from, comment, expires_in, resource_identifier)
    start_zonal_shift(away_from, comment, expires_in, resource_identifier, params::Dict{String,<:Any})

You start a zonal shift to temporarily move load balancer traffic away from an Availability
Zone in an Amazon Web Services Region, to help your application recover immediately, for
example, from a developer's bad code deployment or from an Amazon Web Services
infrastructure failure in a single Availability Zone. You can start a zonal shift in ARC
only for managed resources in your Amazon Web Services account in an Amazon Web Services
Region. Resources are automatically registered with ARC by Amazon Web Services services.

Amazon Application Recovery Controller currently supports enabling the following resources
for zonal shift and zonal autoshift:

- [Amazon EC2 Auto Scaling groups](https://docs.aws.amazon.com/r53recovery/latest/dg/arc-zonal-shift.resource-types.ec2-auto-scaling-groups.html)
- [Amazon Elastic Kubernetes Service](https://docs.aws.amazon.com/r53recovery/latest/dg/arc-zonal-shift.resource-types.eks.html)
- [Application Load Balancer](https://docs.aws.amazon.com/r53recovery/latest/dg/arc-zonal-shift.resource-types.app-load-balancers.html)
- [Network Load Balancer](https://docs.aws.amazon.com/r53recovery/latest/dg/arc-zonal-shift.resource-types.network-load-balancers.html)

When you start a zonal shift, traffic for the resource is no longer routed to the
Availability Zone. The zonal shift is created immediately in ARC. However, it can take a
short time, typically up to a few minutes, for existing, in-progress connections in the
Availability Zone to complete.

For more information, see [Zonal shift](https://docs.aws.amazon.com/r53recovery/latest/dg/arc-zonal-shift.html)
in the Amazon Application Recovery Controller Developer Guide.

# Arguments

- `away_from`: The Availability Zone (for example, `use1-az1`) that traffic is moved away
  from for a resource when you start a zonal shift. Until the zonal shift expires or you
  cancel it, traffic for the resource is instead moved to other Availability Zones in the
  Amazon Web Services Region.

- `comment`: A comment that you enter about the zonal shift. Only the latest comment is
  retained; no comment history is maintained. A new comment overwrites any existing comment
  string.

- `expires_in`: The length of time that you want a zonal shift to be active, which ARC
  converts to an expiry time (expiration time). Zonal shifts are temporary. You can set a
  zonal shift to be active initially for up to three days (72 hours).

  If you want to still keep traffic away from an Availability Zone, you can update the zonal
  shift and set a new expiration. You can also cancel a zonal shift, before it expires, for
  example, if you're ready to restore traffic to the Availability Zone.

  To set a length of time for a zonal shift to be active, specify a whole number, and then
  one of the following, with no space:

  - **A lowercase letter m:** To specify that the value is in minutes.
  - **A lowercase letter h:** To specify that the value is in hours.

  For example: `20h` means the zonal shift expires in 20 hours. `120m` means the zonal shift
  expires in 120 minutes (2 hours).

- `resource_identifier`: The identifier for the resource that Amazon Web Services shifts
  traffic for. The identifier is the Amazon Resource Name (ARN) for the resource.

  Amazon Application Recovery Controller currently supports enabling the following resources
  for zonal shift and zonal autoshift:

  - [Amazon EC2 Auto Scaling groups](https://docs.aws.amazon.com/r53recovery/latest/dg/arc-zonal-shift.resource-types.ec2-auto-scaling-groups.html)
  - [Amazon Elastic Kubernetes Service](https://docs.aws.amazon.com/r53recovery/latest/dg/arc-zonal-shift.resource-types.eks.html)
  - [Application Load Balancer](https://docs.aws.amazon.com/r53recovery/latest/dg/arc-zonal-shift.resource-types.app-load-balancers.html)
  - [Network Load Balancer](https://docs.aws.amazon.com/r53recovery/latest/dg/arc-zonal-shift.resource-types.network-load-balancers.html)
"""
function start_zonal_shift end

function start_zonal_shift(
    awayFrom,
    comment,
    expiresIn,
    resourceIdentifier;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return arc_zonal_shift(
        "POST",
        "/zonalshifts",
        Dict{String,Any}(
            "awayFrom" => awayFrom,
            "comment" => comment,
            "expiresIn" => expiresIn,
            "resourceIdentifier" => resourceIdentifier,
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function start_zonal_shift(
    awayFrom,
    comment,
    expiresIn,
    resourceIdentifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return arc_zonal_shift(
        "POST",
        "/zonalshifts",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "awayFrom" => awayFrom,
                    "comment" => comment,
                    "expiresIn" => expiresIn,
                    "resourceIdentifier" => resourceIdentifier,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_autoshift_observer_notification_status(status)
    update_autoshift_observer_notification_status(status, params::Dict{String,<:Any})

Update the status of autoshift observer notification. Autoshift observer notification
enables you to be notified, through Amazon EventBridge, when there is an autoshift event for
zonal autoshift.

If the status is `ENABLED`, ARC includes all autoshift events when you use the EventBridge
pattern `Autoshift In Progress`. When the status is `DISABLED`, ARC includes only autoshift
events for autoshifts when one or more of your resources is included in the autoshift.

For more information, see [Notifications for practice runs and autoshifts](https://docs.aws.amazon.com/r53recovery/latest/dg/arc-zonal-autoshift.how-it-works.html#ZAShiftNotification)
in the Amazon Application Recovery Controller Developer Guide.

# Arguments

- `status`: The status to set for autoshift observer notification. If the status is
  `ENABLED`, ARC includes all autoshift events when you use the Amazon EventBridge pattern
  `Autoshift In Progress`. When the status is `DISABLED`, ARC includes only autoshift events
  for autoshifts when one or more of your resources is included in the autoshift.
"""
function update_autoshift_observer_notification_status end

function update_autoshift_observer_notification_status(
    status; aws_config::AbstractAWSConfig=current_aws_config()
)
    return arc_zonal_shift(
        "PUT",
        "/autoshift-observer-notification",
        Dict{String,Any}("status" => status);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_autoshift_observer_notification_status(
    status, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return arc_zonal_shift(
        "PUT",
        "/autoshift-observer-notification",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("status" => status), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_practice_run_configuration(resource_identifier)
    update_practice_run_configuration(resource_identifier, params::Dict{String,<:Any})

Update a practice run configuration to change one or more of the following: add, change, or
remove the blocking alarm; change the outcome alarm; or add, change, or remove blocking
dates or time windows.

# Arguments

- `resource_identifier`: The identifier for the resource that you want to update the
  practice run configuration for. The identifier is the Amazon Resource Name (ARN) for the
  resource.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"allowedWindows"`: Add, change, or remove windows of days and times for when you can,
  optionally, allow ARC to start a practice run for a resource.

  The format for allowed windows is: DAY:HH:SS-DAY:HH:SS. Keep in mind, when you specify
  dates, that dates and times for practice runs are in UTC. Also, be aware of potential time
  adjustments that might be required for daylight saving time differences. Separate multiple
  allowed windows with spaces.

  For example, say you want to allow practice runs only on Wednesdays and Fridays from noon
  to 5 p.m. For this scenario, you could set the following recurring days and times as
  allowed windows, for example: `Wed-12:00-Wed:17:00 Fri-12:00-Fri:17:00`.

  !!! important
      The `allowedWindows` have to start and end on the same day. Windows that span multiple
      days aren't supported.

- `"blockedDates"`: Add, change, or remove blocked dates for a practice run in zonal
  autoshift.

  Optionally, you can block practice runs for specific calendar dates. The format for
  blocked dates is: YYYY-MM-DD. Keep in mind, when you specify dates, that dates and times
  for practice runs are in UTC. Separate multiple blocked dates with spaces.

  For example, if you have an application update scheduled to launch on May 1, 2024, and you
  don't want practice runs to shift traffic away at that time, you could set a blocked date
  for `2024-05-01`.

- `"blockedWindows"`: Add, change, or remove windows of days and times for when you can,
  optionally, block ARC from starting a practice run for a resource.

  The format for blocked windows is: DAY:HH:SS-DAY:HH:SS. Keep in mind, when you specify
  dates, that dates and times for practice runs are in UTC. Also, be aware of potential time
  adjustments that might be required for daylight saving time differences. Separate multiple
  blocked windows with spaces.

  For example, say you run business report summaries three days a week. For this scenario,
  you might set the following recurring days and times as blocked windows, for example:
  `MON-20:30-21:30 WED-20:30-21:30 FRI-20:30-21:30`.

- `"blockingAlarms"`: Add, change, or remove the Amazon CloudWatch alarms that you
  optionally specify as the blocking alarms for practice runs.

- `"outcomeAlarms"`: Specify one or more Amazon CloudWatch alarms as the outcome alarms for
  practice runs.
"""
function update_practice_run_configuration end

function update_practice_run_configuration(
    resourceIdentifier; aws_config::AbstractAWSConfig=current_aws_config()
)
    return arc_zonal_shift(
        "PATCH",
        "/configuration/$(resourceIdentifier)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_practice_run_configuration(
    resourceIdentifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return arc_zonal_shift(
        "PATCH",
        "/configuration/$(resourceIdentifier)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_zonal_autoshift_configuration(resource_identifier, zonal_autoshift_status)
    update_zonal_autoshift_configuration(resource_identifier, zonal_autoshift_status, params::Dict{String,<:Any})

The zonal autoshift configuration for a resource includes the practice run configuration and
the status for running autoshifts, zonal autoshift status. When a resource has a practice
run configuration, ARC starts weekly zonal shifts for the resource, to shift traffic away
from an Availability Zone. Weekly practice runs help you to make sure that your application
can continue to operate normally with the loss of one Availability Zone.

You can update the zonal autoshift status to enable or disable zonal autoshift. When zonal
autoshift is `ENABLED`, you authorize Amazon Web Services to shift away resource traffic for
an application from an Availability Zone during events, on your behalf, to help reduce time
to recovery. Traffic is also shifted away for the required weekly practice runs.

# Arguments

- `resource_identifier`: The identifier for the resource that you want to update the zonal
  autoshift configuration for. The identifier is the Amazon Resource Name (ARN) for the
  resource.
- `zonal_autoshift_status`: The zonal autoshift status for the resource that you want to
  update the zonal autoshift configuration for. Choose `ENABLED` to authorize Amazon Web
  Services to shift away resource traffic for an application from an Availability Zone
  during events, on your behalf, to help reduce time to recovery.
"""
function update_zonal_autoshift_configuration end

function update_zonal_autoshift_configuration(
    resourceIdentifier,
    zonalAutoshiftStatus;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return arc_zonal_shift(
        "PUT",
        "/managedresources/$(resourceIdentifier)",
        Dict{String,Any}("zonalAutoshiftStatus" => zonalAutoshiftStatus);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_zonal_autoshift_configuration(
    resourceIdentifier,
    zonalAutoshiftStatus,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return arc_zonal_shift(
        "PUT",
        "/managedresources/$(resourceIdentifier)",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("zonalAutoshiftStatus" => zonalAutoshiftStatus),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_zonal_shift(zonal_shift_id)
    update_zonal_shift(zonal_shift_id, params::Dict{String,<:Any})

Update an active zonal shift in Amazon Application Recovery Controller in your Amazon Web
Services account. You can update a zonal shift to set a new expiration, or edit or replace
the comment for the zonal shift.

# Arguments

- `zonal_shift_id`: The identifier of a zonal shift.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"comment"`: A comment that you enter about the zonal shift. Only the latest comment is
  retained; no comment history is maintained. A new comment overwrites any existing comment
  string.

- `"expiresIn"`: The length of time that you want a zonal shift to be active, which ARC
  converts to an expiry time (expiration time). Zonal shifts are temporary. You can set a
  zonal shift to be active initially for up to three days (72 hours).

  If you want to still keep traffic away from an Availability Zone, you can update the zonal
  shift and set a new expiration. You can also cancel a zonal shift, before it expires, for
  example, if you're ready to restore traffic to the Availability Zone.

  To set a length of time for a zonal shift to be active, specify a whole number, and then
  one of the following, with no space:

  - **A lowercase letter m:** To specify that the value is in minutes.
  - **A lowercase letter h:** To specify that the value is in hours.

  For example: `20h` means the zonal shift expires in 20 hours. `120m` means the zonal shift
  expires in 120 minutes (2 hours).
"""
function update_zonal_shift end

function update_zonal_shift(
    zonalShiftId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return arc_zonal_shift(
        "PATCH", "/zonalshifts/$(zonalShiftId)"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function update_zonal_shift(
    zonalShiftId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return arc_zonal_shift(
        "PATCH",
        "/zonalshifts/$(zonalShiftId)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end
