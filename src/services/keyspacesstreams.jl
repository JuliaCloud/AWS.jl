# This file is auto-generated by AWS.jl
# Note: `SERVICE_FEATURE_SET` is embedded by `@service`
using AWS: AbstractAWSConfig, current_aws_config, _merge
using AWS.AWSServices: keyspacesstreams
using AWS.UUIDs: uuid4

"""
    get_records(shard_iterator)
    get_records(shard_iterator, params::Dict{String,<:Any})

Retrieves data records from a specified shard in an Amazon Keyspaces data stream. This
operation returns a collection of data records from the shard, including the primary key
columns and information about modifications made to the captured table data. Each record
represents a single data modification in the Amazon Keyspaces table and includes metadata
about when the change occurred.

# Arguments

- `shard_iterator`: The unique identifier of the shard iterator. A shard iterator specifies
  the position in the shard from which you want to start reading data records sequentially.
  You obtain this value by calling the `GetShardIterator` operation. Each shard iterator is
  valid for 15 minutes after creation.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"maxResults"`: The maximum number of records to return in a single `GetRecords` request.
  The default value is 100. You can specify a limit between 1 and 1000, but the actual
  number returned might be less than the specified maximum if the size of the data for the
  returned records exceeds the internal size limit.
"""
function get_records end

function get_records(shardIterator; aws_config::AbstractAWSConfig=current_aws_config())
    return keyspacesstreams(
        "GetRecords",
        Dict{String,Any}("shardIterator" => shardIterator);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_records(
    shardIterator,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return keyspacesstreams(
        "GetRecords",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("shardIterator" => shardIterator), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_shard_iterator(shard_id, shard_iterator_type, stream_arn)
    get_shard_iterator(shard_id, shard_iterator_type, stream_arn, params::Dict{String,<:Any})

Returns a shard iterator that serves as a bookmark for reading data from a specific position
in an Amazon Keyspaces data stream's shard. The shard iterator specifies the shard position
from which to start reading data records sequentially. You can specify whether to begin
reading at the latest record, the oldest record, or at a particular sequence number within
the shard.

# Arguments

- `shard_id`: The identifier of the shard within the stream. The shard ID uniquely
  identifies a subset of the stream's data records that you want to access.

- `shard_iterator_type`: Determines how the shard iterator is positioned. Must be one of the
  following:

  - `TRIM_HORIZON` - Start reading at the last untrimmed record in the shard, which is the
    oldest data record in the shard.
  - `AT_SEQUENCE_NUMBER` - Start reading exactly from the specified sequence number.
  - `AFTER_SEQUENCE_NUMBER` - Start reading right after the specified sequence number.
  - `LATEST` - Start reading just after the most recent record in the shard, so that you
    always read the most recent data.

- `stream_arn`: The Amazon Resource Name (ARN) of the stream for which to get the shard
  iterator. The ARN uniquely identifies the stream within Amazon Keyspaces.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"sequenceNumber"`: The sequence number of the data record in the shard from which to
  start reading. Required if `ShardIteratorType` is `AT_SEQUENCE_NUMBER` or
  `AFTER_SEQUENCE_NUMBER`. This parameter is ignored for other iterator types.
"""
function get_shard_iterator end

function get_shard_iterator(
    shardId,
    shardIteratorType,
    streamArn;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return keyspacesstreams(
        "GetShardIterator",
        Dict{String,Any}(
            "shardId" => shardId,
            "shardIteratorType" => shardIteratorType,
            "streamArn" => streamArn,
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_shard_iterator(
    shardId,
    shardIteratorType,
    streamArn,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return keyspacesstreams(
        "GetShardIterator",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "shardId" => shardId,
                    "shardIteratorType" => shardIteratorType,
                    "streamArn" => streamArn,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_stream(stream_arn)
    get_stream(stream_arn, params::Dict{String,<:Any})

Returns detailed information about a specific data capture stream for an Amazon Keyspaces
table. The information includes the stream's Amazon Resource Name (ARN), creation time,
current status, retention period, shard composition, and associated table details. This
operation helps you monitor and manage the configuration of your Amazon Keyspaces data
streams.

# Arguments

- `stream_arn`: The Amazon Resource Name (ARN) of the stream for which detailed information
  is requested. This uniquely identifies the specific stream you want to get information
  about.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"maxResults"`: The maximum number of shard objects to return in a single `GetStream`
  request. The default value is 100. The minimum value is 1 and the maximum value is 100.
- `"nextToken"`: An optional pagination token provided by a previous `GetStream` operation.
  If this parameter is specified, the response includes only records beyond the token, up to
  the value specified by `MaxResults`.
- `"shardFilter"`: Optional filter criteria to apply when retrieving shards. You can filter
  shards based on their parent `shardID` to get a list of children shards to narrow down the
  results returned by the `GetStream` operation.
"""
function get_stream end

function get_stream(streamArn; aws_config::AbstractAWSConfig=current_aws_config())
    return keyspacesstreams(
        "GetStream",
        Dict{String,Any}("streamArn" => streamArn);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_stream(
    streamArn,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return keyspacesstreams(
        "GetStream",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("streamArn" => streamArn), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_streams()
    list_streams(params::Dict{String,<:Any})

Returns a list of all data capture streams associated with your Amazon Keyspaces account or
for a specific keyspace or table. The response includes information such as stream ARNs,
table associations, creation timestamps, and current status. This operation helps you
discover and manage all active data streams in your Amazon Keyspaces environment.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"keyspaceName"`: The name of the keyspace for which to list streams. If specified, only
  streams associated with tables in this keyspace are returned. If omitted, streams from all
  keyspaces are included in the results.
- `"maxResults"`: The maximum number of streams to return in a single `ListStreams` request.
  The default value is 100. The minimum value is 1 and the maximum value is 100.
- `"nextToken"`: An optional pagination token provided by a previous `ListStreams`
  operation. If this parameter is specified, the response includes only records beyond the
  token, up to the value specified by `maxResults`.
- `"tableName"`: The name of the table for which to list streams. Must be used together with
  `keyspaceName`. If specified, only streams associated with this specific table are
  returned.
"""
function list_streams end

function list_streams(; aws_config::AbstractAWSConfig=current_aws_config())
    return keyspacesstreams("ListStreams"; aws_config, feature_set=SERVICE_FEATURE_SET)
end

function list_streams(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return keyspacesstreams(
        "ListStreams", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end
