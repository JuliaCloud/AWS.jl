# This file is auto-generated by AWS.jl
# Note: `SERVICE_FEATURE_SET` is embedded by `@service`
using AWS: AbstractAWSConfig, current_aws_config, _merge
using AWS.AWSServices: securityagent
using AWS.UUIDs: uuid4

"""
    add_artifact(agent_space_id, artifact_content, artifact_type, file_name)
    add_artifact(agent_space_id, artifact_content, artifact_type, file_name, params::Dict{String,<:Any})

Adds an Artifact for the given agent space

# Arguments

- `agent_space_id`: Unique identifier of the agent space
- `artifact_content`: Binary content of the artifact
- `artifact_type`: Type of the artifact file
- `file_name`: Name of the artifact file
"""
function add_artifact end

function add_artifact(
    agentSpaceId,
    artifactContent,
    artifactType,
    fileName;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/AddArtifact",
        Dict{String,Any}(
            "agentSpaceId" => agentSpaceId,
            "artifactContent" => artifactContent,
            "artifactType" => artifactType,
            "fileName" => fileName,
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function add_artifact(
    agentSpaceId,
    artifactContent,
    artifactType,
    fileName,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/AddArtifact",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "agentSpaceId" => agentSpaceId,
                    "artifactContent" => artifactContent,
                    "artifactType" => artifactType,
                    "fileName" => fileName,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    batch_delete_pentests(agent_space_id, pentest_ids)
    batch_delete_pentests(agent_space_id, pentest_ids, params::Dict{String,<:Any})

Deletes multiple pentests in a single request

# Arguments

- `agent_space_id`: ID of the agent space where the pentests exist
- `pentest_ids`: List of pentest IDs to delete
"""
function batch_delete_pentests end

function batch_delete_pentests(
    agentSpaceId, pentestIds; aws_config::AbstractAWSConfig=current_aws_config()
)
    return securityagent(
        "POST",
        "/BatchDeletePentests",
        Dict{String,Any}("agentSpaceId" => agentSpaceId, "pentestIds" => pentestIds);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function batch_delete_pentests(
    agentSpaceId,
    pentestIds,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/BatchDeletePentests",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "agentSpaceId" => agentSpaceId, "pentestIds" => pentestIds
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    batch_get_agent_spaces(agent_space_ids)
    batch_get_agent_spaces(agent_space_ids, params::Dict{String,<:Any})

Retrieves multiple agent spaces in a single request

# Arguments

- `agent_space_ids`: List of agent space IDs to retrieve
"""
function batch_get_agent_spaces end

function batch_get_agent_spaces(
    agentSpaceIds; aws_config::AbstractAWSConfig=current_aws_config()
)
    return securityagent(
        "POST",
        "/BatchGetAgentSpaces",
        Dict{String,Any}("agentSpaceIds" => agentSpaceIds);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function batch_get_agent_spaces(
    agentSpaceIds,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/BatchGetAgentSpaces",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("agentSpaceIds" => agentSpaceIds), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    batch_get_artifact_metadata(agent_space_id, artifact_ids)
    batch_get_artifact_metadata(agent_space_id, artifact_ids, params::Dict{String,<:Any})

Retrieve the list of artifact metadata for the given agent space

# Arguments

- `agent_space_id`: Unique identifier of the agent space
- `artifact_ids`: List of artifact identifiers
"""
function batch_get_artifact_metadata end

function batch_get_artifact_metadata(
    agentSpaceId, artifactIds; aws_config::AbstractAWSConfig=current_aws_config()
)
    return securityagent(
        "POST",
        "/BatchGetArtifactMetadata",
        Dict{String,Any}("agentSpaceId" => agentSpaceId, "artifactIds" => artifactIds);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function batch_get_artifact_metadata(
    agentSpaceId,
    artifactIds,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/BatchGetArtifactMetadata",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "agentSpaceId" => agentSpaceId, "artifactIds" => artifactIds
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    batch_get_findings(agent_space_id, finding_ids)
    batch_get_findings(agent_space_id, finding_ids, params::Dict{String,<:Any})

Retrieves multiple findings in a single request

# Arguments

- `agent_space_id`: ID of the agent space where the findings exist
- `finding_ids`: List of finding IDs to retrieve
"""
function batch_get_findings end

function batch_get_findings(
    agentSpaceId, findingIds; aws_config::AbstractAWSConfig=current_aws_config()
)
    return securityagent(
        "POST",
        "/BatchGetFindings",
        Dict{String,Any}("agentSpaceId" => agentSpaceId, "findingIds" => findingIds);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function batch_get_findings(
    agentSpaceId,
    findingIds,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/BatchGetFindings",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "agentSpaceId" => agentSpaceId, "findingIds" => findingIds
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    batch_get_pentest_job_tasks(agent_space_id, task_ids)
    batch_get_pentest_job_tasks(agent_space_id, task_ids, params::Dict{String,<:Any})

Retrieves multiple tasks for a pentest job in a single request

# Arguments

- `agent_space_id`: ID of the agent space where the pentest exists
- `task_ids`: List of task IDs to retrieve
"""
function batch_get_pentest_job_tasks end

function batch_get_pentest_job_tasks(
    agentSpaceId, taskIds; aws_config::AbstractAWSConfig=current_aws_config()
)
    return securityagent(
        "POST",
        "/BatchGetPentestJobTasks",
        Dict{String,Any}("agentSpaceId" => agentSpaceId, "taskIds" => taskIds);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function batch_get_pentest_job_tasks(
    agentSpaceId,
    taskIds,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/BatchGetPentestJobTasks",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("agentSpaceId" => agentSpaceId, "taskIds" => taskIds),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    batch_get_pentest_jobs(agent_space_id, pentest_job_ids)
    batch_get_pentest_jobs(agent_space_id, pentest_job_ids, params::Dict{String,<:Any})

Retrieves multiple pentest jobs in a single request

# Arguments

- `agent_space_id`: ID of the agent space where the pentest exists
- `pentest_job_ids`: List of pentest job IDs to retrieve
"""
function batch_get_pentest_jobs end

function batch_get_pentest_jobs(
    agentSpaceId, pentestJobIds; aws_config::AbstractAWSConfig=current_aws_config()
)
    return securityagent(
        "POST",
        "/BatchGetPentestJobs",
        Dict{String,Any}("agentSpaceId" => agentSpaceId, "pentestJobIds" => pentestJobIds);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function batch_get_pentest_jobs(
    agentSpaceId,
    pentestJobIds,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/BatchGetPentestJobs",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "agentSpaceId" => agentSpaceId, "pentestJobIds" => pentestJobIds
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    batch_get_pentests(agent_space_id, pentest_ids)
    batch_get_pentests(agent_space_id, pentest_ids, params::Dict{String,<:Any})

Retrieves multiple pentests in a single request

# Arguments

- `agent_space_id`: ID of the agent space where the pentest exists
- `pentest_ids`: List of pentest IDs to retrieve
"""
function batch_get_pentests end

function batch_get_pentests(
    agentSpaceId, pentestIds; aws_config::AbstractAWSConfig=current_aws_config()
)
    return securityagent(
        "POST",
        "/BatchGetPentests",
        Dict{String,Any}("agentSpaceId" => agentSpaceId, "pentestIds" => pentestIds);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function batch_get_pentests(
    agentSpaceId,
    pentestIds,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/BatchGetPentests",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "agentSpaceId" => agentSpaceId, "pentestIds" => pentestIds
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    batch_get_target_domains(target_domain_ids)
    batch_get_target_domains(target_domain_ids, params::Dict{String,<:Any})

Retrieves multiple target domains in a single request

# Arguments

- `target_domain_ids`: List of target domain IDs to retrieve
"""
function batch_get_target_domains end

function batch_get_target_domains(
    targetDomainIds; aws_config::AbstractAWSConfig=current_aws_config()
)
    return securityagent(
        "POST",
        "/BatchGetTargetDomains",
        Dict{String,Any}("targetDomainIds" => targetDomainIds);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function batch_get_target_domains(
    targetDomainIds,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/BatchGetTargetDomains",
        Dict{String,Any}(
            mergewith(
                _merge, Dict{String,Any}("targetDomainIds" => targetDomainIds), params
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_agent_space(name)
    create_agent_space(name, params::Dict{String,<:Any})

Creates an agent space record

# Arguments

- `name`: Name of the agent space

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"awsResources"`: AWS resource configurations associated with the agent space
- `"codeReviewSettings"`: Configuration for code review analysis, including controls
  scanning and general purpose scanning settings
- `"description"`: Description of the agent space
- `"kmsKeyId"`: Identifier of the KMS key used to encrypt data. Can be a key ID, key ARN,
  alias name, or alias ARN. If not specified, an AWS managed key is used.
- `"tags"`: Tags to associate with the agent space
- `"targetDomainIds"`: Target domain IDs to associate with the agent space
"""
function create_agent_space end

function create_agent_space(name; aws_config::AbstractAWSConfig=current_aws_config())
    return securityagent(
        "POST",
        "/CreateAgentSpace",
        Dict{String,Any}("name" => name);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_agent_space(
    name, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return securityagent(
        "POST",
        "/CreateAgentSpace",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("name" => name), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_application()
    create_application(params::Dict{String,<:Any})

Creates a new application

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"defaultKmsKeyId"`: Default KMS key identifier used to encrypt application data
- `"idcInstanceArn"`: ARN of the IAM Identity Center instance used for user authentication.
  Optional for non-IdC applications
- `"roleArn"`: ARN of the IAM role that the application uses to access AWS resources on your
  behalf
- `"tags"`: Tags to associate with the application
"""
function create_application end

function create_application(; aws_config::AbstractAWSConfig=current_aws_config())
    return securityagent(
        "POST", "/CreateApplication"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function create_application(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return securityagent(
        "POST", "/CreateApplication", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    create_integration(input, integration_display_name, provider)
    create_integration(input, integration_display_name, provider, params::Dict{String,<:Any})

Creates the Integration of the Security Agent App with an external Provider

# Arguments

- `input`: Provider-specific input parameters
- `integration_display_name`: Display name for the integration
- `provider`: Provider to integrate with

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"kmsKeyId"`: KMS key ID for encrypting integration details
- `"tags"`: Tags to associate with the integration
"""
function create_integration end

function create_integration(
    input,
    integrationDisplayName,
    provider;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/CreateIntegration",
        Dict{String,Any}(
            "input" => input,
            "integrationDisplayName" => integrationDisplayName,
            "provider" => provider,
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_integration(
    input,
    integrationDisplayName,
    provider,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/CreateIntegration",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "input" => input,
                    "integrationDisplayName" => integrationDisplayName,
                    "provider" => provider,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_membership(agent_space_id, application_id, member_type, membership_id)
    create_membership(agent_space_id, application_id, member_type, membership_id, params::Dict{String,<:Any})

Adds a single member to an agent space with specified role

# Arguments

- `agent_space_id`: Agent space identifier
- `application_id`: Application identifier
- `member_type`: Type of member (USER or AGENT_SPACE)
- `membership_id`: Member identifier (userId or agentSpaceId)

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"config"`: Membership details (user or agent specific)
"""
function create_membership end

function create_membership(
    agentSpaceId,
    applicationId,
    memberType,
    membershipId;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/CreateMembership",
        Dict{String,Any}(
            "agentSpaceId" => agentSpaceId,
            "applicationId" => applicationId,
            "memberType" => memberType,
            "membershipId" => membershipId,
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_membership(
    agentSpaceId,
    applicationId,
    memberType,
    membershipId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/CreateMembership",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "agentSpaceId" => agentSpaceId,
                    "applicationId" => applicationId,
                    "memberType" => memberType,
                    "membershipId" => membershipId,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_pentest(agent_space_id, title)
    create_pentest(agent_space_id, title, params::Dict{String,<:Any})

Creates a new pentest configuration

# Arguments

- `agent_space_id`: ID of the agent space where the pentest should be created
- `title`: Title of the pentest

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"assets"`: Assets to be tested during the pentest
- `"codeRemediationStrategy"`: Strategy for code remediation on findings
- `"excludeRiskTypes"`: A list of risk types excluded from the pentest execution
- `"logConfig"`: CloudWatch log group and stream prefix where pentest execution logs are
  stored
- `"networkTrafficConfig"`: Configuration for network traffic filtering
- `"serviceRole"`: Service role ARN for accessing customer resources
- `"vpcConfig"`: VPC configuration that the Security Agent accesses
"""
function create_pentest end

function create_pentest(
    agentSpaceId, title; aws_config::AbstractAWSConfig=current_aws_config()
)
    return securityagent(
        "POST",
        "/CreatePentest",
        Dict{String,Any}("agentSpaceId" => agentSpaceId, "title" => title);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_pentest(
    agentSpaceId,
    title,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/CreatePentest",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("agentSpaceId" => agentSpaceId, "title" => title),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_target_domain(target_domain_name, verification_method)
    create_target_domain(target_domain_name, verification_method, params::Dict{String,<:Any})

Creates a target domain record

# Arguments

- `target_domain_name`: Domain name of the target domain
- `verification_method`: Verification method for the target domain

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"tags"`: Tags to associate with the target domain
"""
function create_target_domain end

function create_target_domain(
    targetDomainName, verificationMethod; aws_config::AbstractAWSConfig=current_aws_config()
)
    return securityagent(
        "POST",
        "/CreateTargetDomain",
        Dict{String,Any}(
            "targetDomainName" => targetDomainName,
            "verificationMethod" => verificationMethod,
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_target_domain(
    targetDomainName,
    verificationMethod,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/CreateTargetDomain",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "targetDomainName" => targetDomainName,
                    "verificationMethod" => verificationMethod,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_agent_space(agent_space_id)
    delete_agent_space(agent_space_id, params::Dict{String,<:Any})

Deletes an agent space record

# Arguments

- `agent_space_id`: Unique identifier of the agent space to delete
"""
function delete_agent_space end

function delete_agent_space(
    agentSpaceId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return securityagent(
        "POST",
        "/DeleteAgentSpace",
        Dict{String,Any}("agentSpaceId" => agentSpaceId);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_agent_space(
    agentSpaceId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/DeleteAgentSpace",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("agentSpaceId" => agentSpaceId), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_application(application_id)
    delete_application(application_id, params::Dict{String,<:Any})

Deletes an application

# Arguments

- `application_id`: Application ID
"""
function delete_application end

function delete_application(
    applicationId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return securityagent(
        "POST",
        "/DeleteApplication",
        Dict{String,Any}("applicationId" => applicationId);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_application(
    applicationId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/DeleteApplication",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("applicationId" => applicationId), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_artifact(agent_space_id, artifact_id)
    delete_artifact(agent_space_id, artifact_id, params::Dict{String,<:Any})

Delete an Artifact from the given agent space

# Arguments

- `agent_space_id`: Unique identifier of the agent space
- `artifact_id`: Unique identifier of the artifact
"""
function delete_artifact end

function delete_artifact(
    agentSpaceId, artifactId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return securityagent(
        "POST",
        "/DeleteArtifact",
        Dict{String,Any}("agentSpaceId" => agentSpaceId, "artifactId" => artifactId);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_artifact(
    agentSpaceId,
    artifactId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/DeleteArtifact",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "agentSpaceId" => agentSpaceId, "artifactId" => artifactId
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_integration(integration_id)
    delete_integration(integration_id, params::Dict{String,<:Any})

Deletes the Integration of the Security Agent App with an external Provider

# Arguments

- `integration_id`: Unique identifier of the integration
"""
function delete_integration end

function delete_integration(
    integrationId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return securityagent(
        "POST",
        "/DeleteIntegration",
        Dict{String,Any}("integrationId" => integrationId);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_integration(
    integrationId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/DeleteIntegration",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("integrationId" => integrationId), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_membership(agent_space_id, application_id, membership_id)
    delete_membership(agent_space_id, application_id, membership_id, params::Dict{String,<:Any})

Removes a single member associated to an agent space

# Arguments

- `agent_space_id`: Agent space identifier
- `application_id`: Application identifier
- `membership_id`: Member identifier (userId or agentSpaceId)

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"memberType"`: Type of member (USER or AGENT_SPACE)
"""
function delete_membership end

function delete_membership(
    agentSpaceId,
    applicationId,
    membershipId;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/DeleteMembership",
        Dict{String,Any}(
            "agentSpaceId" => agentSpaceId,
            "applicationId" => applicationId,
            "membershipId" => membershipId,
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_membership(
    agentSpaceId,
    applicationId,
    membershipId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/DeleteMembership",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "agentSpaceId" => agentSpaceId,
                    "applicationId" => applicationId,
                    "membershipId" => membershipId,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_target_domain(target_domain_id)
    delete_target_domain(target_domain_id, params::Dict{String,<:Any})

Deletes a target domain record

# Arguments

- `target_domain_id`: Unique identifier of the target domain to delete
"""
function delete_target_domain end

function delete_target_domain(
    targetDomainId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return securityagent(
        "POST",
        "/DeleteTargetDomain",
        Dict{String,Any}("targetDomainId" => targetDomainId);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_target_domain(
    targetDomainId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/DeleteTargetDomain",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("targetDomainId" => targetDomainId), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_application(application_id)
    get_application(application_id, params::Dict{String,<:Any})

Retrieves application details by application ID

# Arguments

- `application_id`: Application ID
"""
function get_application end

function get_application(applicationId; aws_config::AbstractAWSConfig=current_aws_config())
    return securityagent(
        "POST",
        "/GetApplication",
        Dict{String,Any}("applicationId" => applicationId);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_application(
    applicationId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/GetApplication",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("applicationId" => applicationId), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_artifact(agent_space_id, artifact_id)
    get_artifact(agent_space_id, artifact_id, params::Dict{String,<:Any})

Retrieve an Artifact for the given agent space

# Arguments

- `agent_space_id`: Unique identifier of the agent space
- `artifact_id`: Unique identifier of the artifact
"""
function get_artifact end

function get_artifact(
    agentSpaceId, artifactId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return securityagent(
        "POST",
        "/GetArtifact",
        Dict{String,Any}("agentSpaceId" => agentSpaceId, "artifactId" => artifactId);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_artifact(
    agentSpaceId,
    artifactId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/GetArtifact",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "agentSpaceId" => agentSpaceId, "artifactId" => artifactId
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_integration(integration_id)
    get_integration(integration_id, params::Dict{String,<:Any})

Gets Integration metadata from the provided id

# Arguments

- `integration_id`: Unique identifier of the integration
"""
function get_integration end

function get_integration(integrationId; aws_config::AbstractAWSConfig=current_aws_config())
    return securityagent(
        "POST",
        "/GetIntegration",
        Dict{String,Any}("integrationId" => integrationId);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_integration(
    integrationId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/GetIntegration",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("integrationId" => integrationId), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    initiate_provider_registration(provider)
    initiate_provider_registration(provider, params::Dict{String,<:Any})

Initiates the registration of Security Agent App for an external Provider

# Arguments

- `provider`: Provider to register with
"""
function initiate_provider_registration end

function initiate_provider_registration(
    provider; aws_config::AbstractAWSConfig=current_aws_config()
)
    return securityagent(
        "POST",
        "/oauth2/provider/register",
        Dict{String,Any}("provider" => provider);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function initiate_provider_registration(
    provider,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/oauth2/provider/register",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("provider" => provider), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_agent_spaces()
    list_agent_spaces(params::Dict{String,<:Any})

Lists agent spaces

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"maxResults"`: Maximum number of agent spaces to return
- `"nextToken"`: Token for pagination
"""
function list_agent_spaces end

function list_agent_spaces(; aws_config::AbstractAWSConfig=current_aws_config())
    return securityagent(
        "POST", "/ListAgentSpaces"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_agent_spaces(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return securityagent(
        "POST", "/ListAgentSpaces", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    list_applications()
    list_applications(params::Dict{String,<:Any})

Lists all applications in the account

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"maxResults"`: Maximum number of results to return
- `"nextToken"`: Token for pagination
"""
function list_applications end

function list_applications(; aws_config::AbstractAWSConfig=current_aws_config())
    return securityagent(
        "POST", "/ListApplications"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_applications(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return securityagent(
        "POST", "/ListApplications", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    list_artifacts(agent_space_id)
    list_artifacts(agent_space_id, params::Dict{String,<:Any})

Lists the artifacts for the associated agent space

# Arguments

- `agent_space_id`: Unique identifier of the agent space

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"maxResults"`: Maximum number of results to return
- `"nextToken"`: Token for pagination
"""
function list_artifacts end

function list_artifacts(agentSpaceId; aws_config::AbstractAWSConfig=current_aws_config())
    return securityagent(
        "POST",
        "/ListArtifacts",
        Dict{String,Any}("agentSpaceId" => agentSpaceId);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_artifacts(
    agentSpaceId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/ListArtifacts",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("agentSpaceId" => agentSpaceId), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_discovered_endpoints(agent_space_id, pentest_job_id)
    list_discovered_endpoints(agent_space_id, pentest_job_id, params::Dict{String,<:Any})

Lists discovered endpoints associated with a pentest job with optional URI prefix filtering

# Arguments

- `agent_space_id`: ID of the agent space where the pentest job exists
- `pentest_job_id`: Identifier of the pentest job for which to retrieve discovered endpoints

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"maxResults"`: Maximum number of discovered endpoints to return in a single request
  (default: 50)
- `"nextToken"`: Token for pagination
- `"prefix"`: Optional URI prefix filter to narrow down results
"""
function list_discovered_endpoints end

function list_discovered_endpoints(
    agentSpaceId, pentestJobId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return securityagent(
        "POST",
        "/ListDiscoveredEndpoints",
        Dict{String,Any}("agentSpaceId" => agentSpaceId, "pentestJobId" => pentestJobId);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_discovered_endpoints(
    agentSpaceId,
    pentestJobId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/ListDiscoveredEndpoints",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "agentSpaceId" => agentSpaceId, "pentestJobId" => pentestJobId
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_findings(agent_space_id, pentest_job_id)
    list_findings(agent_space_id, pentest_job_id, params::Dict{String,<:Any})

Lists findings with filtering and pagination support. When filters are applied, the actual
number of results returned may be less than the specified limit

# Arguments

- `agent_space_id`: ID of the agent space where the pentest job exists
- `pentest_job_id`: Identifier of the pentest job for which to retrieve associated findings

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"confidence"`: Filter findings by confidence level
- `"maxResults"`: Maximum number of findings to return in a single request (default: 50)
- `"name"`: Filter findings by name (case-insensitive substring search)
- `"nextToken"`: Token for pagination
- `"riskLevel"`: Filter findings by risk level
- `"riskType"`: Filter findings by risk type
- `"status"`: Filter findings by status
"""
function list_findings end

function list_findings(
    agentSpaceId, pentestJobId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return securityagent(
        "POST",
        "/ListFindings",
        Dict{String,Any}("agentSpaceId" => agentSpaceId, "pentestJobId" => pentestJobId);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_findings(
    agentSpaceId,
    pentestJobId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/ListFindings",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "agentSpaceId" => agentSpaceId, "pentestJobId" => pentestJobId
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_integrated_resources(agent_space_id)
    list_integrated_resources(agent_space_id, params::Dict{String,<:Any})

Lists the integrated resources for an agent space

# Arguments

- `agent_space_id`: Unique identifier of the agent space

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"integrationId"`: Filter integrated resources by a specific integration
- `"maxResults"`: Maximum number of results to return
- `"nextToken"`: Token for pagination
- `"resourceType"`: Filter integrated resources by resource type
"""
function list_integrated_resources end

function list_integrated_resources(
    agentSpaceId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return securityagent(
        "POST",
        "/ListIntegratedResources",
        Dict{String,Any}("agentSpaceId" => agentSpaceId);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_integrated_resources(
    agentSpaceId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/ListIntegratedResources",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("agentSpaceId" => agentSpaceId), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_integrations()
    list_integrations(params::Dict{String,<:Any})

Retrieves the Integrations associated with the user's account

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"filter"`: Filter criteria for integrations
- `"maxResults"`: Maximum number of results to return
- `"nextToken"`: Token for pagination
"""
function list_integrations end

function list_integrations(; aws_config::AbstractAWSConfig=current_aws_config())
    return securityagent(
        "POST", "/ListIntegrations"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_integrations(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return securityagent(
        "POST", "/ListIntegrations", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    list_memberships(agent_space_id, application_id)
    list_memberships(agent_space_id, application_id, params::Dict{String,<:Any})

Lists all members associated to an agent space with pagination support

# Arguments

- `agent_space_id`: Agent space identifier
- `application_id`: Application identifier

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"maxResults"`: Maximum number of results to return
- `"memberType"`: Filter by member type
- `"nextToken"`: Token for pagination
"""
function list_memberships end

function list_memberships(
    agentSpaceId, applicationId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return securityagent(
        "POST",
        "/ListMemberships",
        Dict{String,Any}("agentSpaceId" => agentSpaceId, "applicationId" => applicationId);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_memberships(
    agentSpaceId,
    applicationId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/ListMemberships",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "agentSpaceId" => agentSpaceId, "applicationId" => applicationId
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_pentest_job_tasks(agent_space_id)
    list_pentest_job_tasks(agent_space_id, params::Dict{String,<:Any})

Lists tasks associated with a specific pentest job

# Arguments

- `agent_space_id`: ID of the agent space where the pentest exists

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"categoryName"`: Filter tasks by category name.
- `"maxResults"`: Maximum number of tasks to return in a single request
- `"nextToken"`: Token for pagination to retrieve the next set of results
- `"pentestJobId"`: Identifier of the pentest job whose tasks to list
- `"stepName"`: Filter tasks by step name
"""
function list_pentest_job_tasks end

function list_pentest_job_tasks(
    agentSpaceId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return securityagent(
        "POST",
        "/ListPentestJobTasks",
        Dict{String,Any}("agentSpaceId" => agentSpaceId);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_pentest_job_tasks(
    agentSpaceId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/ListPentestJobTasks",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("agentSpaceId" => agentSpaceId), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_pentest_jobs_for_pentest(agent_space_id, pentest_id)
    list_pentest_jobs_for_pentest(agent_space_id, pentest_id, params::Dict{String,<:Any})

Lists pentest jobs associated with a pentest

# Arguments

- `agent_space_id`: ID of the agent space where the pentest exists
- `pentest_id`: Identifier of the pentest for which to retrieve associated jobs

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"maxResults"`: Maximum number of pentest jobs to return in a single request
- `"nextToken"`: Token for pagination
"""
function list_pentest_jobs_for_pentest end

function list_pentest_jobs_for_pentest(
    agentSpaceId, pentestId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return securityagent(
        "POST",
        "/ListPentestJobsForPentest",
        Dict{String,Any}("agentSpaceId" => agentSpaceId, "pentestId" => pentestId);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_pentest_jobs_for_pentest(
    agentSpaceId,
    pentestId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/ListPentestJobsForPentest",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("agentSpaceId" => agentSpaceId, "pentestId" => pentestId),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_pentests(agent_space_id)
    list_pentests(agent_space_id, params::Dict{String,<:Any})

Lists pentests with optional filtering by status

# Arguments

- `agent_space_id`: ID of the agent space where the pentest exists

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"maxResults"`: Maximum number of pentests to return in a single request
- `"nextToken"`: Token for pagination
"""
function list_pentests end

function list_pentests(agentSpaceId; aws_config::AbstractAWSConfig=current_aws_config())
    return securityagent(
        "POST",
        "/ListPentests",
        Dict{String,Any}("agentSpaceId" => agentSpaceId);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_pentests(
    agentSpaceId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/ListPentests",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("agentSpaceId" => agentSpaceId), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_tags_for_resource(resource_arn)
    list_tags_for_resource(resource_arn, params::Dict{String,<:Any})

Lists tags for a Security Agent resource

# Arguments

- `resource_arn`: ARN of the resource to list tags for
"""
function list_tags_for_resource end

function list_tags_for_resource(
    resourceArn; aws_config::AbstractAWSConfig=current_aws_config()
)
    return securityagent(
        "GET", "/tags/$(resourceArn)"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_tags_for_resource(
    resourceArn,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "GET", "/tags/$(resourceArn)", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    list_target_domains()
    list_target_domains(params::Dict{String,<:Any})

Lists target domains

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"maxResults"`: Maximum number of target domains to return
- `"nextToken"`: Token for pagination
"""
function list_target_domains end

function list_target_domains(; aws_config::AbstractAWSConfig=current_aws_config())
    return securityagent(
        "POST", "/ListTargetDomains"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_target_domains(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return securityagent(
        "POST", "/ListTargetDomains", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    start_code_remediation(agent_space_id, finding_ids, pentest_job_id)
    start_code_remediation(agent_space_id, finding_ids, pentest_job_id, params::Dict{String,<:Any})

Starts code remediation for the specified findings

# Arguments

- `agent_space_id`: ID of the agent space where the pentest job exists
- `finding_ids`: Identifiers of the findings to start code remediation for
- `pentest_job_id`: Identifier of the pentest job to start code remediation for
"""
function start_code_remediation end

function start_code_remediation(
    agentSpaceId,
    findingIds,
    pentestJobId;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/StartCodeRemediation",
        Dict{String,Any}(
            "agentSpaceId" => agentSpaceId,
            "findingIds" => findingIds,
            "pentestJobId" => pentestJobId,
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function start_code_remediation(
    agentSpaceId,
    findingIds,
    pentestJobId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/StartCodeRemediation",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "agentSpaceId" => agentSpaceId,
                    "findingIds" => findingIds,
                    "pentestJobId" => pentestJobId,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    start_pentest_job(agent_space_id, pentest_id)
    start_pentest_job(agent_space_id, pentest_id, params::Dict{String,<:Any})

Initiates the execution of a pentest

# Arguments

- `agent_space_id`: ID of the agent space where the pentest exists
- `pentest_id`: Identifier of the pentest to execute
"""
function start_pentest_job end

function start_pentest_job(
    agentSpaceId, pentestId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return securityagent(
        "POST",
        "/StartPentestJob",
        Dict{String,Any}("agentSpaceId" => agentSpaceId, "pentestId" => pentestId);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function start_pentest_job(
    agentSpaceId,
    pentestId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/StartPentestJob",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("agentSpaceId" => agentSpaceId, "pentestId" => pentestId),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    stop_pentest_job(agent_space_id, pentest_job_id)
    stop_pentest_job(agent_space_id, pentest_job_id, params::Dict{String,<:Any})

Stops the execution of a running pentest

# Arguments

- `agent_space_id`: ID of the agent space where the pentest exists
- `pentest_job_id`: Identifier of the pentest job to stop
"""
function stop_pentest_job end

function stop_pentest_job(
    agentSpaceId, pentestJobId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return securityagent(
        "POST",
        "/StopPentestJob",
        Dict{String,Any}("agentSpaceId" => agentSpaceId, "pentestJobId" => pentestJobId);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function stop_pentest_job(
    agentSpaceId,
    pentestJobId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/StopPentestJob",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "agentSpaceId" => agentSpaceId, "pentestJobId" => pentestJobId
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    tag_resource(resource_arn, tags)
    tag_resource(resource_arn, tags, params::Dict{String,<:Any})

Adds tags to a Security Agent resource

# Arguments

- `resource_arn`: ARN of the resource to tag
- `tags`: Tags to add to the resource
"""
function tag_resource end

function tag_resource(resourceArn, tags; aws_config::AbstractAWSConfig=current_aws_config())
    return securityagent(
        "POST",
        "/tags/$(resourceArn)",
        Dict{String,Any}("tags" => tags);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function tag_resource(
    resourceArn,
    tags,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/tags/$(resourceArn)",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("tags" => tags), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    untag_resource(resource_arn, tag_keys)
    untag_resource(resource_arn, tag_keys, params::Dict{String,<:Any})

Removes tags from a Security Agent resource

# Arguments

- `resource_arn`: ARN of the resource to untag
- `tag_keys`: List of tag keys to remove from the resource
"""
function untag_resource end

function untag_resource(
    resourceArn, tagKeys; aws_config::AbstractAWSConfig=current_aws_config()
)
    return securityagent(
        "DELETE",
        "/tags/$(resourceArn)",
        Dict{String,Any}("tagKeys" => tagKeys);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function untag_resource(
    resourceArn,
    tagKeys,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "DELETE",
        "/tags/$(resourceArn)",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("tagKeys" => tagKeys), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_agent_space(agent_space_id)
    update_agent_space(agent_space_id, params::Dict{String,<:Any})

Updates an agent space record

# Arguments

- `agent_space_id`: ID of the agent space to update

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"awsResources"`: AWS resource configurations associated with the agent space
- `"codeReviewSettings"`: Configuration for code review analysis, including controls
  scanning and general purpose scanning settings
- `"description"`: Description of the agent space
- `"name"`: Name of the agent space
- `"targetDomainIds"`: Target domain IDs to associate with the agent space
"""
function update_agent_space end

function update_agent_space(
    agentSpaceId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return securityagent(
        "POST",
        "/UpdateAgentSpace",
        Dict{String,Any}("agentSpaceId" => agentSpaceId);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_agent_space(
    agentSpaceId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/UpdateAgentSpace",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("agentSpaceId" => agentSpaceId), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_application(application_id)
    update_application(application_id, params::Dict{String,<:Any})

Updates application configuration

# Arguments

- `application_id`: Application ID

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"defaultKmsKeyId"`: Default KMS key identifier. Use an empty string to remove the default
  KMS key.
- `"roleArn"`: ARN of the IAM role that the application uses to access AWS resources on your
  behalf
"""
function update_application end

function update_application(
    applicationId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return securityagent(
        "POST",
        "/UpdateApplication",
        Dict{String,Any}("applicationId" => applicationId);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_application(
    applicationId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/UpdateApplication",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("applicationId" => applicationId), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_finding(agent_space_id, finding_id)
    update_finding(agent_space_id, finding_id, params::Dict{String,<:Any})

Updates an existing security finding with new details or status

# Arguments

- `agent_space_id`: ID of the agent space where the finding exists
- `finding_id`: Identifier of the finding to update

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"riskLevel"`: Updated severity level of the identified risk
- `"status"`: Updated status of the finding
"""
function update_finding end

function update_finding(
    agentSpaceId, findingId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return securityagent(
        "POST",
        "/UpdateFinding",
        Dict{String,Any}("agentSpaceId" => agentSpaceId, "findingId" => findingId);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_finding(
    agentSpaceId,
    findingId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/UpdateFinding",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("agentSpaceId" => agentSpaceId, "findingId" => findingId),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_integrated_resources(agent_space_id, integration_id, items)
    update_integrated_resources(agent_space_id, integration_id, items, params::Dict{String,<:Any})

Updates the integrated resources for an agent space

# Arguments

- `agent_space_id`: Unique identifier of the agent space
- `integration_id`: Unique identifier of the integration
- `items`: List of integrated resources to update
"""
function update_integrated_resources end

function update_integrated_resources(
    agentSpaceId, integrationId, items; aws_config::AbstractAWSConfig=current_aws_config()
)
    return securityagent(
        "POST",
        "/UpdateIntegratedResources",
        Dict{String,Any}(
            "agentSpaceId" => agentSpaceId,
            "integrationId" => integrationId,
            "items" => items,
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_integrated_resources(
    agentSpaceId,
    integrationId,
    items,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/UpdateIntegratedResources",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "agentSpaceId" => agentSpaceId,
                    "integrationId" => integrationId,
                    "items" => items,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_pentest(agent_space_id, pentest_id)
    update_pentest(agent_space_id, pentest_id, params::Dict{String,<:Any})

Updates an existing pentest with new configuration or settings

# Arguments

- `agent_space_id`: ID of the agent space where the pentest exists
- `pentest_id`: Identifier of the pentest to update

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"assets"`: Updated assets to be tested
- `"codeRemediationStrategy"`: Strategy for code remediation on findings
- `"excludeRiskTypes"`: A list of risk types excluded from the pentest execution
- `"logConfig"`: CloudWatch log group and stream prefix where pentest execution logs are
  stored
- `"networkTrafficConfig"`: Configuration for network traffic filtering
- `"serviceRole"`: Updated service role ARN for accessing customer resources
- `"title"`: New title for the pentest
- `"vpcConfig"`: VPC configuration that the Security Agent accesses
"""
function update_pentest end

function update_pentest(
    agentSpaceId, pentestId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return securityagent(
        "POST",
        "/UpdatePentest",
        Dict{String,Any}("agentSpaceId" => agentSpaceId, "pentestId" => pentestId);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_pentest(
    agentSpaceId,
    pentestId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/UpdatePentest",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("agentSpaceId" => agentSpaceId, "pentestId" => pentestId),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_target_domain(target_domain_id, verification_method)
    update_target_domain(target_domain_id, verification_method, params::Dict{String,<:Any})

Updates a target domain record

# Arguments

- `target_domain_id`: Unique identifier of the target domain to update
- `verification_method`: Verification method for the target domain
"""
function update_target_domain end

function update_target_domain(
    targetDomainId, verificationMethod; aws_config::AbstractAWSConfig=current_aws_config()
)
    return securityagent(
        "POST",
        "/UpdateTargetDomain",
        Dict{String,Any}(
            "targetDomainId" => targetDomainId, "verificationMethod" => verificationMethod
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_target_domain(
    targetDomainId,
    verificationMethod,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/UpdateTargetDomain",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "targetDomainId" => targetDomainId,
                    "verificationMethod" => verificationMethod,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    verify_target_domain(target_domain_id)
    verify_target_domain(target_domain_id, params::Dict{String,<:Any})

Verifies ownership for a registered target domain

# Arguments

- `target_domain_id`: Unique identifier of the target domain
"""
function verify_target_domain end

function verify_target_domain(
    targetDomainId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return securityagent(
        "POST",
        "/VerifyTargetDomain",
        Dict{String,Any}("targetDomainId" => targetDomainId);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function verify_target_domain(
    targetDomainId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return securityagent(
        "POST",
        "/VerifyTargetDomain",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("targetDomainId" => targetDomainId), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end
