# This file is auto-generated by AWSMetadata.jl
include("../AWSServices.jl")
using Compat
using .AWSServices: mediapackage

"""
    CreateChannel()

Creates a new Channel.

Required Parameters
id => The ID of the Channel. The ID must be unique within the region and it
cannot be changed after a Channel is created.


Optional Parameters
description => A short text description of the Channel.
tags => 
"""
CreateChannel(id) = mediapackage("POST", "/channels", Dict{String, Any}("id"=>id))
CreateChannel(id, args::AbstractDict{String, <: Any}) = mediapackage("POST", "/channels", Dict{String, Any}("id"=>id, args...))
CreateChannel(a...; b...) = CreateChannel(a..., b)

"""
    CreateHarvestJob()

Creates a new HarvestJob record.

Required Parameters
endTime => The end of the time-window which will be harvested

id => The ID of the HarvestJob. The ID must be unique within the region
and it cannot be changed after the HarvestJob is submitted

originEndpointId => The ID of the OriginEndpoint that the HarvestJob will harvest from.
This cannot be changed after the HarvestJob is submitted.

s3Destination => 
startTime => The start of the time-window which will be harvested


"""
CreateHarvestJob(endTime, id, originEndpointId, s3Destination, startTime) = mediapackage("POST", "/harvest_jobs", Dict{String, Any}("endTime"=>endTime, "id"=>id, "originEndpointId"=>originEndpointId, "s3Destination"=>s3Destination, "startTime"=>startTime))
CreateHarvestJob(endTime, id, originEndpointId, s3Destination, startTime, args::AbstractDict{String, <: Any}) = mediapackage("POST", "/harvest_jobs", Dict{String, Any}("endTime"=>endTime, "id"=>id, "originEndpointId"=>originEndpointId, "s3Destination"=>s3Destination, "startTime"=>startTime, args...))
CreateHarvestJob(a...; b...) = CreateHarvestJob(a..., b)

"""
    CreateOriginEndpoint()

Creates a new OriginEndpoint record.

Required Parameters
channelId => The ID of the Channel that the OriginEndpoint will be associated with.
This cannot be changed after the OriginEndpoint is created.

id => The ID of the OriginEndpoint.  The ID must be unique within the region
and it cannot be changed after the OriginEndpoint is created.


Optional Parameters
authorization => 
cmafPackage => 
dashPackage => 
description => A short text description of the OriginEndpoint.
hlsPackage => 
manifestName => A short string that will be used as the filename of the OriginEndpoint URL (defaults to "index").
mssPackage => 
origination => Control whether origination of video is allowed for this OriginEndpoint. If set to ALLOW, the OriginEndpoint
may by requested, pursuant to any other form of access control. If set to DENY, the OriginEndpoint may not be
requested. This can be helpful for Live to VOD harvesting, or for temporarily disabling origination

startoverWindowSeconds => Maximum duration (seconds) of content to retain for startover playback.
If not specified, startover playback will be disabled for the OriginEndpoint.

tags => 
timeDelaySeconds => Amount of delay (seconds) to enforce on the playback of live content.
If not specified, there will be no time delay in effect for the OriginEndpoint.

whitelist => A list of source IP CIDR blocks that will be allowed to access the OriginEndpoint.
"""
CreateOriginEndpoint(channelId, id) = mediapackage("POST", "/origin_endpoints", Dict{String, Any}("channelId"=>channelId, "id"=>id))
CreateOriginEndpoint(channelId, id, args::AbstractDict{String, <: Any}) = mediapackage("POST", "/origin_endpoints", Dict{String, Any}("channelId"=>channelId, "id"=>id, args...))
CreateOriginEndpoint(a...; b...) = CreateOriginEndpoint(a..., b)

"""
    DeleteChannel()

Deletes an existing Channel.

Required Parameters
id => The ID of the Channel to delete.

"""
DeleteChannel(id) = mediapackage("DELETE", "/channels/$(id)")
DeleteChannel(id, args::AbstractDict{String, <: Any}) = mediapackage("DELETE", "/channels/$(id)", args)
DeleteChannel(a...; b...) = DeleteChannel(a..., b)

"""
    DeleteOriginEndpoint()

Deletes an existing OriginEndpoint.

Required Parameters
id => The ID of the OriginEndpoint to delete.

"""
DeleteOriginEndpoint(id) = mediapackage("DELETE", "/origin_endpoints/$(id)")
DeleteOriginEndpoint(id, args::AbstractDict{String, <: Any}) = mediapackage("DELETE", "/origin_endpoints/$(id)", args)
DeleteOriginEndpoint(a...; b...) = DeleteOriginEndpoint(a..., b)

"""
    DescribeChannel()

Gets details about a Channel.

Required Parameters
id => The ID of a Channel.

"""
DescribeChannel(id) = mediapackage("GET", "/channels/$(id)")
DescribeChannel(id, args::AbstractDict{String, <: Any}) = mediapackage("GET", "/channels/$(id)", args)
DescribeChannel(a...; b...) = DescribeChannel(a..., b)

"""
    DescribeHarvestJob()

Gets details about an existing HarvestJob.

Required Parameters
id => The ID of the HarvestJob.

"""
DescribeHarvestJob(id) = mediapackage("GET", "/harvest_jobs/$(id)")
DescribeHarvestJob(id, args::AbstractDict{String, <: Any}) = mediapackage("GET", "/harvest_jobs/$(id)", args)
DescribeHarvestJob(a...; b...) = DescribeHarvestJob(a..., b)

"""
    DescribeOriginEndpoint()

Gets details about an existing OriginEndpoint.

Required Parameters
id => The ID of the OriginEndpoint.

"""
DescribeOriginEndpoint(id) = mediapackage("GET", "/origin_endpoints/$(id)")
DescribeOriginEndpoint(id, args::AbstractDict{String, <: Any}) = mediapackage("GET", "/origin_endpoints/$(id)", args)
DescribeOriginEndpoint(a...; b...) = DescribeOriginEndpoint(a..., b)

"""
    ListChannels()

Returns a collection of Channels.

Optional Parameters
maxResults => Upper bound on number of records to return.
nextToken => A token used to resume pagination from the end of a previous request.
"""
ListChannels() = mediapackage("GET", "/channels")
ListChannels(args::AbstractDict{String, Any}) = mediapackage("GET", "/channels", args)
ListChannels(a...; b...) = ListChannels(a..., b)

"""
    ListHarvestJobs()

Returns a collection of HarvestJob records.

Optional Parameters
includeChannelId => When specified, the request will return only HarvestJobs associated with the given Channel ID.
includeStatus => When specified, the request will return only HarvestJobs in the given status.
maxResults => The upper bound on the number of records to return.
nextToken => A token used to resume pagination from the end of a previous request.
"""
ListHarvestJobs() = mediapackage("GET", "/harvest_jobs")
ListHarvestJobs(args::AbstractDict{String, Any}) = mediapackage("GET", "/harvest_jobs", args)
ListHarvestJobs(a...; b...) = ListHarvestJobs(a..., b)

"""
    ListOriginEndpoints()

Returns a collection of OriginEndpoint records.

Optional Parameters
channelId => When specified, the request will return only OriginEndpoints associated with the given Channel ID.
maxResults => The upper bound on the number of records to return.
nextToken => A token used to resume pagination from the end of a previous request.
"""
ListOriginEndpoints() = mediapackage("GET", "/origin_endpoints")
ListOriginEndpoints(args::AbstractDict{String, Any}) = mediapackage("GET", "/origin_endpoints", args)
ListOriginEndpoints(a...; b...) = ListOriginEndpoints(a..., b)

"""
    ListTagsForResource()



Required Parameters
resource-arn => 

"""
ListTagsForResource(resource_arn) = mediapackage("GET", "/tags/$(resource-arn)")
ListTagsForResource(resource_arn, args::AbstractDict{String, <: Any}) = mediapackage("GET", "/tags/$(resource-arn)", args)
ListTagsForResource(a...; b...) = ListTagsForResource(a..., b)

"""
    RotateChannelCredentials()

Changes the Channel's first IngestEndpoint's username and password. WARNING - This API is deprecated. Please use RotateIngestEndpointCredentials instead

Required Parameters
id => The ID of the channel to update.

"""
RotateChannelCredentials(id) = mediapackage("PUT", "/channels/$(id)/credentials")
RotateChannelCredentials(id, args::AbstractDict{String, <: Any}) = mediapackage("PUT", "/channels/$(id)/credentials", args)
RotateChannelCredentials(a...; b...) = RotateChannelCredentials(a..., b)

"""
    RotateIngestEndpointCredentials()

Rotate the IngestEndpoint's username and password, as specified by the IngestEndpoint's id.

Required Parameters
id => The ID of the channel the IngestEndpoint is on.
ingest_endpoint_id => The id of the IngestEndpoint whose credentials should be rotated

"""
RotateIngestEndpointCredentials(id, ingest_endpoint_id) = mediapackage("PUT", "/channels/$(id)/ingest_endpoints/$(ingest_endpoint_id)/credentials")
RotateIngestEndpointCredentials(id, ingest_endpoint_id, args::AbstractDict{String, <: Any}) = mediapackage("PUT", "/channels/$(id)/ingest_endpoints/$(ingest_endpoint_id)/credentials", args)
RotateIngestEndpointCredentials(a...; b...) = RotateIngestEndpointCredentials(a..., b)

"""
    TagResource()



Required Parameters
resource-arn => 
tags => 

"""
TagResource(resource_arn, tags) = mediapackage("POST", "/tags/$(resource-arn)", Dict{String, Any}("tags"=>tags))
TagResource(resource_arn, tags, args::AbstractDict{String, <: Any}) = mediapackage("POST", "/tags/$(resource-arn)", Dict{String, Any}("tags"=>tags, args...))
TagResource(a...; b...) = TagResource(a..., b)

"""
    UntagResource()



Required Parameters
resource-arn => 
tagKeys => The key(s) of tag to be deleted

"""
UntagResource(resource_arn, tagKeys) = mediapackage("DELETE", "/tags/$(resource-arn)", Dict{String, Any}("tagKeys"=>tagKeys))
UntagResource(resource_arn, tagKeys, args::AbstractDict{String, <: Any}) = mediapackage("DELETE", "/tags/$(resource-arn)", Dict{String, Any}("tagKeys"=>tagKeys, args...))
UntagResource(a...; b...) = UntagResource(a..., b)

"""
    UpdateChannel()

Updates an existing Channel.

Required Parameters
id => The ID of the Channel to update.

Optional Parameters
description => A short text description of the Channel.
"""
UpdateChannel(id) = mediapackage("PUT", "/channels/$(id)")
UpdateChannel(id, args::AbstractDict{String, <: Any}) = mediapackage("PUT", "/channels/$(id)", args)
UpdateChannel(a...; b...) = UpdateChannel(a..., b)

"""
    UpdateOriginEndpoint()

Updates an existing OriginEndpoint.

Required Parameters
id => The ID of the OriginEndpoint to update.

Optional Parameters
authorization => 
cmafPackage => 
dashPackage => 
description => A short text description of the OriginEndpoint.
hlsPackage => 
manifestName => A short string that will be appended to the end of the Endpoint URL.
mssPackage => 
origination => Control whether origination of video is allowed for this OriginEndpoint. If set to ALLOW, the OriginEndpoint
may by requested, pursuant to any other form of access control. If set to DENY, the OriginEndpoint may not be
requested. This can be helpful for Live to VOD harvesting, or for temporarily disabling origination

startoverWindowSeconds => Maximum duration (in seconds) of content to retain for startover playback.
If not specified, startover playback will be disabled for the OriginEndpoint.

timeDelaySeconds => Amount of delay (in seconds) to enforce on the playback of live content.
If not specified, there will be no time delay in effect for the OriginEndpoint.

whitelist => A list of source IP CIDR blocks that will be allowed to access the OriginEndpoint.
"""
UpdateOriginEndpoint(id) = mediapackage("PUT", "/origin_endpoints/$(id)")
UpdateOriginEndpoint(id, args::AbstractDict{String, <: Any}) = mediapackage("PUT", "/origin_endpoints/$(id)", args)
UpdateOriginEndpoint(a...; b...) = UpdateOriginEndpoint(a..., b)
