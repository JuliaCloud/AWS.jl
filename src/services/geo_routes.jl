# This file is auto-generated by AWS.jl
# Note: `SERVICE_FEATURE_SET` is embedded by `@service`
using AWS: AbstractAWSConfig, current_aws_config, _merge
using AWS.AWSServices: geo_routes
using AWS.UUIDs: uuid4

"""
    calculate_isolines(thresholds)
    calculate_isolines(thresholds, params::Dict{String,<:Any})

Calculates areas that can be reached within specified time or distance thresholds from a
given point. For example, you can use this operation to determine the area within a 30-
minute drive of a store location, find neighborhoods within walking distance of a school, or
identify delivery zones based on drive time.

Isolines (also known as isochrones for time-based calculations) are useful for various
applications including:

- Service area visualization - Show customers the area you can serve within promised
  delivery times
- Site selection - Analyze potential business locations based on population within travel
  distance
- Site selection - Determine areas that can be reached within specified response times

!!! note
    Route preferences such as avoiding toll roads or ferries are treated as preferences
    rather than absolute restrictions. If a viable route cannot be calculated while honoring
    all preferences, some may be ignored.

For more information, see [Calculate isolines](https://docs.aws.amazon.com/location/latest/developerguide/calculate-isolines.html)
in the *Amazon Location Service Developer Guide*.

# Arguments

- `thresholds`: The distance or time thresholds used to determine reachable areas. You can
  specify up to five thresholds (which all must be the same type) to calculate multiple
  isolines in a single request. For example, to determine the areas that are reachable
  within 10 and 20 minutes of the origin, specify time thresholds of 600 and 1200 seconds.

  You incur a calculation charge for each threshold. Using a large number of thresholds in a
  request can lead to unexpected charges. For more information, see [Routes pricing](https://docs.aws.amazon.com/location/latest/developerguide/routes-pricing.html)
  in the *Amazon Location Service Developer Guide*.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Allow"`: Enables special road types or features that should be considered for routing
  even if they might be restricted by default for the selected travel mode. These include
  high-occupancy vehicle and toll lanes.

- `"ArrivalTime"`: Determine areas from which `Destination` can be reached by this time,
  taking into account predicted traffic conditions and working backward to account for
  congestion patterns. This attribute cannot be used together with `DepartureTime` or
  `DepartNow`. Specified as an ISO-8601 timestamp with timezone offset.

  Time format: `YYYY-MM-DDThh:mm:ss.sssZ | YYYY-MM-DDThh:mm:ss.sss+hh:mm`

  Examples:

  `2020-04-22T17:57:24Z`

  `2020-04-22T17:57:24+02:00`

- `"Avoid"`: Specifies road types, features, or areas to avoid (if possible) when
  calculating reachable areas. These are treated as preferences rather than strict
  constraints—if a route cannot be calculated without using an avoided feature, that
  avoidance preference may be ignored.

- `"DepartNow"`: When true, uses the current time as the departure time and takes current
  traffic conditions into account. This attribute cannot be used together with
  `DepartureTime` or `ArrivalTime`.

- `"DepartureTime"`: Determine areas that can be reached when departing at this time, taking
  into account predicted traffic conditions. This attribute cannot be used together with
  `ArrivalTime` or `DepartNow`. Specified as an ISO-8601 timestamp with timezone offset.

  Time format:`YYYY-MM-DDThh:mm:ss.sssZ | YYYY-MM-DDThh:mm:ss.sss+hh:mm`

  Examples:

  `2020-04-22T17:57:24Z`

  `2020-04-22T17:57:24+02:00`

- `"Destination"`: An optional destination point, specified as `[longitude, latitude]`
  coordinates. When provided, the service calculates areas from which this destination can
  be reached within the specified thresholds. This reverses the usual isoline calculation to
  show areas that could reach your location, rather than areas you could reach from your
  location. Either `Origin` or `Destination` must be provided.

- `"DestinationOptions"`: Options that control how the destination point is matched to the
  road network and how routes can approach it. These options help improve travel time
  accuracy by accounting for real-world access to the destination.

- `"IsolineGeometryFormat"`: The format of the returned IsolineGeometry.

  Default value:`FlexiblePolyline`

- `"IsolineGranularity"`: Controls the detail level of the generated isolines. Higher
  granularity produces smoother shapes but requires more processing time and results in
  larger responses.

- `"OptimizeIsolineFor"`: Controls the trade-off between calculation speed and isoline
  precision. Choose `FastCalculation` for quicker results with less detail,
  `AccurateCalculation` for more precise results, or `BalancedCalculation` for a middle
  ground.

  Default value: `BalancedCalculation`

- `"OptimizeRoutingFor"`: Determines whether routes prioritize shortest travel time
  (`FastestRoute`) or shortest physical distance (`ShortestRoute`) when calculating
  reachable areas.

  Default value: `FastestRoute`

- `"Origin"`: The starting point for isoline calculations, specified as
  `[longitude, latitude]` coordinates. For example, this could be a store location, service
  center, or any point from which you want to calculate reachable areas. Either `Origin` or
  `Destination` must be provided.

- `"OriginOptions"`: Options that control how the origin point is matched to the road
  network and how routes can depart from it. These options help improve travel time accuracy
  by accounting for real-world access from the origin.

- `"Traffic"`: Configures how real-time and historical traffic data affects isoline
  calculations. Traffic patterns can significantly impact reachable areas, especially during
  peak hours.

- `"TravelMode"`: The mode of transportation to use for calculations. This affects which
  road types or features can be used, estimated speed, and the traffic levels that are
  applied.

  - `Car`—Standard passenger vehicle routing using roads accessible to cars
  - `Pedestrian`—Walking routes using pedestrian paths, sidewalks, and crossings
  - `Scooter`—Light two-wheeled vehicle routing using roads and paths accessible to scooters
  - `Truck`—Commercial truck routing considering vehicle dimensions, weight restrictions,
    and hazardous material regulations

  !!! note
      The mode `Scooter` also applies to motorcycles; set this to `Scooter` when calculating
      isolines for motorcycles.

  Default value: `Car`

- `"TravelModeOptions"`: Additional attributes that refine how reachable areas are
  calculated based on specific vehicle characteristics. These options help produce more
  accurate results by accounting for real-world constraints and capabilities.

  For example:

  - For trucks (`Truck`), specify dimensions, weight limits, and hazardous cargo
    restrictions to ensure isolines only include roads that can physically and legally
    accommodate the vehicle
  - For cars (`Car`), set maximum speed capabilities or indicate high-occupancy vehicle
    eligibility to better estimate reachable areas
  - For scooters (`Scooter`), specify engine type and speed limitations to more accurately
    model their travel capabilities

  Without these options, calculations use default assumptions that may not match your
  specific use case.

- `"key"`: An Amazon Location Service API Key with access to this action. If omitted, the
  request must be signed using Signature Version 4.
"""
function calculate_isolines end

function calculate_isolines(Thresholds; aws_config::AbstractAWSConfig=current_aws_config())
    return geo_routes(
        "POST",
        "/isolines",
        Dict{String,Any}("Thresholds" => Thresholds);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function calculate_isolines(
    Thresholds,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return geo_routes(
        "POST",
        "/isolines",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("Thresholds" => Thresholds), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    calculate_route_matrix(destinations, origins)
    calculate_route_matrix(destinations, origins, params::Dict{String,<:Any})

Use `CalculateRouteMatrix` to compute results for all pairs of Origins to Destinations. Each
row corresponds to one entry in Origins. Each entry in the row corresponds to the route from
that entry in Origins to an entry in Destinations positions.

For more information, see [Calculate route matrix](https://docs.aws.amazon.com/location/latest/developerguide/calculate-route-matrix.html)
in the *Amazon Location Service Developer Guide*.

# Arguments

- `destinations`: List of destinations for the route.

  !!! note
      Route calculations are billed for each origin and destination pair. If you use a large
      matrix of origins and destinations, your costs will increase accordingly. For more
      information, see [Routes pricing](https://docs.aws.amazon.com/location/latest/developerguide/routes-pricing.html)
      in the *Amazon Location Service Developer Guide*.

- `origins`: The position for the origin in World Geodetic System (WGS 84) format: [longitude, latitude].

!!! note
    Route calculations are billed for each origin and destination pair. Using a large amount of Origins in a request can lead you to incur unexpected charges. For more information, see [Routes pricing](https://docs.aws.amazon.com/location/latest/developerguide/routes-pricing.html) in the *Amazon Location Service Developer Guide*.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Allow"`: Features that are allowed while calculating a route.

- `"Avoid"`: Features that are avoided while calculating a route. Avoidance is on a best-
  case basis. If an avoidance can't be satisfied for a particular case, it violates the
  avoidance and the returned response produces a notice for the violation. For [GrabMaps](https://docs.aws.amazon.com/location/latest/developerguide/GrabMaps.html)
  customers, `ap-southeast-1` and `ap-southeast-5` regions support only `TollRoads`,
  `Ferries`, and `ControlledAccessHighways`.

- `"DepartNow"`: Uses the current time as the time of departure.

- `"DepartureTime"`: Time of departure from the origin.

  Time format:`YYYY-MM-DDThh:mm:ss.sssZ | YYYY-MM-DDThh:mm:ss.sss+hh:mm`

  Examples:

  `2020-04-22T17:57:24Z`

  `2020-04-22T17:57:24+02:00`

- `"Exclude"`: Features to be strictly excluded while calculating the route. Not supported
  in `ap-southeast-1` and `ap-southeast-5` regions for [GrabMaps](https://docs.aws.amazon.com/location/latest/developerguide/GrabMaps.html)
  customers.

- `"OptimizeRoutingFor"`: Controls the trade-off between finding the shortest travel time
  (`FastestRoute`) and the shortest distance (`ShortestRoute`) when calculating reachable
  areas.

  Default value: `FastestRoute`

- `"RoutingBoundary"`: Boundary within which the matrix is to be calculated. All data,
  origins and destinations outside the boundary are considered invalid. For [GrabMaps](https://docs.aws.amazon.com/location/latest/developerguide/GrabMaps.html)
  customers, `ap-southeast-1` and `ap-southeast-5` regions support only `Unbounded` set to
  `true`.

  Default value: `Unbounded set to true`

  !!! note
      When request routing boundary was set as AutoCircle, the response routing boundary
      will return Circle derived from the AutoCircle settings.

- `"Traffic"`: Traffic related options. Not supported in `ap-southeast-1` and
  `ap-southeast-5` regions for [GrabMaps](https://docs.aws.amazon.com/location/latest/developerguide/GrabMaps.html)
  customers.

- `"TravelMode"`: Specifies the mode of transport when calculating a route. Used in
  estimating the speed of travel and road compatibility. For [GrabMaps](https://docs.aws.amazon.com/location/latest/developerguide/GrabMaps.html)
  customers, `ap-southeast-1` and `ap-southeast-5` regions support only `Car`, `Pedestrian`,
  and `Scooter`.

  Default value: `Car`

- `"TravelModeOptions"`: Travel mode related options for the provided travel mode. Not
  supported in `ap-southeast-1` and `ap-southeast-5` regions for [GrabMaps](https://docs.aws.amazon.com/location/latest/developerguide/GrabMaps.html)
  customers.

- `"key"`: Optional: The API key to be used for authorization. Either an API key or valid
  SigV4 signature must be provided when making a request.
"""
function calculate_route_matrix end

function calculate_route_matrix(
    Destinations, Origins; aws_config::AbstractAWSConfig=current_aws_config()
)
    return geo_routes(
        "POST",
        "/route-matrix",
        Dict{String,Any}("Destinations" => Destinations, "Origins" => Origins);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function calculate_route_matrix(
    Destinations,
    Origins,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return geo_routes(
        "POST",
        "/route-matrix",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("Destinations" => Destinations, "Origins" => Origins),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    calculate_routes(destination, origin)
    calculate_routes(destination, origin, params::Dict{String,<:Any})

`CalculateRoutes` computes routes given the following required parameters: `Origin` and
`Destination`.

For more information, see [Calculate routes](https://docs.aws.amazon.com/location/latest/developerguide/calculate-routes.html)
in the *Amazon Location Service Developer Guide*.

# Arguments

- `destination`: The final position for the route. In the World Geodetic System (WGS 84)
  format: `[longitude, latitude]`.
- `origin`: The start position for the route in World Geodetic System (WGS 84) format: [longitude, latitude].

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Allow"`: Features that are allowed while calculating a route. Not supported in
  `ap-southeast-1` and `ap-southeast-5` regions for [GrabMaps](https://docs.aws.amazon.com/location/latest/developerguide/GrabMaps.html)
  customers.

- `"ArrivalTime"`: Time of arrival at the destination. Not supported in `ap-southeast-1` and
  `ap-southeast-5` regions for [GrabMaps](https://docs.aws.amazon.com/location/latest/developerguide/GrabMaps.html)
  customers.

  Time format:`YYYY-MM-DDThh:mm:ss.sssZ | YYYY-MM-DDThh:mm:ss.sss+hh:mm`

  Examples:

  `2020-04-22T17:57:24Z`

  `2020-04-22T17:57:24+02:00`

- `"Avoid"`: Features that are avoided while calculating a route. Avoidance is on a best-
  case basis. If an avoidance can't be satisfied for a particular case, it violates the
  avoidance and the returned response produces a notice for the violation. For [GrabMaps](https://docs.aws.amazon.com/location/latest/developerguide/GrabMaps.html)
  customers, `ap-southeast-1` and `ap-southeast-5` regions support only
  `ControlledAccessHighways`, `Ferries`, and `TollRoads`

- `"DepartNow"`: Uses the current time as the time of departure.

- `"DepartureTime"`: Time of departure from the origin.

  Time format:`YYYY-MM-DDThh:mm:ss.sssZ | YYYY-MM-DDThh:mm:ss.sss+hh:mm`

  Examples:

  `2020-04-22T17:57:24Z`

  `2020-04-22T17:57:24+02:00`

- `"DestinationOptions"`: Destination related options. Not supported in `ap-southeast-1` and
  `ap-southeast-5` regions for [GrabMaps](https://docs.aws.amazon.com/location/latest/developerguide/GrabMaps.html)
  customers.

- `"Driver"`: Driver related options. Not supported in `ap-southeast-1` and `ap-southeast-5`
  regions for [GrabMaps](https://docs.aws.amazon.com/location/latest/developerguide/GrabMaps.html)
  customers.

- `"Exclude"`: Features to be strictly excluded while calculating the route. Not supported
  in `ap-southeast-1` and `ap-southeast-5` regions for [GrabMaps](https://docs.aws.amazon.com/location/latest/developerguide/GrabMaps.html)
  customers.

- `"InstructionsMeasurementSystem"`: Measurement system to be used for instructions within
  steps in the response.

- `"Languages"`: List of languages for instructions within steps in the response. Not
  supported in `ap-southeast-1` and `ap-southeast-5` regions for [GrabMaps](https://docs.aws.amazon.com/location/latest/developerguide/GrabMaps.html)
  customers.

  !!! note
      Instructions in the requested language are returned only if they are available.

- `"LegAdditionalFeatures"`: A list of optional additional parameters such as timezone that
  can be requested for each result. For [GrabMaps](https://docs.aws.amazon.com/location/latest/developerguide/GrabMaps.html)
  customers, `ap-southeast-1` and `ap-southeast-5` regions support only
  `PassThroughWaypoints`, `Summary`, and `TravelStepInstructions`

  - `Elevation`: Retrieves the elevation information for each location.
  - `Incidents`: Provides information on traffic incidents along the route.
  - `PassThroughWaypoints`: Indicates waypoints that are passed through without stopping.
  - `Summary`: Returns a summary of the route, including distance and duration.
  - `Tolls`: Supplies toll cost information along the route.
  - `TravelStepInstructions`: Provides step-by-step instructions for travel along the route.
  - `TruckRoadTypes`: Returns information about road types suitable for trucks.
  - `TypicalDuration`: Gives typical travel duration based on historical data.
  - `Zones`: Specifies the time zone information for each waypoint.

- `"LegGeometryFormat"`: Specifies the format of the geometry returned for each leg of the
  route. You can choose between two different geometry encoding formats.

  `FlexiblePolyline`: A compact and precise encoding format for the leg geometry. For more
  information on the format, see the GitHub repository for [https://github.com/aws-geospatial/polyline](https://github.com/aws-geospatial/polyline).

  `Simple`: A less compact encoding, which is easier to decode but may be less precise and
  result in larger payloads.

- `"MaxAlternatives"`: Maximum number of alternative routes to be provided in the response,
  if available. For [GrabMaps](https://docs.aws.amazon.com/location/latest/developerguide/GrabMaps.html)
  customers, `ap-southeast-1` and `ap-southeast-5` regions support only up to 3 alternative
  routes.

- `"OptimizeRoutingFor"`: Controls the trade-off between achieving the shortest travel time
  (`FastestRoute`) and achieving the shortest physical distance ((`ShortestRoute`) when
  calculating each route in the matrix.

  Default value: `FastestRoute`

- `"OriginOptions"`: Specifies how the origin point should be matched to the road network
  and any routing constraints that apply when the traveler is departing the origin. Not
  supported in `ap-southeast-1` and `ap-southeast-5` regions for [GrabMaps](https://docs.aws.amazon.com/location/latest/developerguide/GrabMaps.html)
  customers.

- `"SpanAdditionalFeatures"`: A list of optional features such as `SpeedLimit` that can be
  requested for a Span. A span is a section of a Leg for which the requested features have
  the same values. Not supported in `ap-southeast-1` and `ap-southeast-5` regions for [GrabMaps](https://docs.aws.amazon.com/location/latest/developerguide/GrabMaps.html)
  customers.

- `"Tolls"`: Toll related options. Not supported in `ap-southeast-1` and `ap-southeast-5`
  regions for [GrabMaps](https://docs.aws.amazon.com/location/latest/developerguide/GrabMaps.html)
  customers.

- `"Traffic"`: Traffic related options. Not supported in `ap-southeast-1` and
  `ap-southeast-5` regions for [GrabMaps](https://docs.aws.amazon.com/location/latest/developerguide/GrabMaps.html)
  customers.

- `"TravelMode"`: Specifies the mode of transport when calculating a route. Used in
  estimating the speed of travel and road compatibility. For [GrabMaps](https://docs.aws.amazon.com/location/latest/developerguide/GrabMaps.html)
  customers, `ap-southeast-1` and `ap-southeast-5` regions support only `Car`, `Pedestrian`,
  and `Scooter` values.

  Default value: `Car`

- `"TravelModeOptions"`: Travel mode related options for the provided travel mode. For [GrabMaps](https://docs.aws.amazon.com/location/latest/developerguide/GrabMaps.html)
  customers, `ap-southeast-1` and `ap-southeast-5` regions support only `Car` and
  `Pedestrian` travel mode options.

- `"TravelStepType"`: Type of step returned by the response. `Default` provides basic steps
  intended for web based applications. `TurnByTurn` provides detailed instructions with more
  granularity intended for a turn based navigation system. For [GrabMaps](https://docs.aws.amazon.com/location/latest/developerguide/GrabMaps.html)
  customers, `ap-southeast-1` and `ap-southeast-5` regions `Default` does not return any
  steps.

- `"Waypoints"`: List of waypoints between the Origin and Destination. For [GrabMaps](https://docs.aws.amazon.com/location/latest/developerguide/GrabMaps.html)
  customers, `ap-southeast-1` and `ap-southeast-5` regions max length is `100`.

  Max length: `23`

- `"key"`: Optional: The API key to be used for authorization. Either an API key or valid
  SigV4 signature must be provided when making a request.
"""
function calculate_routes end

function calculate_routes(
    Destination, Origin; aws_config::AbstractAWSConfig=current_aws_config()
)
    return geo_routes(
        "POST",
        "/routes",
        Dict{String,Any}("Destination" => Destination, "Origin" => Origin);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function calculate_routes(
    Destination,
    Origin,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return geo_routes(
        "POST",
        "/routes",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("Destination" => Destination, "Origin" => Origin),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    optimize_waypoints(origin)
    optimize_waypoints(origin, params::Dict{String,<:Any})

`OptimizeWaypoints` calculates the optimal order to travel between a set of waypoints to
minimize either the travel time or the distance travelled during the journey, based on road
network restrictions and the traffic pattern data.

For more information, see [Optimize waypoints](https://docs.aws.amazon.com/location/latest/developerguide/actions-optimize-waypoints.html)
in the *Amazon Location Service Developer Guide*.

# Arguments

- `origin`: The start position for the route in World Geodetic System (WGS 84) format: [longitude, latitude].

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Avoid"`: Features that are avoided. Avoidance is on a best-case basis. If an avoidance
  can't be satisfied for a particular case, this setting is ignored.

- `"Clustering"`: Clustering allows you to specify how nearby waypoints can be clustered to
  improve the optimized sequence.

- `"DepartureTime"`: Departure time from the waypoint.

  Time format:`YYYY-MM-DDThh:mm:ss.sssZ | YYYY-MM-DDThh:mm:ss.sss+hh:mm`

  Examples:

  `2020-04-22T17:57:24Z`

  `2020-04-22T17:57:24+02:00`

- `"Destination"`: The final position for the route in the World Geodetic System (WGS 84)
  format: `[longitude, latitude]`.

- `"DestinationOptions"`: Destination related options.

- `"Driver"`: Driver related options.

- `"Exclude"`: Features to be strictly excluded while calculating the route.

- `"OptimizeSequencingFor"`: Specifies the optimization criteria for the calculated
  sequence.

  Default value: `FastestRoute`.

- `"OriginOptions"`: Origin related options.

- `"Traffic"`: Traffic-related options.

- `"TravelMode"`: Specifies the mode of transport when calculating a route. Used in
  estimating the speed of travel and road compatibility.

  Default value: `Car`

- `"TravelModeOptions"`: Travel mode related options for the provided travel mode.

- `"Waypoints"`: List of waypoints between the `Origin` and `Destination`.

- `"key"`: Optional: The API key to be used for authorization. Either an API key or valid
  SigV4 signature must be provided when making a request.
"""
function optimize_waypoints end

function optimize_waypoints(Origin; aws_config::AbstractAWSConfig=current_aws_config())
    return geo_routes(
        "POST",
        "/optimize-waypoints",
        Dict{String,Any}("Origin" => Origin);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function optimize_waypoints(
    Origin, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return geo_routes(
        "POST",
        "/optimize-waypoints",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("Origin" => Origin), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    snap_to_roads(trace_points)
    snap_to_roads(trace_points, params::Dict{String,<:Any})

`SnapToRoads` matches GPS trace to roads most likely traveled on.

For more information, see [Snap to Roads](https://docs.aws.amazon.com/location/latest/developerguide/snap-to-roads.html)
in the *Amazon Location Service Developer Guide*.

# Arguments

- `trace_points`: List of trace points to be snapped onto the road network.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"SnapRadius"`: The radius around the provided tracepoint that is considered for snapping.

  **Unit**: `meters`

  Default value: `300`

- `"SnappedGeometryFormat"`: Chooses what the returned SnappedGeometry format should be.

  Default value: `FlexiblePolyline`

- `"TravelMode"`: Specifies the mode of transport when calculating a route. Used in
  estimating the speed of travel and road compatibility.

  Default value: `Car`

- `"TravelModeOptions"`: Travel mode related options for the provided travel mode.

- `"key"`: Optional: The API key to be used for authorization. Either an API key or valid
  SigV4 signature must be provided when making a request.
"""
function snap_to_roads end

function snap_to_roads(TracePoints; aws_config::AbstractAWSConfig=current_aws_config())
    return geo_routes(
        "POST",
        "/snap-to-roads",
        Dict{String,Any}("TracePoints" => TracePoints);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function snap_to_roads(
    TracePoints,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return geo_routes(
        "POST",
        "/snap-to-roads",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("TracePoints" => TracePoints), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end
