# This file is auto-generated by AWS.jl
# Note: `SERVICE_FEATURE_SET` is embedded by `@service`
using AWS: AbstractAWSConfig, current_aws_config, _merge
using AWS.AWSServices: notificationscontacts
using AWS.UUIDs: uuid4

"""
    activate_email_contact(arn, code)
    activate_email_contact(arn, code, params::Dict{String,<:Any})

Activates an email contact using an activation code. This code is in the activation email
sent to the email address associated with this email contact.

# Arguments

- `arn`: The Amazon Resource Name (ARN) of the resource.

- `code`: The activation code for this email contact.

  An email contact has a maximum of five activation attempts. Activation codes expire after
  12 hours and are generated by the [SendActivationCode](https://docs.aws.amazon.com/notificationscontacts/latest/APIReference/API_SendActivationCode.html)
API action.
"""
function activate_email_contact end

function activate_email_contact(
    arn, code; aws_config::AbstractAWSConfig=current_aws_config()
)
    return notificationscontacts(
        "PUT",
        "/emailcontacts/$(arn)/activate/$(code)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function activate_email_contact(
    arn,
    code,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return notificationscontacts(
        "PUT",
        "/emailcontacts/$(arn)/activate/$(code)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_email_contact(email_address, name)
    create_email_contact(email_address, name, params::Dict{String,<:Any})

Creates an email contact for the provided email address.

# Arguments

- `email_address`: The email address this email contact points to. The activation email and
  any subscribed emails are sent here.

  !!! note
      This email address can't receive emails until it's activated.

- `name`: The name of the email contact.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"tags"`: A map of tags assigned to a resource. A tag is a string-to-string map of key-
  value pairs.
"""
function create_email_contact end

function create_email_contact(
    emailAddress, name; aws_config::AbstractAWSConfig=current_aws_config()
)
    return notificationscontacts(
        "POST",
        "/2022-09-19/emailcontacts",
        Dict{String,Any}("emailAddress" => emailAddress, "name" => name);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_email_contact(
    emailAddress,
    name,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return notificationscontacts(
        "POST",
        "/2022-09-19/emailcontacts",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("emailAddress" => emailAddress, "name" => name),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_email_contact(arn)
    delete_email_contact(arn, params::Dict{String,<:Any})

Deletes an email contact.

!!! note
    Deleting an email contact removes it from all associated notification configurations.

# Arguments

- `arn`: The Amazon Resource Name (ARN) of the resource.
"""
function delete_email_contact end

function delete_email_contact(arn; aws_config::AbstractAWSConfig=current_aws_config())
    return notificationscontacts(
        "DELETE", "/emailcontacts/$(arn)"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function delete_email_contact(
    arn, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return notificationscontacts(
        "DELETE",
        "/emailcontacts/$(arn)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_email_contact(arn)
    get_email_contact(arn, params::Dict{String,<:Any})

Returns an email contact.

# Arguments

- `arn`: The Amazon Resource Name (ARN) of the email contact to get.
"""
function get_email_contact end

function get_email_contact(arn; aws_config::AbstractAWSConfig=current_aws_config())
    return notificationscontacts(
        "GET", "/emailcontacts/$(arn)"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function get_email_contact(
    arn, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return notificationscontacts(
        "GET", "/emailcontacts/$(arn)", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    list_email_contacts()
    list_email_contacts(params::Dict{String,<:Any})

Lists all email contacts created under the Account.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"maxResults"`: The maximum number of results to include in the response. If more results
  exist than the specified MaxResults value, a token is included in the response so that the
  remaining results can be retrieved.
- `"nextToken"`: An optional token returned from a prior request. Use this token for
  pagination of results from this action. If this parameter is specified, the response
  includes only results beyond the token, up to the value specified by MaxResults.
"""
function list_email_contacts end

function list_email_contacts(; aws_config::AbstractAWSConfig=current_aws_config())
    return notificationscontacts(
        "GET", "/emailcontacts"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_email_contacts(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return notificationscontacts(
        "GET", "/emailcontacts", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    list_tags_for_resource(arn)
    list_tags_for_resource(arn, params::Dict{String,<:Any})

Lists all of the tags associated with the Amazon Resource Name (ARN) that you specify. The
resource can be a user, server, or role.

# Arguments

- `arn`: The ARN you specified to list the tags of.
"""
function list_tags_for_resource end

function list_tags_for_resource(arn; aws_config::AbstractAWSConfig=current_aws_config())
    return notificationscontacts(
        "GET", "/tags/$(arn)"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_tags_for_resource(
    arn, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return notificationscontacts(
        "GET", "/tags/$(arn)", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    send_activation_code(arn)
    send_activation_code(arn, params::Dict{String,<:Any})

Sends an activation email to the email address associated with the specified email contact.

!!! note
    It might take a few minutes for the activation email to arrive. If it doesn't arrive,
    check in your spam folder or try sending another activation email.

# Arguments

- `arn`: The Amazon Resource Name (ARN) of the resource.
"""
function send_activation_code end

function send_activation_code(arn; aws_config::AbstractAWSConfig=current_aws_config())
    return notificationscontacts(
        "POST",
        "/2022-10-31/emailcontacts/$(arn)/activate/send";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function send_activation_code(
    arn, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return notificationscontacts(
        "POST",
        "/2022-10-31/emailcontacts/$(arn)/activate/send",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    tag_resource(arn, tags)
    tag_resource(arn, tags, params::Dict{String,<:Any})

Attaches a key-value pair to a resource, as identified by its Amazon Resource Name (ARN).
Taggable resources in AWS User Notifications Contacts include email contacts.

# Arguments

- `arn`: The ARN of the configuration.
- `tags`: A list of tags to apply to the configuration.
"""
function tag_resource end

function tag_resource(arn, tags; aws_config::AbstractAWSConfig=current_aws_config())
    return notificationscontacts(
        "POST",
        "/tags/$(arn)",
        Dict{String,Any}("tags" => tags);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function tag_resource(
    arn,
    tags,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return notificationscontacts(
        "POST",
        "/tags/$(arn)",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("tags" => tags), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    untag_resource(arn, tag_keys)
    untag_resource(arn, tag_keys, params::Dict{String,<:Any})

Detaches a key-value pair from a resource, as identified by its Amazon Resource Name (ARN).
Taggable resources in AWS User Notifications Contacts include email contacts..

# Arguments

- `arn`: The value of the resource that will have the tag removed. An Amazon Resource Name
  (ARN) is an identifier for a specific AWS resource, such as a server, user, or role.
- `tag_keys`: Specifies a list of tag keys that you want to remove from the specified
  resources.
"""
function untag_resource end

function untag_resource(arn, tagKeys; aws_config::AbstractAWSConfig=current_aws_config())
    return notificationscontacts(
        "DELETE",
        "/tags/$(arn)",
        Dict{String,Any}("tagKeys" => tagKeys);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function untag_resource(
    arn,
    tagKeys,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return notificationscontacts(
        "DELETE",
        "/tags/$(arn)",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("tagKeys" => tagKeys), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end
