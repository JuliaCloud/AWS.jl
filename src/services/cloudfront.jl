# This file is auto-generated by AWS.jl
# Note: `SERVICE_FEATURE_SET` is embedded by `@service`
using AWS: AbstractAWSConfig, current_aws_config, _merge
using AWS.AWSServices: cloudfront
using AWS.UUIDs: uuid4

"""
    associate_alias(alias, target_distribution_id)
    associate_alias(alias, target_distribution_id, params::Dict{String,<:Any})

!!! note
    The `AssociateAlias` API operation only supports standard distributions. To move domains
    between distribution tenants and/or standard distributions, we recommend that you use
    the [UpdateDomainAssociation](https://docs.aws.amazon.com/cloudfront/latest/APIReference/API_UpdateDomainAssociation.html)
    API operation instead.

Associates an alias with a CloudFront standard distribution. An alias is commonly known as a
custom domain or vanity domain. It can also be called a CNAME or alternate domain name.

With this operation, you can move an alias that's already used for a standard distribution
to a different standard distribution. This prevents the downtime that could occur if you
first remove the alias from one standard distribution and then separately add the alias to
another standard distribution.

To use this operation, specify the alias and the ID of the target standard distribution.

For more information, including how to set up the target standard distribution,
prerequisites that you must complete, and other restrictions, see [Moving an alternate domain name to a different standard distribution or distribution tenant](https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/CNAMEs.html#alternate-domain-names-move)
in the *Amazon CloudFront Developer Guide*.

# Arguments

- `alias`: The alias (also known as a CNAME) to add to the target standard distribution.
- `target_distribution_id`: The ID of the standard distribution that you're associating the
  alias with.
"""
function associate_alias end

function associate_alias(
    Alias, TargetDistributionId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "PUT",
        "/2020-05-31/distribution/$(TargetDistributionId)/associate-alias",
        Dict{String,Any}("Alias" => Alias);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function associate_alias(
    Alias,
    TargetDistributionId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "PUT",
        "/2020-05-31/distribution/$(TargetDistributionId)/associate-alias",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("Alias" => Alias), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    associate_distribution_tenant_web_acl(id, web_aclarn)
    associate_distribution_tenant_web_acl(id, web_aclarn, params::Dict{String,<:Any})

Associates the WAF web ACL with a distribution tenant.

# Arguments

- `id`: The ID of the distribution tenant.
- `web_aclarn`: The Amazon Resource Name (ARN) of the WAF web ACL to associate.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"If-Match"`: The current `ETag` of the distribution tenant. This value is returned in the
  response of the `GetDistributionTenant` API operation.
"""
function associate_distribution_tenant_web_acl end

function associate_distribution_tenant_web_acl(
    Id, WebACLArn; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "PUT",
        "/2020-05-31/distribution-tenant/$(Id)/associate-web-acl",
        Dict{String,Any}("WebACLArn" => WebACLArn);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function associate_distribution_tenant_web_acl(
    Id,
    WebACLArn,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "PUT",
        "/2020-05-31/distribution-tenant/$(Id)/associate-web-acl",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("WebACLArn" => WebACLArn), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    associate_distribution_web_acl(id, web_aclarn)
    associate_distribution_web_acl(id, web_aclarn, params::Dict{String,<:Any})

Associates the WAF web ACL with a distribution.

# Arguments

- `id`: The ID of the distribution.
- `web_aclarn`: The Amazon Resource Name (ARN) of the WAF web ACL to associate.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"If-Match"`: The value of the `ETag` header that you received when retrieving the
  distribution that you're associating with the WAF web ACL.
"""
function associate_distribution_web_acl end

function associate_distribution_web_acl(
    Id, WebACLArn; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "PUT",
        "/2020-05-31/distribution/$(Id)/associate-web-acl",
        Dict{String,Any}("WebACLArn" => WebACLArn);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function associate_distribution_web_acl(
    Id,
    WebACLArn,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "PUT",
        "/2020-05-31/distribution/$(Id)/associate-web-acl",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("WebACLArn" => WebACLArn), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    copy_distribution(caller_reference, primary_distribution_id)
    copy_distribution(caller_reference, primary_distribution_id, params::Dict{String,<:Any})

Creates a staging distribution using the configuration of the provided primary distribution.
A staging distribution is a copy of an existing distribution (called the primary
distribution) that you can use in a continuous deployment workflow.

After you create a staging distribution, you can use `UpdateDistribution` to modify the
staging distribution's configuration. Then you can use `CreateContinuousDeploymentPolicy` to
incrementally move traffic to the staging distribution.

This API operation requires the following IAM permissions:

- [GetDistribution](https://docs.aws.amazon.com/cloudfront/latest/APIReference/API_GetDistribution.html)
- [CreateDistribution](https://docs.aws.amazon.com/cloudfront/latest/APIReference/API_CreateDistribution.html)
- [CopyDistribution](https://docs.aws.amazon.com/cloudfront/latest/APIReference/API_CopyDistribution.html)

# Arguments

- `caller_reference`: A value that uniquely identifies a request to create a resource. This
  helps to prevent CloudFront from creating a duplicate resource if you accidentally
  resubmit an identical request.
- `primary_distribution_id`: The identifier of the primary distribution whose configuration
  you are copying. To get a distribution ID, use `ListDistributions`.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Enabled"`: A Boolean flag to specify the state of the staging distribution when it's
  created. When you set this value to `True`, the staging distribution is enabled. When you
  set this value to `False`, the staging distribution is disabled.

  If you omit this field, the default value is `True`.

- `"If-Match"`: The version identifier of the primary distribution whose configuration you
  are copying. This is the `ETag` value returned in the response to `GetDistribution` and
  `GetDistributionConfig`.

- `"Staging"`: The type of distribution that your primary distribution will be copied to.
  The only valid value is `True`, indicating that you are copying to a staging distribution.
"""
function copy_distribution end

function copy_distribution(
    CallerReference,
    PrimaryDistributionId;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "POST",
        "/2020-05-31/distribution/$(PrimaryDistributionId)/copy",
        Dict{String,Any}("CallerReference" => CallerReference);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function copy_distribution(
    CallerReference,
    PrimaryDistributionId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "POST",
        "/2020-05-31/distribution/$(PrimaryDistributionId)/copy",
        Dict{String,Any}(
            mergewith(
                _merge, Dict{String,Any}("CallerReference" => CallerReference), params
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_anycast_ip_list(ip_count, name)
    create_anycast_ip_list(ip_count, name, params::Dict{String,<:Any})

Creates an Anycast static IP list.

# Arguments

- `ip_count`: The number of static IP addresses that are allocated to the Anycast static IP
  list. Valid values: 21 or 3.
- `name`: Name of the Anycast static IP list.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"IpAddressType"`: The IP address type for the Anycast static IP list. You can specify one
  of the following options:

  - `ipv4` only
  - `ipv6` only
  - `dualstack` - Allocate a list of both IPv4 and IPv6 addresses

- `"IpamCidrConfigs"`: A list of IPAM CIDR configurations that specify the IP address ranges
  and IPAM pool settings for creating the Anycast static IP list.

- `"Tags"`:
"""
function create_anycast_ip_list end

function create_anycast_ip_list(
    IpCount, Name; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "POST",
        "/2020-05-31/anycast-ip-list",
        Dict{String,Any}("IpCount" => IpCount, "Name" => Name);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_anycast_ip_list(
    IpCount,
    Name,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "POST",
        "/2020-05-31/anycast-ip-list",
        Dict{String,Any}(
            mergewith(
                _merge, Dict{String,Any}("IpCount" => IpCount, "Name" => Name), params
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_cache_policy(cache_policy_config)
    create_cache_policy(cache_policy_config, params::Dict{String,<:Any})

Creates a cache policy.

After you create a cache policy, you can attach it to one or more cache behaviors. When it's
attached to a cache behavior, the cache policy determines the following:

- The values that CloudFront includes in the *cache key*. These values can include HTTP
  headers, cookies, and URL query strings. CloudFront uses the cache key to find an object
  in its cache that it can return to the viewer.
- The default, minimum, and maximum time to live (TTL) values that you want objects to stay
  in the CloudFront cache.

  !!! important
      If your minimum TTL is greater than 0, CloudFront will cache content for at least the
      duration specified in the cache policy's minimum TTL, even if the
      `Cache-Control: no-cache`, `no-store`, or `private` directives are present in the
      origin headers.

The headers, cookies, and query strings that are included in the cache key are also included
in requests that CloudFront sends to the origin. CloudFront sends a request when it can't
find an object in its cache that matches the request's cache key. If you want to send values
to the origin but *not* include them in the cache key, use `OriginRequestPolicy`.

For more information about cache policies, see [Controlling the cache key](https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/controlling-the-cache-key.html)
in the *Amazon CloudFront Developer Guide*.

# Arguments

- `cache_policy_config`: A cache policy configuration.
"""
function create_cache_policy end

function create_cache_policy(
    CachePolicyConfig; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "POST",
        "/2020-05-31/cache-policy",
        Dict{String,Any}("CachePolicyConfig" => CachePolicyConfig);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_cache_policy(
    CachePolicyConfig,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "POST",
        "/2020-05-31/cache-policy",
        Dict{String,Any}(
            mergewith(
                _merge, Dict{String,Any}("CachePolicyConfig" => CachePolicyConfig), params
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_cloud_front_origin_access_identity(cloud_front_origin_access_identity_config)
    create_cloud_front_origin_access_identity(cloud_front_origin_access_identity_config, params::Dict{String,<:Any})

Creates a new origin access identity. If you're using Amazon S3 for your origin, you can use
an origin access identity to require users to access your content using a CloudFront URL
instead of the Amazon S3 URL. For more information about how to use origin access
identities, see [Serving Private Content through CloudFront](https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/PrivateContent.html)
in the *Amazon CloudFront Developer Guide*.

# Arguments

- `cloud_front_origin_access_identity_config`: The current configuration information for the
  identity.
"""
function create_cloud_front_origin_access_identity end

function create_cloud_front_origin_access_identity(
    CloudFrontOriginAccessIdentityConfig; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "POST",
        "/2020-05-31/origin-access-identity/cloudfront",
        Dict{String,Any}(
            "CloudFrontOriginAccessIdentityConfig" => CloudFrontOriginAccessIdentityConfig
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_cloud_front_origin_access_identity(
    CloudFrontOriginAccessIdentityConfig,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "POST",
        "/2020-05-31/origin-access-identity/cloudfront",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "CloudFrontOriginAccessIdentityConfig" =>
                        CloudFrontOriginAccessIdentityConfig,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_connection_function(connection_function_code, connection_function_config, name)
    create_connection_function(connection_function_code, connection_function_config, name, params::Dict{String,<:Any})

Creates a connection function.

# Arguments

- `connection_function_code`: The code for the connection function.
- `connection_function_config`:
- `name`: A name for the connection function.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Tags"`:
"""
function create_connection_function end

function create_connection_function(
    ConnectionFunctionCode,
    ConnectionFunctionConfig,
    Name;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "POST",
        "/2020-05-31/connection-function",
        Dict{String,Any}(
            "ConnectionFunctionCode" => ConnectionFunctionCode,
            "ConnectionFunctionConfig" => ConnectionFunctionConfig,
            "Name" => Name,
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_connection_function(
    ConnectionFunctionCode,
    ConnectionFunctionConfig,
    Name,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "POST",
        "/2020-05-31/connection-function",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "ConnectionFunctionCode" => ConnectionFunctionCode,
                    "ConnectionFunctionConfig" => ConnectionFunctionConfig,
                    "Name" => Name,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_connection_group(name)
    create_connection_group(name, params::Dict{String,<:Any})

Creates a connection group.

# Arguments

- `name`: The name of the connection group. Enter a friendly identifier that is unique
  within your Amazon Web Services account. This name can't be updated after you create the
  connection group.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"AnycastIpListId"`: The ID of the Anycast static IP list.
- `"Enabled"`: Enable the connection group.
- `"Ipv6Enabled"`: Enable IPv6 for the connection group. The default is `true`. For more
  information, see [Enable IPv6](https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/distribution-web-values-specify.html#DownloadDistValuesEnableIPv6)
  in the *Amazon CloudFront Developer Guide*.
- `"Tags"`:
"""
function create_connection_group end

function create_connection_group(Name; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "POST",
        "/2020-05-31/connection-group",
        Dict{String,Any}("Name" => Name);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_connection_group(
    Name, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "POST",
        "/2020-05-31/connection-group",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("Name" => Name), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_continuous_deployment_policy(continuous_deployment_policy_config)
    create_continuous_deployment_policy(continuous_deployment_policy_config, params::Dict{String,<:Any})

Creates a continuous deployment policy that distributes traffic for a custom domain name to
two different CloudFront distributions.

To use a continuous deployment policy, first use `CopyDistribution` to create a staging
distribution, then use `UpdateDistribution` to modify the staging distribution's
configuration.

After you create and update a staging distribution, you can use a continuous deployment
policy to incrementally move traffic to the staging distribution. This workflow enables you
to test changes to a distribution's configuration before moving all of your domain's
production traffic to the new configuration.

# Arguments

- `continuous_deployment_policy_config`: Contains the configuration for a continuous
  deployment policy.
"""
function create_continuous_deployment_policy end

function create_continuous_deployment_policy(
    ContinuousDeploymentPolicyConfig; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "POST",
        "/2020-05-31/continuous-deployment-policy",
        Dict{String,Any}(
            "ContinuousDeploymentPolicyConfig" => ContinuousDeploymentPolicyConfig
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_continuous_deployment_policy(
    ContinuousDeploymentPolicyConfig,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "POST",
        "/2020-05-31/continuous-deployment-policy",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "ContinuousDeploymentPolicyConfig" => ContinuousDeploymentPolicyConfig
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_distribution(distribution_config)
    create_distribution(distribution_config, params::Dict{String,<:Any})

Creates a CloudFront distribution.

# Arguments

- `distribution_config`: The distribution's configuration information.
"""
function create_distribution end

function create_distribution(
    DistributionConfig; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "POST",
        "/2020-05-31/distribution",
        Dict{String,Any}("DistributionConfig" => DistributionConfig);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_distribution(
    DistributionConfig,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "POST",
        "/2020-05-31/distribution",
        Dict{String,Any}(
            mergewith(
                _merge, Dict{String,Any}("DistributionConfig" => DistributionConfig), params
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_distribution_tenant(distribution_id, domains, name)
    create_distribution_tenant(distribution_id, domains, name, params::Dict{String,<:Any})

Creates a distribution tenant.

# Arguments

- `distribution_id`: The ID of the multi-tenant distribution to use for creating the
  distribution tenant.
- `domains`: The domains associated with the distribution tenant. You must specify at least
  one domain in the request.
- `name`: The name of the distribution tenant. Enter a friendly identifier that is unique
  within your Amazon Web Services account. This name can't be updated after you create the
  distribution tenant.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"ConnectionGroupId"`: The ID of the connection group to associate with the distribution
  tenant.

- `"Customizations"`: Customizations for the distribution tenant. For each distribution
  tenant, you can specify the geographic restrictions, and the Amazon Resource Names (ARNs)
  for the ACM certificate and WAF web ACL. These are specific values that you can override
  or disable from the multi-tenant distribution that was used to create the distribution
  tenant.

- `"Enabled"`: Indicates whether the distribution tenant should be enabled when created. If
  the distribution tenant is disabled, the distribution tenant won't serve traffic.

- `"ManagedCertificateRequest"`: The configuration for the CloudFront managed ACM
  certificate request.

- `"Parameters"`: A list of parameter values to add to the resource. A parameter is
  specified as a key-value pair. A valid parameter value must exist for any parameter that
  is marked as required in the multi-tenant distribution.

- `"Tags"`:
"""
function create_distribution_tenant end

function create_distribution_tenant(
    DistributionId, Domains, Name; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "POST",
        "/2020-05-31/distribution-tenant",
        Dict{String,Any}(
            "DistributionId" => DistributionId, "Domains" => Domains, "Name" => Name
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_distribution_tenant(
    DistributionId,
    Domains,
    Name,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "POST",
        "/2020-05-31/distribution-tenant",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "DistributionId" => DistributionId, "Domains" => Domains, "Name" => Name
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_distribution_with_tags(distribution_config_with_tags)
    create_distribution_with_tags(distribution_config_with_tags, params::Dict{String,<:Any})

Create a new distribution with tags. This API operation requires the following IAM
permissions:

- [CreateDistribution](https://docs.aws.amazon.com/cloudfront/latest/APIReference/API_CreateDistribution.html)
- [TagResource](https://docs.aws.amazon.com/cloudfront/latest/APIReference/API_TagResource.html)

# Arguments

- `distribution_config_with_tags`: The distribution's configuration information.
"""
function create_distribution_with_tags end

function create_distribution_with_tags(
    DistributionConfigWithTags; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "POST",
        "/2020-05-31/distribution?WithTags",
        Dict{String,Any}("DistributionConfigWithTags" => DistributionConfigWithTags);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_distribution_with_tags(
    DistributionConfigWithTags,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "POST",
        "/2020-05-31/distribution?WithTags",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "DistributionConfigWithTags" => DistributionConfigWithTags
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_field_level_encryption_config(field_level_encryption_config)
    create_field_level_encryption_config(field_level_encryption_config, params::Dict{String,<:Any})

Create a new field-level encryption configuration.

# Arguments

- `field_level_encryption_config`: The request to create a new field-level encryption
  configuration.
"""
function create_field_level_encryption_config end

function create_field_level_encryption_config(
    FieldLevelEncryptionConfig; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "POST",
        "/2020-05-31/field-level-encryption",
        Dict{String,Any}("FieldLevelEncryptionConfig" => FieldLevelEncryptionConfig);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_field_level_encryption_config(
    FieldLevelEncryptionConfig,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "POST",
        "/2020-05-31/field-level-encryption",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "FieldLevelEncryptionConfig" => FieldLevelEncryptionConfig
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_field_level_encryption_profile(field_level_encryption_profile_config)
    create_field_level_encryption_profile(field_level_encryption_profile_config, params::Dict{String,<:Any})

Create a field-level encryption profile.

# Arguments

- `field_level_encryption_profile_config`: The request to create a field-level encryption
  profile.
"""
function create_field_level_encryption_profile end

function create_field_level_encryption_profile(
    FieldLevelEncryptionProfileConfig; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "POST",
        "/2020-05-31/field-level-encryption-profile",
        Dict{String,Any}(
            "FieldLevelEncryptionProfileConfig" => FieldLevelEncryptionProfileConfig
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_field_level_encryption_profile(
    FieldLevelEncryptionProfileConfig,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "POST",
        "/2020-05-31/field-level-encryption-profile",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "FieldLevelEncryptionProfileConfig" => FieldLevelEncryptionProfileConfig
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_function(function_code, function_config, name)
    create_function(function_code, function_config, name, params::Dict{String,<:Any})

Creates a CloudFront function.

To create a function, you provide the function code and some configuration information about
the function. The response contains an Amazon Resource Name (ARN) that uniquely identifies
the function.

When you create a function, it's in the `DEVELOPMENT` stage. In this stage, you can test the
function with `TestFunction`, and update it with `UpdateFunction`.

When you're ready to use your function with a CloudFront distribution, use `PublishFunction`
to copy the function from the `DEVELOPMENT` stage to `LIVE`. When it's live, you can attach
the function to a distribution's cache behavior, using the function's ARN.

# Arguments

- `function_code`: The function code. For more information about writing a CloudFront
  function, see [Writing function code for CloudFront Functions](https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/writing-function-code.html)
  in the *Amazon CloudFront Developer Guide*.
- `function_config`: Configuration information about the function, including an optional
  comment and the function's runtime.
- `name`: A name to identify the function.
"""
function create_function end

function create_function(
    FunctionCode, FunctionConfig, Name; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "POST",
        "/2020-05-31/function",
        Dict{String,Any}(
            "FunctionCode" => FunctionCode,
            "FunctionConfig" => FunctionConfig,
            "Name" => Name,
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_function(
    FunctionCode,
    FunctionConfig,
    Name,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "POST",
        "/2020-05-31/function",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "FunctionCode" => FunctionCode,
                    "FunctionConfig" => FunctionConfig,
                    "Name" => Name,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_invalidation(distribution_id, invalidation_batch)
    create_invalidation(distribution_id, invalidation_batch, params::Dict{String,<:Any})

Create a new invalidation. For more information, see [Invalidating files](https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/Invalidation.html)
in the *Amazon CloudFront Developer Guide*.

# Arguments

- `distribution_id`: The distribution's id.
- `invalidation_batch`: The batch information for the invalidation.
"""
function create_invalidation end

function create_invalidation(
    DistributionId, InvalidationBatch; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "POST",
        "/2020-05-31/distribution/$(DistributionId)/invalidation",
        Dict{String,Any}("InvalidationBatch" => InvalidationBatch);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_invalidation(
    DistributionId,
    InvalidationBatch,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "POST",
        "/2020-05-31/distribution/$(DistributionId)/invalidation",
        Dict{String,Any}(
            mergewith(
                _merge, Dict{String,Any}("InvalidationBatch" => InvalidationBatch), params
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_invalidation_for_distribution_tenant(id, invalidation_batch)
    create_invalidation_for_distribution_tenant(id, invalidation_batch, params::Dict{String,<:Any})

Creates an invalidation for a distribution tenant. For more information, see [Invalidating files](https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/Invalidation.html)
in the *Amazon CloudFront Developer Guide*.

# Arguments

- `id`: The ID of the distribution tenant.
- `invalidation_batch`:
"""
function create_invalidation_for_distribution_tenant end

function create_invalidation_for_distribution_tenant(
    Id, InvalidationBatch; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "POST",
        "/2020-05-31/distribution-tenant/$(Id)/invalidation",
        Dict{String,Any}("InvalidationBatch" => InvalidationBatch);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_invalidation_for_distribution_tenant(
    Id,
    InvalidationBatch,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "POST",
        "/2020-05-31/distribution-tenant/$(Id)/invalidation",
        Dict{String,Any}(
            mergewith(
                _merge, Dict{String,Any}("InvalidationBatch" => InvalidationBatch), params
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_key_group(key_group_config)
    create_key_group(key_group_config, params::Dict{String,<:Any})

Creates a key group that you can use with [CloudFront signed URLs and signed cookies](https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/PrivateContent.html).

To create a key group, you must specify at least one public key for the key group. After you
create a key group, you can reference it from one or more cache behaviors. When you
reference a key group in a cache behavior, CloudFront requires signed URLs or signed cookies
for all requests that match the cache behavior. The URLs or cookies must be signed with a
private key whose corresponding public key is in the key group. The signed URL or cookie
contains information about which public key CloudFront should use to verify the signature.
For more information, see [Serving private content](https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/PrivateContent.html)
in the *Amazon CloudFront Developer Guide*.

# Arguments

- `key_group_config`: A key group configuration.
"""
function create_key_group end

function create_key_group(
    KeyGroupConfig; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "POST",
        "/2020-05-31/key-group",
        Dict{String,Any}("KeyGroupConfig" => KeyGroupConfig);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_key_group(
    KeyGroupConfig,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "POST",
        "/2020-05-31/key-group",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("KeyGroupConfig" => KeyGroupConfig), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_key_value_store(name)
    create_key_value_store(name, params::Dict{String,<:Any})

Specifies the key value store resource to add to your account. In your account, the key
value store names must be unique. You can also import key value store data in JSON format
from an S3 bucket by providing a valid `ImportSource` that you own.

# Arguments

- `name`: The name of the key value store. The minimum length is 1 character and the maximum
  length is 64 characters.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Comment"`: The comment of the key value store.
- `"ImportSource"`: The S3 bucket that provides the source for the import. The source must
  be in a valid JSON format.
"""
function create_key_value_store end

function create_key_value_store(Name; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "POST",
        "/2020-05-31/key-value-store",
        Dict{String,Any}("Name" => Name);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_key_value_store(
    Name, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "POST",
        "/2020-05-31/key-value-store",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("Name" => Name), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_monitoring_subscription(distribution_id, monitoring_subscription)
    create_monitoring_subscription(distribution_id, monitoring_subscription, params::Dict{String,<:Any})

Enables or disables additional Amazon CloudWatch metrics for the specified CloudFront
distribution. The additional metrics incur an additional cost.

For more information, see [Viewing additional CloudFront distribution metrics](https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/viewing-cloudfront-metrics.html#monitoring-console.distributions-additional)
in the *Amazon CloudFront Developer Guide*.

# Arguments

- `distribution_id`: The ID of the distribution that you are enabling metrics for.
- `monitoring_subscription`: A monitoring subscription. This structure contains information
  about whether additional CloudWatch metrics are enabled for a given CloudFront
  distribution.
"""
function create_monitoring_subscription end

function create_monitoring_subscription(
    DistributionId,
    MonitoringSubscription;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "POST",
        "/2020-05-31/distributions/$(DistributionId)/monitoring-subscription",
        Dict{String,Any}("MonitoringSubscription" => MonitoringSubscription);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_monitoring_subscription(
    DistributionId,
    MonitoringSubscription,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "POST",
        "/2020-05-31/distributions/$(DistributionId)/monitoring-subscription",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("MonitoringSubscription" => MonitoringSubscription),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_origin_access_control(origin_access_control_config)
    create_origin_access_control(origin_access_control_config, params::Dict{String,<:Any})

Creates a new origin access control in CloudFront. After you create an origin access
control, you can add it to an origin in a CloudFront distribution so that CloudFront sends
authenticated (signed) requests to the origin.

This makes it possible to block public access to the origin, allowing viewers (users) to
access the origin's content only through CloudFront.

For more information about using a CloudFront origin access control, see [Restricting access to an Amazon Web Services origin](https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/private-content-restricting-access-to-origin.html)
in the *Amazon CloudFront Developer Guide*.

# Arguments

- `origin_access_control_config`: Contains the origin access control.
"""
function create_origin_access_control end

function create_origin_access_control(
    OriginAccessControlConfig; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "POST",
        "/2020-05-31/origin-access-control",
        Dict{String,Any}("OriginAccessControlConfig" => OriginAccessControlConfig);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_origin_access_control(
    OriginAccessControlConfig,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "POST",
        "/2020-05-31/origin-access-control",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("OriginAccessControlConfig" => OriginAccessControlConfig),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_origin_request_policy(origin_request_policy_config)
    create_origin_request_policy(origin_request_policy_config, params::Dict{String,<:Any})

Creates an origin request policy.

After you create an origin request policy, you can attach it to one or more cache behaviors.
When it's attached to a cache behavior, the origin request policy determines the values that
CloudFront includes in requests that it sends to the origin. Each request that CloudFront
sends to the origin includes the following:

- The request body and the URL path (without the domain name) from the viewer request.
- The headers that CloudFront automatically includes in every origin request, including
  `Host`, `User-Agent`, and `X-Amz-Cf-Id`.
- All HTTP headers, cookies, and URL query strings that are specified in the cache policy or
  the origin request policy. These can include items from the viewer request and, in the
  case of headers, additional ones that are added by CloudFront.

CloudFront sends a request when it can't find a valid object in its cache that matches the
request. If you want to send values to the origin and also include them in the cache key,
use `CachePolicy`.

For more information about origin request policies, see [Controlling origin requests](https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/controlling-origin-requests.html)
in the *Amazon CloudFront Developer Guide*.

# Arguments

- `origin_request_policy_config`: An origin request policy configuration.
"""
function create_origin_request_policy end

function create_origin_request_policy(
    OriginRequestPolicyConfig; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "POST",
        "/2020-05-31/origin-request-policy",
        Dict{String,Any}("OriginRequestPolicyConfig" => OriginRequestPolicyConfig);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_origin_request_policy(
    OriginRequestPolicyConfig,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "POST",
        "/2020-05-31/origin-request-policy",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("OriginRequestPolicyConfig" => OriginRequestPolicyConfig),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_public_key(public_key_config)
    create_public_key(public_key_config, params::Dict{String,<:Any})

Uploads a public key to CloudFront that you can use with [signed URLs and signed cookies](https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/PrivateContent.html),
or with [field-level encryption](https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/field-level-encryption.html).

# Arguments

- `public_key_config`: A CloudFront public key configuration.
"""
function create_public_key end

function create_public_key(
    PublicKeyConfig; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "POST",
        "/2020-05-31/public-key",
        Dict{String,Any}("PublicKeyConfig" => PublicKeyConfig);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_public_key(
    PublicKeyConfig,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "POST",
        "/2020-05-31/public-key",
        Dict{String,Any}(
            mergewith(
                _merge, Dict{String,Any}("PublicKeyConfig" => PublicKeyConfig), params
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_realtime_log_config(end_points, field, name, sampling_rate)
    create_realtime_log_config(end_points, field, name, sampling_rate, params::Dict{String,<:Any})

Creates a real-time log configuration.

After you create a real-time log configuration, you can attach it to one or more cache
behaviors to send real-time log data to the specified Amazon Kinesis data stream.

For more information about real-time log configurations, see [Real-time logs](https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/real-time-logs.html)
in the *Amazon CloudFront Developer Guide*.

# Arguments

- `end_points`: Contains information about the Amazon Kinesis data stream where you are
  sending real-time log data.

- `field`: A list of fields to include in each real-time log record.

  For more information about fields, see [Real-time log configuration fields](https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/real-time-logs.html#understand-real-time-log-config-fields)
  in the *Amazon CloudFront Developer Guide*.

- `name`: A unique name to identify this real-time log configuration.

- `sampling_rate`: The sampling rate for this real-time log configuration. You can specify a
  whole number between 1 and 100 (inclusive) to determine the percentage of viewer requests
  that are represented in the real-time log data.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Fields"`: A list of fields to include in each real-time log record.

  For more information about fields, see [Real-time log configuration fields](https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/real-time-logs.html#understand-real-time-log-config-fields)
  in the *Amazon CloudFront Developer Guide*.
"""
function create_realtime_log_config end

function create_realtime_log_config(
    EndPoints, Field, Name, SamplingRate; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "POST",
        "/2020-05-31/realtime-log-config",
        Dict{String,Any}(
            "EndPoints" => EndPoints,
            "Field" => Field,
            "Name" => Name,
            "SamplingRate" => SamplingRate,
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_realtime_log_config(
    EndPoints,
    Field,
    Name,
    SamplingRate,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "POST",
        "/2020-05-31/realtime-log-config",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "EndPoints" => EndPoints,
                    "Field" => Field,
                    "Name" => Name,
                    "SamplingRate" => SamplingRate,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_response_headers_policy(response_headers_policy_config)
    create_response_headers_policy(response_headers_policy_config, params::Dict{String,<:Any})

Creates a response headers policy.

A response headers policy contains information about a set of HTTP headers. To create a
response headers policy, you provide some metadata about the policy and a set of
configurations that specify the headers.

After you create a response headers policy, you can use its ID to attach it to one or more
cache behaviors in a CloudFront distribution. When it's attached to a cache behavior, the
response headers policy affects the HTTP headers that CloudFront includes in HTTP responses
to requests that match the cache behavior. CloudFront adds or removes response headers
according to the configuration of the response headers policy.

For more information, see [Adding or removing HTTP headers in CloudFront responses](https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/modifying-response-headers.html)
in the *Amazon CloudFront Developer Guide*.

# Arguments

- `response_headers_policy_config`: Contains metadata about the response headers policy, and
  a set of configurations that specify the HTTP headers.
"""
function create_response_headers_policy end

function create_response_headers_policy(
    ResponseHeadersPolicyConfig; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "POST",
        "/2020-05-31/response-headers-policy",
        Dict{String,Any}("ResponseHeadersPolicyConfig" => ResponseHeadersPolicyConfig);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_response_headers_policy(
    ResponseHeadersPolicyConfig,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "POST",
        "/2020-05-31/response-headers-policy",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "ResponseHeadersPolicyConfig" => ResponseHeadersPolicyConfig
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_streaming_distribution(streaming_distribution_config)
    create_streaming_distribution(streaming_distribution_config, params::Dict{String,<:Any})

This API is deprecated. Amazon CloudFront is deprecating real-time messaging protocol (RTMP)
distributions on December 31, 2020. For more information, [read the announcement](http://forums.aws.amazon.com/ann.jspa?annID=7356)
on the Amazon CloudFront discussion forum.

# Arguments

- `streaming_distribution_config`: The streaming distribution's configuration information.
"""
function create_streaming_distribution end

function create_streaming_distribution(
    StreamingDistributionConfig; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "POST",
        "/2020-05-31/streaming-distribution",
        Dict{String,Any}("StreamingDistributionConfig" => StreamingDistributionConfig);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_streaming_distribution(
    StreamingDistributionConfig,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "POST",
        "/2020-05-31/streaming-distribution",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "StreamingDistributionConfig" => StreamingDistributionConfig
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_streaming_distribution_with_tags(streaming_distribution_config_with_tags)
    create_streaming_distribution_with_tags(streaming_distribution_config_with_tags, params::Dict{String,<:Any})

This API is deprecated. Amazon CloudFront is deprecating real-time messaging protocol (RTMP)
distributions on December 31, 2020. For more information, [read the announcement](http://forums.aws.amazon.com/ann.jspa?annID=7356)
on the Amazon CloudFront discussion forum.

# Arguments

- `streaming_distribution_config_with_tags`: The streaming distribution's configuration
  information.
"""
function create_streaming_distribution_with_tags end

function create_streaming_distribution_with_tags(
    StreamingDistributionConfigWithTags; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "POST",
        "/2020-05-31/streaming-distribution?WithTags",
        Dict{String,Any}(
            "StreamingDistributionConfigWithTags" => StreamingDistributionConfigWithTags
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_streaming_distribution_with_tags(
    StreamingDistributionConfigWithTags,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "POST",
        "/2020-05-31/streaming-distribution?WithTags",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "StreamingDistributionConfigWithTags" =>
                        StreamingDistributionConfigWithTags,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_trust_store(ca_certificates_bundle_source, name)
    create_trust_store(ca_certificates_bundle_source, name, params::Dict{String,<:Any})

Creates a trust store.

# Arguments

- `ca_certificates_bundle_source`: The CA certificates bundle source for the trust store.
- `name`: A name for the trust store.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Tags"`:
"""
function create_trust_store end

function create_trust_store(
    CaCertificatesBundleSource, Name; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "POST",
        "/2020-05-31/trust-store",
        Dict{String,Any}(
            "CaCertificatesBundleSource" => CaCertificatesBundleSource, "Name" => Name
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_trust_store(
    CaCertificatesBundleSource,
    Name,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "POST",
        "/2020-05-31/trust-store",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "CaCertificatesBundleSource" => CaCertificatesBundleSource,
                    "Name" => Name,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    create_vpc_origin(vpc_origin_endpoint_config)
    create_vpc_origin(vpc_origin_endpoint_config, params::Dict{String,<:Any})

Create an Amazon CloudFront VPC origin.

# Arguments

- `vpc_origin_endpoint_config`: The VPC origin endpoint configuration.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Tags"`:
"""
function create_vpc_origin end

function create_vpc_origin(
    VpcOriginEndpointConfig; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "POST",
        "/2020-05-31/vpc-origin",
        Dict{String,Any}("VpcOriginEndpointConfig" => VpcOriginEndpointConfig);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function create_vpc_origin(
    VpcOriginEndpointConfig,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "POST",
        "/2020-05-31/vpc-origin",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("VpcOriginEndpointConfig" => VpcOriginEndpointConfig),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_anycast_ip_list(id, if-_match)
    delete_anycast_ip_list(id, if-_match, params::Dict{String,<:Any})

Deletes an Anycast static IP list.

# Arguments

- `id`: The ID of the Anycast static IP list.
- `if-_match`: The current version (`ETag` value) of the Anycast static IP list that you are
  deleting.
"""
function delete_anycast_ip_list end

function delete_anycast_ip_list(
    Id, If_Match; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "DELETE",
        "/2020-05-31/anycast-ip-list/$(Id)",
        Dict{String,Any}("headers" => Dict{String,Any}("If-Match" => If_Match));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_anycast_ip_list(
    Id,
    If_Match,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "DELETE",
        "/2020-05-31/anycast-ip-list/$(Id)",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("headers" => Dict{String,Any}("If-Match" => If_Match)),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_cache_policy(id)
    delete_cache_policy(id, params::Dict{String,<:Any})

Deletes a cache policy.

You cannot delete a cache policy if it's attached to a cache behavior. First update your
distributions to remove the cache policy from all cache behaviors, then delete the cache
policy.

To delete a cache policy, you must provide the policy's identifier and version. To get these
values, you can use `ListCachePolicies` or `GetCachePolicy`.

# Arguments

- `id`: The unique identifier for the cache policy that you are deleting. To get the
  identifier, you can use `ListCachePolicies`.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"If-Match"`: The version of the cache policy that you are deleting. The version is the
  cache policy's `ETag` value, which you can get using `ListCachePolicies`,
  `GetCachePolicy`, or `GetCachePolicyConfig`.
"""
function delete_cache_policy end

function delete_cache_policy(Id; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "DELETE",
        "/2020-05-31/cache-policy/$(Id)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_cache_policy(
    Id, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "DELETE",
        "/2020-05-31/cache-policy/$(Id)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_cloud_front_origin_access_identity(id)
    delete_cloud_front_origin_access_identity(id, params::Dict{String,<:Any})

Delete an origin access identity.

# Arguments

- `id`: The origin access identity's ID.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"If-Match"`: The value of the `ETag` header you received from a previous `GET` or `PUT`
  request. For example: `E2QWRUHAPOMQZL`.
"""
function delete_cloud_front_origin_access_identity end

function delete_cloud_front_origin_access_identity(
    Id; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "DELETE",
        "/2020-05-31/origin-access-identity/cloudfront/$(Id)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_cloud_front_origin_access_identity(
    Id, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "DELETE",
        "/2020-05-31/origin-access-identity/cloudfront/$(Id)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_connection_function(id, if-_match)
    delete_connection_function(id, if-_match, params::Dict{String,<:Any})

Deletes a connection function.

# Arguments

- `id`: The connection function's ID.
- `if-_match`: The current version (`ETag` value) of the connection function you are
  deleting.
"""
function delete_connection_function end

function delete_connection_function(
    Id, If_Match; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "DELETE",
        "/2020-05-31/connection-function/$(Id)",
        Dict{String,Any}("headers" => Dict{String,Any}("If-Match" => If_Match));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_connection_function(
    Id,
    If_Match,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "DELETE",
        "/2020-05-31/connection-function/$(Id)",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("headers" => Dict{String,Any}("If-Match" => If_Match)),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_connection_group(id, if-_match)
    delete_connection_group(id, if-_match, params::Dict{String,<:Any})

Deletes a connection group.

# Arguments

- `id`: The ID of the connection group to delete.
- `if-_match`: The value of the `ETag` header that you received when retrieving the
  connection group to delete.
"""
function delete_connection_group end

function delete_connection_group(
    Id, If_Match; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "DELETE",
        "/2020-05-31/connection-group/$(Id)",
        Dict{String,Any}("headers" => Dict{String,Any}("If-Match" => If_Match));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_connection_group(
    Id,
    If_Match,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "DELETE",
        "/2020-05-31/connection-group/$(Id)",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("headers" => Dict{String,Any}("If-Match" => If_Match)),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_continuous_deployment_policy(id)
    delete_continuous_deployment_policy(id, params::Dict{String,<:Any})

Deletes a continuous deployment policy.

You cannot delete a continuous deployment policy that's attached to a primary distribution.
First update your distribution to remove the continuous deployment policy, then you can
delete the policy.

# Arguments

- `id`: The identifier of the continuous deployment policy that you are deleting.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"If-Match"`: The current version (`ETag` value) of the continuous deployment policy that
  you are deleting.
"""
function delete_continuous_deployment_policy end

function delete_continuous_deployment_policy(
    Id; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "DELETE",
        "/2020-05-31/continuous-deployment-policy/$(Id)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_continuous_deployment_policy(
    Id, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "DELETE",
        "/2020-05-31/continuous-deployment-policy/$(Id)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_distribution(id)
    delete_distribution(id, params::Dict{String,<:Any})

Delete a distribution.

!!! important
    Before you can delete a distribution, you must disable it, which requires permission to
    update the distribution. Once deleted, a distribution cannot be recovered.

# Arguments

- `id`: The distribution ID.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"If-Match"`: The value of the `ETag` header that you received when you disabled the
  distribution. For example: `E2QWRUHAPOMQZL`.
"""
function delete_distribution end

function delete_distribution(Id; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "DELETE",
        "/2020-05-31/distribution/$(Id)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_distribution(
    Id, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "DELETE",
        "/2020-05-31/distribution/$(Id)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_distribution_tenant(id, if-_match)
    delete_distribution_tenant(id, if-_match, params::Dict{String,<:Any})

Deletes a distribution tenant. If you use this API operation to delete a distribution tenant
that is currently enabled, the request will fail.

To delete a distribution tenant, you must first disable the distribution tenant by using the
`UpdateDistributionTenant` API operation.

# Arguments

- `id`: The ID of the distribution tenant to delete.
- `if-_match`: The value of the `ETag` header that you received when retrieving the
  distribution tenant. This value is returned in the response of the `GetDistributionTenant`
  API operation.
"""
function delete_distribution_tenant end

function delete_distribution_tenant(
    Id, If_Match; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "DELETE",
        "/2020-05-31/distribution-tenant/$(Id)",
        Dict{String,Any}("headers" => Dict{String,Any}("If-Match" => If_Match));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_distribution_tenant(
    Id,
    If_Match,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "DELETE",
        "/2020-05-31/distribution-tenant/$(Id)",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("headers" => Dict{String,Any}("If-Match" => If_Match)),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_field_level_encryption_config(id)
    delete_field_level_encryption_config(id, params::Dict{String,<:Any})

Remove a field-level encryption configuration.

# Arguments

- `id`: The ID of the configuration you want to delete from CloudFront.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"If-Match"`: The value of the `ETag` header that you received when retrieving the
  configuration identity to delete. For example: `E2QWRUHAPOMQZL`.
"""
function delete_field_level_encryption_config end

function delete_field_level_encryption_config(
    Id; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "DELETE",
        "/2020-05-31/field-level-encryption/$(Id)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_field_level_encryption_config(
    Id, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "DELETE",
        "/2020-05-31/field-level-encryption/$(Id)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_field_level_encryption_profile(id)
    delete_field_level_encryption_profile(id, params::Dict{String,<:Any})

Remove a field-level encryption profile.

# Arguments

- `id`: Request the ID of the profile you want to delete from CloudFront.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"If-Match"`: The value of the `ETag` header that you received when retrieving the profile
  to delete. For example: `E2QWRUHAPOMQZL`.
"""
function delete_field_level_encryption_profile end

function delete_field_level_encryption_profile(
    Id; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "DELETE",
        "/2020-05-31/field-level-encryption-profile/$(Id)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_field_level_encryption_profile(
    Id, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "DELETE",
        "/2020-05-31/field-level-encryption-profile/$(Id)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_function(if-_match, name)
    delete_function(if-_match, name, params::Dict{String,<:Any})

Deletes a CloudFront function.

You cannot delete a function if it's associated with a cache behavior. First, update your
distributions to remove the function association from all cache behaviors, then delete the
function.

To delete a function, you must provide the function's name and version (`ETag` value). To
get these values, you can use `ListFunctions` and `DescribeFunction`.

# Arguments

- `if-_match`: The current version (`ETag` value) of the function that you are deleting,
  which you can get using `DescribeFunction`.
- `name`: The name of the function that you are deleting.
"""
function delete_function end

function delete_function(If_Match, Name; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "DELETE",
        "/2020-05-31/function/$(Name)",
        Dict{String,Any}("headers" => Dict{String,Any}("If-Match" => If_Match));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_function(
    If_Match,
    Name,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "DELETE",
        "/2020-05-31/function/$(Name)",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("headers" => Dict{String,Any}("If-Match" => If_Match)),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_key_group(id)
    delete_key_group(id, params::Dict{String,<:Any})

Deletes a key group.

You cannot delete a key group that is referenced in a cache behavior. First update your
distributions to remove the key group from all cache behaviors, then delete the key group.

To delete a key group, you must provide the key group's identifier and version. To get these
values, use `ListKeyGroups` followed by `GetKeyGroup` or `GetKeyGroupConfig`.

# Arguments

- `id`: The identifier of the key group that you are deleting. To get the identifier, use
  `ListKeyGroups`.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"If-Match"`: The version of the key group that you are deleting. The version is the key
  group's `ETag` value. To get the `ETag`, use `GetKeyGroup` or `GetKeyGroupConfig`.
"""
function delete_key_group end

function delete_key_group(Id; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "DELETE", "/2020-05-31/key-group/$(Id)"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function delete_key_group(
    Id, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "DELETE",
        "/2020-05-31/key-group/$(Id)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_key_value_store(if-_match, name)
    delete_key_value_store(if-_match, name, params::Dict{String,<:Any})

Specifies the key value store to delete.

# Arguments

- `if-_match`: The key value store to delete, if a match occurs.
- `name`: The name of the key value store.
"""
function delete_key_value_store end

function delete_key_value_store(
    If_Match, Name; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "DELETE",
        "/2020-05-31/key-value-store/$(Name)",
        Dict{String,Any}("headers" => Dict{String,Any}("If-Match" => If_Match));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_key_value_store(
    If_Match,
    Name,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "DELETE",
        "/2020-05-31/key-value-store/$(Name)",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("headers" => Dict{String,Any}("If-Match" => If_Match)),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_monitoring_subscription(distribution_id)
    delete_monitoring_subscription(distribution_id, params::Dict{String,<:Any})

Disables additional CloudWatch metrics for the specified CloudFront distribution.

# Arguments

- `distribution_id`: The ID of the distribution that you are disabling metrics for.
"""
function delete_monitoring_subscription end

function delete_monitoring_subscription(
    DistributionId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "DELETE",
        "/2020-05-31/distributions/$(DistributionId)/monitoring-subscription";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_monitoring_subscription(
    DistributionId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "DELETE",
        "/2020-05-31/distributions/$(DistributionId)/monitoring-subscription",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_origin_access_control(id)
    delete_origin_access_control(id, params::Dict{String,<:Any})

Deletes a CloudFront origin access control.

You cannot delete an origin access control if it's in use. First, update all distributions
to remove the origin access control from all origins, then delete the origin access control.

# Arguments

- `id`: The unique identifier of the origin access control that you are deleting.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"If-Match"`: The current version (`ETag` value) of the origin access control that you are
  deleting.
"""
function delete_origin_access_control end

function delete_origin_access_control(
    Id; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "DELETE",
        "/2020-05-31/origin-access-control/$(Id)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_origin_access_control(
    Id, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "DELETE",
        "/2020-05-31/origin-access-control/$(Id)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_origin_request_policy(id)
    delete_origin_request_policy(id, params::Dict{String,<:Any})

Deletes an origin request policy.

You cannot delete an origin request policy if it's attached to any cache behaviors. First
update your distributions to remove the origin request policy from all cache behaviors, then
delete the origin request policy.

To delete an origin request policy, you must provide the policy's identifier and version. To
get the identifier, you can use `ListOriginRequestPolicies` or `GetOriginRequestPolicy`.

# Arguments

- `id`: The unique identifier for the origin request policy that you are deleting. To get
  the identifier, you can use `ListOriginRequestPolicies`.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"If-Match"`: The version of the origin request policy that you are deleting. The version
  is the origin request policy's `ETag` value, which you can get using
  `ListOriginRequestPolicies`, `GetOriginRequestPolicy`, or `GetOriginRequestPolicyConfig`.
"""
function delete_origin_request_policy end

function delete_origin_request_policy(
    Id; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "DELETE",
        "/2020-05-31/origin-request-policy/$(Id)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_origin_request_policy(
    Id, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "DELETE",
        "/2020-05-31/origin-request-policy/$(Id)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_public_key(id)
    delete_public_key(id, params::Dict{String,<:Any})

Remove a public key you previously added to CloudFront.

# Arguments

- `id`: The ID of the public key you want to remove from CloudFront.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"If-Match"`: The value of the `ETag` header that you received when retrieving the public
  key identity to delete. For example: `E2QWRUHAPOMQZL`.
"""
function delete_public_key end

function delete_public_key(Id; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "DELETE",
        "/2020-05-31/public-key/$(Id)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_public_key(
    Id, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "DELETE",
        "/2020-05-31/public-key/$(Id)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_realtime_log_config()
    delete_realtime_log_config(params::Dict{String,<:Any})

Deletes a real-time log configuration.

You cannot delete a real-time log configuration if it's attached to a cache behavior. First
update your distributions to remove the real-time log configuration from all cache
behaviors, then delete the real-time log configuration.

To delete a real-time log configuration, you can provide the configuration's name or its
Amazon Resource Name (ARN). You must provide at least one. If you provide both, CloudFront
uses the name to identify the real-time log configuration to delete.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"ARN"`: The Amazon Resource Name (ARN) of the real-time log configuration to delete.
- `"Name"`: The name of the real-time log configuration to delete.
"""
function delete_realtime_log_config end

function delete_realtime_log_config(; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "POST",
        "/2020-05-31/delete-realtime-log-config";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_realtime_log_config(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "POST",
        "/2020-05-31/delete-realtime-log-config",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_resource_policy(resource_arn)
    delete_resource_policy(resource_arn, params::Dict{String,<:Any})

Deletes the resource policy attached to the CloudFront resource.

# Arguments

- `resource_arn`: The Amazon Resource Name (ARN) of the CloudFront resource for which the
  resource policy should be deleted.
"""
function delete_resource_policy end

function delete_resource_policy(
    ResourceArn; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "POST",
        "/2020-05-31/delete-resource-policy",
        Dict{String,Any}("ResourceArn" => ResourceArn);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_resource_policy(
    ResourceArn,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "POST",
        "/2020-05-31/delete-resource-policy",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("ResourceArn" => ResourceArn), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_response_headers_policy(id)
    delete_response_headers_policy(id, params::Dict{String,<:Any})

Deletes a response headers policy.

You cannot delete a response headers policy if it's attached to a cache behavior. First
update your distributions to remove the response headers policy from all cache behaviors,
then delete the response headers policy.

To delete a response headers policy, you must provide the policy's identifier and version.
To get these values, you can use `ListResponseHeadersPolicies` or
`GetResponseHeadersPolicy`.

# Arguments

- `id`: The identifier for the response headers policy that you are deleting.

  To get the identifier, you can use `ListResponseHeadersPolicies`.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"If-Match"`: The version of the response headers policy that you are deleting.

  The version is the response headers policy's `ETag` value, which you can get using
  `ListResponseHeadersPolicies`, `GetResponseHeadersPolicy`, or
  `GetResponseHeadersPolicyConfig`.
"""
function delete_response_headers_policy end

function delete_response_headers_policy(
    Id; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "DELETE",
        "/2020-05-31/response-headers-policy/$(Id)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_response_headers_policy(
    Id, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "DELETE",
        "/2020-05-31/response-headers-policy/$(Id)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_streaming_distribution(id)
    delete_streaming_distribution(id, params::Dict{String,<:Any})

Delete a streaming distribution. To delete an RTMP distribution using the CloudFront API,
perform the following steps.

**To delete an RTMP distribution using the CloudFront API**:

1. Disable the RTMP distribution.
2. Submit a `GET Streaming Distribution Config` request to get the current configuration and
   the `Etag` header for the distribution.
3. Update the XML document that was returned in the response to your
   `GET Streaming Distribution Config` request to change the value of `Enabled` to `false`.
4. Submit a `PUT Streaming Distribution Config` request to update the configuration for your
   distribution. In the request body, include the XML document that you updated in Step 3.
   Then set the value of the HTTP `If-Match` header to the value of the `ETag` header that
   CloudFront returned when you submitted the `GET Streaming Distribution Config` request in
   Step 2.
5. Review the response to the `PUT Streaming Distribution Config` request to confirm that
   the distribution was successfully disabled.
6. Submit a `GET Streaming Distribution Config` request to confirm that your changes have
   propagated. When propagation is complete, the value of `Status` is `Deployed`.
7. Submit a `DELETE Streaming Distribution` request. Set the value of the HTTP `If-Match`
   header to the value of the `ETag` header that CloudFront returned when you submitted the
   `GET Streaming Distribution Config` request in Step 2.
8. Review the response to your `DELETE Streaming Distribution` request to confirm that the
   distribution was successfully deleted.

For information about deleting a distribution using the CloudFront console, see [Deleting a Distribution](https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/HowToDeleteDistribution.html)
in the *Amazon CloudFront Developer Guide*.

# Arguments

- `id`: The distribution ID.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"If-Match"`: The value of the `ETag` header that you received when you disabled the
  streaming distribution. For example: `E2QWRUHAPOMQZL`.
"""
function delete_streaming_distribution end

function delete_streaming_distribution(
    Id; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "DELETE",
        "/2020-05-31/streaming-distribution/$(Id)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_streaming_distribution(
    Id, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "DELETE",
        "/2020-05-31/streaming-distribution/$(Id)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_trust_store(id, if-_match)
    delete_trust_store(id, if-_match, params::Dict{String,<:Any})

Deletes a trust store.

# Arguments

- `id`: The trust store's ID.
- `if-_match`: The current version (`ETag` value) of the trust store you are deleting.
"""
function delete_trust_store end

function delete_trust_store(
    Id, If_Match; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "DELETE",
        "/2020-05-31/trust-store/$(Id)",
        Dict{String,Any}("headers" => Dict{String,Any}("If-Match" => If_Match));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_trust_store(
    Id,
    If_Match,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "DELETE",
        "/2020-05-31/trust-store/$(Id)",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("headers" => Dict{String,Any}("If-Match" => If_Match)),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    delete_vpc_origin(id, if-_match)
    delete_vpc_origin(id, if-_match, params::Dict{String,<:Any})

Delete an Amazon CloudFront VPC origin.

# Arguments

- `id`: The VPC origin ID.
- `if-_match`: The version identifier of the VPC origin to delete. This is the `ETag` value
  returned in the response to `GetVpcOrigin`.
"""
function delete_vpc_origin end

function delete_vpc_origin(Id, If_Match; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "DELETE",
        "/2020-05-31/vpc-origin/$(Id)",
        Dict{String,Any}("headers" => Dict{String,Any}("If-Match" => If_Match));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function delete_vpc_origin(
    Id,
    If_Match,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "DELETE",
        "/2020-05-31/vpc-origin/$(Id)",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("headers" => Dict{String,Any}("If-Match" => If_Match)),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    describe_connection_function(identifier)
    describe_connection_function(identifier, params::Dict{String,<:Any})

Describes a connection function.

# Arguments

- `identifier`: The connection function's identifier.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Stage"`: The connection function's stage.
"""
function describe_connection_function end

function describe_connection_function(
    Identifier; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/connection-function/$(Identifier)/describe";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function describe_connection_function(
    Identifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "GET",
        "/2020-05-31/connection-function/$(Identifier)/describe",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    describe_function(name)
    describe_function(name, params::Dict{String,<:Any})

Gets configuration information and metadata about a CloudFront function, but not the
function's code. To get a function's code, use `GetFunction`.

To get configuration information and metadata about a function, you must provide the
function's name and stage. To get these values, you can use `ListFunctions`.

# Arguments

- `name`: The name of the function that you are getting information about.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Stage"`: The function's stage, either `DEVELOPMENT` or `LIVE`.
"""
function describe_function end

function describe_function(Name; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "GET",
        "/2020-05-31/function/$(Name)/describe";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function describe_function(
    Name, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/function/$(Name)/describe",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    describe_key_value_store(name)
    describe_key_value_store(name, params::Dict{String,<:Any})

Specifies the key value store and its configuration.

# Arguments

- `name`: The name of the key value store.
"""
function describe_key_value_store end

function describe_key_value_store(Name; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "GET",
        "/2020-05-31/key-value-store/$(Name)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function describe_key_value_store(
    Name, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/key-value-store/$(Name)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    disassociate_distribution_tenant_web_acl(id)
    disassociate_distribution_tenant_web_acl(id, params::Dict{String,<:Any})

Disassociates a distribution tenant from the WAF web ACL.

# Arguments

- `id`: The ID of the distribution tenant.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"If-Match"`: The current version of the distribution tenant that you're disassociating
  from the WAF web ACL. This is the `ETag` value returned in the response to the
  `GetDistributionTenant` API operation.
"""
function disassociate_distribution_tenant_web_acl end

function disassociate_distribution_tenant_web_acl(
    Id; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "PUT",
        "/2020-05-31/distribution-tenant/$(Id)/disassociate-web-acl";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function disassociate_distribution_tenant_web_acl(
    Id, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "PUT",
        "/2020-05-31/distribution-tenant/$(Id)/disassociate-web-acl",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    disassociate_distribution_web_acl(id)
    disassociate_distribution_web_acl(id, params::Dict{String,<:Any})

Disassociates a distribution from the WAF web ACL.

# Arguments

- `id`: The ID of the distribution.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"If-Match"`: The value of the `ETag` header that you received when retrieving the
  distribution that you're disassociating from the WAF web ACL.
"""
function disassociate_distribution_web_acl end

function disassociate_distribution_web_acl(
    Id; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "PUT",
        "/2020-05-31/distribution/$(Id)/disassociate-web-acl";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function disassociate_distribution_web_acl(
    Id, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "PUT",
        "/2020-05-31/distribution/$(Id)/disassociate-web-acl",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_anycast_ip_list(id)
    get_anycast_ip_list(id, params::Dict{String,<:Any})

Gets an Anycast static IP list.

# Arguments

- `id`: The ID of the Anycast static IP list.
"""
function get_anycast_ip_list end

function get_anycast_ip_list(Id; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "GET",
        "/2020-05-31/anycast-ip-list/$(Id)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_anycast_ip_list(
    Id, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/anycast-ip-list/$(Id)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_cache_policy(id)
    get_cache_policy(id, params::Dict{String,<:Any})

Gets a cache policy, including the following metadata:

- The policy's identifier.
- The date and time when the policy was last modified.

To get a cache policy, you must provide the policy's identifier. If the cache policy is
attached to a distribution's cache behavior, you can get the policy's identifier using
`ListDistributions` or `GetDistribution`. If the cache policy is not attached to a cache
behavior, you can get the identifier using `ListCachePolicies`.

# Arguments

- `id`: The unique identifier for the cache policy. If the cache policy is attached to a
  distribution's cache behavior, you can get the policy's identifier using
  `ListDistributions` or `GetDistribution`. If the cache policy is not attached to a cache
  behavior, you can get the identifier using `ListCachePolicies`.
"""
function get_cache_policy end

function get_cache_policy(Id; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "GET", "/2020-05-31/cache-policy/$(Id)"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function get_cache_policy(
    Id, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/cache-policy/$(Id)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_cache_policy_config(id)
    get_cache_policy_config(id, params::Dict{String,<:Any})

Gets a cache policy configuration.

To get a cache policy configuration, you must provide the policy's identifier. If the cache
policy is attached to a distribution's cache behavior, you can get the policy's identifier
using `ListDistributions` or `GetDistribution`. If the cache policy is not attached to a
cache behavior, you can get the identifier using `ListCachePolicies`.

# Arguments

- `id`: The unique identifier for the cache policy. If the cache policy is attached to a
  distribution's cache behavior, you can get the policy's identifier using
  `ListDistributions` or `GetDistribution`. If the cache policy is not attached to a cache
  behavior, you can get the identifier using `ListCachePolicies`.
"""
function get_cache_policy_config end

function get_cache_policy_config(Id; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "GET",
        "/2020-05-31/cache-policy/$(Id)/config";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_cache_policy_config(
    Id, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/cache-policy/$(Id)/config",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_cloud_front_origin_access_identity(id)
    get_cloud_front_origin_access_identity(id, params::Dict{String,<:Any})

Get the information about an origin access identity.

# Arguments

- `id`: The identity's ID.
"""
function get_cloud_front_origin_access_identity end

function get_cloud_front_origin_access_identity(
    Id; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/origin-access-identity/cloudfront/$(Id)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_cloud_front_origin_access_identity(
    Id, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/origin-access-identity/cloudfront/$(Id)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_cloud_front_origin_access_identity_config(id)
    get_cloud_front_origin_access_identity_config(id, params::Dict{String,<:Any})

Get the configuration information about an origin access identity.

# Arguments

- `id`: The identity's ID.
"""
function get_cloud_front_origin_access_identity_config end

function get_cloud_front_origin_access_identity_config(
    Id; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/origin-access-identity/cloudfront/$(Id)/config";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_cloud_front_origin_access_identity_config(
    Id, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/origin-access-identity/cloudfront/$(Id)/config",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_connection_function(identifier)
    get_connection_function(identifier, params::Dict{String,<:Any})

Gets a connection function.

# Arguments

- `identifier`: The connection function's identifier.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Stage"`: The connection function's stage.
"""
function get_connection_function end

function get_connection_function(
    Identifier; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/connection-function/$(Identifier)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_connection_function(
    Identifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "GET",
        "/2020-05-31/connection-function/$(Identifier)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_connection_group(identifier)
    get_connection_group(identifier, params::Dict{String,<:Any})

Gets information about a connection group.

# Arguments

- `identifier`: The ID, name, or Amazon Resource Name (ARN) of the connection group.
"""
function get_connection_group end

function get_connection_group(
    Identifier; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/connection-group/$(Identifier)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_connection_group(
    Identifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "GET",
        "/2020-05-31/connection-group/$(Identifier)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_connection_group_by_routing_endpoint(routing_endpoint)
    get_connection_group_by_routing_endpoint(routing_endpoint, params::Dict{String,<:Any})

Gets information about a connection group by using the endpoint that you specify.

# Arguments

- `routing_endpoint`: The routing endpoint for the target connection group, such as
  d111111abcdef8.cloudfront.net.
"""
function get_connection_group_by_routing_endpoint end

function get_connection_group_by_routing_endpoint(
    RoutingEndpoint; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/connection-group",
        Dict{String,Any}("RoutingEndpoint" => RoutingEndpoint);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_connection_group_by_routing_endpoint(
    RoutingEndpoint,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "GET",
        "/2020-05-31/connection-group",
        Dict{String,Any}(
            mergewith(
                _merge, Dict{String,Any}("RoutingEndpoint" => RoutingEndpoint), params
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_continuous_deployment_policy(id)
    get_continuous_deployment_policy(id, params::Dict{String,<:Any})

Gets a continuous deployment policy, including metadata (the policy's identifier and the
date and time when the policy was last modified).

# Arguments

- `id`: The identifier of the continuous deployment policy that you are getting.
"""
function get_continuous_deployment_policy end

function get_continuous_deployment_policy(
    Id; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/continuous-deployment-policy/$(Id)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_continuous_deployment_policy(
    Id, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/continuous-deployment-policy/$(Id)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_continuous_deployment_policy_config(id)
    get_continuous_deployment_policy_config(id, params::Dict{String,<:Any})

Gets configuration information about a continuous deployment policy.

# Arguments

- `id`: The identifier of the continuous deployment policy whose configuration you are
  getting.
"""
function get_continuous_deployment_policy_config end

function get_continuous_deployment_policy_config(
    Id; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/continuous-deployment-policy/$(Id)/config";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_continuous_deployment_policy_config(
    Id, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/continuous-deployment-policy/$(Id)/config",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_distribution(id)
    get_distribution(id, params::Dict{String,<:Any})

Get the information about a distribution.

# Arguments

- `id`: The distribution's ID. If the ID is empty, an empty distribution configuration is
  returned.
"""
function get_distribution end

function get_distribution(Id; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "GET", "/2020-05-31/distribution/$(Id)"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function get_distribution(
    Id, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/distribution/$(Id)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_distribution_config(id)
    get_distribution_config(id, params::Dict{String,<:Any})

Get the configuration information about a distribution.

# Arguments

- `id`: The distribution's ID. If the ID is empty, an empty distribution configuration is
  returned.
"""
function get_distribution_config end

function get_distribution_config(Id; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "GET",
        "/2020-05-31/distribution/$(Id)/config";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_distribution_config(
    Id, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/distribution/$(Id)/config",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_distribution_tenant(identifier)
    get_distribution_tenant(identifier, params::Dict{String,<:Any})

Gets information about a distribution tenant.

# Arguments

- `identifier`: The identifier of the distribution tenant. You can specify the ARN, ID, or
  name of the distribution tenant.
"""
function get_distribution_tenant end

function get_distribution_tenant(
    Identifier; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/distribution-tenant/$(Identifier)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_distribution_tenant(
    Identifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "GET",
        "/2020-05-31/distribution-tenant/$(Identifier)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_distribution_tenant_by_domain(domain)
    get_distribution_tenant_by_domain(domain, params::Dict{String,<:Any})

Gets information about a distribution tenant by the associated domain.

# Arguments

- `domain`: A domain name associated with the target distribution tenant.
"""
function get_distribution_tenant_by_domain end

function get_distribution_tenant_by_domain(
    domain; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/distribution-tenant",
        Dict{String,Any}("domain" => domain);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_distribution_tenant_by_domain(
    domain, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/distribution-tenant",
        Dict{String,Any}(mergewith(_merge, Dict{String,Any}("domain" => domain), params));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_field_level_encryption(id)
    get_field_level_encryption(id, params::Dict{String,<:Any})

Get the field-level encryption configuration information.

# Arguments

- `id`: Request the ID for the field-level encryption configuration information.
"""
function get_field_level_encryption end

function get_field_level_encryption(Id; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "GET",
        "/2020-05-31/field-level-encryption/$(Id)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_field_level_encryption(
    Id, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/field-level-encryption/$(Id)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_field_level_encryption_config(id)
    get_field_level_encryption_config(id, params::Dict{String,<:Any})

Get the field-level encryption configuration information.

# Arguments

- `id`: Request the ID for the field-level encryption configuration information.
"""
function get_field_level_encryption_config end

function get_field_level_encryption_config(
    Id; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/field-level-encryption/$(Id)/config";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_field_level_encryption_config(
    Id, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/field-level-encryption/$(Id)/config",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_field_level_encryption_profile(id)
    get_field_level_encryption_profile(id, params::Dict{String,<:Any})

Get the field-level encryption profile information.

# Arguments

- `id`: Get the ID for the field-level encryption profile information.
"""
function get_field_level_encryption_profile end

function get_field_level_encryption_profile(
    Id; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/field-level-encryption-profile/$(Id)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_field_level_encryption_profile(
    Id, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/field-level-encryption-profile/$(Id)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_field_level_encryption_profile_config(id)
    get_field_level_encryption_profile_config(id, params::Dict{String,<:Any})

Get the field-level encryption profile configuration information.

# Arguments

- `id`: Get the ID for the field-level encryption profile configuration information.
"""
function get_field_level_encryption_profile_config end

function get_field_level_encryption_profile_config(
    Id; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/field-level-encryption-profile/$(Id)/config";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_field_level_encryption_profile_config(
    Id, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/field-level-encryption-profile/$(Id)/config",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_function(name)
    get_function(name, params::Dict{String,<:Any})

Gets the code of a CloudFront function. To get configuration information and metadata about
a function, use `DescribeFunction`.

To get a function's code, you must provide the function's name and stage. To get these
values, you can use `ListFunctions`.

# Arguments

- `name`: The name of the function whose code you are getting.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Stage"`: The function's stage, either `DEVELOPMENT` or `LIVE`.
"""
function get_function end

function get_function(Name; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "GET", "/2020-05-31/function/$(Name)"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function get_function(
    Name, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/function/$(Name)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_invalidation(distribution_id, id)
    get_invalidation(distribution_id, id, params::Dict{String,<:Any})

Get the information about an invalidation.

# Arguments

- `distribution_id`: The distribution's ID.
- `id`: The identifier for the invalidation request, for example, `IDFDVBD632BHDS5`.
"""
function get_invalidation end

function get_invalidation(
    DistributionId, Id; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/distribution/$(DistributionId)/invalidation/$(Id)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_invalidation(
    DistributionId,
    Id,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "GET",
        "/2020-05-31/distribution/$(DistributionId)/invalidation/$(Id)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_invalidation_for_distribution_tenant(distribution_tenant_id, id)
    get_invalidation_for_distribution_tenant(distribution_tenant_id, id, params::Dict{String,<:Any})

Gets information about a specific invalidation for a distribution tenant.

# Arguments

- `distribution_tenant_id`: The ID of the distribution tenant.
- `id`: The ID of the invalidation to retrieve.
"""
function get_invalidation_for_distribution_tenant end

function get_invalidation_for_distribution_tenant(
    DistributionTenantId, Id; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/distribution-tenant/$(DistributionTenantId)/invalidation/$(Id)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_invalidation_for_distribution_tenant(
    DistributionTenantId,
    Id,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "GET",
        "/2020-05-31/distribution-tenant/$(DistributionTenantId)/invalidation/$(Id)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_key_group(id)
    get_key_group(id, params::Dict{String,<:Any})

Gets a key group, including the date and time when the key group was last modified.

To get a key group, you must provide the key group's identifier. If the key group is
referenced in a distribution's cache behavior, you can get the key group's identifier using
`ListDistributions` or `GetDistribution`. If the key group is not referenced in a cache
behavior, you can get the identifier using `ListKeyGroups`.

# Arguments

- `id`: The identifier of the key group that you are getting. To get the identifier, use
  `ListKeyGroups`.
"""
function get_key_group end

function get_key_group(Id; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "GET", "/2020-05-31/key-group/$(Id)"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function get_key_group(
    Id, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/key-group/$(Id)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_key_group_config(id)
    get_key_group_config(id, params::Dict{String,<:Any})

Gets a key group configuration.

To get a key group configuration, you must provide the key group's identifier. If the key
group is referenced in a distribution's cache behavior, you can get the key group's
identifier using `ListDistributions` or `GetDistribution`. If the key group is not
referenced in a cache behavior, you can get the identifier using `ListKeyGroups`.

# Arguments

- `id`: The identifier of the key group whose configuration you are getting. To get the
  identifier, use `ListKeyGroups`.
"""
function get_key_group_config end

function get_key_group_config(Id; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "GET",
        "/2020-05-31/key-group/$(Id)/config";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_key_group_config(
    Id, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/key-group/$(Id)/config",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_managed_certificate_details(identifier)
    get_managed_certificate_details(identifier, params::Dict{String,<:Any})

Gets details about the CloudFront managed ACM certificate.

# Arguments

- `identifier`: The identifier of the distribution tenant. You can specify the ARN, ID, or
  name of the distribution tenant.
"""
function get_managed_certificate_details end

function get_managed_certificate_details(
    Identifier; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/managed-certificate/$(Identifier)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_managed_certificate_details(
    Identifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "GET",
        "/2020-05-31/managed-certificate/$(Identifier)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_monitoring_subscription(distribution_id)
    get_monitoring_subscription(distribution_id, params::Dict{String,<:Any})

Gets information about whether additional CloudWatch metrics are enabled for the specified
CloudFront distribution.

# Arguments

- `distribution_id`: The ID of the distribution that you are getting metrics information
  for.
"""
function get_monitoring_subscription end

function get_monitoring_subscription(
    DistributionId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/distributions/$(DistributionId)/monitoring-subscription";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_monitoring_subscription(
    DistributionId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "GET",
        "/2020-05-31/distributions/$(DistributionId)/monitoring-subscription",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_origin_access_control(id)
    get_origin_access_control(id, params::Dict{String,<:Any})

Gets a CloudFront origin access control, including its unique identifier.

# Arguments

- `id`: The unique identifier of the origin access control.
"""
function get_origin_access_control end

function get_origin_access_control(Id; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "GET",
        "/2020-05-31/origin-access-control/$(Id)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_origin_access_control(
    Id, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/origin-access-control/$(Id)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_origin_access_control_config(id)
    get_origin_access_control_config(id, params::Dict{String,<:Any})

Gets a CloudFront origin access control configuration.

# Arguments

- `id`: The unique identifier of the origin access control.
"""
function get_origin_access_control_config end

function get_origin_access_control_config(
    Id; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/origin-access-control/$(Id)/config";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_origin_access_control_config(
    Id, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/origin-access-control/$(Id)/config",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_origin_request_policy(id)
    get_origin_request_policy(id, params::Dict{String,<:Any})

Gets an origin request policy, including the following metadata:

- The policy's identifier.
- The date and time when the policy was last modified.

To get an origin request policy, you must provide the policy's identifier. If the origin
request policy is attached to a distribution's cache behavior, you can get the policy's
identifier using `ListDistributions` or `GetDistribution`. If the origin request policy is
not attached to a cache behavior, you can get the identifier using
`ListOriginRequestPolicies`.

# Arguments

- `id`: The unique identifier for the origin request policy. If the origin request policy is
  attached to a distribution's cache behavior, you can get the policy's identifier using
  `ListDistributions` or `GetDistribution`. If the origin request policy is not attached to
  a cache behavior, you can get the identifier using `ListOriginRequestPolicies`.
"""
function get_origin_request_policy end

function get_origin_request_policy(Id; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "GET",
        "/2020-05-31/origin-request-policy/$(Id)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_origin_request_policy(
    Id, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/origin-request-policy/$(Id)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_origin_request_policy_config(id)
    get_origin_request_policy_config(id, params::Dict{String,<:Any})

Gets an origin request policy configuration.

To get an origin request policy configuration, you must provide the policy's identifier. If
the origin request policy is attached to a distribution's cache behavior, you can get the
policy's identifier using `ListDistributions` or `GetDistribution`. If the origin request
policy is not attached to a cache behavior, you can get the identifier using
`ListOriginRequestPolicies`.

# Arguments

- `id`: The unique identifier for the origin request policy. If the origin request policy is
  attached to a distribution's cache behavior, you can get the policy's identifier using
  `ListDistributions` or `GetDistribution`. If the origin request policy is not attached to
  a cache behavior, you can get the identifier using `ListOriginRequestPolicies`.
"""
function get_origin_request_policy_config end

function get_origin_request_policy_config(
    Id; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/origin-request-policy/$(Id)/config";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_origin_request_policy_config(
    Id, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/origin-request-policy/$(Id)/config",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_public_key(id)
    get_public_key(id, params::Dict{String,<:Any})

Gets a public key.

# Arguments

- `id`: The identifier of the public key you are getting.
"""
function get_public_key end

function get_public_key(Id; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "GET", "/2020-05-31/public-key/$(Id)"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function get_public_key(
    Id, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/public-key/$(Id)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_public_key_config(id)
    get_public_key_config(id, params::Dict{String,<:Any})

Gets a public key configuration.

# Arguments

- `id`: The identifier of the public key whose configuration you are getting.
"""
function get_public_key_config end

function get_public_key_config(Id; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "GET",
        "/2020-05-31/public-key/$(Id)/config";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_public_key_config(
    Id, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/public-key/$(Id)/config",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_realtime_log_config()
    get_realtime_log_config(params::Dict{String,<:Any})

Gets a real-time log configuration.

To get a real-time log configuration, you can provide the configuration's name or its Amazon
Resource Name (ARN). You must provide at least one. If you provide both, CloudFront uses the
name to identify the real-time log configuration to get.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"ARN"`: The Amazon Resource Name (ARN) of the real-time log configuration to get.
- `"Name"`: The name of the real-time log configuration to get.
"""
function get_realtime_log_config end

function get_realtime_log_config(; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "POST",
        "/2020-05-31/get-realtime-log-config";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_realtime_log_config(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "POST",
        "/2020-05-31/get-realtime-log-config",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_resource_policy(resource_arn)
    get_resource_policy(resource_arn, params::Dict{String,<:Any})

Retrieves the resource policy for the specified CloudFront resource that you own and have
shared.

# Arguments

- `resource_arn`: The Amazon Resource Name (ARN) of the CloudFront resource that is
  associated with the resource policy.
"""
function get_resource_policy end

function get_resource_policy(
    ResourceArn; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "POST",
        "/2020-05-31/get-resource-policy",
        Dict{String,Any}("ResourceArn" => ResourceArn);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_resource_policy(
    ResourceArn,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "POST",
        "/2020-05-31/get-resource-policy",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("ResourceArn" => ResourceArn), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_response_headers_policy(id)
    get_response_headers_policy(id, params::Dict{String,<:Any})

Gets a response headers policy, including metadata (the policy's identifier and the date and
time when the policy was last modified).

To get a response headers policy, you must provide the policy's identifier. If the response
headers policy is attached to a distribution's cache behavior, you can get the policy's
identifier using `ListDistributions` or `GetDistribution`. If the response headers policy is
not attached to a cache behavior, you can get the identifier using
`ListResponseHeadersPolicies`.

# Arguments

- `id`: The identifier for the response headers policy.

  If the response headers policy is attached to a distribution's cache behavior, you can get
  the policy's identifier using `ListDistributions` or `GetDistribution`. If the response
  headers policy is not attached to a cache behavior, you can get the identifier using
  `ListResponseHeadersPolicies`.
"""
function get_response_headers_policy end

function get_response_headers_policy(Id; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "GET",
        "/2020-05-31/response-headers-policy/$(Id)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_response_headers_policy(
    Id, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/response-headers-policy/$(Id)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_response_headers_policy_config(id)
    get_response_headers_policy_config(id, params::Dict{String,<:Any})

Gets a response headers policy configuration.

To get a response headers policy configuration, you must provide the policy's identifier. If
the response headers policy is attached to a distribution's cache behavior, you can get the
policy's identifier using `ListDistributions` or `GetDistribution`. If the response headers
policy is not attached to a cache behavior, you can get the identifier using
`ListResponseHeadersPolicies`.

# Arguments

- `id`: The identifier for the response headers policy.

  If the response headers policy is attached to a distribution's cache behavior, you can get
  the policy's identifier using `ListDistributions` or `GetDistribution`. If the response
  headers policy is not attached to a cache behavior, you can get the identifier using
  `ListResponseHeadersPolicies`.
"""
function get_response_headers_policy_config end

function get_response_headers_policy_config(
    Id; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/response-headers-policy/$(Id)/config";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_response_headers_policy_config(
    Id, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/response-headers-policy/$(Id)/config",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_streaming_distribution(id)
    get_streaming_distribution(id, params::Dict{String,<:Any})

Gets information about a specified RTMP distribution, including the distribution
configuration.

# Arguments

- `id`: The streaming distribution's ID.
"""
function get_streaming_distribution end

function get_streaming_distribution(Id; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "GET",
        "/2020-05-31/streaming-distribution/$(Id)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_streaming_distribution(
    Id, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/streaming-distribution/$(Id)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_streaming_distribution_config(id)
    get_streaming_distribution_config(id, params::Dict{String,<:Any})

Get the configuration information about a streaming distribution.

# Arguments

- `id`: The streaming distribution's ID.
"""
function get_streaming_distribution_config end

function get_streaming_distribution_config(
    Id; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/streaming-distribution/$(Id)/config";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_streaming_distribution_config(
    Id, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/streaming-distribution/$(Id)/config",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_trust_store(identifier)
    get_trust_store(identifier, params::Dict{String,<:Any})

Gets a trust store.

# Arguments

- `identifier`: The trust store's identifier.
"""
function get_trust_store end

function get_trust_store(Identifier; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "GET",
        "/2020-05-31/trust-store/$(Identifier)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function get_trust_store(
    Identifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "GET",
        "/2020-05-31/trust-store/$(Identifier)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    get_vpc_origin(id)
    get_vpc_origin(id, params::Dict{String,<:Any})

Get the details of an Amazon CloudFront VPC origin.

# Arguments

- `id`: The VPC origin ID.
"""
function get_vpc_origin end

function get_vpc_origin(Id; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "GET", "/2020-05-31/vpc-origin/$(Id)"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function get_vpc_origin(
    Id, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/vpc-origin/$(Id)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_anycast_ip_lists()
    list_anycast_ip_lists(params::Dict{String,<:Any})

Lists your Anycast static IP lists.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Marker"`: Use this field when paginating results to indicate where to begin in your
  list. The response includes items in the list that occur after the marker. To get the next
  page of the list, set this field's value to the value of `NextMarker` from the current
  page's response.
- `"MaxItems"`: The maximum number of Anycast static IP lists that you want returned in the
  response.
"""
function list_anycast_ip_lists end

function list_anycast_ip_lists(; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "GET", "/2020-05-31/anycast-ip-list"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_anycast_ip_lists(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/anycast-ip-list",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_cache_policies()
    list_cache_policies(params::Dict{String,<:Any})

Gets a list of cache policies.

You can optionally apply a filter to return only the managed policies created by Amazon Web
Services, or only the custom policies created in your Amazon Web Services account.

You can optionally specify the maximum number of items to receive in the response. If the
total number of items in the list exceeds the maximum that you specify, or the default
maximum, the response is paginated. To get the next page of items, send a subsequent request
that specifies the `NextMarker` value from the current response as the `Marker` value in the
subsequent request.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Marker"`: Use this field when paginating results to indicate where to begin in your list
  of cache policies. The response includes cache policies in the list that occur after the
  marker. To get the next page of the list, set this field's value to the value of
  `NextMarker` from the current page's response.

- `"MaxItems"`: The maximum number of cache policies that you want in the response.

- `"Type"`: A filter to return only the specified kinds of cache policies. Valid values are:

  - `managed` – Returns only the managed policies created by Amazon Web Services.
  - `custom` – Returns only the custom policies created in your Amazon Web Services account.
"""
function list_cache_policies end

function list_cache_policies(; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "GET", "/2020-05-31/cache-policy"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_cache_policies(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/cache-policy",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_cloud_front_origin_access_identities()
    list_cloud_front_origin_access_identities(params::Dict{String,<:Any})

Lists origin access identities.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Marker"`: Use this when paginating results to indicate where to begin in your list of
  origin access identities. The results include identities in the list that occur after the
  marker. To get the next page of results, set the `Marker` to the value of the `NextMarker`
  from the current page's response (which is also the ID of the last identity on that page).
- `"MaxItems"`: The maximum number of origin access identities you want in the response
  body.
"""
function list_cloud_front_origin_access_identities end

function list_cloud_front_origin_access_identities(;
    aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/origin-access-identity/cloudfront";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_cloud_front_origin_access_identities(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/origin-access-identity/cloudfront",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_conflicting_aliases(alias, distribution_id)
    list_conflicting_aliases(alias, distribution_id, params::Dict{String,<:Any})

!!! note
    The `ListConflictingAliases` API operation only supports standard distributions. To list
    domain conflicts for both standard distributions and distribution tenants, we recommend
    that you use the [ListDomainConflicts](https://docs.aws.amazon.com/cloudfront/latest/APIReference/API_ListDomainConflicts.html)
    API operation instead.

Gets a list of aliases that conflict or overlap with the provided alias, and the associated
CloudFront standard distribution and Amazon Web Services accounts for each conflicting
alias. An alias is commonly known as a custom domain or vanity domain. It can also be called
a CNAME or alternate domain name.

In the returned list, the standard distribution and account IDs are partially hidden, which
allows you to identify the standard distribution and accounts that you own, and helps to
protect the information of ones that you don't own.

Use this operation to find aliases that are in use in CloudFront that conflict or overlap
with the provided alias. For example, if you provide `www.example.com` as input, the
returned list can include `www.example.com` and the overlapping wildcard alternate domain
name (`*.example.com`), if they exist. If you provide `*.example.com` as input, the returned
list can include `*.example.com` and any alternate domain names covered by that wildcard
(for example, `www.example.com`, `test.example.com`, `dev.example.com`, and so on), if they
exist.

To list conflicting aliases, specify the alias to search and the ID of a standard
distribution in your account that has an attached TLS certificate that includes the provided
alias. For more information, including how to set up the standard distribution and
certificate, see [Moving an alternate domain name to a different standard distribution or distribution tenant](https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/CNAMEs.html#alternate-domain-names-move)
in the *Amazon CloudFront Developer Guide*.

You can optionally specify the maximum number of items to receive in the response. If the
total number of items in the list exceeds the maximum that you specify, or the default
maximum, the response is paginated. To get the next page of items, send a subsequent request
that specifies the `NextMarker` value from the current response as the `Marker` value in the
subsequent request.

# Arguments

- `alias`: The alias (also called a CNAME) to search for conflicting aliases.
- `distribution_id`: The ID of a standard distribution in your account that has an attached
  TLS certificate that includes the provided alias.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Marker"`: Use this field when paginating results to indicate where to begin in the list
  of conflicting aliases. The response includes conflicting aliases in the list that occur
  after the marker. To get the next page of the list, set this field's value to the value of
  `NextMarker` from the current page's response.
- `"MaxItems"`: The maximum number of conflicting aliases that you want in the response.
"""
function list_conflicting_aliases end

function list_conflicting_aliases(
    Alias, DistributionId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/conflicting-alias",
        Dict{String,Any}("Alias" => Alias, "DistributionId" => DistributionId);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_conflicting_aliases(
    Alias,
    DistributionId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "GET",
        "/2020-05-31/conflicting-alias",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("Alias" => Alias, "DistributionId" => DistributionId),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_connection_functions()
    list_connection_functions(params::Dict{String,<:Any})

Lists connection functions.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Marker"`: Use this field when paginating results to indicate where to begin in your
  list. The response includes items in the list that occur after the marker. To get the next
  page of the list, set this field's value to the value of `NextMarker` from the current
  page's response.
- `"MaxItems"`: The maximum number of connection functions that you want returned in the
  response.
- `"Stage"`: The connection function's stage.
"""
function list_connection_functions end

function list_connection_functions(; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "POST",
        "/2020-05-31/connection-functions";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_connection_functions(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "POST",
        "/2020-05-31/connection-functions",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_connection_groups()
    list_connection_groups(params::Dict{String,<:Any})

Lists the connection groups in your Amazon Web Services account.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"AssociationFilter"`: Filter by associated Anycast IP list ID.
- `"Marker"`: The marker for the next set of connection groups to retrieve.
- `"MaxItems"`: The maximum number of connection groups to return.
"""
function list_connection_groups end

function list_connection_groups(; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "POST", "/2020-05-31/connection-groups"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_connection_groups(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "POST",
        "/2020-05-31/connection-groups",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_continuous_deployment_policies()
    list_continuous_deployment_policies(params::Dict{String,<:Any})

Gets a list of the continuous deployment policies in your Amazon Web Services account.

You can optionally specify the maximum number of items to receive in the response. If the
total number of items in the list exceeds the maximum that you specify, or the default
maximum, the response is paginated. To get the next page of items, send a subsequent request
that specifies the `NextMarker` value from the current response as the `Marker` value in the
subsequent request.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Marker"`: Use this field when paginating results to indicate where to begin in your list
  of continuous deployment policies. The response includes policies in the list that occur
  after the marker. To get the next page of the list, set this field's value to the value of
  `NextMarker` from the current page's response.
- `"MaxItems"`: The maximum number of continuous deployment policies that you want returned
  in the response.
"""
function list_continuous_deployment_policies end

function list_continuous_deployment_policies(;
    aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/continuous-deployment-policy";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_continuous_deployment_policies(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/continuous-deployment-policy",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_distribution_tenants()
    list_distribution_tenants(params::Dict{String,<:Any})

Lists the distribution tenants in your Amazon Web Services account.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"AssociationFilter"`:
- `"Marker"`: The marker for the next set of results.
- `"MaxItems"`: The maximum number of distribution tenants to return.
"""
function list_distribution_tenants end

function list_distribution_tenants(; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "POST",
        "/2020-05-31/distribution-tenants";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_distribution_tenants(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "POST",
        "/2020-05-31/distribution-tenants",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_distribution_tenants_by_customization()
    list_distribution_tenants_by_customization(params::Dict{String,<:Any})

Lists distribution tenants by the customization that you specify.

You must specify either the `CertificateArn` parameter or `WebACLArn` parameter, but not
both in the same request.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"CertificateArn"`: Filter by the ARN of the associated ACM certificate.
- `"Marker"`: The marker for the next set of results.
- `"MaxItems"`: The maximum number of distribution tenants to return by the specified
  customization.
- `"WebACLArn"`: Filter by the ARN of the associated WAF web ACL.
"""
function list_distribution_tenants_by_customization end

function list_distribution_tenants_by_customization(;
    aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "POST",
        "/2020-05-31/distribution-tenants-by-customization";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_distribution_tenants_by_customization(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "POST",
        "/2020-05-31/distribution-tenants-by-customization",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_distributions()
    list_distributions(params::Dict{String,<:Any})

List CloudFront distributions.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Marker"`: Use this when paginating results to indicate where to begin in your list of
  distributions. The results include distributions in the list that occur after the marker.
  To get the next page of results, set the `Marker` to the value of the `NextMarker` from
  the current page's response (which is also the ID of the last distribution on that page).
- `"MaxItems"`: The maximum number of distributions you want in the response body.
"""
function list_distributions end

function list_distributions(; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "GET", "/2020-05-31/distribution"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_distributions(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/distribution",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_distributions_by_anycast_ip_list_id(anycast_ip_list_id)
    list_distributions_by_anycast_ip_list_id(anycast_ip_list_id, params::Dict{String,<:Any})

Lists the distributions in your account that are associated with the specified
`AnycastIpListId`.

# Arguments

- `anycast_ip_list_id`: The ID of the Anycast static IP list.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Marker"`: Use this field when paginating results to indicate where to begin in your
  list. The response includes items in the list that occur after the marker. To get the next
  page of the list, set this field's value to the value of `NextMarker` from the current
  page's response.
- `"MaxItems"`: The maximum number of distributions that you want returned in the response.
"""
function list_distributions_by_anycast_ip_list_id end

function list_distributions_by_anycast_ip_list_id(
    AnycastIpListId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/distributionsByAnycastIpListId/$(AnycastIpListId)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_distributions_by_anycast_ip_list_id(
    AnycastIpListId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "GET",
        "/2020-05-31/distributionsByAnycastIpListId/$(AnycastIpListId)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_distributions_by_cache_policy_id(cache_policy_id)
    list_distributions_by_cache_policy_id(cache_policy_id, params::Dict{String,<:Any})

Gets a list of distribution IDs for distributions that have a cache behavior that's
associated with the specified cache policy.

You can optionally specify the maximum number of items to receive in the response. If the
total number of items in the list exceeds the maximum that you specify, or the default
maximum, the response is paginated. To get the next page of items, send a subsequent request
that specifies the `NextMarker` value from the current response as the `Marker` value in the
subsequent request.

# Arguments

- `cache_policy_id`: The ID of the cache policy whose associated distribution IDs you want
  to list.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Marker"`: Use this field when paginating results to indicate where to begin in your list
  of distribution IDs. The response includes distribution IDs in the list that occur after
  the marker. To get the next page of the list, set this field's value to the value of
  `NextMarker` from the current page's response.
- `"MaxItems"`: The maximum number of distribution IDs that you want in the response.
"""
function list_distributions_by_cache_policy_id end

function list_distributions_by_cache_policy_id(
    CachePolicyId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/distributionsByCachePolicyId/$(CachePolicyId)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_distributions_by_cache_policy_id(
    CachePolicyId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "GET",
        "/2020-05-31/distributionsByCachePolicyId/$(CachePolicyId)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_distributions_by_connection_function(connection_function_identifier)
    list_distributions_by_connection_function(connection_function_identifier, params::Dict{String,<:Any})

Lists distributions by connection function.

# Arguments

- `connection_function_identifier`: The distributions by connection function identifier.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Marker"`: Use this field when paginating results to indicate where to begin in your
  list. The response includes items in the list that occur after the marker. To get the next
  page of the list, set this field's value to the value of `NextMarker` from the current
  page's response.
- `"MaxItems"`: The maximum number of distributions that you want returned in the response.
"""
function list_distributions_by_connection_function end

function list_distributions_by_connection_function(
    ConnectionFunctionIdentifier; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/distributionsByConnectionFunction",
        Dict{String,Any}("ConnectionFunctionIdentifier" => ConnectionFunctionIdentifier);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_distributions_by_connection_function(
    ConnectionFunctionIdentifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "GET",
        "/2020-05-31/distributionsByConnectionFunction",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "ConnectionFunctionIdentifier" => ConnectionFunctionIdentifier
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_distributions_by_connection_mode(connection_mode)
    list_distributions_by_connection_mode(connection_mode, params::Dict{String,<:Any})

Lists the distributions by the connection mode that you specify.

# Arguments

- `connection_mode`: This field specifies whether the connection mode is through a standard
  distribution (direct) or a multi-tenant distribution with distribution tenants (tenant-
  only).

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Marker"`: The marker for the next set of distributions to retrieve.
- `"MaxItems"`: The maximum number of distributions to return.
"""
function list_distributions_by_connection_mode end

function list_distributions_by_connection_mode(
    ConnectionMode; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/distributionsByConnectionMode/$(ConnectionMode)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_distributions_by_connection_mode(
    ConnectionMode,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "GET",
        "/2020-05-31/distributionsByConnectionMode/$(ConnectionMode)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_distributions_by_key_group(key_group_id)
    list_distributions_by_key_group(key_group_id, params::Dict{String,<:Any})

Gets a list of distribution IDs for distributions that have a cache behavior that references
the specified key group.

You can optionally specify the maximum number of items to receive in the response. If the
total number of items in the list exceeds the maximum that you specify, or the default
maximum, the response is paginated. To get the next page of items, send a subsequent request
that specifies the `NextMarker` value from the current response as the `Marker` value in the
subsequent request.

# Arguments

- `key_group_id`: The ID of the key group whose associated distribution IDs you are listing.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Marker"`: Use this field when paginating results to indicate where to begin in your list
  of distribution IDs. The response includes distribution IDs in the list that occur after
  the marker. To get the next page of the list, set this field's value to the value of
  `NextMarker` from the current page's response.
- `"MaxItems"`: The maximum number of distribution IDs that you want in the response.
"""
function list_distributions_by_key_group end

function list_distributions_by_key_group(
    KeyGroupId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/distributionsByKeyGroupId/$(KeyGroupId)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_distributions_by_key_group(
    KeyGroupId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "GET",
        "/2020-05-31/distributionsByKeyGroupId/$(KeyGroupId)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_distributions_by_origin_request_policy_id(origin_request_policy_id)
    list_distributions_by_origin_request_policy_id(origin_request_policy_id, params::Dict{String,<:Any})

Gets a list of distribution IDs for distributions that have a cache behavior that's
associated with the specified origin request policy.

You can optionally specify the maximum number of items to receive in the response. If the
total number of items in the list exceeds the maximum that you specify, or the default
maximum, the response is paginated. To get the next page of items, send a subsequent request
that specifies the `NextMarker` value from the current response as the `Marker` value in the
subsequent request.

# Arguments

- `origin_request_policy_id`: The ID of the origin request policy whose associated
  distribution IDs you want to list.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Marker"`: Use this field when paginating results to indicate where to begin in your list
  of distribution IDs. The response includes distribution IDs in the list that occur after
  the marker. To get the next page of the list, set this field's value to the value of
  `NextMarker` from the current page's response.
- `"MaxItems"`: The maximum number of distribution IDs that you want in the response.
"""
function list_distributions_by_origin_request_policy_id end

function list_distributions_by_origin_request_policy_id(
    OriginRequestPolicyId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/distributionsByOriginRequestPolicyId/$(OriginRequestPolicyId)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_distributions_by_origin_request_policy_id(
    OriginRequestPolicyId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "GET",
        "/2020-05-31/distributionsByOriginRequestPolicyId/$(OriginRequestPolicyId)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_distributions_by_owned_resource(resource_arn)
    list_distributions_by_owned_resource(resource_arn, params::Dict{String,<:Any})

Lists the CloudFront distributions that are associated with the specified resource that you
own.

# Arguments

- `resource_arn`: The ARN of the CloudFront resource that you've shared with other Amazon
  Web Services accounts.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Marker"`: Use this field when paginating results to indicate where to begin in your list
  of distributions. The response includes distributions in the list that occur after the
  marker. To get the next page of the list, set this field's value to the value of
  `NextMarker` from the current page's response.
- `"MaxItems"`: The maximum number of distributions to return.
"""
function list_distributions_by_owned_resource end

function list_distributions_by_owned_resource(
    ResourceArn; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/distributionsByOwnedResource/$(ResourceArn)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_distributions_by_owned_resource(
    ResourceArn,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "GET",
        "/2020-05-31/distributionsByOwnedResource/$(ResourceArn)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_distributions_by_realtime_log_config()
    list_distributions_by_realtime_log_config(params::Dict{String,<:Any})

Gets a list of distributions that have a cache behavior that's associated with the specified
real-time log configuration.

You can specify the real-time log configuration by its name or its Amazon Resource Name
(ARN). You must provide at least one. If you provide both, CloudFront uses the name to
identify the real-time log configuration to list distributions for.

You can optionally specify the maximum number of items to receive in the response. If the
total number of items in the list exceeds the maximum that you specify, or the default
maximum, the response is paginated. To get the next page of items, send a subsequent request
that specifies the `NextMarker` value from the current response as the `Marker` value in the
subsequent request.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Marker"`: Use this field when paginating results to indicate where to begin in your list
  of distributions. The response includes distributions in the list that occur after the
  marker. To get the next page of the list, set this field's value to the value of
  `NextMarker` from the current page's response.
- `"MaxItems"`: The maximum number of distributions that you want in the response.
- `"RealtimeLogConfigArn"`: The Amazon Resource Name (ARN) of the real-time log
  configuration whose associated distributions you want to list.
- `"RealtimeLogConfigName"`: The name of the real-time log configuration whose associated
  distributions you want to list.
"""
function list_distributions_by_realtime_log_config end

function list_distributions_by_realtime_log_config(;
    aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "POST",
        "/2020-05-31/distributionsByRealtimeLogConfig";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_distributions_by_realtime_log_config(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "POST",
        "/2020-05-31/distributionsByRealtimeLogConfig",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_distributions_by_response_headers_policy_id(response_headers_policy_id)
    list_distributions_by_response_headers_policy_id(response_headers_policy_id, params::Dict{String,<:Any})

Gets a list of distribution IDs for distributions that have a cache behavior that's
associated with the specified response headers policy.

You can optionally specify the maximum number of items to receive in the response. If the
total number of items in the list exceeds the maximum that you specify, or the default
maximum, the response is paginated. To get the next page of items, send a subsequent request
that specifies the `NextMarker` value from the current response as the `Marker` value in the
subsequent request.

# Arguments

- `response_headers_policy_id`: The ID of the response headers policy whose associated
  distribution IDs you want to list.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Marker"`: Use this field when paginating results to indicate where to begin in your list
  of distribution IDs. The response includes distribution IDs in the list that occur after
  the marker. To get the next page of the list, set this field's value to the value of
  `NextMarker` from the current page's response.
- `"MaxItems"`: The maximum number of distribution IDs that you want to get in the response.
"""
function list_distributions_by_response_headers_policy_id end

function list_distributions_by_response_headers_policy_id(
    ResponseHeadersPolicyId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/distributionsByResponseHeadersPolicyId/$(ResponseHeadersPolicyId)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_distributions_by_response_headers_policy_id(
    ResponseHeadersPolicyId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "GET",
        "/2020-05-31/distributionsByResponseHeadersPolicyId/$(ResponseHeadersPolicyId)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_distributions_by_trust_store(trust_store_identifier)
    list_distributions_by_trust_store(trust_store_identifier, params::Dict{String,<:Any})

Lists distributions by trust store.

# Arguments

- `trust_store_identifier`: The distributions by trust store identifier.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Marker"`: Use this field when paginating results to indicate where to begin in your
  list. The response includes items in the list that occur after the marker. To get the next
  page of the list, set this field's value to the value of `NextMarker` from the current
  page's response.
- `"MaxItems"`: The maximum number of distributions that you want returned in the response.
"""
function list_distributions_by_trust_store end

function list_distributions_by_trust_store(
    TrustStoreIdentifier; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/distributionsByTrustStore",
        Dict{String,Any}("TrustStoreIdentifier" => TrustStoreIdentifier);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_distributions_by_trust_store(
    TrustStoreIdentifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "GET",
        "/2020-05-31/distributionsByTrustStore",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("TrustStoreIdentifier" => TrustStoreIdentifier),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_distributions_by_vpc_origin_id(vpc_origin_id)
    list_distributions_by_vpc_origin_id(vpc_origin_id, params::Dict{String,<:Any})

List CloudFront distributions by their VPC origin ID.

# Arguments

- `vpc_origin_id`: The VPC origin ID.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Marker"`: The marker associated with the VPC origin distributions list.
- `"MaxItems"`: The maximum number of items included in the list.
"""
function list_distributions_by_vpc_origin_id end

function list_distributions_by_vpc_origin_id(
    VpcOriginId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/distributionsByVpcOriginId/$(VpcOriginId)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_distributions_by_vpc_origin_id(
    VpcOriginId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "GET",
        "/2020-05-31/distributionsByVpcOriginId/$(VpcOriginId)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_distributions_by_web_aclid(web_aclid)
    list_distributions_by_web_aclid(web_aclid, params::Dict{String,<:Any})

List the distributions that are associated with a specified WAF web ACL.

# Arguments

- `web_aclid`: The ID of the WAF web ACL that you want to list the associated distributions.
  If you specify "null" for the ID, the request returns a list of the distributions that
  aren't associated with a web ACL.

  For WAFV2, this is the ARN of the web ACL, such as
  `arn:aws:wafv2:us-east-1:123456789012:global/webacl/ExampleWebACL/a1b2c3d4-5678-90ab-cdef-EXAMPLE11111`.

  For WAF Classic, this is the ID of the web ACL, such as
  `a1b2c3d4-5678-90ab-cdef-EXAMPLE11111`.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Marker"`: Use `Marker` and `MaxItems` to control pagination of results. If you have more
  than `MaxItems` distributions that satisfy the request, the response includes a
  `NextMarker` element. To get the next page of results, submit another request. For the
  value of `Marker`, specify the value of `NextMarker` from the last response. (For the
  first request, omit `Marker`.)

- `"MaxItems"`: The maximum number of distributions that you want CloudFront to return in
  the response body. The maximum and default values are both 100.
"""
function list_distributions_by_web_aclid end

function list_distributions_by_web_aclid(
    WebACLId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/distributionsByWebACLId/$(WebACLId)";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_distributions_by_web_aclid(
    WebACLId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "GET",
        "/2020-05-31/distributionsByWebACLId/$(WebACLId)",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_domain_conflicts(domain, domain_control_validation_resource)
    list_domain_conflicts(domain, domain_control_validation_resource, params::Dict{String,<:Any})

!!! note
    We recommend that you use the `ListDomainConflicts` API operation to check for domain
    conflicts, as it supports both standard distributions and distribution tenants. [ListConflictingAliases](https://docs.aws.amazon.com/cloudfront/latest/APIReference/API_ListConflictingAliases.html)
    performs similar checks but only supports standard distributions.

Lists existing domain associations that conflict with the domain that you specify.

You can use this API operation to identify potential domain conflicts when moving domains
between standard distributions and/or distribution tenants. Domain conflicts must be
resolved first before they can be moved.

For example, if you provide `www.example.com` as input, the returned list can include
`www.example.com` and the overlapping wildcard alternate domain name (`*.example.com`), if
they exist. If you provide `*.example.com` as input, the returned list can include
`*.example.com` and any alternate domain names covered by that wildcard (for example,
`www.example.com`, `test.example.com`, `dev.example.com`, and so on), if they exist.

To list conflicting domains, specify the following:

- The domain to search for
- The ID of a standard distribution or distribution tenant in your account that has an
  attached TLS certificate, which covers the specified domain

For more information, including how to set up the standard distribution or distribution
tenant, and the certificate, see [Moving an alternate domain name to a different standard distribution or distribution tenant](https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/CNAMEs.html#alternate-domain-names-move)
in the *Amazon CloudFront Developer Guide*.

You can optionally specify the maximum number of items to receive in the response. If the
total number of items in the list exceeds the maximum that you specify, or the default
maximum, the response is paginated. To get the next page of items, send a subsequent request
that specifies the `NextMarker` value from the current response as the `Marker` value in the
subsequent request.

# Arguments

- `domain`: The domain to check for conflicts.
- `domain_control_validation_resource`: The distribution resource identifier. This can be
  the standard distribution or distribution tenant that has a valid certificate, which
  covers the domain that you specify.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Marker"`: The marker for the next set of domain conflicts.
- `"MaxItems"`: The maximum number of domain conflicts to return.
"""
function list_domain_conflicts end

function list_domain_conflicts(
    Domain,
    DomainControlValidationResource;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "POST",
        "/2020-05-31/domain-conflicts",
        Dict{String,Any}(
            "Domain" => Domain,
            "DomainControlValidationResource" => DomainControlValidationResource,
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_domain_conflicts(
    Domain,
    DomainControlValidationResource,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "POST",
        "/2020-05-31/domain-conflicts",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "Domain" => Domain,
                    "DomainControlValidationResource" => DomainControlValidationResource,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_field_level_encryption_configs()
    list_field_level_encryption_configs(params::Dict{String,<:Any})

List all field-level encryption configurations that have been created in CloudFront for this
account.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Marker"`: Use this when paginating results to indicate where to begin in your list of
  configurations. The results include configurations in the list that occur after the
  marker. To get the next page of results, set the `Marker` to the value of the `NextMarker`
  from the current page's response (which is also the ID of the last configuration on that
  page).

- `"MaxItems"`: The maximum number of field-level encryption configurations you want in the
  response body.
"""
function list_field_level_encryption_configs end

function list_field_level_encryption_configs(;
    aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/field-level-encryption";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_field_level_encryption_configs(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/field-level-encryption",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_field_level_encryption_profiles()
    list_field_level_encryption_profiles(params::Dict{String,<:Any})

Request a list of field-level encryption profiles that have been created in CloudFront for
this account.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Marker"`: Use this when paginating results to indicate where to begin in your list of
  profiles. The results include profiles in the list that occur after the marker. To get the
  next page of results, set the `Marker` to the value of the `NextMarker` from the current
  page's response (which is also the ID of the last profile on that page).
- `"MaxItems"`: The maximum number of field-level encryption profiles you want in the
  response body.
"""
function list_field_level_encryption_profiles end

function list_field_level_encryption_profiles(;
    aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/field-level-encryption-profile";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_field_level_encryption_profiles(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/field-level-encryption-profile",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_functions()
    list_functions(params::Dict{String,<:Any})

Gets a list of all CloudFront functions in your Amazon Web Services account.

You can optionally apply a filter to return only the functions that are in the specified
stage, either `DEVELOPMENT` or `LIVE`.

You can optionally specify the maximum number of items to receive in the response. If the
total number of items in the list exceeds the maximum that you specify, or the default
maximum, the response is paginated. To get the next page of items, send a subsequent request
that specifies the `NextMarker` value from the current response as the `Marker` value in the
subsequent request.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Marker"`: Use this field when paginating results to indicate where to begin in your list
  of functions. The response includes functions in the list that occur after the marker. To
  get the next page of the list, set this field's value to the value of `NextMarker` from
  the current page's response.
- `"MaxItems"`: The maximum number of functions that you want in the response.
- `"Stage"`: An optional filter to return only the functions that are in the specified
  stage, either `DEVELOPMENT` or `LIVE`.
"""
function list_functions end

function list_functions(; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "GET", "/2020-05-31/function"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_functions(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET", "/2020-05-31/function", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    list_invalidations(distribution_id)
    list_invalidations(distribution_id, params::Dict{String,<:Any})

Lists invalidation batches.

# Arguments

- `distribution_id`: The distribution's ID.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Marker"`: Use this parameter when paginating results to indicate where to begin in your
  list of invalidation batches. Because the results are returned in decreasing order from
  most recent to oldest, the most recent results are on the first page, the second page will
  contain earlier results, and so on. To get the next page of results, set `Marker` to the
  value of the `NextMarker` from the current page's response. This value is the same as the
  ID of the last invalidation batch on that page.

- `"MaxItems"`: The maximum number of invalidation batches that you want in the response
  body.
"""
function list_invalidations end

function list_invalidations(
    DistributionId; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/distribution/$(DistributionId)/invalidation";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_invalidations(
    DistributionId,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "GET",
        "/2020-05-31/distribution/$(DistributionId)/invalidation",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_invalidations_for_distribution_tenant(id)
    list_invalidations_for_distribution_tenant(id, params::Dict{String,<:Any})

Lists the invalidations for a distribution tenant.

# Arguments

- `id`: The ID of the distribution tenant.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Marker"`: Use this parameter when paginating results to indicate where to begin in your
  list of invalidation batches. Because the results are returned in decreasing order from
  most recent to oldest, the most recent results are on the first page, the second page will
  contain earlier results, and so on. To get the next page of results, set `Marker` to the
  value of the `NextMarker` from the current page's response. This value is the same as the
  ID of the last invalidation batch on that page.

- `"MaxItems"`: The maximum number of invalidations to return for the distribution tenant.
"""
function list_invalidations_for_distribution_tenant end

function list_invalidations_for_distribution_tenant(
    Id; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/distribution-tenant/$(Id)/invalidation";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_invalidations_for_distribution_tenant(
    Id, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/distribution-tenant/$(Id)/invalidation",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_key_groups()
    list_key_groups(params::Dict{String,<:Any})

Gets a list of key groups.

You can optionally specify the maximum number of items to receive in the response. If the
total number of items in the list exceeds the maximum that you specify, or the default
maximum, the response is paginated. To get the next page of items, send a subsequent request
that specifies the `NextMarker` value from the current response as the `Marker` value in the
subsequent request.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Marker"`: Use this field when paginating results to indicate where to begin in your list
  of key groups. The response includes key groups in the list that occur after the marker.
  To get the next page of the list, set this field's value to the value of `NextMarker` from
  the current page's response.
- `"MaxItems"`: The maximum number of key groups that you want in the response.
"""
function list_key_groups end

function list_key_groups(; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "GET", "/2020-05-31/key-group"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_key_groups(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET", "/2020-05-31/key-group", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    list_key_value_stores()
    list_key_value_stores(params::Dict{String,<:Any})

Specifies the key value stores to list.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Marker"`: The marker associated with the key value stores list.
- `"MaxItems"`: The maximum number of items in the key value stores list.
- `"Status"`: The status of the request for the key value stores list.
"""
function list_key_value_stores end

function list_key_value_stores(; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "GET", "/2020-05-31/key-value-store"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_key_value_stores(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/key-value-store",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_origin_access_controls()
    list_origin_access_controls(params::Dict{String,<:Any})

Gets the list of CloudFront origin access controls (OACs) in this Amazon Web Services
account.

You can optionally specify the maximum number of items to receive in the response. If the
total number of items in the list exceeds the maximum that you specify, or the default
maximum, the response is paginated. To get the next page of items, send another request that
specifies the `NextMarker` value from the current response as the `Marker` value in the next
request.

!!! note
    If you're not using origin access controls for your Amazon Web Services account, the [`list_origin_access_controls`](@ref)
    operation doesn't return the `Items` element in the response.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Marker"`: Use this field when paginating results to indicate where to begin in your list
  of origin access controls. The response includes the items in the list that occur after
  the marker. To get the next page of the list, set this field's value to the value of
  `NextMarker` from the current page's response.
- `"MaxItems"`: The maximum number of origin access controls that you want in the response.
"""
function list_origin_access_controls end

function list_origin_access_controls(; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "GET",
        "/2020-05-31/origin-access-control";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_origin_access_controls(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/origin-access-control",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_origin_request_policies()
    list_origin_request_policies(params::Dict{String,<:Any})

Gets a list of origin request policies.

You can optionally apply a filter to return only the managed policies created by Amazon Web
Services, or only the custom policies created in your Amazon Web Services account.

You can optionally specify the maximum number of items to receive in the response. If the
total number of items in the list exceeds the maximum that you specify, or the default
maximum, the response is paginated. To get the next page of items, send a subsequent request
that specifies the `NextMarker` value from the current response as the `Marker` value in the
subsequent request.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Marker"`: Use this field when paginating results to indicate where to begin in your list
  of origin request policies. The response includes origin request policies in the list that
  occur after the marker. To get the next page of the list, set this field's value to the
  value of `NextMarker` from the current page's response.

- `"MaxItems"`: The maximum number of origin request policies that you want in the response.

- `"Type"`: A filter to return only the specified kinds of origin request policies. Valid
  values are:

  - `managed` – Returns only the managed policies created by Amazon Web Services.
  - `custom` – Returns only the custom policies created in your Amazon Web Services account.
"""
function list_origin_request_policies end

function list_origin_request_policies(; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "GET",
        "/2020-05-31/origin-request-policy";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_origin_request_policies(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/origin-request-policy",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_public_keys()
    list_public_keys(params::Dict{String,<:Any})

List all public keys that have been added to CloudFront for this account.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Marker"`: Use this when paginating results to indicate where to begin in your list of
  public keys. The results include public keys in the list that occur after the marker. To
  get the next page of results, set the `Marker` to the value of the `NextMarker` from the
  current page's response (which is also the ID of the last public key on that page).
- `"MaxItems"`: The maximum number of public keys you want in the response body.
"""
function list_public_keys end

function list_public_keys(; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "GET", "/2020-05-31/public-key"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_public_keys(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET", "/2020-05-31/public-key", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    list_realtime_log_configs()
    list_realtime_log_configs(params::Dict{String,<:Any})

Gets a list of real-time log configurations.

You can optionally specify the maximum number of items to receive in the response. If the
total number of items in the list exceeds the maximum that you specify, or the default
maximum, the response is paginated. To get the next page of items, send a subsequent request
that specifies the `NextMarker` value from the current response as the `Marker` value in the
subsequent request.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Marker"`: Use this field when paginating results to indicate where to begin in your list
  of real-time log configurations. The response includes real-time log configurations in the
  list that occur after the marker. To get the next page of the list, set this field's value
  to the value of `NextMarker` from the current page's response.
- `"MaxItems"`: The maximum number of real-time log configurations that you want in the
  response.
"""
function list_realtime_log_configs end

function list_realtime_log_configs(; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "GET",
        "/2020-05-31/realtime-log-config";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_realtime_log_configs(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/realtime-log-config",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_response_headers_policies()
    list_response_headers_policies(params::Dict{String,<:Any})

Gets a list of response headers policies.

You can optionally apply a filter to get only the managed policies created by Amazon Web
Services, or only the custom policies created in your Amazon Web Services account.

You can optionally specify the maximum number of items to receive in the response. If the
total number of items in the list exceeds the maximum that you specify, or the default
maximum, the response is paginated. To get the next page of items, send a subsequent request
that specifies the `NextMarker` value from the current response as the `Marker` value in the
subsequent request.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Marker"`: Use this field when paginating results to indicate where to begin in your list
  of response headers policies. The response includes response headers policies in the list
  that occur after the marker. To get the next page of the list, set this field's value to
  the value of `NextMarker` from the current page's response.

- `"MaxItems"`: The maximum number of response headers policies that you want to get in the
  response.

- `"Type"`: A filter to get only the specified kind of response headers policies. Valid
  values are:

  - `managed` – Gets only the managed policies created by Amazon Web Services.
  - `custom` – Gets only the custom policies created in your Amazon Web Services account.
"""
function list_response_headers_policies end

function list_response_headers_policies(;
    aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/response-headers-policy";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_response_headers_policies(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/response-headers-policy",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_streaming_distributions()
    list_streaming_distributions(params::Dict{String,<:Any})

List streaming distributions.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Marker"`: The value that you provided for the `marker` request parameter.
- `"MaxItems"`: The value that you provided for the `max_items` request parameter.
"""
function list_streaming_distributions end

function list_streaming_distributions(; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "GET",
        "/2020-05-31/streaming-distribution";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_streaming_distributions(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/streaming-distribution",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_tags_for_resource(resource)
    list_tags_for_resource(resource, params::Dict{String,<:Any})

List tags for a CloudFront resource. For more information, see [Tagging a distribution](https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/tagging.html)
in the *Amazon CloudFront Developer Guide*.

# Arguments

- `resource`: An ARN of a CloudFront resource.
"""
function list_tags_for_resource end

function list_tags_for_resource(
    Resource; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET",
        "/2020-05-31/tagging",
        Dict{String,Any}("Resource" => Resource);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function list_tags_for_resource(
    Resource,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "GET",
        "/2020-05-31/tagging",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("Resource" => Resource), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_trust_stores()
    list_trust_stores(params::Dict{String,<:Any})

Lists trust stores.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Marker"`: Use this field when paginating results to indicate where to begin in your
  list. The response includes items in the list that occur after the marker. To get the next
  page of the list, set this field's value to the value of `NextMarker` from the current
  page's response.
- `"MaxItems"`: The maximum number of trust stores that you want returned in the response.
"""
function list_trust_stores end

function list_trust_stores(; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "POST", "/2020-05-31/trust-stores"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_trust_stores(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "POST",
        "/2020-05-31/trust-stores",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    list_vpc_origins()
    list_vpc_origins(params::Dict{String,<:Any})

List the CloudFront VPC origins in your account.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Marker"`: The marker associated with the VPC origins list.
- `"MaxItems"`: The maximum number of items included in the list.
"""
function list_vpc_origins end

function list_vpc_origins(; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "GET", "/2020-05-31/vpc-origin"; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

function list_vpc_origins(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "GET", "/2020-05-31/vpc-origin", params; aws_config, feature_set=SERVICE_FEATURE_SET
    )
end

"""
    publish_connection_function(id, if-_match)
    publish_connection_function(id, if-_match, params::Dict{String,<:Any})

Publishes a connection function.

# Arguments

- `id`: The connection function ID.
- `if-_match`: The current version (`ETag` value) of the connection function.
"""
function publish_connection_function end

function publish_connection_function(
    Id, If_Match; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "POST",
        "/2020-05-31/connection-function/$(Id)/publish",
        Dict{String,Any}("headers" => Dict{String,Any}("If-Match" => If_Match));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function publish_connection_function(
    Id,
    If_Match,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "POST",
        "/2020-05-31/connection-function/$(Id)/publish",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("headers" => Dict{String,Any}("If-Match" => If_Match)),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    publish_function(if-_match, name)
    publish_function(if-_match, name, params::Dict{String,<:Any})

Publishes a CloudFront function by copying the function code from the `DEVELOPMENT` stage to
`LIVE`. This automatically updates all cache behaviors that are using this function to use
the newly published copy in the `LIVE` stage.

When a function is published to the `LIVE` stage, you can attach the function to a
distribution's cache behavior, using the function's Amazon Resource Name (ARN).

To publish a function, you must provide the function's name and version (`ETag` value). To
get these values, you can use `ListFunctions` and `DescribeFunction`.

# Arguments

- `if-_match`: The current version (`ETag` value) of the function that you are publishing,
  which you can get using `DescribeFunction`.
- `name`: The name of the function that you are publishing.
"""
function publish_function end

function publish_function(
    If_Match, Name; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "POST",
        "/2020-05-31/function/$(Name)/publish",
        Dict{String,Any}("headers" => Dict{String,Any}("If-Match" => If_Match));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function publish_function(
    If_Match,
    Name,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "POST",
        "/2020-05-31/function/$(Name)/publish",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("headers" => Dict{String,Any}("If-Match" => If_Match)),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    put_resource_policy(policy_document, resource_arn)
    put_resource_policy(policy_document, resource_arn, params::Dict{String,<:Any})

Creates a resource control policy for a given CloudFront resource.

# Arguments

- `policy_document`: The JSON-formatted resource policy to create.
- `resource_arn`: The Amazon Resource Name (ARN) of the CloudFront resource for which the
  policy is being created.
"""
function put_resource_policy end

function put_resource_policy(
    PolicyDocument, ResourceArn; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "POST",
        "/2020-05-31/put-resource-policy",
        Dict{String,Any}("PolicyDocument" => PolicyDocument, "ResourceArn" => ResourceArn);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function put_resource_policy(
    PolicyDocument,
    ResourceArn,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "POST",
        "/2020-05-31/put-resource-policy",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "PolicyDocument" => PolicyDocument, "ResourceArn" => ResourceArn
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    tag_resource(resource, tags)
    tag_resource(resource, tags, params::Dict{String,<:Any})

Add tags to a CloudFront resource. For more information, see [Tagging a distribution](https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/tagging.html)
in the *Amazon CloudFront Developer Guide*.

# Arguments

- `resource`: An ARN of a CloudFront resource.
- `tags`: A complex type that contains zero or more `Tag` elements.
"""
function tag_resource end

function tag_resource(Resource, Tags; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "POST",
        "/2020-05-31/tagging?Operation=Tag",
        Dict{String,Any}("Resource" => Resource, "Tags" => Tags);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function tag_resource(
    Resource,
    Tags,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "POST",
        "/2020-05-31/tagging?Operation=Tag",
        Dict{String,Any}(
            mergewith(
                _merge, Dict{String,Any}("Resource" => Resource, "Tags" => Tags), params
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    test_connection_function(connection_object, id, if-_match)
    test_connection_function(connection_object, id, if-_match, params::Dict{String,<:Any})

Tests a connection function.

# Arguments

- `connection_object`: The connection object.
- `id`: The connection function ID.
- `if-_match`: The current version (`ETag` value) of the connection function.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Stage"`: The connection function stage.
"""
function test_connection_function end

function test_connection_function(
    ConnectionObject, Id, If_Match; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "POST",
        "/2020-05-31/connection-function/$(Id)/test",
        Dict{String,Any}(
            "ConnectionObject" => ConnectionObject,
            "headers" => Dict{String,Any}("If-Match" => If_Match),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function test_connection_function(
    ConnectionObject,
    Id,
    If_Match,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "POST",
        "/2020-05-31/connection-function/$(Id)/test",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "ConnectionObject" => ConnectionObject,
                    "headers" => Dict{String,Any}("If-Match" => If_Match),
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    test_function(event_object, if-_match, name)
    test_function(event_object, if-_match, name, params::Dict{String,<:Any})

Tests a CloudFront function.

To test a function, you provide an *event object* that represents an HTTP request or
response that your CloudFront distribution could receive in production. CloudFront runs the
function, passing it the event object that you provided, and returns the function's result
(the modified event object) in the response. The response also contains function logs and
error messages, if any exist. For more information about testing functions, see [Testing functions](https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/managing-functions.html#test-function)
in the *Amazon CloudFront Developer Guide*.

To test a function, you provide the function's name and version (`ETag` value) along with
the event object. To get the function's name and version, you can use `ListFunctions` and
`DescribeFunction`.

# Arguments

- `event_object`: The event object to test the function with. For more information about the
  structure of the event object, see [Testing functions](https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/managing-functions.html#test-function)
  in the *Amazon CloudFront Developer Guide*.
- `if-_match`: The current version (`ETag` value) of the function that you are testing,
  which you can get using `DescribeFunction`.
- `name`: The name of the function that you are testing.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Stage"`: The stage of the function that you are testing, either `DEVELOPMENT` or `LIVE`.
"""
function test_function end

function test_function(
    EventObject, If_Match, Name; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "POST",
        "/2020-05-31/function/$(Name)/test",
        Dict{String,Any}(
            "EventObject" => EventObject,
            "headers" => Dict{String,Any}("If-Match" => If_Match),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function test_function(
    EventObject,
    If_Match,
    Name,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "POST",
        "/2020-05-31/function/$(Name)/test",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "EventObject" => EventObject,
                    "headers" => Dict{String,Any}("If-Match" => If_Match),
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    untag_resource(resource, tag_keys)
    untag_resource(resource, tag_keys, params::Dict{String,<:Any})

Remove tags from a CloudFront resource. For more information, see [Tagging a distribution](https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/tagging.html)
in the *Amazon CloudFront Developer Guide*.

# Arguments

- `resource`: An ARN of a CloudFront resource.
- `tag_keys`: A complex type that contains zero or more `Tag` key elements.
"""
function untag_resource end

function untag_resource(
    Resource, TagKeys; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "POST",
        "/2020-05-31/tagging?Operation=Untag",
        Dict{String,Any}("Resource" => Resource, "TagKeys" => TagKeys);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function untag_resource(
    Resource,
    TagKeys,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "POST",
        "/2020-05-31/tagging?Operation=Untag",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("Resource" => Resource, "TagKeys" => TagKeys),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_anycast_ip_list(id, if-_match)
    update_anycast_ip_list(id, if-_match, params::Dict{String,<:Any})

Updates an Anycast static IP list.

# Arguments

- `id`: The ID of the Anycast static IP list.
- `if-_match`: The current version (ETag value) of the Anycast static IP list that you are
  updating.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"IpAddressType"`: The IP address type for the Anycast static IP list. You can specify one
  of the following options:

  - `ipv4` only
  - `ipv6` only
  - `dualstack` - Allocate a list of both IPv4 and IPv6 addresses

- `"IpamCidrConfigs"`: A list of IPAM CIDR configurations that specify the IP address ranges
  and IPAM pool settings for updating the Anycast static IP list.
"""
function update_anycast_ip_list end

function update_anycast_ip_list(
    Id, If_Match; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "PUT",
        "/2020-05-31/anycast-ip-list/$(Id)",
        Dict{String,Any}("headers" => Dict{String,Any}("If-Match" => If_Match));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_anycast_ip_list(
    Id,
    If_Match,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "PUT",
        "/2020-05-31/anycast-ip-list/$(Id)",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("headers" => Dict{String,Any}("If-Match" => If_Match)),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_cache_policy(cache_policy_config, id)
    update_cache_policy(cache_policy_config, id, params::Dict{String,<:Any})

Updates a cache policy configuration.

When you update a cache policy configuration, all the fields are updated with the values
provided in the request. You cannot update some fields independent of others. To update a
cache policy configuration:

1. Use `GetCachePolicyConfig` to get the current configuration.
2. Locally modify the fields in the cache policy configuration that you want to update.
3. Call `UpdateCachePolicy` by providing the entire cache policy configuration, including
   the fields that you modified and those that you didn't.

!!! important
    If your minimum TTL is greater than 0, CloudFront will cache content for at least the
    duration specified in the cache policy's minimum TTL, even if the
    `Cache-Control: no-cache`, `no-store`, or `private` directives are present in the origin
    headers.

# Arguments

- `cache_policy_config`: A cache policy configuration.
- `id`: The unique identifier for the cache policy that you are updating. The identifier is
  returned in a cache behavior's `CachePolicyId` field in the response to
  `GetDistributionConfig`.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"If-Match"`: The version of the cache policy that you are updating. The version is
  returned in the cache policy's `ETag` field in the response to `GetCachePolicyConfig`.
"""
function update_cache_policy end

function update_cache_policy(
    CachePolicyConfig, Id; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "PUT",
        "/2020-05-31/cache-policy/$(Id)",
        Dict{String,Any}("CachePolicyConfig" => CachePolicyConfig);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_cache_policy(
    CachePolicyConfig,
    Id,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "PUT",
        "/2020-05-31/cache-policy/$(Id)",
        Dict{String,Any}(
            mergewith(
                _merge, Dict{String,Any}("CachePolicyConfig" => CachePolicyConfig), params
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_cloud_front_origin_access_identity(cloud_front_origin_access_identity_config, id)
    update_cloud_front_origin_access_identity(cloud_front_origin_access_identity_config, id, params::Dict{String,<:Any})

Update an origin access identity.

# Arguments

- `cloud_front_origin_access_identity_config`: The identity's configuration information.
- `id`: The identity's id.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"If-Match"`: The value of the `ETag` header that you received when retrieving the
  identity's configuration. For example: `E2QWRUHAPOMQZL`.
"""
function update_cloud_front_origin_access_identity end

function update_cloud_front_origin_access_identity(
    CloudFrontOriginAccessIdentityConfig,
    Id;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "PUT",
        "/2020-05-31/origin-access-identity/cloudfront/$(Id)/config",
        Dict{String,Any}(
            "CloudFrontOriginAccessIdentityConfig" => CloudFrontOriginAccessIdentityConfig
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_cloud_front_origin_access_identity(
    CloudFrontOriginAccessIdentityConfig,
    Id,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "PUT",
        "/2020-05-31/origin-access-identity/cloudfront/$(Id)/config",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "CloudFrontOriginAccessIdentityConfig" =>
                        CloudFrontOriginAccessIdentityConfig,
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_connection_function(connection_function_code, connection_function_config, id, if-_match)
    update_connection_function(connection_function_code, connection_function_config, id, if-_match, params::Dict{String,<:Any})

Updates a connection function.

# Arguments

- `connection_function_code`: The connection function code.
- `connection_function_config`:
- `id`: The connection function ID.
- `if-_match`: The current version (`ETag` value) of the connection function you are
  updating.
"""
function update_connection_function end

function update_connection_function(
    ConnectionFunctionCode,
    ConnectionFunctionConfig,
    Id,
    If_Match;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "PUT",
        "/2020-05-31/connection-function/$(Id)",
        Dict{String,Any}(
            "ConnectionFunctionCode" => ConnectionFunctionCode,
            "ConnectionFunctionConfig" => ConnectionFunctionConfig,
            "headers" => Dict{String,Any}("If-Match" => If_Match),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_connection_function(
    ConnectionFunctionCode,
    ConnectionFunctionConfig,
    Id,
    If_Match,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "PUT",
        "/2020-05-31/connection-function/$(Id)",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "ConnectionFunctionCode" => ConnectionFunctionCode,
                    "ConnectionFunctionConfig" => ConnectionFunctionConfig,
                    "headers" => Dict{String,Any}("If-Match" => If_Match),
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_connection_group(id, if-_match)
    update_connection_group(id, if-_match, params::Dict{String,<:Any})

Updates a connection group.

# Arguments

- `id`: The ID of the connection group.
- `if-_match`: The value of the `ETag` header that you received when retrieving the
  connection group that you're updating.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"AnycastIpListId"`: The ID of the Anycast static IP list.
- `"Enabled"`: Whether the connection group is enabled.
- `"Ipv6Enabled"`: Enable IPv6 for the connection group. For more information, see [Enable IPv6](https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/distribution-web-values-specify.html#DownloadDistValuesEnableIPv6)
  in the *Amazon CloudFront Developer Guide*.
"""
function update_connection_group end

function update_connection_group(
    Id, If_Match; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "PUT",
        "/2020-05-31/connection-group/$(Id)",
        Dict{String,Any}("headers" => Dict{String,Any}("If-Match" => If_Match));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_connection_group(
    Id,
    If_Match,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "PUT",
        "/2020-05-31/connection-group/$(Id)",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("headers" => Dict{String,Any}("If-Match" => If_Match)),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_continuous_deployment_policy(continuous_deployment_policy_config, id)
    update_continuous_deployment_policy(continuous_deployment_policy_config, id, params::Dict{String,<:Any})

Updates a continuous deployment policy. You can update a continuous deployment policy to
enable or disable it, to change the percentage of traffic that it sends to the staging
distribution, or to change the staging distribution that it sends traffic to.

When you update a continuous deployment policy configuration, all the fields are updated
with the values that are provided in the request. You cannot update some fields independent
of others. To update a continuous deployment policy configuration:

1. Use `GetContinuousDeploymentPolicyConfig` to get the current configuration.
2. Locally modify the fields in the continuous deployment policy configuration that you want
   to update.
3. Use `UpdateContinuousDeploymentPolicy`, providing the entire continuous deployment policy
   configuration, including the fields that you modified and those that you didn't.

# Arguments

- `continuous_deployment_policy_config`: The continuous deployment policy configuration.
- `id`: The identifier of the continuous deployment policy that you are updating.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"If-Match"`: The current version (`ETag` value) of the continuous deployment policy that
  you are updating.
"""
function update_continuous_deployment_policy end

function update_continuous_deployment_policy(
    ContinuousDeploymentPolicyConfig, Id; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "PUT",
        "/2020-05-31/continuous-deployment-policy/$(Id)",
        Dict{String,Any}(
            "ContinuousDeploymentPolicyConfig" => ContinuousDeploymentPolicyConfig
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_continuous_deployment_policy(
    ContinuousDeploymentPolicyConfig,
    Id,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "PUT",
        "/2020-05-31/continuous-deployment-policy/$(Id)",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "ContinuousDeploymentPolicyConfig" => ContinuousDeploymentPolicyConfig
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_distribution(distribution_config, id)
    update_distribution(distribution_config, id, params::Dict{String,<:Any})

Updates the configuration for a CloudFront distribution.

The update process includes getting the current distribution configuration, updating it to
make your changes, and then submitting an `UpdateDistribution` request to make the updates.

**To update a web distribution using the CloudFront API**

1. Use `GetDistributionConfig` to get the current configuration, including the version
   identifier (`ETag`).
2. Update the distribution configuration that was returned in the response. Note the
   following important requirements and restrictions:

- You must copy the `ETag` field value from the response. (You'll use it for the `IfMatch`
  parameter in your request.) Then, remove the `ETag` field from the distribution
  configuration.
- You can't change the value of `CallerReference`.
3. Submit an `UpdateDistribution` request, providing the updated distribution configuration.
   The new configuration replaces the existing configuration. The values that you specify in
   an `UpdateDistribution` request are not merged into your existing configuration. Make
   sure to include all fields: the ones that you modified and also the ones that you didn't.

# Arguments

- `distribution_config`: The distribution's configuration information.
- `id`: The distribution's id.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"If-Match"`: The value of the `ETag` header that you received when retrieving the
  distribution's configuration. For example: `E2QWRUHAPOMQZL`.
"""
function update_distribution end

function update_distribution(
    DistributionConfig, Id; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "PUT",
        "/2020-05-31/distribution/$(Id)/config",
        Dict{String,Any}("DistributionConfig" => DistributionConfig);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_distribution(
    DistributionConfig,
    Id,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "PUT",
        "/2020-05-31/distribution/$(Id)/config",
        Dict{String,Any}(
            mergewith(
                _merge, Dict{String,Any}("DistributionConfig" => DistributionConfig), params
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_distribution_tenant(id, if-_match)
    update_distribution_tenant(id, if-_match, params::Dict{String,<:Any})

Updates a distribution tenant.

# Arguments

- `id`: The ID of the distribution tenant.
- `if-_match`: The value of the `ETag` header that you received when retrieving the
  distribution tenant to update. This value is returned in the response of the
  `GetDistributionTenant` API operation.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"ConnectionGroupId"`: The ID of the target connection group.

- `"Customizations"`: Customizations for the distribution tenant. For each distribution
  tenant, you can specify the geographic restrictions, and the Amazon Resource Names (ARNs)
  for the ACM certificate and WAF web ACL. These are specific values that you can override
  or disable from the multi-tenant distribution that was used to create the distribution
  tenant.

- `"DistributionId"`: The ID for the multi-tenant distribution.

- `"Domains"`: The domains to update for the distribution tenant. A domain object can
  contain only a domain property. You must specify at least one domain. Each distribution
  tenant can have up to 5 domains.

- `"Enabled"`: Indicates whether the distribution tenant should be updated to an enabled
  state. If you update the distribution tenant and it's not enabled, the distribution tenant
  won't serve traffic.

- `"ManagedCertificateRequest"`: An object that contains the CloudFront managed ACM
  certificate request.

- `"Parameters"`: A list of parameter values to add to the resource. A parameter is
  specified as a key-value pair. A valid parameter value must exist for any parameter that
  is marked as required in the multi-tenant distribution.
"""
function update_distribution_tenant end

function update_distribution_tenant(
    Id, If_Match; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "PUT",
        "/2020-05-31/distribution-tenant/$(Id)",
        Dict{String,Any}("headers" => Dict{String,Any}("If-Match" => If_Match));
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_distribution_tenant(
    Id,
    If_Match,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "PUT",
        "/2020-05-31/distribution-tenant/$(Id)",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("headers" => Dict{String,Any}("If-Match" => If_Match)),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_distribution_with_staging_config(id)
    update_distribution_with_staging_config(id, params::Dict{String,<:Any})

Copies the staging distribution's configuration to its corresponding primary distribution.
The primary distribution retains its `Aliases` (also known as alternate domain names or
CNAMEs) and `ContinuousDeploymentPolicyId` value, but otherwise its configuration is
overwritten to match the staging distribution.

You can use this operation in a continuous deployment workflow after you have tested
configuration changes on the staging distribution. After using a continuous deployment
policy to move a portion of your domain name's traffic to the staging distribution and
verifying that it works as intended, you can use this operation to copy the staging
distribution's configuration to the primary distribution. This action will disable the
continuous deployment policy and move your domain's traffic back to the primary
distribution.

This API operation requires the following IAM permissions:

- [GetDistribution](https://docs.aws.amazon.com/cloudfront/latest/APIReference/API_GetDistribution.html)
- [UpdateDistribution](https://docs.aws.amazon.com/cloudfront/latest/APIReference/API_UpdateDistribution.html)

# Arguments

- `id`: The identifier of the primary distribution to which you are copying a staging
  distribution's configuration.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"If-Match"`: The current versions (`ETag` values) of both primary and staging
  distributions. Provide these in the following format:

  `&lt;primary ETag&gt;, &lt;staging ETag&gt;`

- `"StagingDistributionId"`: The identifier of the staging distribution whose configuration
  you are copying to the primary distribution.
"""
function update_distribution_with_staging_config end

function update_distribution_with_staging_config(
    Id; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "PUT",
        "/2020-05-31/distribution/$(Id)/promote-staging-config";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_distribution_with_staging_config(
    Id, params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "PUT",
        "/2020-05-31/distribution/$(Id)/promote-staging-config",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_domain_association(domain, target_resource)
    update_domain_association(domain, target_resource, params::Dict{String,<:Any})

!!! note
    We recommend that you use the `UpdateDomainAssociation` API operation to move a domain
    association, as it supports both standard distributions and distribution tenants. [AssociateAlias](https://docs.aws.amazon.com/cloudfront/latest/APIReference/API_AssociateAlias.html)
    performs similar checks but only supports standard distributions.

Moves a domain from its current standard distribution or distribution tenant to another one.

You must first disable the source distribution (standard distribution or distribution
tenant) and then separately call this operation to move the domain to another target
distribution (standard distribution or distribution tenant).

To use this operation, specify the domain and the ID of the target resource (standard
distribution or distribution tenant). For more information, including how to set up the
target resource, prerequisites that you must complete, and other restrictions, see [Moving an alternate domain name to a different standard distribution or distribution tenant](https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/CNAMEs.html#alternate-domain-names-move)
in the *Amazon CloudFront Developer Guide*.

# Arguments

- `domain`: The domain to update.
- `target_resource`: The target standard distribution or distribution tenant resource for
  the domain. You can specify either `DistributionId` or `DistributionTenantId`, but not
  both.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"If-Match"`: The value of the `ETag` identifier for the standard distribution or
  distribution tenant that will be associated with the domain.
"""
function update_domain_association end

function update_domain_association(
    Domain, TargetResource; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "POST",
        "/2020-05-31/domain-association",
        Dict{String,Any}("Domain" => Domain, "TargetResource" => TargetResource);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_domain_association(
    Domain,
    TargetResource,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "POST",
        "/2020-05-31/domain-association",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("Domain" => Domain, "TargetResource" => TargetResource),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_field_level_encryption_config(field_level_encryption_config, id)
    update_field_level_encryption_config(field_level_encryption_config, id, params::Dict{String,<:Any})

Update a field-level encryption configuration.

# Arguments

- `field_level_encryption_config`: Request to update a field-level encryption configuration.
- `id`: The ID of the configuration you want to update.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"If-Match"`: The value of the `ETag` header that you received when retrieving the
  configuration identity to update. For example: `E2QWRUHAPOMQZL`.
"""
function update_field_level_encryption_config end

function update_field_level_encryption_config(
    FieldLevelEncryptionConfig, Id; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "PUT",
        "/2020-05-31/field-level-encryption/$(Id)/config",
        Dict{String,Any}("FieldLevelEncryptionConfig" => FieldLevelEncryptionConfig);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_field_level_encryption_config(
    FieldLevelEncryptionConfig,
    Id,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "PUT",
        "/2020-05-31/field-level-encryption/$(Id)/config",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "FieldLevelEncryptionConfig" => FieldLevelEncryptionConfig
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_field_level_encryption_profile(field_level_encryption_profile_config, id)
    update_field_level_encryption_profile(field_level_encryption_profile_config, id, params::Dict{String,<:Any})

Update a field-level encryption profile.

# Arguments

- `field_level_encryption_profile_config`: Request to update a field-level encryption
  profile.
- `id`: The ID of the field-level encryption profile request.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"If-Match"`: The value of the `ETag` header that you received when retrieving the profile
  identity to update. For example: `E2QWRUHAPOMQZL`.
"""
function update_field_level_encryption_profile end

function update_field_level_encryption_profile(
    FieldLevelEncryptionProfileConfig,
    Id;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "PUT",
        "/2020-05-31/field-level-encryption-profile/$(Id)/config",
        Dict{String,Any}(
            "FieldLevelEncryptionProfileConfig" => FieldLevelEncryptionProfileConfig
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_field_level_encryption_profile(
    FieldLevelEncryptionProfileConfig,
    Id,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "PUT",
        "/2020-05-31/field-level-encryption-profile/$(Id)/config",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "FieldLevelEncryptionProfileConfig" => FieldLevelEncryptionProfileConfig
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_function(function_code, function_config, if-_match, name)
    update_function(function_code, function_config, if-_match, name, params::Dict{String,<:Any})

Updates a CloudFront function.

You can update a function's code or the comment that describes the function. You cannot
update a function's name.

To update a function, you provide the function's name and version (`ETag` value) along with
the updated function code. To get the name and version, you can use `ListFunctions` and
`DescribeFunction`.

# Arguments

- `function_code`: The function code. For more information about writing a CloudFront
  function, see [Writing function code for CloudFront Functions](https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/writing-function-code.html)
  in the *Amazon CloudFront Developer Guide*.
- `function_config`: Configuration information about the function.
- `if-_match`: The current version (`ETag` value) of the function that you are updating,
  which you can get using `DescribeFunction`.
- `name`: The name of the function that you are updating.
"""
function update_function end

function update_function(
    FunctionCode,
    FunctionConfig,
    If_Match,
    Name;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "PUT",
        "/2020-05-31/function/$(Name)",
        Dict{String,Any}(
            "FunctionCode" => FunctionCode,
            "FunctionConfig" => FunctionConfig,
            "headers" => Dict{String,Any}("If-Match" => If_Match),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_function(
    FunctionCode,
    FunctionConfig,
    If_Match,
    Name,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "PUT",
        "/2020-05-31/function/$(Name)",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "FunctionCode" => FunctionCode,
                    "FunctionConfig" => FunctionConfig,
                    "headers" => Dict{String,Any}("If-Match" => If_Match),
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_key_group(id, key_group_config)
    update_key_group(id, key_group_config, params::Dict{String,<:Any})

Updates a key group.

When you update a key group, all the fields are updated with the values provided in the
request. You cannot update some fields independent of others. To update a key group:

1. Get the current key group with `GetKeyGroup` or `GetKeyGroupConfig`.
2. Locally modify the fields in the key group that you want to update. For example, add or
   remove public key IDs.
3. Call `UpdateKeyGroup` with the entire key group object, including the fields that you
   modified and those that you didn't.

# Arguments

- `id`: The identifier of the key group that you are updating.
- `key_group_config`: The key group configuration.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"If-Match"`: The version of the key group that you are updating. The version is the key
  group's `ETag` value.
"""
function update_key_group end

function update_key_group(
    Id, KeyGroupConfig; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "PUT",
        "/2020-05-31/key-group/$(Id)",
        Dict{String,Any}("KeyGroupConfig" => KeyGroupConfig);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_key_group(
    Id,
    KeyGroupConfig,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "PUT",
        "/2020-05-31/key-group/$(Id)",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("KeyGroupConfig" => KeyGroupConfig), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_key_value_store(comment, if-_match, name)
    update_key_value_store(comment, if-_match, name, params::Dict{String,<:Any})

Specifies the key value store to update.

# Arguments

- `comment`: The comment of the key value store to update.
- `if-_match`: The key value store to update, if a match occurs.
- `name`: The name of the key value store to update.
"""
function update_key_value_store end

function update_key_value_store(
    Comment, If_Match, Name; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "PUT",
        "/2020-05-31/key-value-store/$(Name)",
        Dict{String,Any}(
            "Comment" => Comment, "headers" => Dict{String,Any}("If-Match" => If_Match)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_key_value_store(
    Comment,
    If_Match,
    Name,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "PUT",
        "/2020-05-31/key-value-store/$(Name)",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "Comment" => Comment,
                    "headers" => Dict{String,Any}("If-Match" => If_Match),
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_origin_access_control(id, origin_access_control_config)
    update_origin_access_control(id, origin_access_control_config, params::Dict{String,<:Any})

Updates a CloudFront origin access control.

# Arguments

- `id`: The unique identifier of the origin access control that you are updating.
- `origin_access_control_config`: An origin access control.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"If-Match"`: The current version (`ETag` value) of the origin access control that you are
  updating.
"""
function update_origin_access_control end

function update_origin_access_control(
    Id, OriginAccessControlConfig; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "PUT",
        "/2020-05-31/origin-access-control/$(Id)/config",
        Dict{String,Any}("OriginAccessControlConfig" => OriginAccessControlConfig);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_origin_access_control(
    Id,
    OriginAccessControlConfig,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "PUT",
        "/2020-05-31/origin-access-control/$(Id)/config",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("OriginAccessControlConfig" => OriginAccessControlConfig),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_origin_request_policy(id, origin_request_policy_config)
    update_origin_request_policy(id, origin_request_policy_config, params::Dict{String,<:Any})

Updates an origin request policy configuration.

When you update an origin request policy configuration, all the fields are updated with the
values provided in the request. You cannot update some fields independent of others. To
update an origin request policy configuration:

1. Use `GetOriginRequestPolicyConfig` to get the current configuration.
2. Locally modify the fields in the origin request policy configuration that you want to
   update.
3. Call `UpdateOriginRequestPolicy` by providing the entire origin request policy
   configuration, including the fields that you modified and those that you didn't.

# Arguments

- `id`: The unique identifier for the origin request policy that you are updating. The
  identifier is returned in a cache behavior's `OriginRequestPolicyId` field in the response
  to `GetDistributionConfig`.
- `origin_request_policy_config`: An origin request policy configuration.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"If-Match"`: The version of the origin request policy that you are updating. The version
  is returned in the origin request policy's `ETag` field in the response to
  `GetOriginRequestPolicyConfig`.
"""
function update_origin_request_policy end

function update_origin_request_policy(
    Id, OriginRequestPolicyConfig; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "PUT",
        "/2020-05-31/origin-request-policy/$(Id)",
        Dict{String,Any}("OriginRequestPolicyConfig" => OriginRequestPolicyConfig);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_origin_request_policy(
    Id,
    OriginRequestPolicyConfig,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "PUT",
        "/2020-05-31/origin-request-policy/$(Id)",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}("OriginRequestPolicyConfig" => OriginRequestPolicyConfig),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_public_key(id, public_key_config)
    update_public_key(id, public_key_config, params::Dict{String,<:Any})

Update public key information. Note that the only value you can change is the comment.

# Arguments

- `id`: The identifier of the public key that you are updating.
- `public_key_config`: A public key configuration.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"If-Match"`: The value of the `ETag` header that you received when retrieving the public
  key to update. For example: `E2QWRUHAPOMQZL`.
"""
function update_public_key end

function update_public_key(
    Id, PublicKeyConfig; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "PUT",
        "/2020-05-31/public-key/$(Id)/config",
        Dict{String,Any}("PublicKeyConfig" => PublicKeyConfig);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_public_key(
    Id,
    PublicKeyConfig,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "PUT",
        "/2020-05-31/public-key/$(Id)/config",
        Dict{String,Any}(
            mergewith(
                _merge, Dict{String,Any}("PublicKeyConfig" => PublicKeyConfig), params
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_realtime_log_config()
    update_realtime_log_config(params::Dict{String,<:Any})

Updates a real-time log configuration.

When you update a real-time log configuration, all the parameters are updated with the
values provided in the request. You cannot update some parameters independent of others. To
update a real-time log configuration:

1. Call `GetRealtimeLogConfig` to get the current real-time log configuration.
2. Locally modify the parameters in the real-time log configuration that you want to update.
3. Call this API (`UpdateRealtimeLogConfig`) by providing the entire real-time log
   configuration, including the parameters that you modified and those that you didn't.

You cannot update a real-time log configuration's `Name` or `ARN`.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"ARN"`: The Amazon Resource Name (ARN) for this real-time log configuration.

- `"EndPoints"`: Contains information about the Amazon Kinesis data stream where you are
  sending real-time log data.

- `"Fields"`: A list of fields to include in each real-time log record.

  For more information about fields, see [Real-time log configuration fields](https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/real-time-logs.html#understand-real-time-log-config-fields)
  in the *Amazon CloudFront Developer Guide*.

- `"Name"`: The name for this real-time log configuration.

- `"SamplingRate"`: The sampling rate for this real-time log configuration. The sampling
  rate determines the percentage of viewer requests that are represented in the real-time
  log data. You must provide an integer between 1 and 100, inclusive.
"""
function update_realtime_log_config end

function update_realtime_log_config(; aws_config::AbstractAWSConfig=current_aws_config())
    return cloudfront(
        "PUT",
        "/2020-05-31/realtime-log-config";
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_realtime_log_config(
    params::AbstractDict{String}; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "PUT",
        "/2020-05-31/realtime-log-config",
        params;
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_response_headers_policy(id, response_headers_policy_config)
    update_response_headers_policy(id, response_headers_policy_config, params::Dict{String,<:Any})

Updates a response headers policy.

When you update a response headers policy, the entire policy is replaced. You cannot update
some policy fields independent of others. To update a response headers policy configuration:

1. Use `GetResponseHeadersPolicyConfig` to get the current policy's configuration.
2. Modify the fields in the response headers policy configuration that you want to update.
3. Call `UpdateResponseHeadersPolicy`, providing the entire response headers policy
   configuration, including the fields that you modified and those that you didn't.

# Arguments

- `id`: The identifier for the response headers policy that you are updating.
- `response_headers_policy_config`: A response headers policy configuration.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"If-Match"`: The version of the response headers policy that you are updating.

  The version is returned in the cache policy's `ETag` field in the response to
  `GetResponseHeadersPolicyConfig`.
"""
function update_response_headers_policy end

function update_response_headers_policy(
    Id, ResponseHeadersPolicyConfig; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "PUT",
        "/2020-05-31/response-headers-policy/$(Id)",
        Dict{String,Any}("ResponseHeadersPolicyConfig" => ResponseHeadersPolicyConfig);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_response_headers_policy(
    Id,
    ResponseHeadersPolicyConfig,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "PUT",
        "/2020-05-31/response-headers-policy/$(Id)",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "ResponseHeadersPolicyConfig" => ResponseHeadersPolicyConfig
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_streaming_distribution(id, streaming_distribution_config)
    update_streaming_distribution(id, streaming_distribution_config, params::Dict{String,<:Any})

Update a streaming distribution.

# Arguments

- `id`: The streaming distribution's id.
- `streaming_distribution_config`: The streaming distribution's configuration information.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"If-Match"`: The value of the `ETag` header that you received when retrieving the
  streaming distribution's configuration. For example: `E2QWRUHAPOMQZL`.
"""
function update_streaming_distribution end

function update_streaming_distribution(
    Id, StreamingDistributionConfig; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "PUT",
        "/2020-05-31/streaming-distribution/$(Id)/config",
        Dict{String,Any}("StreamingDistributionConfig" => StreamingDistributionConfig);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_streaming_distribution(
    Id,
    StreamingDistributionConfig,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "PUT",
        "/2020-05-31/streaming-distribution/$(Id)/config",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "StreamingDistributionConfig" => StreamingDistributionConfig
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_trust_store(ca_certificates_bundle_source, id, if-_match)
    update_trust_store(ca_certificates_bundle_source, id, if-_match, params::Dict{String,<:Any})

Updates a trust store.

# Arguments

- `ca_certificates_bundle_source`: The CA certificates bundle source.
- `id`: The trust store ID.
- `if-_match`: The current version (`ETag` value) of the trust store you are updating.
"""
function update_trust_store end

function update_trust_store(
    CaCertificatesBundleSource,
    Id,
    If_Match;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "PUT",
        "/2020-05-31/trust-store/$(Id)",
        Dict{String,Any}(
            "CaCertificatesBundleSource" => CaCertificatesBundleSource,
            "headers" => Dict{String,Any}("If-Match" => If_Match),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_trust_store(
    CaCertificatesBundleSource,
    Id,
    If_Match,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "PUT",
        "/2020-05-31/trust-store/$(Id)",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "CaCertificatesBundleSource" => CaCertificatesBundleSource,
                    "headers" => Dict{String,Any}("If-Match" => If_Match),
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    update_vpc_origin(id, if-_match, vpc_origin_endpoint_config)
    update_vpc_origin(id, if-_match, vpc_origin_endpoint_config, params::Dict{String,<:Any})

Update an Amazon CloudFront VPC origin in your account.

# Arguments

- `id`: The VPC origin ID.
- `if-_match`: The VPC origin to update, if a match occurs.
- `vpc_origin_endpoint_config`: The VPC origin endpoint configuration.
"""
function update_vpc_origin end

function update_vpc_origin(
    Id,
    If_Match,
    VpcOriginEndpointConfig;
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "PUT",
        "/2020-05-31/vpc-origin/$(Id)",
        Dict{String,Any}(
            "VpcOriginEndpointConfig" => VpcOriginEndpointConfig,
            "headers" => Dict{String,Any}("If-Match" => If_Match),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function update_vpc_origin(
    Id,
    If_Match,
    VpcOriginEndpointConfig,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "PUT",
        "/2020-05-31/vpc-origin/$(Id)",
        Dict{String,Any}(
            mergewith(
                _merge,
                Dict{String,Any}(
                    "VpcOriginEndpointConfig" => VpcOriginEndpointConfig,
                    "headers" => Dict{String,Any}("If-Match" => If_Match),
                ),
                params,
            ),
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

"""
    verify_dns_configuration(identifier)
    verify_dns_configuration(identifier, params::Dict{String,<:Any})

Verify the DNS configuration for your domain names. This API operation checks whether your
domain name points to the correct routing endpoint of the connection group, such as
d111111abcdef8.cloudfront.net. You can use this API operation to troubleshoot and resolve
DNS configuration issues.

# Arguments

- `identifier`: The identifier of the distribution tenant. You can specify the ARN, ID, or
  name of the distribution tenant.

# Optional Parameters

Optional parameters can be passed as a `params::Dict{String,<:Any}`. Valid keys are:

- `"Domain"`: The domain name that you're verifying.
"""
function verify_dns_configuration end

function verify_dns_configuration(
    Identifier; aws_config::AbstractAWSConfig=current_aws_config()
)
    return cloudfront(
        "POST",
        "/2020-05-31/verify-dns-configuration",
        Dict{String,Any}("Identifier" => Identifier);
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end

function verify_dns_configuration(
    Identifier,
    params::AbstractDict{String};
    aws_config::AbstractAWSConfig=current_aws_config(),
)
    return cloudfront(
        "POST",
        "/2020-05-31/verify-dns-configuration",
        Dict{String,Any}(
            mergewith(_merge, Dict{String,Any}("Identifier" => Identifier), params)
        );
        aws_config,
        feature_set=SERVICE_FEATURE_SET,
    )
end
